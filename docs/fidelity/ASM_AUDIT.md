# ASM-vs-C Audit Queue — progress

> Generated by `tools/fidelity/asm_audit.py build`/`record`. Do not hand-edit; source of truth is `docs/fidelity/asm_audit_queue.json`.
> Drives S-tier exit criterion 4 (FID-0042). Review contract: `docs/fidelity/CHARTER.md` #audit.

**285/388 reviewed** (73.5%). Verdict counts: finding-filed=56, unreviewed=103, verified-equivalent=207, waived=22.

Risk rank: (a) prior-defect files (gun/bondview/chrobjhandler/stan/prop) first, (b) instruction-count x has-jump-table (dispatch = proven failure mode), (c) bg/model bulk last.

## Top 30 ranked unreviewed

| Rank | Symbol | File | Instrs | Jump tables | Reason |
|-----:|--------|------|-------:|-------------|--------|
| 3 | `object_interaction` | src/game/chrobjhandler.c | 4641 |  | prior-defect-file 4641i |
| 11 | `playerTickBeams` | src/game/bondview.c | 754 |  | prior-defect-file 754i |
| 12 | `playerTickBeams` | src/game/bondview.c | 754 |  | prior-defect-file 754i |
| 91 | `spectrum_hw_emulation` | src/game/spectrum.c | 15687 | jpt_8005C52C, jpt_8005C62C, jpt_8005C64C, jpt_8005C66C, jpt_8005C68C, jpt_8005C6AC, jpt_8005C71C | dispatch(jpt_8005C52C,jpt_8005C62C,jpt_8005C64C,jpt_8005C66C,jpt_8005C68C,jpt_8005C6AC,jpt_8005C71C) 15687i |
| 290 | `modelCopyAnimForMerge` | src/game/model.c | 62 |  | bulk-last 62i |
| 291 | `sub_GAME_7F0B95D8` | src/game/bg.c | 61 |  | bulk-last 61i |
| 292 | `sub_GAME_7F0B9CC8` | src/game/bg.c | 61 |  | bulk-last 61i |
| 293 | `sub_GAME_7F0B5FAC` | src/game/bg.c | 60 |  | bulk-last 60i |
| 294 | `sub_GAME_7F0B5FAC` | src/game/bg.c | 60 |  | bulk-last 60i |
| 295 | `sub_GAME_7F0B8EFC` | src/game/bg.c | 53 |  | bulk-last 53i |
| 296 | `sub_GAME_7F06FCFC` | src/game/model.c | 52 |  | bulk-last 52i |
| 297 | `sub_GAME_7F0B37EC` | src/game/bg.c | 50 |  | bulk-last 50i |
| 298 | `sub_GAME_7F06DE04` | src/game/model.c | 47 |  | bulk-last 47i |
| 299 | `delete_room_data` | src/game/bg.c | 46 |  | bulk-last 46i |
| 300 | `roomsHandleStateDebugging` | src/game/bg.c | 45 |  | bulk-last 45i |
| 301 | `roomsHandleStateDebugging` | src/game/bg.c | 45 |  | bulk-last 45i |
| 302 | `sub_GAME_7F0B8DF4` | src/game/bg.c | 41 |  | bulk-last 41i |
| 303 | `sub_GAME_7F0B7EE4` | src/game/bg.c | 40 |  | bulk-last 40i |
| 304 | `sub_GAME_7F06D3F4` | src/game/model.c | 39 |  | bulk-last 39i |
| 305 | `sub_GAME_7F0B7EE4` | src/game/bg.c | 34 |  | bulk-last 34i |
| 306 | `sub_GAME_7F0B92B4` | src/game/bg.c | 33 |  | bulk-last 33i |
| 307 | `sub_GAME_7F0B9990` | src/game/bg.c | 33 |  | bulk-last 33i |
| 308 | `sub_GAME_7F0B8FD0` | src/game/bg.c | 28 |  | bulk-last 28i |
| 309 | `sub_GAME_7F0B9A7C` | src/game/bg.c | 26 |  | bulk-last 26i |
| 310 | `bgDebPrintPORTALID` | src/game/bg.c | 25 |  | bulk-last 25i |
| 311 | `bgDebPrintROOMID` | src/game/bg.c | 25 |  | bulk-last 25i |
| 312 | `sub_GAME_7F0B3BC4` | src/game/bg.c | 22 |  | bulk-last 22i |
| 313 | `sub_GAME_7F0B9A2C` | src/game/bg.c | 20 |  | bulk-last 20i |
| 314 | `sub_GAME_7F0B3BC4` | src/game/bg.c | 18 |  | bulk-last 18i |
| 315 | `sub_GAME_7F06C418` | src/game/model.c | 18 |  | bulk-last 18i |

## Recorded verdicts

| Rank | Symbol | File | Verdict | Note |
|-----:|--------|------|---------|------|
| 1 | `handle_weapon_id_values_possibly_1st_person_animation` | src/game/gun.c | `finding-filed:FID-0051` | Re-derived jpt_80053F24 (weapon-func) + jpt_80054084 (shooting, idx=weapon_id-2, dispatch gun.c:20144-20154) from ASM. Known fixes confirmed present in C (M16/FNP90->machinegun, TANKSHELLS->pistol, TRIGGER->pistol == ASM). Residual divergence: ROCKETLAUNCH/REMOTEMINE/PLASTIQUE shooting groups differ from ASM -> FID-0051. |
| 2 | `handles_firing_or_throwing_weapon_in_hand` | src/game/gun.c | `finding-filed:FID-0052` | jpt_weapon_bullet_type[weapon_id-4] bounds<0x14. pistol handler=sub_GAME_7F061BF4+field_0x30++ then common tail; shotgun_mine=tail only; none=sub_GAME_7F061BF4 only. ASM maps SHOTGUN+AUTOSHOT->shotgun_mine but C switch(item) puts AUTOSHOT in pistol case -> FID-0052. SHOTGUN/LASER/WATCHLASER match. |
| 4 | `proplvreset2` | src/game/prop.c | `verified-equivalent` | Both dispatch tables map index-for-index onto the two live-C switch(phead->type) passes. jpt_object_type_expansion (prop.c:4638): idx=type-1 (addiu $t0,$v1,-1 prop.c:5252), bounds<0x2f (sltiu prop.c:5254), = C first-pass switch prop.c:3299; obj_03_expand repeats for the PROP/ALARM/MAGAZINE/... default-obj group matching C fall-through prop.c:3365. jpt_8004F02C (prop.c:4692): idx=type-3 (prop.c:5796), bounds<0x2d, = C second-pass reparent switch prop.c:3567 (12-case INSIDEANOTHEROBJ group matches). Maximally reachable (per-stage loader, every level load). NATIVE_PORT-only endian converters preceding dispatch have no ASM counterpart (out of scope). |
| 5 | `process_monitor_animation_microcode` | src/game/chrobjhandler.c | `verified-equivalent` | jpt_80052C6C = 16-entry microcode opcode table (command00..0F). Dispatch chrobjhandler.c:29057-29063 idx=opcode bounds<0x10 else default. Live C switch(temp_t3) (chrobjhandler.c:28529) has cases 0..15 in 1:1 opcode order + default; dispatch structure/bounds equivalent. |
| 6 | `solo_char_load` | src/game/bondview.c | `verified-equivalent` | Solo Bond viewer body/head model selection. jpt_bondcuff[bondtype] (bondview.c:3499) -> sp+0x44 body, bound sltiu $v1,9 (bondview.c:3588), v1=player->0x41c; jpt_bondhead[bondtype] (bondview.c:3512) -> sp+0x40 head, entered only when folderBond==0. All 9 cuff indices (CUFF_BLUE..FOLDER) and 9 head indices match live C nested switch (bondview.c:3081, body switch 3153, head switch 3183) incl defaults body=23/head=75. folderBond!=0 -> head=78 (BOND_CONNERY/DALTON/MOORE) matches ASM 3652. moore/dalton handler labels name-swapped vs enum but write identical values (body5/head78) => no behavior diff. Reachable every solo model load. NATIVE_PORT buffer-packing divergences (R1/red-shard fixes, commit 50f3f61) are landed fidelity fixes, not dispatch, out of scope. |
| 7 | `mp_respawn_handler` | src/game/bondview.c | `verified-equivalent` | jpt_mpspawntype[type] (7 entries), dispatch bondview.c:20201-20210 idx=type bounds<7, loop while(type!=9). All 8 per-case pointer strides match C switch(type) (gun-style intro cmd interp): 0=+0xC,1=+0x10,2=+0x10,3=+0x20,4=+0x8,5=+0x8,6=+0x28,default=+0x4. Full function traced. |
| 8 | `mp_respawn_handler` | src/game/bondview.c | `verified-equivalent` | MP respawn/intro setup-cmd interpreter. jpt_mpspawntype (7 entries, bondview.c:20301) dispatch: type=cmd[0], exit type==9 (beq $v0,9 bondview.c:20502), bounds sltiu $at,$v0,7 (bondview.c:20503) -> idx>=7 = mp_spawntype_default. All 8 per-type pointer strides match live C switch (bondview.c:19863, NONMATCHING): 0=+0xC,1=+0x10,2=+0x10,3=+0x20,4=+0x8,5=+0x8,6=+0x28,default=+0x4. Handler bodies (type1 give-inv, type2 give-ammo w/ check_ramrom_flags==cmd[3]) match C 19948-19960. Reachable every MP respawn w/ g_CurrentSetup.intro!=NULL. FLAGGED (not filed): retail .late_rodata scale divisors read 0.2004/0.1188 vs C 0.17/0.1, but the LEFTOVERDEBUG copy of the same fn agrees w/ C at 0.17/0.1 + label/reloc mismatch => likely dump artifact; needs ROM byte check to resolve. |
| 9 | `sub_GAME_7F06359C` | src/game/gun.c | `finding-filed:FID-0072` | Watch controller-page 3D joypad renderer, ASM 16597-17568 vs C 16183-16607. THREE divergences: (1) held-button depress -10.0 applied to the D_80035D44 pos block (arg0 of sub_GAME_7F06351C, PRE-rotation; e.g. i==4 swc1 sp+0x194 @17047-17049, same shape all 7 cases i=1,4,5,6,7,8,9) while C adds to the vertices-derived rotN (arg4, POST-rotation, C 16424-16426 etc) -> button travel direction/depth differs under the -pi/3 X rot; (2) retail loads ITEM_JOYPAD into hand 0 GUNRIGHT (move $a0,$zero; li $a1,85 @16687-16689 + all hand queries a0=0) vs C GUNLEFT throughout; (3) phantom per-frame save/restore of prev_watchmenu_item (C 16256/16601-16606) with no ASM counterpart. All 12 case masks/consts/else-if chains + subdraw + f32->s32 loops otherwise equivalent. See FID-0072. |
| 10 | `sub_GAME_7F061E18` | src/game/gun.c | `verified-equivalent` | Muzzle-flash/laser-beam/star-flare billboard DL builder. Live C NONMATCHING gun.c:12602-12946 = ASM gun.c:12973-13812 (US glabel 12973). Re-verified: random laser color path %50+0x96 into cn[3], %5==0 gate (bnezl), %100 then 255-r into cn[1]/cn[0] (ASM 13048-13068) = C:12661-12667, same RNG ordering; laser endpoint x<2.0 strict half-scale (c.lt.s f16,2.0 ASM 13375=gun.c:13373) = C:12784; star scale pick dist_sq2<dist_sq1 (ASM 13585) = C:12856; item_id lb sign-ext re-read, width<<5, all Gfx immediates match. Renderer/cosmetic, writes nothing to sim, RNG calls identically ordered. Only diff = port muzzle-flash disable + texDebug instrumentation + dyn-overflow guard (behavior-neutral). |
| 13 | `bgTestHitOnObj` | src/game/chrobjhandler.c | `finding-filed:FID-0071` | Ray-vs-object collision DL walker, ASM 36044-36617 vs C 35471-35789. Divergence in the post-hit texture backward-scan boundary: retail exits the 8-byte-step scan with search==gdlstart and the bnel v0,s3 (36320) then yields texturenum=-1 EVEN when *gdlstart is the G_SETTIMG (loop 36310-36317 re-derived; same in TRI4 path 36557-36580); port helper hit_find_texture_for_triangle_command scans INCLUSIVE of gdlstart (while search>=gdlstart, break only after examining it, C 35452-35480) -> real texture id where retail gives -1, changing bullet-impact material classification for DLs whose texture cmd opens the list. Benign: 0xC0 G_SETTEX direct recognition (port-data counterpart of retail load-time rewrite), continue-past-invalid-0xFD (malformed data only). Dispatch senses, vtx/TRI1/TRI4 index lanes, bbox+intersect ordering, s32-trunc distance, HitThing stores all verified equivalent. See FID-0071. |
| 14 | `sub_GAME_7F052D8C` | src/game/chrobjhandler.c | `verified-equivalent` | Door-vertex bbox clamp + texcoord un-squish. Live C NONMATCHING chrobjhandler.c:45918-46052 = ASM chrobjhandler.c:46058-46615. Re-verified anchors: cutoff s16 trunc (trunc.w.s + sll/sra 0x10, ASM 46098) = C:45964; loop bound numVertices/4 signed round-to-zero (+3/bgez) + blez guard (ASM 46113) = C:45984; comparison polarity VERTICAL y>=cutoff (slt $a0,$t5 + bnez skip, ASM 46203) = C:46003, horizontal x<=cutoff (slt $t5,$a1 + bnez skip, ASM 46397) = C:46028. Coord offsets x@0/y@2, sibling order, interp signs match. Reachable (door init 46736, tick 48020, DOORFLAG_0004). Only diff = additive port NULL guards; byte-identical on valid doors. |
| 15 | `handles_projectile_motion` | src/game/chrobjhandler.c | `finding-filed:FID-0065` | Endpoint pull-back block: inverted result guard (ASM bnezl result skips block when result!=0 -> runs on result==0/hit, chrobjhandler.c:5660; C 'if(result!=0)' runs on no-hit, :5207) AND wrong base operand (ASM base=($s3)=arg2 :5698; C base=dest=arg1 :5220). Rest of fn verified-equivalent. Retail clamps endpoint to bg_hit-0.1*travel on wall hit; port doesn't. See FID-0065. |
| 16 | `sub_GAME_7F087E74` | src/game/bondview.c | `finding-filed:FID-0068` | [US variant audited] Watch-scene renderer ASM 17756-18273 vs C 17339-17751. Tint written to WRONG FIELD: retail sw to sp+0x1ec = renderdata(sp+0x1b8)+0x34 = envcolour (205 @18203-18205 states 5/12; r<<24\|g<<16\|b<<8\|a from lbu 0xfdc..0xfdf @18207-18217) feeding gDPSetFogColor via PropType-4 material path; C 17677/17679 writes cullmode (+0x3c, inert) with LE-byte-reversed *(u32*)&tileColor -> pause arm loses room tint + raise/lower darkening. Plus guPerspective aspect 0x3FBA2E8C=1.4545455 (17820-17821) vs C 1.456f (JP variant 18341-18342 shows same retail bits). Rest verified equivalent. See FID-0068. |
| 17 | `sub_GAME_7F044B38` | src/game/chrobjhandler.c | `verified-equivalent` | Vehicle ground-alignment matrix build. ASM chrobjhandler.c:8585 = live C 8331 (NATIVE_PORT), traced instruction-by-instruction. Y-rot x scale x pos matrix, transform 4 corner verts (Switches[1..4]), walkTilesBetweenPoints_NoCallback + stanGetPositionYValue per corner (any fail -> valid=0). Fallback (valid==0, beqz :8720): plain Y-rot + stanY-(vert1.y+ymin)*scale (C:8458). Else: 4 normalized edge vecs, pick adjacent-edge cross with max Y (crossNN_y ASM 0x4E58/4E70/4EC4/4EF0 = C:8486-8492; cross12/23/34/41 = C:8495/8507/8514/8521), build orthonormal obj->mtx (up=normal m[1] obj+0x28, fwd r0 m[0] obj+0x18, right=row0xrow1 m[2] obj+0x38), pos.y=pt1.y-ymin*scale-pt2.y (C:8556). Return=valid(0x108). Only diff = additive NATIVE_PORT modelGetSwitchDataSafe null-guards + bbox fallback (behavior-preserving on valid data). |
| 18 | `object_collectability_routines` | src/game/chrobjhandler.c | `verified-equivalent` | Object pickup/collectability gate + proximity/LOS. ASM chrobjhandler.c:42561 = live C 41972-42251 (NATIVE_PORT). All paths traced-match: collectable/flag gate (type!=0x11 flag 0x100000 else 0x40000, 0x80000 inhibit) = C 41978; weapon type8 throwing{0x1A,0x57}/explosive sets + dual sign test sll flags,3;bgez->right else left = C 42042; magazine type7 lw 0x80 ammoType, curAmmo>=max return0 = C 42066; PROPDEF_AMMO type0x14 (prior ammo-crate hot spot) FAITHFUL: reads slots[i].quantity via lhu 0x82, stride +4, 13 iters, ammotype=i+1 w/ i==1->9mm (bondtypes.h:3395) = corrected C 42086; armour type0x15; proximity vrot<-0.7854 + magnet, far magnet>=60 distSq<=122500\|dy\|<=500, near <=10000\|dy\|<=200, dx=0x58-8 etc, LOS stanTestLineUnobstructed tile!=prop->stan reject = C 42126. Only deviation = commented NATIVE_PORT same-room-tagged LOS relaxation (42162), intentional. Maximally reachable (per-pickup tick). |
| 19 | `generate_ammo_total_microcode` | src/game/gun.c | `finding-filed:FID-0064` | Port omits retail mpmenuon early-out (ammo HUD renders over MP watch menu). ASM has two guards (gunammooff@0x1064 + mpmenuon@0x29c4, gun.c:32351-32356); C checks only gunammooff (gun.c:32056). Reachability confirmed (caller bondview.c:21384 unconditional; caller mpmenuon block only overlays). See FID-0064. |
| 20 | `maybe_detonate_object` | src/game/chrobjhandler.c | `finding-filed:FID-0074` | 5 divergences re-derived from ASM 38027-38457 vs live C 37425-37667: (D1) retail type-20 destroyed block (randomGetNext%13 + slot scan + ammocrateAllocate salvage magazine spawn, 38215-38326) entirely missing from C switch 37607-37656 (also RNG stream desync); (D2) armour type-21 amount INVERTED (ASM bnezl 38418: destroyed->0.0, else formula; C 37646-37652 backwards); (D3) unarmed non-collectable gate INVERTED (ASM bgezl 38072 proceeds iff flags&0x01000000 CLEAR; C 37467 proceeds iff SET); (D4) armed-path INVINCIBLE 0x20000 early-out (bltzl 38079) dropped -> timer=0/chain-flag side effects on invincible explosives; (D5) type-7/8 non-matching cases return in retail (38102/38128 -> bare epilogue) but fall through to objIsMortal+damage in C. See FID-0074. |
| 21 | `sub_GAME_7F08AAE8` | src/game/bondview.c | `verified-equivalent` | [US variant audited] Upper-text HUD window + debug manpos line renderer. ASM 23224-23622 vs C 23057-23215, traced fully: 151-byte stringbuffer stride via sll/addu/sll/subu/sll3/subu chain (23245-23252) = stringbuffer_top[2][0x97]; four early gates bnez/bltz/beqz/bnez all -> debug path (23235-23257) = C 23081-23093 (upper_text_window_timer s32 so bltz==signed <0); cameraBufferToggle two-arm layout incl signed (top+0x20)/11*11-2 (23288-23322) = C 23105-23110; main menus call y-2 vs debug path y2 undecremented + bottom+1 (23331-23338 / 23481-23490 = C 23115/23177, spot-verified); s16 trunc of viGetX via sh/lh delay-slot store (23342/23353) = (s16) cast; all 6 textRender 11-arg stacks matched 1:1; compass trunc-toward-zero *180/pi (late_rodata 0x40490fdb) + (deg+0x16)/45 idx*3 (23381-23464) = C 23137-23170; 5 rodata format strings byte-verified. Benign: 27- vs 30-byte compass table copy (max reachable idx 8 -> byte 26), snprintf bounds, NATIVE_PORT tile NULL guard. No branch-likely hazards; no jump table. |
| 22 | `sub_GAME_7F088CD8` | src/game/bondview.c | `verified-equivalent` | End-credits scroll renderer (Cradle stage 54). ASM 20824-21186 vs live C 20597-20822 (NONMATCHING guard), traced fully: three early-outs stage!=54 / credits_state!=1 (bnel w/ delay lw ra only-when-taken) / credits_pointer NULL (20840-20852) = C 20618-20628, all return original gdl pre-microcode_constructor; signed div-by-16 with +15 negative bias + endrow+1 + startrow<0 clamp (20868-20883) = C 20642-20656; skip-loop with mid-loop i++ in always-executed delay slot (20915-20916) + both-TextId-zero exit setting credits_state=2 (20891-20899) = C 20663-20691 (CreditsEntry stride 0xC); lhu u16 TextId1/2 vs 0x5011, lh s16 Position1/2 bltzl-skip, Alignment via (s16) cast reproducing lh (20889-21068) = C 20672-20723; alignment grouping ==1 right / ==2 center (sra 1) / default left in both text blocks (20997-21016/21099-21118, spot-verified) = C 20733-20742/20779-20788; menus call ypos-1 only on the register copy, textRender gets undecremented ypos, s16 viewheight trunc per block (20972-21086) = C 20726-20773; render-loop entry/exit incl TextId1==0/TextId2!=0 langGet(0) quirk preserved; final combiner_bayer_lod_perspective from all four exits (21167-21170) = C 20818. No jump table; single caller maybe_mp_interface. |
| 23 | `sub_GAME_7F04AC20` | src/game/chrobjhandler.c | `verified-equivalent` | Recursive per-prop DL builder (monitor microcode, door cull/env, destroyed fade, portal projection swaps, impact decals, subdraw + child recursion). ASM 32123-32488 vs C 31743-32146: entry flags&2 beql early-out (32157-32161 = C 31785, spot-verified); portal double test w/ field_10E0 re-derive (32166-32174 = C 31797-31802); monitor arg4 selection incl screen-1 0x10000000 vs screen-2 0x30000000 mask difference (32190-32270 = C 31831-31923, spot-verified) and screens 3-4 reusing screen-2 arg4; Switches[0..3] to +0x80/+0xF4/+0x168/+0x1DC stride 0x74; needsAnimation rodata Vertices-vs-rwdata compare via u16 RwDataIndex (32345-32360 = C 32036-32049); destroyed-level double call + cullmode 3/1 stores in non-likely delay slots (32368-32391 = C 32054-32066); alpha x50+0x64 strength-reduced clamp 255 (32392-32409 = C 32070-32078); door pre/post subdraw B6000000/3000 emits + doorFlags&8 (32314-32340/32436-32446 = C 32012-32029/32101-32109); bullet-impact gate lbu obj+2 & 1<<pane (32448-32457 = C 32111); child chain 0x20/0x24 recursion (32476-32488 = C 32126-32132); tail transform count lh +0xE (32491-32506 = C 32134-32146). NATIVE_PORT deviations documented as substrate/guard family: glass portal-projection skip (C 31804-31819, commented native camera-space matrix adaptation — faithful transcription would double-apply the camera basis on native; renderer-surface, pixel-verifiable if ever suspected), G_MTX_FLOAT_PORT marker, modelGetSwitchNodeSafe/monitorFindDrawableNode identity-on-valid-data resolution. |
| 24 | `microcode_generation_ammo_related` | src/game/gun.c | `finding-filed:FID-0067` | NATIVE_PORT rewrite (C 31196-31242) faithful in GBI plumbing but position/flip math diverges from ASM 31418-31716 while callers pass retail values: y>=0 anchor y-h*0.5 (ASM 31513-31525) vs y; y<0 anchor H+S-(h*0.5-(h>>1)) (31536-31552) vs H+S-h*0.5 (floor(h/2) px shift); retail flipX slot constant 0 (sw zero 0x18 31669, incoming flipX only negates subpixel frac 31502-31505) vs port passes flipX through -> left dual-wield icon mirrored; minor: dropped +/-fracW x term + forced G_TF_POINT. Guard census clean (zero retail early-outs). See FID-0067. |
| 25 | `set_enviro_fog_for_items_in_solo_watch_menu` | src/game/gun.c | `finding-filed:FID-0073` | Watch-menu item renderer, ASM 15779-16118 vs C 15578-15790. Divergence: phantom per-frame save/restore of hands[GUNRIGHT].weaponnum_watchmenu (C 15619 + done: 15782-15787 calling 7F05DA8C/7F05DAE4) with no ASM counterpart -- retail sets the item once (single jal 7F05DA8C a0=0 @15829, {30,23}->60 remap 15821-15827) and leaves it pinned between frames (teardown elsewhere); zero jal sub_GAME_7F05DAE4 in any GLOBAL_ASM repo-wide. State-visible: bondview raise/lower model-swap consumers see equipped weapon instead of menu item while browsing. Rest fully equivalent: guard senses/order incl bitflag 0x4000, renderdata template, render_pos-before-modelInit ordering, revolver skeleton compare + slots 4/5, shell loop 18/23+j, lights item set + GBI words + dual 7F078474 calls, alpha>=255 PropType split, f32->s32 loop. See FID-0073. |
| 26 | `sub_GAME_7F04EA68` | src/game/chrobjhandler.c | `finding-filed:FID-0069` | Object-hit shot handler ASM 39219-39554 vs C 38959-39247. Impact-FX suppression gate on WRONG WEAPON ID: retail li $at,23 + beq on shotdata->weapon (39354-39356) = ITEM_WATCHLASER (enum pos verified, ITEM_TRIGGER=30 anchor); C 39069 gates on ITEM_TASER (31) -> watch laser spawns impact FX + extra randomGetNext (RNG desync); corroborated by chrprop.c:3040 using ITEM_WATCHLASER for the identical construct. Rest verified equivalent (ancestor walk, 26.0f impact math, material table stride-8 hitSound&0xf, door/cctv grouping, damage scaling, bounce flag senses). See FID-0069. |
| 27 | `maybe_mp_interface` | src/game/bondview.c | `finding-filed:FID-0070` | [LEFTOVERDEBUG variant @7F089208] HUD/interface frame renderer, audited first-hand line-by-line vs C 21194-21406. Divergence: retail calls ONLY bondviewGetIfCurrentPlayerHealthShowTime for the health-bar draw gate (jal 21486; target 0FC227AF decodes to 0x7F089EBC, distinct from DamageShowTime 0x7F089EA4; sub_GAME_7F088618 jal 0FC22186 decodes exactly to 0x7F088618 confirming symbol fidelity) + watch_animation_state==0; C 21253 ORs in DamageShowTime (damageshowtime>=0, active during damage flash) -> port draws health/armour bars during damage flashes retail gates behind sub_GAME_7F0C6048, and skips the healthdisplaytime decrement (C 21257-21268 = ASM 21499-21521) in that window. All else equivalent: intro-camera path, mpmenu rect s16 truncs, death/music/missionstate sequence, ClockTimer 1:2 pick, c.le.s field28 gate, kill_counts stride-0x70 loop, scenario!=1\|\|kills<2, joyGetButtons 0xB000, HUD tail order. See FID-0070. |
| 28 | `maybe_mp_interface` | src/game/bondview.c | `finding-filed:FID-0070` | [retail US variant @7F089370] Same structure as the LEFTOVERDEBUG variant, audited first-hand: single HealthShowTime call (jal 21816, word 0FC2281C -> 0x7F08A070 US) + lw 0x1c8 watch gate (21818-21822); no DamageShowTime reference anywhere in the block (regional struct shifts only: mpmenuon 0x29bc vs 0x29c4, healthdisplaytime 0x29f8 vs 0x2a00). C 21253 ORs in DamageShowTime -> same divergence. See FID-0070. |
| 29 | `sub_GAME_7F042A0C` | src/game/chrobjhandler.c | `verified-equivalent` | Projectile/prop slide-to-stan resolver. ASM chrobjhandler.c:6101-6418 vs live C 5966-6099, traced fully: early-outs dest==pos.xyz -> 1 and !(runtime_bitflags&0x80) -> 1; heights branch senses (proj flags&0x40 bnezl; destY<posY c.lt.s swap -> height1=max-side/height2=min-side, else 0/1); both stanTestLineUnobstructed calls (a3=destX/newX, 0x10=destZ/newZ, 31, h1,h2, 0,1) + stan!=NULL + stanTestVolume<0 (bgez skip) senses; first success block returns result=1 (0xb0 untouched) w/ pos+runtime_pos update = C 6029-6037; fail path getCollisionEdge(A@sp+0x80,B@sp+0x74), arg3=(B.z-A.z, 0, A.x-B.x) w/ zero->z=1 else guNormalize; delta path chrlvStanLineDirIntersection + calculateNormalizedLineIntersection(linePoint{width,pos.x,pos.z}, A.xz, B.xz, dir{dx,dz}) then newX/Z = pos + dir*intersect*0.99000001 (D_80052A5C) and pos.y=savedY(=destY)/result=0 tail. The arg2-all-equal branch (E7C) is unreachable in both (top equality check already returned); retail reads uninit savedY there vs C 0.0f — dead code both sides. |
| 30 | `collect_or_interact_object` | src/game/chrobjhandler.c | `verified-equivalent` | [US variant] Pickup/interact dispatcher. Retail dispatch = object_interaction_table fn-pointer table idx=type-3, bounds sltiu<0x2d (chrobjhandler.c:40910-40919): idx1=key(type4) idx4=magazine(7) idx5=weapon(8) idx17=ammobox(20) idx18=bodyarmor(21) else default — maps 1:1 onto live C switch (PROPDEF enum verified bondconstants.h:3436). Entry gate bonddead\|\|!g_ClockTimer -> 0 = C 40616. Handlers traced: key sfx 0xE5 + ActivatedText\|\|langGet(0xA43C) -> v1=4; magazine add_ammo(lw 0x80 ammoType, get_ammo_in_magazine, 1, show) -> v1=1; ammobox 13-slot loop lhu 0x82 stride 4 w/ i==1->9mm (AMMO enum verified), getPlayerCount==1 solo multiplier trunc, sfx 0xEA -> v1=1 (= corrected FID-landed loop); weapon: 29->AddInvItem(30), 88->EquipWrapper(0,88), DESTROYED 0x400 branch w/ weaponnum<33 AddWeaponByProp + pass=1 text block -> v1=4, else AddWeaponByProp!=0 pass=1 + ActivatedTextWeapon/else-pass display -> v1=1, ammo tail (type!=0, collected>0, cur<max slt sense, give=cur+collected, (pass==0&&show) display); armour amount lwc1 0x84 + sfx 0x51 + playercount<3 text pick -> v1=1; default sfx 0xE5 + 0xA43F -> v1=4. Tail: v1==1 && !(runtime_bitflags&0x10) -> objFree(obj,0,state@2 &4) return 1(INV_ITEM_WEAPON); v1!=0 -> AddPropToInv return 4(INV_ITEM_PICKUP); else 0 — INV enum verified 3333-3340. |
| 31 | `pickDeathCameraAngles` | src/game/bondview.c | `verified-equivalent` | Death-camera position sampler (129 angle-seeds x 16 sectors x 4 scale steps). ASM bondview.c:5458-5761 vs live C 5306-5443, traced fully: RANDOMFRAC = u32 randomGetNext w/ +2^32 bgez correction x 2^-32 (all 3 sites); angle += 0.39269909 w/ >=tau wrap (c.le.s f20,f12); dir=sin/0/cos; candidate = dir*dist + srcPos; per-probe tile=stan(arg5@0xf0) + 7F03D058(prop,0/1) bracket around stanTestLineUnobstructed(&tile, target.xz, cand.xz, 19, 0,1,0,1); lineResult==0 -> chrlvStanPointPointIntersection + sqrt recompute of distance; distance-=radius then distance<minDist(0xf4) continue sense (c.le.s minDist,dist bc1fl); rangeDiff=dist-minDist; randDist=(frac*rangeDiff)*scale+minDist; bss pos = dir*randDist+src; line test again + stanTestVolume(&tile,bss.xz,radius,31,0,1) bgez reject; y = frac*(185-radius)+(floorY+radius); yDiff<=-1000 reject (c.lt.s -1000,yDiff bc1f), yDiff<1000 -> found=1; scale-=0.25 loop while 0<scale && !found; counters fp<16 / outer<0x81 both w/ found exits; collision_radius reloaded from player+0x4b0 per outer iter (loop-invariant, C hoists — equivalent). Return found; found path skips further RNG identically both sides. |
| 32 | `sub_GAME_7F0431E4` | src/game/chrobjhandler.c | `finding-filed:FID-0078` | Projectile release/spin setup. Fully traced ASM chrobjhandler.c:6750-7048 vs C 6573-6733: ~0x10000 clear + 0x80 gate, type==1 projectileFree, ownerprop@0x88=NULL + flags &~1 &~4, matrix/quat chain (get_rot -> set_rot(mtx1@0xcc) -> quat(unk68) -> inverse(mtx2@0x8c) -> multiply(mtx3@0x4c)), unkC0/C4/C8 row magnitudes (outer-add commutative swaps only, IEEE-exact), dirX/dirZ zero-branch + normalize, mtx2 rows incl 0<=m11 sign branch, quat(unk78) + ensure_shortest_path, dot(m2 row0 . m1 row0), both 0.05/(theta*4/tau) gate chains w/ senses verified, theta==0 -> unk64=1.0, \|.\|/clamp [0.03,0.15] tail. ONE divergence: acos-operand sum association a+(b+c) vs C (a+b)+c -> FID-0078. |
| 33 | `gunFireTankShell` | src/game/gun.c | `verified-equivalent` | [US/JP block] Tank-shell / chrrocket spawn. ASM gun.c:5513-5815 vs live C 5382-5509, traced fully: hand stride 0x3A8 -> player+0x870; weapon==32 (ITEM_TANKSHELLS) branch w/ tank flags&2 (byte@1) dual check — bondviewSet3dCoord7F07CEB0 vs 7F068190+RotateVec; velocity = dir*66.666664 (+ (pos-prev)/delta when g_ClockTimer>0, register pairs re-verified pos-minus-prev in BOTH branches); tank position = model->render_pos[4].pos.m[3][x/y/z] (+0x130/134/138) + TransformVec, else player pos; setSixExplosionAndSmokeEntries. Else branch: bullet_path_from_screen_center(origin,dir,hand), pos=hand->field_B58(+0x2e8), base=dir*1.111111, vel=base*delta (+ pos-prev correction). Common tail: matrix_4x4_copy(hands[hand].throw_item_pos_related@+0xad8), m[3][0..2]=0, rocket reuse (firedrocket=1) vs create(202=PROP_CHRROCKET, 86=ITEM_NULL86), &~0x60000 owner + timer@0x82=-1 + playernum<<17 (RUNTIMEBITFLAG/SHIFT_OWNER verified bondconstants.h:613-614), sub_GAME_7F05EC1C(obj,&pos,&mtx,&vel,&identity), DEPOSIT 0x80 gate -> proj flags\|=0x80, weapon!=32 -> \|=0x20 + unkB0=runtime_pos.y(0x5c) + unkB4=speed.y + unk10=base + refreshrate=60 + sound1(0x98)/sound2(0x9c) else-if sndPlaySfx(bank,1,&slot). Only deviation: C adds g_GlobalTimerDelta!=0 guard on the (pos-prev)/delta correction (both branches) with no ASM counterpart — additive div-by-zero hardening, unreachable-differing while delta>0 whenever g_ClockTimer>0. |
| 34 | `sub_GAME_7F062BE4` | src/game/gun.c | `finding-filed:FID-0077` | [US/JP block] FP-weapon per-hand DL builder (2-hand loop, stride 0x3A8). Retail traced ASM gun.c:15010-15292 vs C 14738-15006: field_87F gate (lb +0xf), WATCHLASER 23 head-skip/tail-emit of 7F061E18(&field_A54@+0x1e4), lights group {19,18,2,3,20,21} 5-cmd GBI block (BC000002/80000040 NumLights1, 0386@+8/0388@+0 D_80032440 lights, 0384/0382 LookAtX/Y via 7F078474()+0/+0x10), numSwitches>=17 (lh 0xc of field_B70) switch-array[16]!=NULL gate + rwdata[switch17]=1, ROCKETLAUNCH 25 -> crosshair img + monitor-microcode(arg 0,4) else taser(0,1), renderdata PropType=4/envcolour r<<24\|g<<16\|b<<8\|a (0xfdc-0xfdf)/zbuffer=0/gdl sync, 7F058C64, rocket subdraw + TransformManyPos(render_pos, header+0xe count) + firedrocket clear, mirror-dual 0x400 B6/3000 + cullmode i==0?3:2 both sides of subdraw, 7F058C88, BC00000E viGetPerspNorm tail, loop i!=2, *arg0=gdl. TWO divergences (perspnorm arg 0.0->1.0 => word 436->435; perspnorm emitted before monitor-microcode in retail, after in C) -> FID-0077. Port-substrate deviations documented in-code (added weapon projection matrix behind g_pcViewmodelFov/portSkipFpWeaponProjection + gdl-sync bugfix ordering, DL-region registration, field_B68 pointer-vs-embedded layout, portGetWeaponSwitchNode w/ additive switch17 NULL guard, portSkipFpWeaponRender hooks). |
| 35 | `sub_GAME_7F041400` | src/game/chrobjhandler.c | `verified-equivalent` | Prop collision-bounds line intersect (projectile-vs-prop edge pick). ASM chrobjhandler.c:3978-4242 vs live C 3862-3975, traced fully: chraiGetCollisionBounds out-params mapped (0xd4 vertices/0xd0 numverts/0xcc ymax/0xc8 ymin, usage-verified); numverts<=0 -> 0; Y-band rejects (ymax<y1&&ymax<y2, y1<ymin&&y2<ymin) senses verified; edge loop (i+1)%numverts w/ 8-byte vertex pairs, 7F0B0688(line, v[i], v[next]) gate, calculateLineIntersectionFactor(&start1@0xa0=arg1.xz, &end1@0x98=arg2.xz, edge) w/ dot<best (c.lt.s f0,f20) best_edge=i; best_edge<=0 -> 0 (blez — edge 0 never selectable, quirk preserved in C); chrlvLineLineIntersection(p1{v[be],0,v[be+1]}, p2{v[next2]}, arg1, arg3, &result); dot = (r-a1).x*n.x + (r-a1).y*n.y + n.z*(r-a1).z (outer add commutative swap only); !(dot<*arg6) -> 0; *arg6=dot, arg4=result, arg5=(-n.x,0,-n.z) w/ zero->z=1 else guNormalize; D_80030B0C=prop, bodypartshot=0, g_CurrentProjectileModel=0, bss_80075B74=0, return 1. div-by-zero break guards are compiler artifacts. |
| 36 | `sub_GAME_7F0AF20C` | src/game/stan.c | `finding-filed:FID-0079` | Highest-floor tile-at-position finder. ASM stan.c:949-1203 vs live NATIVE_PORT C 708-822 (F2 gated-ON faithful path), traced fully: scale+clamp-Y(32767 senses)+trunc s16 triple; room loop w/ firststaninroom NULL skip and 5 bbox slt rejects (x vs [0]/[3], z vs [2]/[5], y vs [1]) all senses verified; tile walk *tile!=0 && room byte@3 match; 3-edge getShortest2dDispToInfTripleEdge(tile,e,sx,sz) loop w/ disp<-2 tile-skip (bc1tl) and disp<2 sticky on-edge; 7F0AF760 degenerate skip; on-edge midpoint walk-back (getTileMidPoint -> walkTilesBetweenPoints_NoCallback(&walkTile, mid.xz, pos[0]/pos[2])) w/ fail-or-different-tile reject; highest-floor pick tile_y<=pos[1] && best<tile_y; advance via (tail>>12)&0xF size table; *out_dist=best_y when found. TWO divergences: (1) roomset filter element width u8-vs-s32 -> FID-0079 (live defect, P2); (2) retail on-edge flag seeded from uninit sp+0x88 (lw s2 @1026) -> FID-0081 (n64-quirk, port zero-init). |
| 37 | `sub_GAME_7F07E090` | src/game/bondview.c | `verified-equivalent` | Watch raise/lower vv_verta transition + speedverta. ASM bondview.c:9232-9435 vs C 9145-9219, traced fully: state 1 blends 0x208(pause_related)->0x20c(pause_target_angle), state 2 blends 0x20c->0x208, both via blend=(1-cosf(frac*6.2831855*0.5))*0.5 w/ field_210 += delta*watch_transition_time and field_210<field_214 sense (bc1fl), completion writes target + state 3/0; operand mapping cross-checked via completion stores (case1 ends at 0x20c, case2 at 0x208) — consistent both cases. Wrap senses c.lt.s(-180) / c.le.s(180<=v); g_ClockTimer>0 gate (blez); speedverta = new-old, <0 +=360, 180<v -=360, /= (delta+delta), clamp [-0.69999999, 0.69999999] exact rodata constants. No jump table; no early-outs beyond state dispatch. |
| 38 | `sub_GAME_7F0B1DDC` | src/game/stan.c | `verified-equivalent` | Tile-radius BFS locus search. ASM stan.c:4949-5155 vs live NATIVE_PORT C 4791-4930, traced fully: scaled x/z/radius; BFS queue at sp+0xb0 seeded *arg0, readIdx(0xa4)<depth(s4) loop; callback_A(curTile, cbRecord) per visit; pointCount (tail>>12)&0xF blez skip; per edge: getShortest2dDispToInfTileEdge->f22, distToTilePnt2D(i)->f20, distToTilePnt2D((i+1)%pc)->f24, qualify = edgeDist<r && (p0<r \|\| p1<r \|\| 7F0B0198!=0) — all senses verified; callback_B(tile, edge, edgeDist, distPt0, distPt1@0x10, cbRecord) nonzero -> stanSavedColl_tile/pointI + callback_C(stack, depth, rec)==1 continue else return 2; link@points[i].link (lhu 0xe+8i) >>4 nonzero -> dup-scan top-down then push standTileStart+link*8, ==0 -> same collision path; depth>=0x29(41) -> return 5 after each tile; exhausted -> callback_C + return -2. Port deviations additive-only: per-push depth<41 bounds guard (retail pushes unguarded past its 40-slot region — caller-frame UB), env-gated trace print. |
| 39 | `sub_GAME_7F088618` | src/game/bondview.c | `finding-filed:FID-0080` | [LEFTOVERDEBUG variant] HUD health/armour bar overlay DL builder. Retail stream re-derived first-hand (see FID-0080 note): 7F0A2F30/3330 pairs w/ uncached aliases, guOrtho extents x D_800364CC, fixed-point proj+modelview loads, E7/BA-1CYCLE/B9-AA_XLU/B9000002-alphacompare-off/FC-SHADE/FA-primcolor/B6-CULL_BOTH, two 06 DL calls (player+0x19f8/0x2128), projection-only restore. Live NATIVE_PORT rewrite diverges in ortho scale, alpha-compare omission, geometry-mode family, extra modelview restore, float-matrix substrate -> FID-0080 (parity-divergence, pixel parity vs ares unverified). |
| 40 | `sub_GAME_7F088618` | src/game/bondview.c | `finding-filed:FID-0080` | [retail US variant @7F088780] Same structure as the LEFTOVERDEBUG variant, independently spot-verified: identical command sequence and constants, regional struct offset shifts only (armor vals 0x12b8->0x12b0, health 0x1598->0x1590, DLs 0x19f8->0x19f0 / 0x2128->0x2120, g_CurrentPlayer 8008A0B0->80078BC0). Same divergence set vs live NATIVE_PORT rewrite -> FID-0080. |
| 41 | `sub_GAME_7F0B0914` | src/game/stan.c | `finding-filed:FID-0081` | walkTilesBetweenPoints_withCallback (tile line walker). ASM stan.c:3423-3620 vs live NATIVE_PORT C 3264-3415, traced fully: scaled endpoints, zDist=-(dz2-dz1)/xDist=dx2-dx1; per-iteration func(curTile, prevTile, funcData); edge loop w/ (i+1)%pc, edge_cross=(npt_z-pt_z)*xDist + zDist*(npt_x-pt_x) exact operand order, c.le.s <=0 gate, 7F0B07BC(start,dest,pt,npt,(link>>4)!=0); on hit crossCount++ then prev/prevprev backtrack rejects (beq s6/s7), lastCrossEdge=i in delay slot (always on accepted-cross path, matches C unconditional assign), nextTile=standTileStart+link*8 or NULL by link>>4; tile shift prevprev=prev/prev=cur/cur=next w/ oldCur==next -> crossCount=0; crossCount==0 -> return TRUE (*tileStack==prevTile via prior continue store — C's explicit store is state-identical); pre-increment iterCount<0x1F5 cap (501 steps, C comment documents the delay-slot increment semantics) and nextTile==NULL -> savedColl tile/pointI + return FALSE; continue stores *tileStack=nextTile, crossCount=0, edgeIdx=0 (delay slot). ONE divergence: retail lastCrossEdge seeded from uninit sp+0x98 (lw fp @3463) -> FID-0081 (n64-quirk; port zero-init). |
| 42 | `sub_GAME_7F07B2A0` | src/game/bondview.c | `verified-equivalent` | Intro-swirl spline point sampler. ASM bondview.c:6663-6855 vs live C 6582-6659 (NONMATCHING), traced fully: t=timer/unk18 gated 0.0<unk18 (c.lt.s + bc1f, div only when true, f2 zero-init at entry); 4-iter loop i=-1..2: i<0 clamp cur=max(base,target) via sltu pointer compare; i>=0 cur<target walk while !(cur[1].unk04&1) (lw 0x24, bnezl exit) w/ cur++ then cur>=target break — senses/order match C exactly; unk04&2 rotated path points[i+1] = (tt.f[2]*unk08 + unk10*tt.f[0], unk0C, unk10*tt.f[2] - tt.f[0]*unk08) operand order matched, else plain unk08/0C/10 copy; call sub_GAME_7F05B024(&points[0..3], t, entry->unk14, pos) stack args 0x10/0x14/0x18 verified; pos += field_3C4/8/C; pos2 = field_3C4/8/C; 4-way scale select on unk04&4 of entry/entry[1] (0/t/1-t/1, all four bnezl/beql senses verified, dead mov.s in branch shadow); pos2 += applied_view.f[i]*40*scale. OFFSET NOTE: header doc comments in struct collision434 are STALE (+4 from pos3 on); declared layout (tt@0x10, radius@0x28, applied_view@0x38 -> player 0x498/0x4B0/0x4C0) matches ASM and the batch-4 pickDeathCameraAngles 0x4b0 anchor — C field names resolve to the exact ASM offsets. No jump table; no early-outs; no uninit locals. |
| 43 | `sub_GAME_7F0B1410` | src/game/stan.c | `verified-equivalent` | stanLineDoor: prop line-intersect picker over door/prop cd types. ASM stan.c:4274-4457 vs live C 4185-4269 (NATIVE_PORT real body; PORT_FIXME_STUBS non-native stub not live), traced fully: sub_GAME_7F0B0C24(&tileLocal(sp+0xf0), sx, sz, dx, dz, roomBuffer(sp+0x98), &spEC(sp+0xec)=0, 20) stack args 0x10-0x1c verified; spEC>=21 clamp to 20 (slti/bnezl, DEBUG print release-omitted); lineStart{sx,sz}@0x118 / lineDest{dx,dz}@0x110 stored BEFORE the cdtypes==0 early-out (beqz 0x13c arg) returning fp=NULL — order matches C; roomBuffer[spEC]=-1 + roomGetProps after the gate; spLookup walk (lh, bltzl/bgezl >=0 senses, +2 stride, spill 0x94); prop = pos_data_entry + idx*52 (0x34 stride via sll/subu/sll/addu/sll chain); propIsOfCdType gate beqz; chraiGetCollisionBoundsWithoutY(&polygon 0x90, &numverts 0x8c); numverts blezl guard (+dead second blez, compiler artifact); inner loop next=(i+1)%numverts (div/mfhi w/ break-guard artifacts), sub_GAME_7F0B0688(sx,sz,dx,dz, pts[i].xz@0x10/0x14, pts[next].xz@0x18/0x1c) beql skip; hit path edgeStart=pts[i]@0x100 edgeEnd=pts[next]@0xf8, calculateLineIntersectionFactor(s4 lineStart, s5 lineDest, s6 edgeStart, s7 edgeEnd), c.lt.s f0<bestFactor(f20 init 1.0) bc1fl skip else best=f0/fp=prop; i<numverts slt/bnezl; return v0=fp. Uninit-local sweep clean (every lw'd slot stored in prologue, by caller arg slots 0x138/0x13c, or by callee out-params). No jump table. |
| 44 | `sub_GAME_7F041160` | src/game/chrobjhandler.c | `verified-equivalent` | Object hit resolver (ray vs obj model -> world hit pos + normal). ASM chrobjhandler.c:3675-3852 vs live C 3597-3671 (NONMATCHING), traced fully: surface_node(0x9c)/surface_id(0x64) zero-init at prologue; type byte lbu 3(a0)==1 -> sub_GAME_7F0747D0(arg0+0xD0, model->render_pos(+0xc), arg1, arg2) + surface_node=model->obj->RootNode stored in non-likely blez delay slot (== C assign before if) + result>0 gate then sub_GAME_7F04D9B0(model, RootNode, arg1, arg2, &hitthing@0x6c, &mtx_index@0x68, &surface_id@0x64; stack args 0x10/0x14/0x18) fail -> result=0; else do{7F0752FC(model,arg1,arg2,&surface_node)}while(result>0) with D9B0-success break (bnez/bgtzl senses verified, same &surface_node each iter); result<=0 -> return 0 (v0 zeroed in delay, overwritten by li 1 on success tail); arg3=hitthing.unk00, bone_mtx=render_pos+mtx_index*64 (sll 6), TransformVecInPlace bone then Matrix10D4; arg4=hitthing.unk0c, RotateVecInPlace bone; dot = nz*z + (nx*x + ny*y) vs C (nx*x+ny*y)+nz*z — single commutative add + one commutative mul swap, IEEE-identical; 0<dot -> negate (bc1f sense ok); RotateVecInPlace 10D4; all-zero -> z=1.0 (three c.eq.s + bc1tl) else guNormalize(&x,&y,&z); *arg5=model, *arg6=surface_id, return 1. mtx_index read only on D9B0-success paths, identical exposure in C. No jump table. |
| 45 | `sub_GAME_7F04E720` | src/game/chrobjhandler.c | `finding-filed:FID-0083` | Recursive object shot-test dispatcher. ASM chrobjhandler.c:38686-38867 vs live C 38470-38681, traced fully: runtime_bitflags&0x1000 (bnezl) + prop->flags&2 offscreen (lbu 1(a0), beql) early-outs; child chain recursion via child->prev (lw 0x24) matches; node=NULL init 0xa8; type==1 door path gated (doorFlags&4 lhu 0x98 \|\| doorType 6/7 lhu 0x9a) -> sub_GAME_7F0747D0(&door->bbox +0xd0, render_pos, &screen_pos(shotdata+0), &screen_dir(+0xc)) + node=RootNode in non-likely delay slot + 7F04D9B0(...,&hitthing@0x78,&mtx_index@0x74,&hit_node@0x60) fail->hr=0; generic do{7F0752FC}while(hr>0) w/ D9B0 break (bgtzl); hr<=0 return; transformed=hitthing.unk00 -> TransformVecInPlace(render_pos+mtx*64); DEPTH GATE c.le.s -z<=unk34 bc1fl return = retail reject — LIVE C ADDS default-ON NATIVE_PORT glass tolerance accept (default 2.0) -> FID-0083; do_damage: flags&0x20000 (sll 14/bltzl sign test) skip, type 42/47 ->0, skeleton_door && hit_node==file_hdr->Switches[3] (*(*(hdr+8)+0xc), port modelGetSwitchNodeSafe additive guard) ->0; tail sub_GAME_7F03B9C0(shotdata,prop,neg_z,hr,node,&hitthing,mtx,hit_node,model,do_damage,(flags2&0x100000)!=0 sltu) stack args 0x10-0x28 verified. objectTrace* calls = port instrumentation (env-gated, behavior-neutral). ShotData screen_pos@0/screen_dir@0xc verified vs header. Rest retail-equivalent. |
| 46 | `sub_GAME_7F06A334` | src/game/gun.c | `finding-filed:FID-0084` | [US variant audited; EU variant separate queue entry] Watch ammo panel renderer. Retail ASM gun.c:33452-33619 vs live NATIVE_PORT C 33214-33313: retail early-outs = rightWeapon==0, ammoType==0, hands[GUNRIGHT].when_detonating_mines_is_0==6/7 (lw 0x894), HIDE_AMMO_DISPLAY bitflag; port drops the state-6/7 (weapon-switch animation) gate -> FID-0084. Everything else equivalent: ammo_related[ammoType] stride 12; icon draw set_rgba_redirect_generate_microcode(gdl, image, 200.0f, 180.0f, (f32)(viGetViewTop()+viGetViewHeight()-20), 0, stats+8 f32, 1) args verified vs stack slots 0x10-0x1c; iconWidth = lbu image+4 else 5; microcode_constructor; NO_CLIP_RELOADS 0x400000 split (clip=0 & reserve=0x89C+ammoheldarr[0x1130+4*type] +0xC44 when left==right / clip=0x89C reserve=ammoheldarr); clip draw gated !noClipReloads at x=196-(w/2) HUDHALIGN_RIGHT y=177 HUDVALIGN_MIDDLE; reserve draw gated reserve>0\|\|noClipReloads at x=((w+1)/2)+203 HUDHALIGN_LEFT (bgtzl + re-call bitflags senses verified; a1=0x400000 preserved via delay-slot lui on both paths); combiner_bayer_lod_perspective tail. Enums HIDE=1<<19/NOCLIP=1<<22/HALIGN R0 L1/VALIGN M2 verified vs bondconstants.h. Port helpers (portGetAmmoImage compiled globals, yoffset bit-reconstruction of stats field_08/field_0A, placeholder, validation) = substrate + env-gated diagnostics; dropped retail dead get_ptr_item_statistics call (pure, result unused). |
| 47 | `seems_to_load_cuff_microcode` | src/game/bondview.c | `verified-equivalent` | Bond cuff/outfit switch-visibility setter (6 consecutive switch nodes from base index arg2, callers pass 29). ASM bondview.c:7F087C14 (17186-17346) vs live C 16987-17024 (PORT_FIXME_STUBS port body w/ portSetCuffSwitchVisible), traced fully: per node k=0..5 retail gates on hdr->Switches[arg2+k]!=0 (Switches = ptr array at hdr+8, lw 8 + idx*4) then modelGetNodeRwData(model,node) word-store of visibility — port modelGetSwitchNodeSafe+rwdata NULL guards reproduce the same skip semantics (additive on valid data). Visibility predicates all verified vs xori/sltiu chains against player->bondtype (0x41c, offset verified via header 0x408..0x420 run): k0 ==3; k1 ==1\|\|==6\|\|==7\|\|(==8 && folder!=1) (bnezl store-1 chain + sltu 0<(folder^1)); k2 ==5\|\|(==8 && folder==1); k3 ==0; k4 ==2; k5 ==4. fileGetBondForCurrentFolder captured once pre-loop both sides. Retail reloads bondtype per block, port hoists — no interleaved mutation. Retail return = garbage v0 (last rwdata ptr); port returns 0/1 — all 3 callers (bondview.c:17485, gun.c:3914/6919) discard it. GE007_DISABLE_CUFF_MICROCODE early-return = env-gated diagnostic, default-off. No jump table. |
| 48 | `sub_GAME_7F041BB8` | src/game/chrobjhandler.c | `verified-equivalent` | Chr prop projectile/shot line intersect (dot-window + body-part hit + best-depth pick). ASM chrobjhandler.c:4731-4885 vs live C 4644-4726 (NATIVE_PORT), traced fully: prop=chr+0x18, getinstsize(chr->model +0x1c); dot = arg2.(prop->pos(+8/0xc/0x10) - arg1) with z+(x+y) vs C (x+y)+z single commutative add (IEEE-identical); reject senses exact on finite data: bc1fl !(-instsize<=dot) == dot<-instsize, bc1fl !(dot<=arg3+instsize) == dot>arg3+instsize (unordered/NaN would diverge retail-reject vs port-proceed — unreachable for finite sim positions); prop->flags&2 (lbu 1) gate; sub_GAME_7F06C010(&local_head=chr->field_20(0x20), arg4, arg5, &local_model@0x40, &local_node@0x3c) — retail passes UNINITIALIZED out-slots, port zero-inits (strictly safer; slots only read after hit_result>0 where callee fills them); blez reject; modelFindNodeMtx(model,node,0) + port-only NULL dyn-overflow guard (documented additive); dot2 = arg5.(mtx->m[3] components at +0x30/34/38 - arg4) same add shape; bc1fl !(dot2<*arg8) == dot2>=*arg8 reject; *arg8=dot2; arg6 = arg2*dot2+arg1 per component; arg7 = (-arg2.x, 0, -arg2.z) w/ x==0&&z==0 -> z=1.0 (c.eq pair + bc1tl) else guNormalize(&x,&y,&z); globals D_80030B0C=prop, bodypartshot=hit_result, g_CurrentProjectileModel=local_model, bss_80075B74=local_node; return 1. No jump table. |
| 49 | `set_color_shading_from_tile` | src/game/chrobjhandler.c | `verified-equivalent` | Prop shadow tint from stan tile color. ASM chrobjhandler.c:2722-2866 vs live C 2647-2718 (NONMATCHING), traced fully: copy_tile_RGB_as_24bit(prop->stan(0x14), pos.x(8), pos.z(0x10), color); luminance strength-reduction chains re-derived (5b*4+b=21b, 5r*16-r=79r, ((5g)*8-g)*4=156g) = C (r*79+g*156+b*21)>>8; alpha = trunc((f32)(255-lum)*0.75) incl FCSR round-mode-1 trunc expansion = C (u8) cast; max/min seed r<g slt sense; three-way b placement verified branch-by-branch incl beql delay-slot loads and dead li in branch shadow: maxb) max_val<b -> mid=old max, max_ptr=&rgba[2], max=b; else mid=min then min_val<b -> mid=2 else min=2; blez max_val skip; saturation stretch *min=0, range=max-min_v, *mid=mid_v*range/max (multu/div w/ break-guard artifacts), *max=range, r/g/b reloaded (skip path keeps originals in regs = C non-reload semantics); final r/g/b >>1 halving stores. Port-only NULL prop/stan -> 128/128/128/96 fallback = additive guard on invalid data (retail would fault). No jump table; no uninit reads. |
| 50 | `sub_GAME_7F0417DC` | src/game/chrobjhandler.c | `verified-equivalent` | Obj shot candidate gate + dispatch (onscreen model-trace vs offscreen bbox/edge fallback). ASM chrobjhandler.c:4310-4444 vs live C 4250-4306 (NONMATCHING), traced fully: instsize=getinstsize(obj->model +0x14); dot=0 when prop->parent(+0x1c) !=NULL (bnezl w/ neg.s in taken-delay, duplicated on fall-through — both paths f8=-instsize) else dot=(runtime_pos(0x58/5c/60)-arg1).arg3 (z+(x+y) commutative add); gates c.le.s/bc1fl written in C as !(-instsize<=dot) / !(dot<=arg4+instsize) — exact ASM senses incl unordered; prop->flags&2 (lbu 1): onscreen -> sub_GAME_7F041160(obj, arg5, arg6, out_normal, out_pos, out_model, out_node; stack 0x10/14/18 verified — note C's out_normal/out_pos names are semantically position/normal-swapped vs 041160's internals but wiring identical both sides), re-dot vs out slot, !(0<=dot) / !(dot<=arg4) gates (no +instsize, matches), *out_dist=dot return 1; else re-getinstsize(model), sub_GAME_7F041074(arg1, arg3, &obj->runtime_pos, instsize) gate, *out_dist=arg4 pre-store, sub_GAME_7F041400(prop, arg1, arg2, arg3, out_normal, out_pos, out_dist) gate, *out_model=model, *out_node=model->obj->RootNode, return 1. prop spill 0x3c stored before read; all other reads are caller arg slots. No jump table. |
| 51 | `sub_GAME_7F05EC1C` | src/game/gun.c | `verified-equivalent` | Projectile spawn placement + blocked-spawn fallback + room-set seed (called from gunFireTankShell — rank-33 anchor). ASM gun.c:4200-4336 vs live C 4018-4104 (PORT_FIXME_STUBS port body), traced fully: obj->prop(0x10) gate; get_curplayer_positiondata -> s0; bondviewGetPlayerStanHeight(g_CurrentPlayer); height_high/low swap on arg1->y < prop->pos.y(0xc) — bc1fl delay-slot nullification traced both arms, 0xb8=high/0xb4=low consistent, stack args 0x18/0x1c ordered (high,low) = C; UpdateGuardTankFlagsRelated(prop,0) / (prop,1) bracket (a0 preserved via beqz delay move); stanTestLineUnobstructed(&spawn_stan@0xc4=prop->stan(0x14), pos.x(8), pos.z(0x10), arg1->x, arg1->z@0x10, 31@0x14 (=OBJS\|DOORS\|PLAYERS\|CHRS\|PATHBLOCKER), high, low, 0.0@0x20, 1.0@0x24); fail -> spawn_pos=prop->pos + stan reset + blocked=1, success -> spawn_pos=*arg1; sub_GAME_7F05EB0C(obj,&spawn_pos,stan,arg2,arg3@0x10,arg4@0x14,prop@0x18); DEPOSIT 0x80(0x64) gate; blocked -> proj(0x6c)->flags\|=0x100 + unkD4/D8/DC=*arg1; rooms seed sb room@0xa8 + sb 0xFF@0xa9 (RETAIL leaves rooms[2..7] + portal_output@0x54 + portal_count@0x50 UNINIT — verified harmless: 7F0B4AB4 (bg.c:6746) walks room sets to first 0xFF terminator and writes *portalCountOut/portalOutput before any read, so port's full memset/zero-init is behavior-identical, same class as FID-0081 but no reachable read); sub_GAME_7F0B4AB4(position3, &spawn_pos, rooms, proj+0xCC, out@0x54, &count@0x50, 20). Port-only additive: arg/prop/projectile NULL guards. No jump table. |
| 52 | `sub_GAME_7F05F73C` | src/game/gun.c | `finding-filed:FID-0085` | [US variant audited; EU separate queue entry] Grenade-launcher/flare-pistol/piton-gun round spawner (dispatch gun.c:7018-7034, hand 0/1). ASM gun.c:4795-4919 vs live NATIVE_PORT C 4716-4782, traced fully: hand stride 936 strength-reduction chain; get_curplayer_positiondata/get_BONDdata_field408; identity mtx @0xa0; bullet_path_from_screen_center(&origin@0x44,&dir@0x38,hand); Matrix10D4 RotateVec; velocity=dir*33.333332 (rodata 0x42055555 verified) + g_ClockTimer>0 (pos-prev)/g_GlobalTimerDelta correction (posdata pos@8/c/10 minus field408 xyz — port adds delta!=0 guard, additive family per rank-33); DIVERGENCE: matrix_4x4_copy source — retail player+hand_offset+0xAD8 = &hands[hand].throw_item_pos_related; native C keeps raw offset with hand_offset pinned 0 -> wrong field on 64-bit layout + dropped per-hand stride -> FID-0085; m[3][0..2]=0; create_new_item_instance_of_model(203,87); flags &0xFFF9FFFF, sh 1200@0x82, playernum<<17 or; sub_GAME_7F05EC1C(newobj, hand+0x2E8, &local_mtx, &velocity, &identity@0x10); DEPOSIT 0x80 -> proj(0x6c) unk8C=0.30000001 (0x3e99999a) / unk94=0.13333333 (0x3e088888) / refreshrate(0xbc)=60. All constants/senses otherwise equivalent. |
| 53 | `stanGetMoveBondCollisionTiles` | src/game/stan.c | `verified-equivalent` | Ladder move-collision tile pair + 12-rotation vertex fill. ASM stan.c:5630-5761 vs live C 5557-5625 (NATIVE_PORT real body), traced fully: srcTile = bss_8007BA0C; numSides = STAN_TAIL_E = (lh +6 >>12)&0xF (macro verified stan.h:24, tail.half s16); blez guard; per-side link = lhu 0xe+8*side, gate (link>>4)==0 continue, neighbor = standTileStart + link*8 (FULL link value, low-nibble-included quirk consistent w/ rank-38/41 audits, C standTileStart[link] identical); flag gate D_80040F30[mid.half>>12] & 0x40 — SIGNED index (lh +4 / sra 12, no mask) == C s16>>0xc (bondtypes.h:425 s16), negative-index under-read quirk preserved identically; found: *arg0=src *arg1=neighbor, startTriple=(side+2)%3 (div/mfhi); rotate 0..11: base=rotate>>2 (sra), i 0..2: tripleIndex=(base+i)%3, slotIndex=(rotate+i)&3, 7F0B23AC(neighbor, tripleIndex, slots+12*slotIndex); then 7F0B23AC(src, startTriple, slots+12*((rotate+3)&3)); height gates slots[2].y(0x1c)<slots[0].y(0x04) / <slots[1].y(0x10) / slots[3].y(0x28)< both — all four c.lt.s + bc1tl continue senses verified, pass -> return, 12 exhaust -> return (never resumes side loop, matches C); side++/tile cursor +8. div break guards = compiler artifacts. No uninit frame locals (0x40 spill stored before reload). No jump table. |
| 54 | `sub_GAME_7F043650` | src/game/chrobjhandler.c | `verified-equivalent` | [US/LEFTOVERDEBUG variant @7F043650 — the config the port builds (CMakeLists VERSION_US+LEFTOVERDEBUG; Makefile US BYTEMATCH also defines LEFTOVERDEBUG)] Throwing-knife whoosh sound cycler. ASM chrobjhandler.c:7169-7298 vs live C 7061-7162, traced fully: gates runtime_bitflags&0x80 (0x64) early-out, proj->flags&1 (lw 0x0), unk90 bgtzl signed >0 -> else (C u32 <=0 == ==0; unk90 only ever 0/1/++ per 2420/34396/9688 — never negative, equivalent on reachable data), bitflags&0x20; then-path: unkA4 saved_idx (retail UNCLAMPED index into sound slots; C clamp+unkA4=0 store is additive, unreachable — projectileReset:2425 inits 0, only write is 1-saved_idx), randomGetNext%3 divu (u32 mod, same single RNG call), Throwing_knife_SFX[3] s16 table (retail stack-copies 6 bytes then lh — equivalent), deactivate window slt (s32)unkA0 < g_GlobalTimer-6 (addiu -6 @0x781DC) = C exactly, sndGetPlayingState/sndDeactivate on slot idx*4+0x98, *slot!=NULL -> return (bnezl), lvlGetControlsLockedFlag -> return, sndPlaySfx(g_musicSfxBufferPtr, sfx, &proj->soundN slot ADDRESS a2=proj+idx*4+0x98) — NATIVE_PORT passes same address + assigns return (port snd substrate returns state instead of writing through; slot was NULL on entry, end-state identical), chrobjSndCreatePostEventDefault(*slot, &prop->pos prop+8), unkA0=g_GlobalTimer, unkA4=1-saved_idx. Else path: bitflags&=~0x20 store (0x64, both delay-slot and-computations), deactivate sound1(0x98)/sound2(0x9c) if playing — matches; NATIVE_PORT chrobjClearStaleSoundSlot + slot-nulling = port sound-lifetime substrate (retail post-event nulls slots). Additive-only: proj NULL guard. No jump table; no uninit frame locals (0x2c/0x30/0x34 all stored before read). |
| 55 | `sub_GAME_7F043650` | src/game/chrobjhandler.c | `verified-equivalent` | [EU/!LEFTOVERDEBUG variant @VRAM 7F043710] Same function, instruction-identical structure to the US variant (independently traced: same gates, RNG, slot indexing, sndPlaySfx slot-address a2, else-path deactivates; only symbol-address lui/lo shifts). SINGLE delta vs live C: sound-deactivate window addiu $t0,$t9,-5 (@0x7619C) vs US/C -6 — PAL 50Hz regional retiming of the same ~100ms window (5 ticks@50Hz == 6 ticks@60Hz), not a port defect; the port builds the US/LEFTOVERDEBUG config and C matches that variant exactly. No finding. |
| 56 | `sub_GAME_7F0419E4` | src/game/chrobjhandler.c | `verified-equivalent` | Recursive obj shot-candidate tester w/ best-dist pick + child recursion. ASM chrobjhandler.c:4504-4627 vs live C 4456-4499 (NONMATCHING), traced fully: prop captured pre-call (lw 0x10(a0) spill 0x6c, reload post-call = C capture-before semantics); sub_GAME_7F0417DC(obj,arg1,arg2,arg3,arg4@0x10,arg5@0x14,arg6@0x18,&normal sp+0x8c@0x1c,&pos sp+0x80@0x20,&dist sp+0x7c@0x24,&model sp+0x78@0x28,&node sp+0x74@0x2c) — all 12 arg slots verified vs incoming 0xa8-0xbc; hit path gated c.lt.s local_dist < *arg9 (bc1fl skip w/ prop-flags lbu in delay), then *arg9=dist, arg7(0xb4)=normal 0x8c/90/94, arg8(0xb8)=pos 0x80/84/88, D_80030B0C=obj->prop (fresh lw 0x10(s0), same value), bodypartshot=-1, g_CurrentProjectileModel=model(0x78), dword_CODE_bss_80075B74=node(0x74), result=1 — exact C order/values; child scan gate prop->flags&2 (lbu 1(v1)) sense = C proceed-iff-set; chain child=prop+0x20, advance prev=+0x24, per-child flags&2 (lbu 1(s0)) gate, recursive call child->obj (lw 4) w/ arg7/arg8 (s1/s2 reloaded 0xb4/0xb8, NOT the locals) + arg9 passthrough — stack slots 0x10-0x24 verified; recursion nonzero -> result=1 (beql v0,0 w/ advance-in-delay = C if-TRUE). Out-locals only read under 0417DC success where callee fills them (per rank-50 audit) — no uninit exposure; C identical. Return s6 0/1. No jump table; no early-outs beyond the C's. |
| 57 | `sub_GAME_7F0AF038` | src/game/stan.c | `verified-equivalent` | Room bbox init / first-tile-per-room walker (called from stanLoadFile per level load). ASM stan.c:551-678 vs live NATIVE_PORT C 351-403, traced fully: B9DC=0 + clear B128..B350 (sw zero x3 + 0x10-stride loop to B354 excl) = firststaninroom clear; tile source retail *(stanfile+4) raw binary word (lw stan_prefix; lw 4) = port stan_prefix.ptr_firstroom populated by stanDetermineEOF from the same file offset 4 (endian/reloc substrate); *(s32*)tile==0 pre-check + bottom bnezl do-while = C if-return + while; room change gate beql a1,lbu 3(a0) (room u8 @3, struct verified bondtypes.h:420) = C curroom!=(tile->room&0xFF); count ratchet slt curroom<B9DC else count=curroom+1 = C >= sense; firststaninroom[room]=tile (sw, room*4) + bbox extremes init B358+room*12: +0/+2/+4=0x7FFF, +6/+8/+0xa=-0x8000 = C [0..2]/[3..5]; pointCount (lh +6 sra 12 andi 0xF, s16 sign bits masked) recomputed per point iter (tile invariant — C hoists, equivalent); vertex loop lh tile+8+8i+{0,2,4} vs bbox axis min strict slt coord<min -> min=coord, max strict slt max<coord -> max=coord (original coord reloaded, unchanged) = C independent < / > tests; advance tile += lbu list_of_tilesizes[pointCount] both paths (blez-skip keeps pointCount=0 -> tilesizes[0]). Port deviations all additive on shipped data: STAN_MAX_ROOMS=256 arrays + bounds guards vs retail UNGUARDED 139-entry BSS run (B128-B354 ptrs, B358-B9DC bbox — room>=139 would corrupt adjacent BSS in retail, rooms never reach 139 in shipped levels; port room==0xFF first-tile writes its in-bounds bbox[255] where retail would OOB at B358+0xBF4 — malformed-data-only); stan_room_bbox memset (retail leaves stale cross-level bbox for absent rooms — unreachable, every consumer gates on firststaninroom[room]!=NULL per rank-36 audit); NULL-tile guard. No jump table; no frame locals at all (leaf-frame regs only). |
| 58 | `sub_GAME_7F068EC4` | src/game/gun.c | `finding-filed:FID-0088` | [US variant audited; EU variant @7F069658 separate queue entry] Ejected shell-casing DL builder. ASM gun.c:30832-30951 vs live NONMATCHING C 30748-30819, traced fully: saved_gdl=*gdl; header=casing+0x9c (US); dynAllocate(lh hdr+0xe <<6) = modelAllocRenderPosForObject (substrate, + additive NULL guard); 16-word D_80035EB0 template copy (words {0,1,3,0..}, 16th word = adjacent dword_D_80035EEC=0 -> port memset+zbufferenabled=1+flags=3 equivalent, ModelRenderData offsets verified bondtypes.h:697); modelCalculateRwDataLen + modelInit(&model,hdr,NULL) + render_pos=alloc (model+0xc); matrix chain copy(casing+0x1c)/scalar(0.10000001 rodata 0x3dccccce)/set_position(casing+4)/camGetWorldToScreenMtxf multiply into alloc; translation validity: 3 floats at alloc+0x30/34/38, strict >30000 (c.lt.s 30000<f0) \|\| < -30000 -> valid=0, senses exact; valid path: zbuf=0(+4), PropType=4(+0x30), gdl(+0xc), mtxlist(+0x10), subdraw(&rd,&model), *gdl=rd.gdl, TransformManyPos(alloc, lh hdr+0xe). ONE divergence: envcolour packs lbu player 0xfdc-0xfdf (tileColor rgba) but live C uses raw (u8*)g_CurrentPlayer+0xFDC on the native 64-bit struct (offset invalid past pointer-bearing hands[]/ptr_hand_weapon_buffer) -> garbage tint; sibling 7F062BE4 does it right via player->tileColor (gun.c:14925). FID-0085/0087 class. See FID-0088. |
| 59 | `sub_GAME_7F05C6FC` | src/game/gun.c | `verified-equivalent` | Keyframe interpolator for 36-byte gun-anim entries (quat slerp chain + spline position). ASM gun.c:1140-1256 vs live C 1070-1136 (NONMATCHING), traced fully: entry gate c.le.s *(f32*)(data+0x44) <= time (== first dur at (data+0x24)+0x20, consistent both sides); scan loop time-=dur/index++ with flags(s0+0x6c)&1 break (bnez non-likely delay-slot s0+=0x24 advance is DEAD — both sides recompute s0=data+index*36 after via v0*9<<2 chain) and continue-iff dur<=time (bc1tl, flags reload only-when-taken = C's post-break load placement); keyframe-static path flags(s0+0x48)&1 -> set_rotation(s0+0x10)+set_position(s0+4) return 0; interp path frac=time/dur(s0+0x20) spill 0x98, angular=s0+0x1c spill 0x94, four quaternion_set_rotation_around_xyzf (s0-0x14->q0@0x68, +0x10->q1@0x58, +0x34->q2@0x48, +0x58->q3@0x38), ensure_shortest_path (q1,q2)/(q2,q3)/(q1,q0) exact order+arg order, quaternion_7F05C2F0(q0,q1,q2,q3,frac@0x10,qresult@0x78), sub_GAME_7F05B024(s0-0x20, s0+4, s0+0x28, s0+0x4c, frac, angular, &presult@0x88) stack slots 0x10/0x14/0x18 verified; flag==1 mirror negates presult.x + qresult[0]/[1] (0x88/0x78/0x7c) exactly; quaternion_to_matrix(qresult, matrix) + set_position(presult, matrix), return 1. Raw u8* offsets identical both sides on the same keyframe blob (not a repacked native struct — no FID-0085 class exposure). No jump table; all frame locals stored before read. |
| 60 | `sub_GAME_7F0AFB78` | src/game/stan.c | `verified-equivalent` | Nearest-reachable-tile-point finder (all-tiles scan, 4 candidate points/tile). ASM stan.c:2181-2295 vs live NATIVE_PORT C 2124-2176, traced fully: local_coords copy 0x64/0x68/0x6c from *arg_x/y/z; best=4294967296.0 (0x4F800000); tile = *(stanfile+4) = C stan_prefix.ptr_firstroom (stanDetermineEOF substrate) + additive NULL guard; outer pre-check lw(tile)==0 -> exit + bottom bnezl = C while; per-tile gates: bit15 of first u16 (lhu/sra 0xf/andi 1, beql skip w/ tail lh in delay) = C !(*(u16*)tile>>15 & 1); sub_GAME_7F0AF760(tile) nonzero -> skip = C ==0 gate; inner i=0..3 (fp=4, s6=3): i==3 getTileMidPoint(tile,&point@0x58) else getPointJustInsideOfTileTriple(tile,i,&point), mfc1 a3,f22 arg3 both paths; tileStack=tile (sw 0x7c) fresh per candidate, sub_GAME_7F0B20D0(&tileStack, point.x@0x58, point.z@0x60, arg3) proceed-iff <0 (bgez skip) = C; dist=sub_GAME_7F0AFB1C(&point,&local) c.lt.s dist<best strict (bc1fl skip w/ s0++ delay, hit path s0++ at converge — both increment once) -> result=tile(0x78), best=dist, *arg_x/y/z=point.x/y/z (0x58/5c/60); advance tile += lbu list_of_tilesizes[(lh +6 >>12)&0xF] all three paths converge. Return 0x78. No jump table; no uninit frame locals (0x78 zeroed at prologue; 0x58-0x6c callee/prologue-filled before reads). |
| 61 | `sub_GAME_7F0B2718` | src/game/stan.c | `verified-equivalent` | stanFillSearch — BFS over tile link graph w/ predicate. ASM stan.c:6028-6129 vs live NATIVE_PORT C 5872-5930, traced fully: entry tilePred(srcTile) nonzero -> return srcTile; seed bfsTileStack[0]=src, seenCount(s1)=1, countForIter(0x58)=1; wave loop blez s1 (== countForIter at that point, just stored) guards inner; per-tile pointCount = STAN_TAIL_E (lh +6, sra 0xc, andi 0xf — live C correctly uses TAIL_E; old #else body's POINT_COUNT low-nibble is the documented LIE); link = lhu 0xe + 8*linkI (s3 += 8 in loop delay), gate (link>>4)!=0, linkTile = standTileStart + FULL link*8 (low-nibble-included quirk, = C standTileStart[link], consistent w/ rank-38/41/53); visited scan i<seenCount beql-match -> next_link w/ s2++ in delay = C goto; tilePred(linkTile) nonzero -> return linkTile; push+overflow: retail sw at index old-seenCount ALWAYS (non-likely bnez delay) THEN sltiu new<0x15F(351) else return NULL — port checks seenCount+1>350 FIRST and returns NULL WITHOUT the final [350] store: accept/reject boundary IDENTICAL (both bail at old==350, both store+continue for old<=349); only retail's dead pre-bail store differs, an OOB/adjacent-BSS clobber quirk on >350-tile exhaustion (FID-0081 / rank-38 depth-guard family — unreproducible-by-construction on native layout, port bfsTileStack[352] anyway); pointCount re-read after push (lh 6(s4), tile unchanged) = C re-read; pntI advance 0x44+=4 / fp<countForIter; wave end seenCount==countForIter -> return NULL else countForIter=seenCount (delay-slot store) loop. Inner slt signed vs C u32 compare equivalent on 0..15 domain. No jump table; frame slots 0x44/0x58 stored before every read. |
| 62 | `create_new_item_instance_of_model` | src/game/chrobjhandler.c | `verified-equivalent` | Weapon-pickup object factory. ASM chrobjhandler.c:45174-45277 vs live NATIVE_PORT C 44968-45005 (PORT_FIXME_STUBS clean body), traced fully: header = PitemZ_entries[modelnum] word 0 (stride-12 chain sll/subu/sll; ItemModelFileRecord.header field 0 verified bondtypes.h:1423); modelLoad w/ a0 still = modelnum (live C passes modelnum — old #else body's no-arg call is the usual LIE); chrpropAllocate -> prop(s0); get_obj_instance_controller_for_header(header) -> model(0xac); weaponCreate(sltiu prop<1, sltiu model<1, header) = C (prop==NULL, model==NULL, header); retry chrpropAllocate iff prop NULL (a3 spill/restore) + retry get_obj_instance iff model NULL — exact C retry order; success gate newgun && prop && model: 0x88-byte template copy blank_08_object_preset_1 -> sp+0x20 -> newgun (two-stage 12-byte-stride loops + final word = C single struct assign, temp invisible), sb weaponid 0x80 = (s8) weaponnum, sh modelnum 4 = (s16) obj, complete_object_data_block_return_position_entry(newgun, header, prop, model); fail path newgun=NULL, clear_model_obj(model) iff model, chrpropFree(prop) iff prop — same order. Return newgun. No jump table; spills 0xb4/0xb8/0xbc/0xac all stored before read. |
| 63 | `sub_GAME_7F0523F8` | src/game/chrobjhandler.c | `verified-equivalent` | Chr equipped-weapon draw appender (per-hand weapon model onto chr DL/hit list). ASM chrobjhandler.c:45526-45620 vs live C 45408-45519 (NONMATCHING), traced fully: chr=prop+4 spill 0xb4; chrGetEquippedWeaponProp(chr, arg1); weaponprop NULL -> return; obj=prop+4; runtime_bitflags(0x64)&0x800 (bnezl) and flags2(0xc)&0x80000 (sll 12/bltz) both -> flags(byte@1) &= ~2 return — exact senses/order; 16-word template copy D_800322A4 block (data 45403-45406 = {0,1,3,0x0..} = C memset+zbufferenabled=1+flags=3, N64-contiguity note in-code); flags \|= 2 (ori 2 sb) after copy = C order; modelFindNodeMtx(chr->model(0x1c(chr)), model->attachedto_objinst(0x1c(model)), 0) -> renderdata.unk_matrix (sp+0x68 word 0); arg1==1 mirror path set_rotation_around_z(pi 0x40490fdb) into sp+0x20, multiply_in_place(mtx, tmp), unk_matrix=&tmp; render-pos alloc lh model->obj(+8)->count(0xe) <<6 dynAllocate -> mtxlist(+0x10) = C modelAllocRenderPos (model.c:1665 dynAllocate(size) + additive size<=0/NULL guards, no hidden render_pos store); instcalcmatrices(&rd, model); arg2!=NULL && !(runtime_bitflags&0x80) -> *arg2 = sub_GAME_7F06B120(*arg2, model). Port deviations: additive NULL guards (obj/model/attachedto/chr->model/mtx/dyn-overflow render_pos invalidation, all documented in-code); NATIVE arg1==2 hat slot branch = ALREADY-DOCUMENTED gated port extension (chr.c:59 charter 4.4-item-3, chrHatHitListEnabled default-ON w/ --faithful hatch; retail callers only pass 0/1, ASM populates hands 0/1 only) — pre-adjudicated parity-divergence, no new finding; hatTracePrintf env-gated diagnostics. No jump table. |
| 64 | `getShortest2dDispToInfTripleEdge` | src/game/stan.c | `verified-equivalent` | Signed 2D displacement from point to infinite triple-edge line. ASM stan.c:2444-2535 vs live C 2377-2438 (NONMATCHING), traced fully: end3index = start3index==2 ? 0 : +1 (beq/addiu-in-delay); both point indices via srav(tail.half lh +6, 8-4*idx) & 0xF = STAN_TRIPLE_TO_PNT_INDEX exactly; edge_x = next.x - curr.x (lh +8), edge_z = next.z - curr.z (lh +0xc), s16 subu then cvt.s.w = C (f32)(s16-s16); edge_len = sqrtf(ex*ex + ez*ez) same order; degenerate c.eq.s ==0 -> return sqrtf((p_x-next.x)^2 + (p_z-next.z)^2) using NEXT point both sides; else cross = (p_z-curr.z)*(-edge_x) + edge_z*(p_x-curr.x) vs C edge_z*(p_x-curr.x) + -edge_x*(p_z-curr.z) — single commutative outer-add + one commutative mul swap, IEEE-identical; return cross/edge_len. p_x/p_z from arg spills 0x50/0x54; a0/a2 point cursors spilled 0x18/0x1c across sqrtf. No jump table; no uninit reads; no early-outs. |
| 65 | `sub_GAME_7F0B07BC` | src/game/stan.c | `verified-equivalent` | Segment-cross qualifier (two rotational-direction sign products vs threshold). ASM stan.c:3124-3212 (LEFTOVERDEBUG variant = port config) vs live C 3070-3118 (NONMATCHING), traced fully vs MIPS ABI slots (f12/f14/a2/a3 + stack 0x78-0x88): rc=1(0x44); factor pairs exact — call1 gRDB(a_x=arg2-arg0, a_z=arg3-arg1, b_x=-(arg0-arg4) neg.s, b_z=-(arg1-arg5) neg.s), call2 gRDB(a_x, a_z, c_x=arg6-arg0, c_z=arg7-arg1) reloading a_x/a_z from 0x20/0x1c spills, val1=multu product (s32 low-32 identical, values in {-1,0,1}); call3 gRDB(a_x2=arg6-arg4, a_z2=arg7-arg5, b_x2=arg0-arg4 from 0x28, b_z2=arg1-arg5 from 0x24 — unnegated reuse matches C), call4 gRDB(a_x2, a_z2, c_x2=arg2-arg4, c_z2=arg3-arg5), val2=multu; gates slt val1<arg8 bnezl skip else rc=0, slt val2<arg8 bnez skip else rc=0 = C's two independent >= checks (both always evaluated); return rc. Call order matches C left-to-right factors. The !LEFTOVERDEBUG C at 3219 is not compiled in the port config. No jump table; spills 0x1c/0x20/0x24/0x28/0x2c/0x44/0x4c all stored before read. |
| 66 | `sub_GAME_7F05F928` | src/game/gun.c | `verified-equivalent` | Rocket-in-hand render/transform builder. ASM gun.c(US glabel, 7F05F928) = live NONMATCHING C. KEY delay-slot: 'sw v0,0x7c(sp)' in the jal get_curplayer_positiondata delay stores the OLD v0 (hand base g_CurrentPlayer+arg0*296 +0x870), NOT the return; so matrix_4x4_copy src (0x7c+0x268=hand+0xAD8=throw_item_pos_related) and objChangeShading arg1 (0x7c+0x2e8=hand+0xB58=field_B58) are hand-relative == C, while the real posdata (g_CurrentPlayer->prop, saved at 0x30) feeds only arg3=posdata->stan(0x14). objChangeShading/chrobjCollisionRelated, render_pos alloc (model->obj->0xe<<6), obj->mtx copy(+0x18)/set_position(+0x58), cam multiply_homogeneous, modelUpdateRelationsQuick(model->obj->RootNode), prop->flags\|=2 (byte@1), zDepth=-render_pos->pos.m[3] z-component at +0x38 all match. Only add = NATIVE_PORT render_pos NULL guard (behavior-preserving). |
| 67 | `sub_GAME_7F0B0198` | src/game/stan.c | `verified-equivalent` | Edge-projection test. Traced the full messy ASM branch tree (7F0B0198): it reduces to 'return (0 < dot && dot < edge_len_sq) ? 1 : 0' with STRICT c.lt.s on both bounds, exactly the NATIVE_PORT C (dot>0.0f && dot<edge_len_sq). pointCount=(tail.half>>12)&0xF (correct top nibble), nextIndex=(index+1)%pointCount via div/mfhi, edge=next-curr, d=p-curr, dot=dx*ex+dz*ez, len_sq=ex^2+ez^2 all match; points x@8/z@0xc, stride 8. |
| 68 | `sub_GAME_7F049B58` | src/game/chrobjhandler.c | `verified-equivalent` | Visible-prop beam DL walker (7F049B58). ASM iterates prop list (prev@0x24): type==3 & type==6(!=cur player, voidp!=0) render two beams at chr+0x180/+0x1AC; type==1 autogun reads def-type byte@+3==13 then beam@+0xCC. Live NATIVE_PORT branches use struct fields (&chr->unk180[0]/[1], autogun->beam) that map to the same retail offsets — deliberate 64-bit pointer-widening fixes; raw-offset casts exist only in the non-compiled #else. Additive NULL guards behavior-preserving. getPlayerPointerIndex(prop)/get_cur_playernum compare, return gfx all match. |
| 69 | `sub_GAME_7F0AF638` | src/game/stan.c | `verified-equivalent` | stanFillin polarity-flip BFS (7F0AF638). Live NATIVE_PORT C = ASM: stack push start; pop (depth s1); if desiredPolarity!=(id>>15&1) flip id^=0x8000, if sub_GAME_7F0AF760==0 flipCount++; numSides=(tail.half>>12)&0xF, walk points[i] (a0+=8, link@0xe) push &standTileStart[link] (link*8; sizeof(StandTile)==8: 24b id+u8 room+2 s16 unions, points[] flexible) when neighbor polarity mismatches, re-read numSides after push. Back-edges bnez i<numSides / bnezl depth!=0; return flipCount(s7). |
| 70 | `sub_GAME_7F0AF808` | src/game/stan.c | `finding-filed:FID-0090` | findSomeTile (7F0AF808). Body matches ASM (firstroom walk, sub_GAME_7F0AF760/isPointInsideTri/sub_GAME_7F0B20D0/tile==stack[0]/stanGetPositionYValue<f2 chain) EXCEPT the tile advance: retail list_of_tilesizes[(tail.half>>12)&0xF] (pointCount, bits12-15) vs C STAN_POINT_COUNT(tile)=tail.half&0xF (bits0-3=headerE, a point index) -> wrong stride. Every other advance site uses >>12 (stan.c:836/2174/4723). LATENT: no callers anywhere. Filed FID-0090. |
| 71 | `sub_GAME_7F0B0D0C` | src/game/stan.c | `verified-equivalent` | Room-path resolver (7F0B0D0C). ASM=live C: *tilePtr!=0 -> roomA=tile->room(byte@3),roomB=(*tilePtr)->room; equal -> *roomBuf=roomA,return 1; sub_GAME_7F0B8FD0(roomA&0xFF,roomB&0xFF)!=0 -> roomBuf[0]=tile->room,roomBuf[1]=(*tilePtr)->room,return 2. Else sub_GAME_7F0B0C24(tileStack,start_x,start_z,end_x,end_z,roomBuf,&bufCount,maxBufSize); ==0->0; else clamp bufCount to maxBufSize, unkTile=*tilePtr (NULL->=tileStack[0]), tileStack[0]!=unkTile->0, return bufCount. All 8 args & stack layout match. |
| 72 | `posIsInFrontOfDoor` | src/game/chrobjhandler.c | `verified-equivalent` | Door front/back test (7F055A70). ASM=live C: pad=&g_CurrentSetup.boundpads[door->pad] (door->pad lh@6, boundpads@0x1C, stride 0x44); normal = up(0xc) x look(0x18) cross product (all 3 components byte-verified); value = sum normal[i]*(prop->pos[i]-pad->pos[i]) (prop.pos@8, pad.pos@0); other=value, negate if door->doorFlags(0x98)&8; return other<0?0:(other>0?1:1) i.e. other>=0 -> 1. c.lt.s strictness matches; float add commutativity bit-identical. |
| 73 | `getPointJustInsideOfTileTriple` | src/game/stan.c | `verified-equivalent` | 10%-toward-centre tile point (7F0AFA1C). ASM=live C: pntIndex=STAN_TRIPLE_TO_PNT_INDEX=(tail.half>>(8-4*tripleIndex))&0xF via srav (matches macro exactly); pnt.xyz=points[pntIndex].xyz*inv_level_scale; getTileMidPoint(tile,&mid); pnt=mid*0.1f + pnt*0.9f componentwise. Constants D_800585D8=0x3dcccccd(0.1f), D_800585DC=0x3f666666(0.9f) match. DEBUG assert(c<3) inert. |
| 74 | `sub_GAME_7F07B1A4` | src/game/bondview.c | `verified-equivalent` | Camera-mode state machine (7F07B1A4). GLOBAL_ASM retained under #if 0; live always-compiled C 6511-6579 = that ASM: mode=g_CameraMode; g_CameraMode=g_CameraAfterCinema=0; dispatch INTRO(1)->SetMode(FADESWIRL 2); FADESWIRL(2)->ResetIntroCameraMessageDialogs+SetMode(SWIRL 3); MP(9)/FP(4) no-op; SWIRL(3)->maybe_solo_intro_camera_handler+currentPlayerStartChrFade(0,1)+SetMode(FP 4); DEATH_CAM_SP(5)->SetMode(MP 6); DEATH_CAM_MP(6)->camera_mode++, if<3 SetMode(SP 5). NATIVE_PORT portApplyGameplaySpawnFromIntro block is an additive ledgered default-ON fix (GE007_NO_POSTINTRO_SPAWN_FIX), not a divergence. |
| 75 | `getTileMidPoint` | src/game/stan.c | `verified-equivalent` | Tile triangle centroid (7F0AF924). ASM=live C: three point indices via STAN_TRIPLE_TO_PNT_INDEX (bits {8-11,4-7,0-3}); midPnt.{x,y,z} = (pA+pB+pC)/3 * inv_level_scale. ASM associates (A+(B+C)) vs C ((A+B)+C) but s16 coords sum <2^24 so exactly representable -> bit-identical; centroid is symmetric so the A/B/C label permutation between macro (idxA=bits8-11) and ASM (A=bits0-3) is irrelevant. points x@8/y@0xa/z@0xc, stride 8. |
| 76 | `sub_GAME_7F056690` | src/game/chrobjhandler.c | `verified-equivalent` | Stale-DL-collision obj free sweep (7F056690). ASM=live C (source comment: only 7 regalloc instrs differ): iterate prop list (prev@0x24); type==1 && !(flags@1 & 2) && obj->state(byte@2)&0x80 -> node=sub_GAME_7F04B478(obj), NULL->return; Data(+4) NULL->return; if sub_GAME_7F04B590(model->obj, node) then compare Data->Vertices(+8) vs runtime vertices = *(model->0x10 + Data->0x1a*4) (ASM inlines this; C factors it into modelGetDlColRuntimePointers) -> differ -> objFreePermanently(obj,1); return. Relies on that helper being faithful (out of scope). |
| 77 | `getTileEdgePoints` | src/game/stan.c | `verified-equivalent` | Edge endpoints (7F0B1CF8). ASM=live C: currPntRtn=points[pointI].xyz*inv_level_scale; pointCount=STAN_TAIL_E(tile)=(tail.half>>12)&0xF (CORRECT top nibble, cf FID-0090); nextPointRtn=points[(pointI+1)%pointCount].xyz*inv_level_scale via div/mfhi. points x@8/y@0xa/z@0xc stride 8; div-by-zero break traps inert on valid data. |
| 78 | `stanDetermineEOF` | src/game/stan.c | `verified-equivalent` | Stan file-load substrate (byte-swap + host-pointer relocation). Core matches ASM stan.c:6810-6857: standTileStart=relocated_ptr_firstroom-0x80 (ASM t7=lw(a0+4)+offset; t8=t7-0x80 @6817-6819 == port base+first_room_off-0x80 stan.c:6723), ptr_firstroom_0=relocated (@6823-6824), tile-EOF walk sets D_80040F60=last tile via pointCount=(tail.half>>12)&0xf stride list_of_tilesizes[pc] (@6846-6854 == port 6745-6756). Substrate deviations behavior-neutral on valid data: retail relocates room-ptr array in place (*roomPtr+=offset @6832-6837), port only counts num_rooms and routes tile access via stan_prefix.ptr_firstroom/firststaninroom (rank-57 model); port adds stanByteSwapBinary + additive pointCount>10 abort guard (6746, retail unguarded, malformed-data-only). Endian/reloc has no 1:1 ASM counterpart (out of scope). |
| 79 | `sub_GAME_7F0B260C` | src/game/stan.c | `verified-equivalent` | Height-below-two-adjacent-triple-points test. ASM stan.c:5799-5846 vs live C 5779-5793: height=*(f32*)&arg5->unk00 (lwc1 (a2) @5807, arg5=lw sp+0x14; port memcpy reinterpret bit-identical), gate1 height<(f32)points[index].y (lh +0xa @5804, c.lt.s+bc1fl @5809-5811 -> return 0 if not <), gate2 nextIndex=(index+1)%STAN_TAIL_E (div by (tail.half>>12)&0xf @5813-5818), height<(f32)points[nextIndex].y (c.lt.s+bc1fl @5836-5839) -> return 1 else 0. Caller sub_GAME_7F0B26B8 passes &(radius*level_scale) cast to record => unk00 holds float bits. Senses/offsets exact; no jump table. |
| 80 | `stanMatchTileName` | src/game/stan.c | `verified-equivalent` | Tile lookup by packed name id. ASM stan.c:6550-6596 vs live NATIVE_PORT C 6495-6545: name[0]==0->NULL (bnez @6555), stanPackId(name,&hi@sp+0x22,&lo@sp+0x21), tile=stan_prefix.ptr_firstroom (retail *(stanfile+4) @6563-6566, port substrate), *(u32*)tile==0->NULL, per-tile match *(u16*)tile==stanIdHi && *((u8*)tile+2)==stanIdLo (@6567-6577), advance tile+=list_of_tilesizes[(tail.half>>12)&0xf] (@6583-6587). STRUCTURAL DIFF (benign, identical return): retail returns immediately on first match (b .L7F0B2EF0;move v0,v1 @6579-6580); port scans full list tracking first_match then returns it (no side effects in extra iters; GE007_TRACE_TILE_NAME env-gated fprintf only). tile bytes read from byte-swapped file data = same bytes ASM lhu/lbu reads (on-disk POD, not native-repacked struct; no FID-0085 class). |
| 81 | `sub_GAME_7F0AF760` | src/game/stan.c | `verified-equivalent` | Degenerate-tile cross-product==0 test. ASM stan.c:1676-1718 vs live NATIVE_PORT C 1614-1627: iA=(tail.half>>8)&0xf, iB=(>>4)&0xf, iC=(&0xf); AB=(points[iB]-points[iA]).xz, AC=(points[iC]-points[iA]).xz (lh x@+8/z@+0xc within tile+idx*8); cross=(AB.z*AC.x)-(AB.x*AC.z) (multu low-word == signed low-word), return (cross==0) via sltiu<1 (@6718). Index masks/point-offsets/operand-order exact; no jump table; no uninit (AB/AC spilled before mul). |
| 82 | `sub_GAME_7F0AEF3C` | src/game/stan.c | `verified-equivalent` | Tile debug-id string formatter (-stanlinelog). ASM stan.c:266-311 vs live NATIVE_PORT/PORT_FIXME_STUBS C 218-234: buf=D_80040F64+D_80040FAC*9 (old idx via sll3+addu @273-276), then D_80040FAC=(FAC+1)&7; word0=lhu(arg0), byte2=lbu(arg0+2); sprintf(buf, %c%d%c%c, flag=((word0>>15)&1)+0x70, room=word0&0x7fff, letter=((byte2>>3)&0x1f)+0x61, suffix=(byte2&7)?+0x30:0). Arg order a2/a3/sp+0x10/sp+0x14 @301-306; return buf. Only diff = additive port NULL guard (retail derefs unguarded). Debug-only reachability. |
| 83 | `sub_GAME_7F0B2274` | src/game/stan.c | `verified-equivalent` | Ladder/special-tile neighbor flag test. ASM stan.c:5396-5441 vs live C 5362-5391: link=points[arg1].link (lhu +0xe), (link>>4)==0->return 0 (beql @5404); neighbor=&standTileStart[link] (full link*8 @5407-5409); val=neighbor->mid.half>>0xc (lh @5410 then sra 0xc); D_80040F30[val]&2 -> arg5->unk00=1,return 1; else D_80040F30[val]&0x40 -> dword_CODE_bss_8007BA0C=neighbor, arg5->count=1, return 0; else 0. SIGNED-index quirk preserved (mid.half s16>>0xc -> D_80040F30[negative] under-read, same as rank-53). val recomputed twice (decomp comment) inert. Senses exact. |
| 84 | `sub_GAME_7F0B2C74` | src/game/stan.c | `verified-equivalent` | Single-vertex height -> heights[0]/[1] + return. ASM stan.c:6312-6356 vs live C 6295-6306: y=(f32)points[STAN_TRIPLE_TO_PNT_INDEX(tile,0)].y (idx=(tail.half>>8)&0xf, lh +0xa @6320); inlined min/max macro over 4 copies of y (the c.lt.s f2,f2 filler @6324-6349) collapses to y; heights[0]=heights[1]=y*inv_level_scale (@6351-6356) IDENTICAL to C. DEAD RETURN DIVERGENCE: ASM returns f0=y UNSCALED (mov.s f0,f2 @6325, f0 never re-touched), C returns y*inv_level_scale (SCALED); sole caller sub_GAME_7F0B2D14 (stan.c:6366) DISCARDS the return and reads vs[0]=heights[0] (scaled, matches) -> non-observable (chain 2C74<-2D14<-3004 also has no live caller). Equivalent on all observable outputs; latent return diff noted, not filed. |
| 85 | `sub_GAME_7F0B2110` | src/game/stan.c | `verified-equivalent` | addTileRoomIfNew. ASM stan.c:5244-5282 vs live NATIVE_PORT C 5196-5214: roomBuf=data->unk00; for i<data->count: roomBuf[i]==tile->room (lw vs lbu room@+3) -> return 0 (bnel loop @5254); post-loop data->count<data->bufMax (slt @5269) -> roomBuf[count]=tile->room (sw @5276), count++. Loop reloads count (loop-invariant) equivalent. Record is host stack struct (named-field access, not ROM-overlaid; unk00 pointer-width repack is standard substrate, no FID-0085 class). Senses/offsets exact. |
| 86 | `sub_GAME_7F0B00C4` | src/game/stan.c | `verified-equivalent` | Scaled point-distance. ASM stan.c:2601-2632 vs live C 2585-2596: v_x=p_x*level_scale-(f32)points[index].x (lh +8), v_z=p_z*level_scale-(f32)points[index].z (lh +0xc), dist=sqrtf(v_x*v_x+v_z*v_z), return dist*inv_level_scale. Instruction-exact; no jump table. |
| 87 | `stanLoadFile` | src/game/stan.c | `verified-equivalent` | Stan file activate. ASM stan.c:1269-1302 vs live NATIVE_PORT C 1237-1246: m_stanRegion=1, stan_prefix.stanfile=arg0, tokenFind(1, -stanlinelog)!=0 -> stanlinelog_flag=1, sub_GAME_7F0AF038(), setLevelScale(1.0f). ONLY diff: retail also computes standTileStart=ptr_firstroom-0x80 here (t9=lw(a0+4)-0x80 @1280-1284); port defers to stanDetermineEOF substrate which sets the SAME value (relocated ptr_firstroom-0x80, verified rank-78) - value-equivalent, set once. The #else reference body -0x10 is the lying stub (ignored). |
| 88 | `sub_GAME_7F0B23AC` | src/game/stan.c | `verified-equivalent` | Triple-index -> coord3d fill (inv-scaled). ASM stan.c:5508-5537 vs live C 5494-5501: pntIndex=STAN_TRIPLE_TO_PNT_INDEX(tile,tripleIndex)=(tail.half>>(8-tripleIndex*4))&0xf (srav @5513); pnt->x=(f32)points[pntIndex].x*inv_level_scale (lh +8), pnt->y=(lh +0xa)*inv, pnt->z=(lh +0xc)*inv (@5517-5537). Named-field coord access; offsets/scale exact; no jump table. |
| 89 | `sub_GAME_7F0B3138` | src/game/stan.c | `verified-equivalent` | Thin wrapper. ASM stan.c:7025-7052 vs live NATIVE_PORT C 7009-7012: return stanTestLineUnobstructed(pTile,p_x,p_z,dest_x,dest_z,cdtypes,unkHeight,unkA, 0.0f, 1.0f). Arg shuffle exact (a0=pTile from arg1, trailing 0 via mtc1 zero f10@sp+0x20, 1.0 via f16@sp+0x24); unused arg0 spilled unused. Equivalent. |
| 90 | `sub_GAME_7F0B0140` | src/game/stan.c | `verified-equivalent` | Scaled dot-product. ASM stan.c:2660-2682 vs live C 2646-2655: d_x=(p_x*level_scale)*(f32)points[index].x (lh +8), d_z=(p_z*level_scale)*(f32)points[index].z (lh +0xc), return (d_x+d_z)*inv_level_scale. ASM forms d_z-term (points.z*(p_z*ls)) before d_x-term and adds them - IEEE mul/add commute exactly, equivalent. No jump table. |
| 92 | `mp_watch_menu_display` | src/game/mp_watch.c | `verified-equivalent` | MP watch pause/scoreboard menu renderer (US variant). ASM glabel mp_watch.c:2171 vs live NONMATCHING C mp_watch.c:1475. Title-text dispatch jpt_MP_menu_window_text: idx=g_CurrentPlayer->mpmenumode at +0x29c8, bounds<8 (sltiu 8), else no-text default .L7F0C4190. MPMENU enum bondconstants.h:1649 GOWOC=0 LOSSES=1 KILLS=2 SCORES=3 PAUSE=4 EXIT=5 MENU_6=6 MENU_7=7 maps 1:1 to jpt: text_play idx0-3 (gameover-conditional langGet 40981 PLAY / 40982 GAMEOVER via time_trigger_alt_gameover_msg / 40983 STARTTOEXIT), text_pause_unpause idx4 (g_pausedFlag -> 40984 PAUSED + who_paused==get_cur_playernum highlight else 40985 PAUSE), text_exit idx5-6 (40986 EXIT + x=viGetViewLeft+viewleft_offset+0x41), text_blank idx7 = ascii_MP_watch_menu_BLANK = C case MENU_7. All 8 cases with langGet IDs, conditionals, highlight and x-offset traced exact. Body scoreboard ordered call sequence matches C composition: microcode_constructor, nav arrows mpwatchMenuCanGoLeft/Right + textRender, per-player row loop get_points_for_mp_player / mpwatchShouldDisplayRank / write_playerrank_to_buffer / mpwatchShouldDisplayScore / get_scenario / display_text_for_playerdata_on_MP_menu / textMeasure / textRender / textRenderGlow. Renderer domain (charter rule 2: pixel-exactness is ares-capture territory); dispatch semantics fully verified. |
| 93 | `sndHandleEvent` | src/snd.c | `verified-equivalent` | Audio event dispatcher. ASM binary-search over event->common.type (glabel snd.c:2049): type<257 -> type<65 -> type<17 jump-table jpt_80029160 index=type-1 bounds 0..15; plus explicit compares 512/1024/2048/4096, 128/256, and 64. Maps 1:1 onto live C switch snd.c:1629: PLAY(1); STOP(2) and DEACTIVATE(1024) share target .L70008388 = C fallthrough group; UNKNOWN_12(4096) enters .L7000838C same group; PAN(4) VOL(8) PITCH(16) FX(256) RELEASE(2048) DECAY(64) END(128) PLAY_SFX(512) each distinct; API(32) has no handler -> default tail = C default. Tail .L700088D8 snd.c:2777: msgtype = type AND 0x2d1, done_state = state->unk3e AND 1 only when nextState!=0 and msgtype==0, break on done_state or state==NULL, continue on msgtype==0, else prevState=state then break -- all match C snd.c:2014-2038. Top early-outs (nextState evt copy, snd==NULL sndCountAllocList return) match. Port startVolume telemetry additive. |
| 94 | `draw_watch_mission_briefing_page` | src/game/watch.c | `verified-equivalent` | Watch mission-briefing page renderer. ASM glabel watch.c:11720 (LEFTOVERDEBUG variant) vs live NONMATCHING C watch.c:11360. Dispatch idx=mission_brief_index bounds<5 (sltiu 5) into jpt_80058570, else falls past switch = C no-default. Enum WATCH_BRIEF_INDEX watch.h:101 BACKGROUND=0 M=1 Q=2 MONEYPENNY=3 OBJECTIVES=4 maps 1:1 to the 5 jpt targets. Cases 0-3 verified: langGet 44088/44089/44090/44091, get_ptr_text_for_watch_breifing_page(1/2/3/4)=BRIEFING_OVERVIEW/M/Q/MONEYPENNY, sub_GAME_7F0AEB64(210,...), and background/m/q/moneypenny navigation calls all match C. OBJECTIVES case (idx4) ASM call sequence matches C order exactly: setTextOverlapCorrection, langGet 44092, objectiveGetCount loop, get_difficulty_for_objective + lvlGetSelectedDifficulty continue-gate, get_text_for_objective, spacing appends, letter-prefix, viGetX/viGetY textRender 0x00FF00B0, sub_GAME_7F0AC0E8/7F0AC120, get_status_of_objective status coloring. Only mechanism diff = LEFTOVERDEBUG sprintf/strcat/strlen where live C uses watchCopyText/watchAppendText/watchBoundedLen (documented safe-PC bounded-copy, behavior-preserving; C 11412-11418 explicitly guards the debug memcpy 0xBB8 overflow). |
| 95 | `sub_GAME_7F06B29C` | src/game/objecthandler.c | `verified-equivalent` | Model-node z-bias depth walker. ASM glabel objecthandler.c:1392 vs live C objecthandler.c:1049. Dispatch idx=(node->Opcode AND 0xff)-1 bounds<21 into jpt_800544E4; handler targets map 1:1 onto C switch cases 1,2,3,11,12,13,14,15,16,21 at indices 0,1,2,10,11,12,13,14,15,20, with .L7F06B6A4 default for types 4-10 and 17-20 = C default. Verified cases: type1 m3-2 average with FirstGroup at data+4; type13 rwdata ground-minus-posY subtraction sign correct plus Shadow fields at +0/+4; type15 min-pick c.lt.s neg1<neg2 strict picks neg1 else neg2; type16 special=entry side effect via sp 0x110 slot zero-init at prologue. Post-loop special block: f2<0 vs 0<=f2 and f18<0 vs 0<=f18 comparison trees match; epsilon subtractions 0x38000000=2^-15, 0x38800000=2^-14, 0x38C00000=9.15e-05, 0x37800000=2^-16 all match C constants at same branch sites. Loop advances entry=entry+0xC then node=entry+4 while entry!=NULL. |
| 96 | `draw_watch_mission_briefing_page` | src/game/watch.c | `verified-equivalent` | Region-sibling variant of the same shared C body watch.c:11360 (ASM glabel watch.c:12336). Second GLOBAL_ASM block with its own jpt_80058570 (5 entries, same structure). Dispatch idx=mission_brief_index bounds<5 and the 5 briefing cases are identical in structure to the US variant audited at asm_start 11709 (verified-equivalent); no per-region behavior difference in the shared live C path. |
| 97 | `texLoadFromGdl` | src/game/unk_0CC4C0.c | `verified-equivalent` | Texture-preload DL parser. ASM glabel unk_0CC4C0.c:1847 vs live C unk_0CC4C0.c:1546. Outer dispatch: cmdByte>=0xC1 copy-through with E7 setting pipeSyncWritten via .L7F0CE650, else sltiu (cmdByte-0xB1)<0x10 into jpt_tlb_ptrDL_type_B1toC0 else copy = C 3-way. jpt idx0 for 0xB1 and idx14 for 0xBF share .L7F0CE66C = C case 0xB1/0xBF firstTexCmd=1 pipeSync=0; idx9 0xBA=.L7F0CE6B8; idx10 0xBB=.L7F0CE694 prevTexGdl=gdl; idx15 0xC0=.L7F0CE1D8; rest .L7F0CE720 default copy. C0 preamble pipeSync, isLight save, texturenum=w1 AND 0xFFF, override gate texturenum==D_800483C4 and D_800483C8!=NULL and unk_0<0xFF. Inner switch word0 AND 7, sltiu<5 into jpt_tlb_ptr_C0_image_recall_types 5 handlers else default = C. Case0 bit fields smode=w0>>22 AND 3, min=w1>>24 AND 0xFF, tmode=w0>>20 AND 3, offset=w0>>18 AND 3, shifts=w0>>14 AND 0xF, shiftt=w0>>10 AND 0xF; entry rewrite masks 0x1F/0xE3/0x1C with (15-shifts)<<5 and (15-shiftt)<<2, s7=15 confirmed; override recompute 15-(unk_1>>5), 15-((u32 entry<<11)>>29), min=unk_0, plus BA-injection literals 0xBA001402/0xBA001001/0xBA001102 all match. 0xBA case: hasOverride==0 copy else skip src++ only when mwIdx signed lb src+2 in 0x10/0x11/0x14. Return gdl-dst byte delta. |
| 98 | `get_points_for_mp_player` | src/game/mp_watch.c | `verified-equivalent` | MP scoreboard points-per-player scorer. ASM glabel mp_watch.c:859 vs live C mp_watch.c:791. Dispatch idx=get_scenario() bounds<8 (sltiu 8) into jpt_MP_overlays_scoring, else default .L7F0C3C80 leaving points=0 = C no-default. Enum MPSCENARIOS bondconstants.h:1661 NORMAL=0 YOLT=1 TLD=2 MWTGG=3 LTK=4 2v2=5 3v1=6 2v1=7 matches jpt grouping: num_player_kills at idx 0,3,4 = C NORMAL/MWTGG/LTK; num_deaths idx1 = YOLT; time_with_flag idx2 = TLD; team_player_kills idx 5,6,7 = C 2v2/3v1/2v1. Bodies verified: num_deaths = 4 - order_out_in_yolt at +0x68; time_with_flag = flag_counter at +0x60; num_player_kills loop sign i==arg0 subtract else add, tail killed_gg_owner_count at +0x20 times (playerCount-2); team loop filters team=have_token_or_goldengun at +0x69. g_playerPlayerData stride 0x70. |
| 99 | `probably_damage_detail_blood_effect_related` | src/game/objecthandler.c | `verified-equivalent` | jpt_damage_detail[opcode-1] (24 entries), dispatch objecthandler.c:3174-3180 idx=opcode-1 bounds<0x18. Grouping {1,2,3,21}/{11-16}/8/10/17/18/23/default maps 1:1 to both C switch(opcode) blocks (objecthandler.c:2907 measure + 3016 render). Two-pass dispatch fully equivalent. |
| 100 | `sub_GAME_7F0A9AB8` | src/game/watch.c | `verified-equivalent` | Watch controls-mapping page renderer. ASM glabel watch.c:7252 (LEFTOVERDEBUG variant) vs live C watch.c:7007. No jump table. Structural match confirmed: 18 sub_GAME_7F0A9398 label-render calls partition exactly as C -- 7 left-column at x=50 (L_TRIG col4 y=82, D-pad col7, pause col8, Z col3; pressed+unpressed each except pause single), 9 right-column at x=270 (R_TRIG col5, C-buttons col6 incl multi-press dim goto, B col2, A col1), 2 move-sight at x=250 y=195. joyGetButtons masks appear at matching gates; pressed color 0x007000A0 and 0x003000B0 via lui+ori 7357-7360 vs unpressed 0xAA00B0; column index via ctrl_type*5 base (sll 2 then addu = times 5, word-scaled to times 40) plus col into D_800409DC; up/down label swap reads game_options_entries+0x8 current_value==1. move_sight_flag set from AIM-string compare on L/Z/R columns. LEFTOVERDEBUG glyph-string buf_ copies from aA/aB/aZ/... are behavior-preserving. |
| 101 | `sub_GAME_7F0A9AB8` | src/game/watch.c | `verified-equivalent` | Region-sibling variant of the same shared C body watch.c:7007 (ASM glabel watch.c:8132). Second GLOBAL_ASM block; same 18-call left/right/move-sight render partition and no jump table. Structurally identical to the US variant audited at asm_start 7249 (verified-equivalent); shared live C path has no per-region behavior difference. |
| 102 | `lvlManageMpGame` | src/game/lvl.c | `finding-filed:FID-0095` | US variant. ASM glabel lvl.c:3173 vs live C lvl.c:2114 (documented full N64 convergence). Sections A (timer: controls-locked/paused->g_ClockTimer=0 else speedgraphframes + D_80048380++, then g_GlobalTimerDelta=(f32)clockTimer, g_GlobalTimer+=clockTimer; NATIVE_PORT overlaySoloPause/cap/minimap additions correctly absent from retail), B (cheat activation s0=1..CHEAT_INVALID=75 with g_CheatActivated gate + cheatIsEnemyRockets skip, LEVELID_TITLE=90/D_80048394==0/clockTimer>0 guards), C1 (mission-state-6 dead/finished count -> set_missionstate 1), C2 (time-limit: one-minute-left hudmsg loop, 10s alarm sndPlaySfx 0xA1, stop-alarm sndDeactivate, mpCalculateAwards at limit), C3 (kill-limit g_MpPoint: dying-anim count + get_points_for_mp_player>=MpPoint -> mpCalculateAwards or mpwatchSetStopPlayFlag), D (time accum), E (idle input timer), F (viSetUseZBuf, title cheat/menu/langTick vs gameplay update_* chain), G (LEFTOVERDEBUG) all verified equivalent to ASM call sequence + traced A/B. DIVERGENCE: Section C4 YOLT round-end block is default-disabled via NATIVE_PORT GE007_MP_YOLT gate (lvl.c:2358) while retail runs it unconditionally -> FID-0095 (parity-divergence). |
| 103 | `chrTickBeams` | src/game/chr.c | `waived:Live C chr.c:5208 is an explicitly-annotated PARTIAL NATIVE_PORT reimplementation of chrTickBeams, not a transcription: header comment states the retail body is ~800 instrs handling many chr action states and this version does the essentials only (AI tick, anim advance, matrix, render). Under active fidelity management with filed/landed findings for its retail-divergent paths: FID-0014 (WAYMODE_MAGIC patrol-freeze dispatch, landed/verified), FID-0054 (guard-state derivation), FID-0091 (CHRFLAG_CULL_USING_HITBOX magic bypass). Port carries ASM-faithful gates on specific documented paths (CHRFLAG_HIDDEN + 0x40000 skip US 7F020F64, freeze-aware animTick US 7F02101C, magic dispatch US 7F021178-7F02121C, H5 background-AI 0x200 flag US 7F0213C8, H6 held-weapon RUNTIMEBITFLAG_REMOVE free US 7F02106C) but the full retail action-state dispatch is NOT transcribed. verified-equivalent is impossible (not a 1:1 body) and exhaustive new-divergence enumeration is a dedicated task -- same class as the deferred object_interaction and playerTickBeams. WAIVED. Retest when a chrTickBeams full-body parity oracle exists (FID-0012 Phase C visibility work referenced in-body).` | Live C chr.c:5208 is an explicitly-annotated PARTIAL NATIVE_PORT reimplementation of chrTickBeams, not a transcription: header comment states the retail body is ~800 instrs handling many chr action states and this version does the essentials only (AI tick, anim advance, matrix, render). Under active fidelity management with filed/landed findings for its retail-divergent paths: FID-0014 (WAYMODE_MAGIC patrol-freeze dispatch, landed/verified), FID-0054 (guard-state derivation), FID-0091 (CHRFLAG_CULL_USING_HITBOX magic bypass). Port carries ASM-faithful gates on specific documented paths (CHRFLAG_HIDDEN + 0x40000 skip US 7F020F64, freeze-aware animTick US 7F02101C, magic dispatch US 7F021178-7F02121C, H5 background-AI 0x200 flag US 7F0213C8, H6 held-weapon RUNTIMEBITFLAG_REMOVE free US 7F02106C) but the full retail action-state dispatch is NOT transcribed. verified-equivalent is impossible (not a 1:1 body) and exhaustive new-divergence enumeration is a dedicated task -- same class as the deferred object_interaction and playerTickBeams. WAIVED. Retest when a chrTickBeams full-body parity oracle exists (FID-0012 Phase C visibility work referenced in-body). |
| 104 | `sub_GAME_7F06B120` | src/game/objecthandler.c | `verified-equivalent` | Model-node free-list slot binder. ASM glabel objecthandler.c:548 vs live C objecthandler.c:430. Dispatch idx=(node->Opcode AND 0xff)-1 bounds<21 (sltiu 0x15) into jpt_80054490; loc_CODE_7F06B174 for indices 0,1,2,10,11,12,13,14,15,20 = types 1,2,3,11,12,13,14,15,16,21 = C switch cases exactly, all else def_7F06B16C = C default. Handler writes cursor+0=model, cursor+4=node, then cursor=cursor->0xC. Node walk offsets Child 0x14 Next 0xC Parent 0x8 match C child/sibling/parent traversal + break on node==NULL or cursor==NULL. Reconnect .L7F06B1D8: early return when cursor==*head; result==NULL -> result=*head; else append *head to result chain tail via +0xC walk then set head+0x10=tail; head update sets *head=cursor severing back-link (*(back+0xC)=0, *(cursor+0x10)=0) when back!=0, all match C 484-517. Return=result. |
| 105 | `construct_fontchar_microcode` | src/game/textrelated.c | `verified-equivalent` | Font-glyph microcode/DL builder. ASM textrelated.c:1292-1937 vs live NATIVE_PORT C 919-1288, traced fully. Prologue: ybase=*pypos+unk, kerning=kerntbl at index fc_prev.kerningindex*13+fc.kerningindex (sll/subu*13 chain), newx=*pxpos-(kerning+text_spacing)+1 stored back. Early-outs all sense-verified: text_orientation!=0 skips screen culls (bnezl), newx<=0/viGetX<*pxpos/viGetY<ybase+baseline, xend=ulx+lrx<*pxpos, yend<ytop->skip_render, *pxpos<ulx, ytop+height<uly. Texture split fc.index<0x80 (CI4/I4) vs >=0x80 (CI8). CI4 LOADBLOCK math matches: alignw=(width+7)&mask, texels=(alignw*height+1)/2-1 clamp 0x7FF, dwords=alignw>>3 clamp 1, dxt=(dwords+0x7FF)/dwords2 signed div, w1 word assembled identically. CI8 D_80040EA8 one-shot TLUT-load gate (beqz, then zero it) + two osVirtualToPhysical TLUT loads + render-tile/tilesize all constant-verified. Post-texture: xright=*pxpos+width<xend, four rect variants (clipped-top when ytop<uly, clipped-bottom when yend<ybot, rotated E5-flip when text_orientation!=0, normal E4) all with text_x/text_y/text_s/text_t offset math and B4/B3 words matched 1:1. Tail *pxpos+=fc.width, return DL. Deviations: (1) retail masks alignw with andi 0xf8 vs C and-not-7 -- identical for font-glyph widths under 249 (benign truncation, unreachable-differing); (2) NATIVE_PORT text_audit_log_glyph is an env-gated diagnostic, behavior-neutral. Renderer/microcode: writes only the DL and the *pxpos layout cursor (identical both sides), no other sim state. No jump table. |
| 106 | `sub_GAME_7F03ECC0` | src/game/chrprop.c | `finding-filed:FID-0096` | Bbox-to-2D convex-hull projector. ASM chrprop.c:8243-8821 vs live NONMATCHING C 7942-8237. 8 vertex projections, four extreme-corner searches (min-x/max-y/max-x/min-y with tiebreaks, all comparison senses verified), remaining-vertex collector, and four cross-product edge-insert loops (px*edge_dy < py*edge_dx) all verified equivalent. DIVERGENCE at tail: retail stores raw hull vertex count into data unk00 and translates all count vertices with NO clamp (chrprop.c:8710-8712, unrolled loop adds matrix row-3 components at plus 0x30 / plus 0x38); live C adds 'if (count > 4) count = 4' at 8229. Reachable when projected hull is a pentagon/hexagon (up to 6 verts). rect4f holds only 4 coord2d and callers pass rect = collision_data unk04 (contiguous with count), so retail emits 5-6 vertices where port emits 4 -- smaller collision polygon. C comment 'matches original behavior' is incorrect. See FID-0096 (parity-divergence, P3). |
| 107 | `chraiDefaultWeaponFireHandler` | src/game/chrprop.c | `finding-filed:FID-0097` | Default hitscan weapon-fire handler. ASM chrprop.c:3537-4116 vs live NATIVE_PORT C 2561-3139, traced fully. DIVERGENCE: retail final impact block (chrprop.c:4055-4064) reads g_Textures at texture_index and runs the water/snow(5/6) check + explosionCreate with NO texture_index>=0 guard; live C 3115 wraps it in 'if (texture_index >= 0)'. Reachable on background-only hits (hit_bg=1, sub_GAME_7F03AB58<=0 leaves texture_index=-1) -> retail indexes g_Textures at -1 (OOB) and conditionally spawns the impact effect where the port skips it. See FID-0097. Everything else equivalent: bullet-path/transform, both walkTiles early-outs, bg hit + portal-room branch (g_BgPortals nonzero -> ADF4 else AF5C), scale2 gate, AB58 hit split with s16 texturenum, screen-transform gate + neg_z, WATCHLASER(23) 300-threshold clamps, prop loop dispatch by type (3/6->022648 with non-self VIEWER check, 1/4/2->04E9BC), hit loop (3/6->022980, 1/4/2->04EA68), shoot-through hit_count>=GetObjectsShootThrough clears flags, no-impact return, imgsnd selection, single randomGetNext%thing2_len impact_type, explosionCreateBulletImpact, light check, 26.0 pushback + 067B4C + do_effects-gated 0A3E1C tail. NATIVE_PORT portTrace/shotBgTracePrintf are env-gated diagnostics. |
| 108 | `draw_watch_inventory_page` | src/game/watch.c | `finding-filed:FID-0098` | Watch inventory-page renderer. ASM watch.c:4027-4504 (VERSION_US) vs live NATIVE_PORT C 3855-4018, traced fully. DIVERGENCE: guPerspective aspect a3 = immediate 0x3FAAAAAB = 1.3333334 (4/3) in retail (watch.c:4147) vs 1.2857143f in C (watch.c:3926, compiles to 0x3FA49249, does not round-trip); near 10.0, far D_80058528=10000.0, scale 1.0, fovy different45angle all match. Renderer-surface HUD-space item aspect. See FID-0098. Everything else equivalent: transition/health-armor early paths, dynAllocateMatrix, seven bondinv getters, debug gunwatchpos block (gitem stride 0x38, six joyGetButtons adjusting watch_pos_x/y/z with 2.0 and D_80058520=0.98 / D_80058524=1.0204082), gSPMatrix, both rotation matrices + matrix_4x4_7F059694, enviro-fog call (64, 0xA0FFA03C), inventory strcat loop, D_800409C4 decrement, j_text_trigger 14-vs-12 metrics, xpos 0x4E ypos 0x8C yoffset constants, both microcode_constructor_related_to_menus (colors 0 / 0x800050), textRender 0x00AA00B0, sprintf(aDDDF) f64-promoted D_800409BC, D_800409C0 gate, D_800409C4==0 textRender(0xA0FFA0F0) vs textRenderGlow(0x007000A0) split + 7F0A8378. |
| 109 | `sub_GAME_7F0AADC0` | src/game/watch.c | `verified-equivalent` | Dual-controller watch 3D-background renderer. ASM watch.c:9274-9725 (VERSION_US) vs live NONMATCHING C 9120-9252, traced fully. 12-byte position copy from D_80040B5C/B60/B64, three 55-element vertex unroll-copies (D_80040B68/C44/D20, compiler unroll-by-3 + trailing word), flag2=0/flag1=1, sub_GAME_7F0A9684, identity, D_80040B24 += D_80040B28*speedgraphframes*0.5 + watchWrapAroundPI + rot_z, watch_item_is_actively_selected && controller_options_index==1 stick_y(0) path (D_80058550=6.2831855, 4.0) else 0.0, rot_x(-D_80040B20 - D_80058554=0.78539819), four matrix ops, controllerCheckDual arm: vertices2 + matrix_4x4_7F059694(D_80058558=495.0,2500.0,32.0,...) else vertices1 + (-5.0,2000.0,-168.0,...), guPerspective(50.5, aspect 0x3FAAAAAB=1.3333334, 1000.0, D_8005855C=3000.0, 1.0) -- aspect CORRECT here (corroborates FID-0098 in the sibling), gSPMatrix, g_WatchBackgroundGreen<0xE0 -> sub_GAME_7F06359C(green-6) else sub_GAME_7F064364, then the mirrored second dual-controller block (D_80040B3C/38/34, stick_y(1), D_80058560/64, mtxf_cam2 D_80058568=-505.0, guPerspective aspect 0x3FAAAAAB again, vertices3/flag1), final controllerCheckDual -> display_text_buttons_dual_control else sub_GAME_7F0A9AB8. All camera/angle constants byte-verified against the US late_rodata. Renderer/DL builder; the only sim-touching writes (D_80040B20/24/30/34 accumulators) are identical both sides. No jump table. NATIVE_PORT note about non-adjacent BSS position copy is a substrate comment, behavior-neutral. |
| 110 | `sub_GAME_7F0ADDAC` | src/game/textrelated.c | `verified-equivalent` | Glow-text font-glyph microcode/DL builder. ASM textrelated.c:2966-3413 vs live NONMATCHING C 2708-2962, traced fully. Prologue y_total=*y+lineheight, kerning=kerning_table at index prev_char.kerningindex*13+fontchar.kerningindex (sll/subu*13 chain), *x=*x-(kerning+text_spacing)+1. All seven early-outs sense-verified: *x<=0, viGetX<*x, viGetY<y_bottom (y_total+baseline), vp_x+vp_w<xval, vp_y+vp_h<y_bottom, xval<vp_x, y_bottom+height<vp_y (three jump to L_AE414, rest to L_AE418, both reach epilogue). Texture split fontchar.index<0x80 CI4/I4 vs >=0x80 CI8 -- identical structure and constants to construct_fontchar_microcode (rank 105): LOADBLOCK alignw=(width+7)&0xF8, texels=(alignw*height+1)/2-1 clamp 0x7FF, dwords=alignw>>3 clamp 1, dxt=(dwords+0x7FF)/dwords2 signed div; CI8 D_80040EA8 one-shot TLUT gate + two osVirtualToPhysical loads + render-tile 0xF5400200 w1=((index&1)<<20)\|0x00080200 + tilesize; C uses and-0xF8 matching the ASM andi 0xf8 (no truncation gap). Glow tail: FA000000 prim color from glow_color reconstructed byte-by-byte, then the 8-offset outline loop (xoff -1..1, yoff -1..1) skipping the (0,0) center via beql, each iter sub_GAME_7F0AE45C(xval+xoff, y_total+yoff, fontchar, vp_x/vp_y/vp_w/vp_h) with xval reloaded from *x; then main FA000000 prim from color and final sub_GAME_7F0AE45C(gdl+1, xval, y_total, ...); epilogue *x+=fontchar.width, return gdl. Renderer/microcode: writes only the DL and *x cursor. NATIVE_PORT text_audit_log_glyph is an env-gated diagnostic. No jump table. |
| 111 | `constructor_menu0F_mpcharsel` | src/game/front.c | `verified-equivalent` | MP character-select menu constructor (decomp.me 99.89%, only a stack-slot alloc diff). ASM front.c:6610-7054 vs live NONMATCHING C 6467-6606, traced fully. Setup: get_selected_num_players, viSetFillColor(0,0,0), viFillScreen, frontSetupMenuBackground, microcode_constructor, microcode_constructor_related_to_menus(0x26,0xA9,0x184,0xAB,0x90); spE4>=3 gate adds a second menu box (0xD4,0x1E,0xD6,0x136,0x80). Per-player loop (base pointers for player_has_selected_char/mp_char_cur/dword_bss_80069730/mp_char_prev/size all post-incremented by 4 = [i]): layout math verified both branches -- spE4==2 (var_s2=0x26, var_s4=0x15E, var_s7=(i>0?0x8c:0)+0x1E) and spE4!=2 (var_s4=0xAF, var_s7=(i>=2?0x8c:0)+0x1E, var_s2=(i&1?0xAF:0)+0x26); gDPSetScissor coords (var_s2+6, var_s7+5, var_s2+var_s4-6, var_s7+0x87) scaled x4; not-selected langGet(0x9C55) text block with spB4=(var_s4>>1)+var_s2-(width>>1); mp_chr_setup[mp_char_cur_select_player[i]].text_preset (stride 12, u16 lhu) text block; main frontRenderCharacterPortrait at (var_s2+0xD, var_s2+var_s4-0xE, (var_s4>>1)+var_s2-dword_bss[i], var_s7+0x46, prev, size). Neighbor block gate ((has_selected==0 && size==0) \|\| prev!=cur) verified; all six neighbor portraits verified with correct gates (prev>=3/>=2/>=1, prev<num-1/-2/-3), x-offsets (-0xFC/-0xA8/-0x54/+0x54/+0xA8/+0xFC) and char deltas (prev-3/-2/-1/+1/+2/+3). Tail combiner_bayer_lod_perspective. Renderer/menu (front.c, not a prior-defect file); reads menu state, writes only the DL. No jump table. |
| 112 | `sub_GAME_7F0A3F04` | src/game/unk_0A1DA0.c | `verified-equivalent` | Bullet-spark billboard-quad DL builder. ASM unk_0A1DA0.c:4297-4695 vs live NATIVE_PORT C 4123-4292, traced fully. Early-outs sense-verified: arg0.unk04<=0 (lh blezl), arg0.unk00<0 (bltzl), !camIsPosInScreen(&unk10, unk24). vtxtemplate = D_80040980 (16B copy); currentPlayerGetMatrix10D4; gdl=*gdl_ptr; dynAllocate7F0BD6C4(4); roomPos=getRoomPositionByIndex(unk06); vtxtemplate.cn[0..3]=unk28 bytes; frameIndex=(s32)((f32)unk00 * unk08); posX/Y/Z=unk10/14/18. All 12 mtx-row-by-scale products verified (mrow0/mrow1 x scaleA=unk1c / scaleB=unk20). Four vtxtemplate copies, then all four billboard corners with correct sign patterns: v0 (-A-B), v1 (+B-A), v2 (+A+B), v3 (-B+A), each component computed as (pos +/- mrowX +/- mrowY) * get_room_data_float1() - roomPos comp (retail calls get_room_data_float1 per-component, matched), trunc.w.s to s16 ob. Texcoords: v0 tc[0]=frameData[byteOffset+4]<<5 tc[1]=0; v1 tc=0/0; v2 tc[0]=0 tc[1]=byte+5<<5; v3 tc[0]=byte+4<<5 tc[1]=byte+5<<5; byteOffset=frameIndex*12. GBI stream: B7 clear-geom 0x2000, modelview matrix 0x01030040, applyRoomMatrixToDisplayList(roomIndex), texSelect(frameData+byteOffset, 4, arg2, 2), 04300040 vertex, B1000032/0x2010 tri, 01030040 projection matrix, *gdl_ptr=gdl. Port substrate deviations (all behavior-neutral): unk0C used as frameData directly in retail vs effect_image_from_handle(unk0C_handle) handle-resolve in port; G_MTX_FLOAT_PORT marker OR-ed into modelview w0 high word via bondviewGetField10E0GbiFlags (float-matrix marker, not an N64 GBI bit); ge007_bullet_sparks_enabled A/B gate (default-on = retail render); dynAllocate NULL guard; GE007_TRACE_SPARKS diagnostic. No jump table. |
| 113 | `sub_GAME_7F01F614` | src/game/chr.c | `verified-equivalent` | Chr slide-to-wall movement/collision resolver. ASM chr.c:1561-1958 vs live NONMATCHING C 1381-1559, traced fully. chrGetChrWidthHeight, chrSetMoving(0), sub_GAME_7F0B1CC4. Direct move: stanTestLineUnobstructed(old.xz -> new.xz, 31, height, always20, 0.0, 1.0) beqz-skip + stanTestVolume(new, width, 31, height, always20) bgez-skip (both < 0 senses correct); flag -> invalidmove(+9)=0, lastmoveok60(+0xcc)=g_GlobalTimer; result=currentStan. getCollisionEdge_maybe(&edgePt0@0x78, &edgePt1@0x6c) beql-0 -> done. dx/dz = new-old. Degenerate edge (edgePt0.x==edgePt1.x && .z==.z, c.eq.s pair) -> check_edge with hasEdge=0. Edge-normal path: edgeNormZ=pt1.z-pt0.z, edgeNormX=pt1.x-pt0.x, hasEdge=1, sqrtf+1.0/len normalize, dot=normalZ*dz+dx*normalX, slide=normal*dot+old, currentStan=stan. check_edge: hasEdge && line-clear && volume<0 -> newpos=slide, invalidmove=2, result. radiusSq=width*width; the !(normalZ^2+normalX^2 <= radiusSq) c.le.s+bc1fl gate selects edgePt0 (within-radius) vs edgePt1 (outside) block; each block: edgePt==old degenerate guard (c.eq.s x then c.eq.s z -> done), perpendicular normalX=-(edgePt.z-old.z) normalZ=edgePt.x-old.x, normalize, dot, slide, line+volume gates -> newpos=slide/invalidmove=2/result. Epilogue chrSetMoving(1), result==NULL -> invalidmove=1, return result. All branch senses, invalidmove values 0/1/2, field offsets, and FP operand order verified. No jump table; no uninit frame locals (hasEdge/slide only read on their set paths). Sim-relevant (chr movement) and retail-exact. |
| 114 | `intersectLineTriangle` | src/game/unk_092890.c | `waived:WAIVED: live NATIVE_PORT body (unk_092890.c:46-219) is a 24.30% decomp.me reimpl of the 367-instruction f64-heavy line-vs-triangle intersection primitive (glabel unk_092890.c:225). Heavily reachable (bg.c bullet-vs-background + chrobjhandler.c ray-vs-object collision). Algorithm shape is plausibly correct (Moller-Trumbore barycentric: edge/normal cross product, parallel-plane==0 early return, intersection param, impact point, three-way branch var_f2/var_f0 selection, in-triangle var_f0>=0 && var_f2>=0 && sum<=1.0, facing-dot>=0 gate, arg7 unk00/04/08/0c/10/14 + unk2a=0 stores, return 1) but at 24% byte-match the f64 operand ordering, branch thresholds, compare senses and exact store set cannot be confirmed equivalent within the per-entry budget. Not a solid verified-equivalent basis; same disposition as chrTickBeams (batch 9). Dedicated f64 forensic audit / oracle route owed. See FID-0099 (coverage-gap, P2). Retest when a line-triangle oracle comparator exists or the decomp match is raised.` | WAIVED: live NATIVE_PORT body (unk_092890.c:46-219) is a 24.30% decomp.me reimpl of the 367-instruction f64-heavy line-vs-triangle intersection primitive (glabel unk_092890.c:225). Heavily reachable (bg.c bullet-vs-background + chrobjhandler.c ray-vs-object collision). Algorithm shape is plausibly correct (Moller-Trumbore barycentric: edge/normal cross product, parallel-plane==0 early return, intersection param, impact point, three-way branch var_f2/var_f0 selection, in-triangle var_f0>=0 && var_f2>=0 && sum<=1.0, facing-dot>=0 gate, arg7 unk00/04/08/0c/10/14 + unk2a=0 stores, return 1) but at 24% byte-match the f64 operand ordering, branch thresholds, compare senses and exact store set cannot be confirmed equivalent within the per-entry budget. Not a solid verified-equivalent basis; same disposition as chrTickBeams (batch 9). Dedicated f64 forensic audit / oracle route owed. See FID-0099 (coverage-gap, P2). Retest when a line-triangle oracle comparator exists or the decomp match is raised. |
| 115 | `sub_GAME_7F0A5B80` | src/game/watch.c | `finding-filed:FID-0100` | Watch inventory scroll/selection input state machine. ASM watch.c:1246-1631 (VERSION_US) vs live NONMATCHING C 1111-1229, traced fully. DIVERGENCE: up button-press snap-scroll gate is \|\| in retail (ASM 1255-1262: bnez pressed -> up-action, else stickY>=0x47 -> up-action) but && in C (watch.c:1120); the symmetric down block correctly uses \|\| in both, so the up && is an asymmetric decomp error -- up C-button/d-pad tap with stick centered no longer snap-scrolls. Secondary: the '> 0' guards on D_800409BC (up-press 1268-1271, hold-up 1326-1329) are truncated-int (s32) in retail vs float in C, differing for D_800409BC in (0,1); the '< invItems-1' checks trunc too but are equivalent for D_800409BC>=0. See FID-0100 (port-defect, P2). All else equivalent: down press block, both held 0.1 blocks, two stick fine-scroll blocks (stickY/300.0), sub_GAME_7F0A5160/519C +/-1.0, is_holding -> D_80040AE4, min/max clamp + g_curWatchItemIndex trunc, j_text_trigger 12/14 metrics, D_800409B4/B0/C0 ease math, final 0.55/0.45 ANY_BUTTON block. Constants D_8005847C..90 byte-verified. |
| 116 | `sub_GAME_7F02083C` | src/game/chr.c | `finding-filed:FID-0101` | [!VERSION_US (JP/EU) variant] Same function/shared C body as the VERSION_US entry; the port builds VERSION_US so glabel chr.c:4069 is authoritative. Same arg0==0 early-return divergence (the C early-return at chr.c:3814-3825 is region-independent). See FID-0101. |
| 117 | `sub_GAME_7F0A33F8` | src/game/unk_0A1DA0.c | `verified-equivalent` | LEFTOVERDEBUG copy (glabel unk_0A1DA0.c:2467, VRAM 7F0A33F8) of the same watch-arc WatchVertex builder. Same source function as the retail non-LEFTOVERDEBUG copy at glabel 2851 (VRAM 7F0A2938); reviewed together. This copy references its own .late_rodata D_8005774C=0x40490fdb (=pi, unk_0A1DA0.c:2465) for the (f32)i*pi/total_f angle multiply, matching both the retail copy (which uses D_8004CE1C=0x40490fdb) and the live C literal 3.1415927f. All vertex/color/loop structure identical to the retail copy verdict. verified-equivalent per that analysis. Found by ASM-audit batch 11, queue ranks 117/118, region US. |
| 118 | `sub_GAME_7F0A33F8` | src/game/unk_0A1DA0.c | `verified-equivalent` | Watch radar/compass arc WatchVertex builder (retail glabel unk_0A1DA0.c:2851, VERSION_US non-LEFTOVERDEBUG, VRAM 7F0A2938). Live C NONMATCHING body unk_0A1DA0.c:2393-2459 traced against retail ASM 2852-3225. arg3!=0 seed vertex (coord1.x=1, color 0/44/0/176) = ASM beqz a3 block 2870-2885. end=arg1-7, guard end>=7 = slti at,s7,7 bnez 2888-2890. Loop i=7; i<=end; i+=2 = s1 init 7, addiu s1,2 + slt end<i beql 2204-2206. Angle = (f32)i times pi over total_f: retail loads D_8004CE1C (=0x40490fdb=3.1415927 confirmed at unk_0A1DA0.c:1952-1953) mul by (f32)i div by (f32)arg1 (2905-2910) = C 3.1415927f literal. sin/cos vertex: sinf then mul 520.0 (f24) then mul arg2 (f30) trunc.w.s = (s16)(s32)(sinf(angle)*520*arg2) 2911-2921; coord1.x=sin+1, coord1.z=-(cos*520*arg2 truncation), coord2 all 0. Color r=(u8)(u32)(0.0-cosf*0.0)=0, g=(u8)(u32)(44.0-cosf*20.0), b=0, a=176 via three cosf calls with the standard 0x4F000000 unsigned-overflow cvt sequences (2940-3061) = C. Second vertex guarded i!=0 && i<arg1 = beqz s1 + slt i<arg1 beqz 3062-3065; coord1.x=1-sin, coord1.z=-cos, r/g/b preset 255 then overwritten by same cosf color formulas, a=176 (3066-3202) = C incl the redundant 0xFF pre-store. NOTE: the retail dump declares .late_rodata D_8005774C=0x3fb501e2 in this block but the body never references it (angle uses D_8004CE1C); it is a pooled-constant disasm artifact, not a divergence. Second queue entry asm 2464-2844 is the LEFTOVERDEBUG copy (glabel 2467, VRAM 7F0A33F8) of the same source fn; it references its own D_8005774C=0x40490fdb=pi (unk_0A1DA0.c:2465) for the identical multiply, so both builds and the C agree. Found by ASM-audit batch 11, queue ranks 117/118, region US. |
| 119 | `sub_GAME_7F02083C` | src/game/chr.c | `finding-filed:FID-0101` | [VERSION_US variant, authoritative] Chr aim-bone pose-matrix builder (g_aimBoneCallback). ASM chr.c:4069-4445 vs live NONMATCHING C 3783-4063, traced fully. DIVERGENCE: retail dispatch (chr.c:4098-4106) PROCEEDS for arg0==0 (bnezl arg0 branch-likely returns only when arg0 != 0; arg0==0 falls through to the gun-hand aim block L7F020974) whereas the live C early-return (chr.c:3814-3825 'else { return; }') returns for arg0==0, making the C's own arg0==0 block (3877-3907) dead. arg0==0 is the gun-hand aim bone (g_aimBoneCallback invoked per bone at model.c:3701/4012); port skips its pose. See FID-0101 (port-defect, P2). All else equivalent: cheatIsActive(12) scale, arg0 1/2/3 aim selections + hidden&0x400 D_80051D30/34/38 clamps, flinchcnt<0 skip + flinch adjustments (D_80051D44/48, hidden 0x1000/2000/4000/8000), all-zero+scale==1.0 return, subroty, f14/f12 adjustments, player-matrix premult, m[3] save/zero/restore, Y/X/Z rotation chain around subroty (D_80051D4C/50/54), scale mult, camera premult. |
| 120 | `sub_GAME_7F0A611C` | src/game/watch.c | `verified-equivalent` | Generic watch-page scroll/ease controller (glabel watch.c:1795, VERSION_US, 9 args). Live C NONMATCHING body watch.c:1639-1778 fully traced against retail ASM 1796-2137. UP-GATE (watch.c:1652) correctly uses \|\| : ASM 1804-1815 joyGetButtonsPressedThisFrame(P1,0x808) bnezl -> up-action, else joyGetStickY slti 0x47 bnez -> down-gate, else fall to up-action = 'pressed OR stickY>=0x47' (NOTE: this is the correct polarity; its sibling sub_GAME_7F0A5B80 had the && defect filed as FID-0100 - this function is NOT affected). DOWN-GATE (watch.c:1664) 0x404 + stickY<-0x46 \|\| likewise matches (ASM 1830-1856). Up/down button-press scroll: (s32)val<=0 via blezl and (s32)val>=arg2-1 via slt, +/-1.0 gated on arg7 = C truncated-int compares. Hold-button 0.1 blocks (D_80058494/98) gated (s32)val>0 && arg7 / (s32)val<arg2-1 && arg7 (ASM 1859-1896). Analog fine-scroll ranges stickY in 0x1f..0x46 and -0x45..-0x1e, both subtract stickY/300.0 (0x43960000) gated *arg1>0/(s32)val<arg2-1 && arg7 (ASM 1897-1957) = C 1694-1707. Helper sub_GAME_7F0A5160/7F0A519C +/-1.0 blocks (ASM 1959-1993) = C 1711-1723. D_80040AE4 = up\|\|down holding (ASM 1995-2009) = C. Clamp val to arg2-0.5 upper / -0.5 lower via c.lt.s (ASM 2011-2034) = C 1734-1746. ival=(s32)val trunc; *arg1=ival; target = (u32)(-ival)*arg8 + (u32)arg6*arg8 via multu/mflo/addu (ASM 2036-2051) = C 1749-1752; the 3-way target vs scrollval ease (target<scrollval: scrollval-(scrollval-target)/3-1,*arg5=0; scrollval<target: scrollval+(target-scrollval)/3+1,*arg5=0; else *arg5=1) via signed div by 3 (ASM 2055-2083) = C 1755-1763. Drift-correction: *arg0 > *arg1+0.55 (D_8005849C) && !joyGetButtons(0xFFFF) -> -0.1 (D_800584A0) then return; *arg0 <= *arg1+0.45 (D_800584A4) && !held -> +0.1 (D_800584A8) via c.lt.s/c.le.s (ASM 2084-2137) = C 1766-1777. No divergence. Found by ASM-audit batch 11, queue rank 120, region US. |
| 121 | `init_path_table_links` | src/game/initpathtablelinks.c | `finding-filed:FID-0102` | Port NATIVE_PORT body (initpathtablelinks.c:27-33) returns immediately, compiling none of the retail work. Retail glabel initpathtablelinks.c:344 (VRAM 7F006890) assigns waypoint groupNum in loop 3 (store sw t1,8(a1) at initpathtablelinks.c:201) and writes waypoint dist in loop 5 (sw 0xc(...) at 259/262/294), then hangs on error. groupNum is consumed by LIVE AI pathfinding (padhalllv.c do_BFS_withinPathSet:295, findPadWithDistAndSet:312/330/358, 419-420, 555-556); it is otherwise set only by the setup converter (prop.c:2775) from ROM bytes. Skipping the load-time groupNum fixup is a real semantic divergence -> FID-0102. Severity is conditional on whether ROM setup waypoints ship groupNum<0 (Ryan note + PD/GE convention say yes -> potential OOB waygroup index); needs a ROM-byte check (no-run/no-ROM lane cannot settle it). Only the trailing while(1) error-hang is correctly skipped. Found by ASM-audit batch 11, queue rank 121, region US. |
| 122 | `sub_GAME_7F01B240` | src/game/unk_01B240.c | `verified-equivalent` | Per-scanline color-gradient Gfx DL builder (eye/gunbarrel iris; glabel unk_01B240.c:195, VERSION_US). Live C body unk_01B240.c:44-187 (the PORT_FIXME_STUBS #else, compiled under NATIVE_PORT) traced against retail ASM 196-503. Prologue: r/g/b_base = (f32)color_from unk00/04/08 (f0/f2/f12), r/g/b_target = (f32)color_to unk00/04/08 (f14/f16/f18); D_8002BB0C/10/14 = targets pre-loop (swc1 at unk_01B240.c:234/239/244); r/g/b_delta = target-base (f20/f22/f24 sub.s 248/251/254); divisor 299.0 = D_80051CF0 0x43958000 (unk_01B240.c:249). Loop i=1; i<300 (slti 0x12c at 479); i_prev tracked in t0, move t0,t1 then t1++ (477-478); texture (s0) += 0x1B8 in delay slot (481). Per iteration emits 8 fixed Gfx cmds matching C w0/w1 immediates: 0xFD900000/texture, 0xF5900000/0x07080200, 0xE6000000/0, 0xF3000000/0x070DB026, 0xE7000000/0, 0xF5886E00/0x00080200, 0xF2000000/0x006DC000, 0xFA000000/rgba (stores at 267-304). rgba: r=(u32)(r_delta*iter_f/divisor+r_base) then r<<24 UNMASKED (t7=t6<<0x18 at 343), g=(u32)(...) then (g&0xFF)<<16 (andi 0xff, sll 0x10 at 381-382), b=(u32)(...) then (b&0xFF)<<8 \| 0xFF (andi 0xff, sll 8, ori 0xff at 423-426); each float-to-u32 uses the standard 0x4F000000 unsigned-overflow cvt sequence = C (u32) casts. arg2 branch: bgez s1 -> arg2>=0 uses w0=0xE46DF000\|((i_prev+17)*4-1 & 0xFFF), w1=((arg2*4 & 0xFFF)<<12)\|((i_prev+16)*4 & 0xFFF), then 0xB4000000/0, 0xB3000000/0x04000400 (452-475 = C 165-175); arg2<0 uses same w0, w1=(i_prev+16)*4 & 0xFFF, then 0xB4000000/(-arg2)<<21, 0xB3000000/0x04000400 (429-451 = C 152-163). Epilogue D_8002BB08/04/00 = b/g/r_base (483/486/500), return final gdl (v0=a0). No divergence. The NATIVE_PORT block (unk_01B240.c:84-113) is a behavior-neutral port shim: a GE007_TRACE_EYE_DL debug print plus gfx_ptr_store(texture) pointer-table registration; it emits no Gfx words and does not alter gdl, so it has no ASM counterpart and does not affect the DL contents. Found by ASM-audit batch 11, queue rank 122, region US. |
| 123 | `sub_GAME_7F022980` | src/game/chr.c | `verified-equivalent` | Shot-hit impact/blood application (glabel chr.c:10355, VERSION_US). Live C body chr.c:10149-10350 fully traced against retail ASM 10356-10663. Prologue: pos.x/y = screen_pos - (dist*screen_dir/screen_dir.z), pos.z = screen_pos.z - dist (chr.c:10366-10391), dist = sqrtf(sum of squares), scale = 1.0 - 42.0/dist (0x42280000), scaled = pos*scale then mtx4TransformVecInPlace(currentPlayerGetMatrix10D4, &scaled) (10399-10420). Calls sub_GAME_7F067B4C(&scaled), recall_joy2_hits_edit_detail_edit_flag(shotdata weapon, prop, -1), sub_GAME_7F022078(prop, hit_result, &pos, &scaled) all with matching args (10421-10431). handles_shot_actors(chr, hit_result, &shotdata world_dir at +0x28, shotdata weapon, isPlayer=1); beql v0==0 -> return (10439). hit_type dispatch on hit_result (+8): ==100 weapon loop, ==110 hat-impact, else blood decal. WEAPON LOOP (.L7F022AF0): iterates weapons_held index 0..1 via synced counters s2 (0,4; exit s2==8) and s3 (chr + i*4, reads +0x160); weapon==NULL and hit model (+0x48) != weapon_obj model (+0x14) both continue. weaponnum (lb +0x80) beq set {26,87,86,27,33,29,28} == C throwable set {ITEM_GRENADE=26, ITEM_NULL87=87, ITEM_NULL86=86, ITEM_TIMEDMINE=27, ITEM_BOMBCASE=33, ITEM_REMOTEMINE=29, ITEM_PROXIMITYMINE=28} (all values confirmed against bondconstants.h enum; order-independent OR). Throwable -> propobjSetDropped(weapon,1), chr hidden (lhu/ori/sh +0x12) \|= 1, maybe_detonate_object(weapon_obj, bondwalkItemGetDestructionAmount(weapon), &pos, weapon, get_cur_playernum) (10474-10491). Non-throwable + the 110 branch -> imgsnd = D_8004E86C[0] if texturenum(+0x3a)<0 else D_8004E86C[g_Textures[tex].hitSound & 0xf] (hitSound is a :4 bitfield = lbu+andi 0xf), impact_type = imgsnd thing2[randomGetNext() % imgsnd thing2_len], explosionCreateBulletImpact(&hitthing +0x10, &hitthing +0x1c, impact_type, 1, weapon-or-chr handle_positiondata_hat +0x1d8, mtx_index +0x40, 0). ELSE blood branch (.L7F022CE4): modelFindNodeMtx(model, node, 0) [arg2=0 supplied by the bne-110 delay-slot move a2,zero], localpos = pos + (pos - nodemtx m[3] components at +0x30/34/38)*0.5, three getjointsize calls subtracting jointsize*0.5*screen_dir per axis, matrix_4x4_7F059E64(nodemtx,&inversemtx), mtx4TransformVecInPlace(&inversemtx,&localpos), sub_GAME_7F0221DC(model, hit_result, node, &localpos) (10584-10651) = C 10321-10349. NO behavioral divergence. Port-only additions are behavior-neutral: (a) NATIVE_PORT GEDBG/portTraceGuardHitImpact/portTraceBulletImpactMaterial debug traces emit nothing to sim; (b) the NATIVE_PORT nodemtx==NULL early-return (chr.c:10325-10327) is a dyn-matrix-pool overflow guard with no ASM counterpart (port-only condition); (c) the C adds a defensive 'imgsnd != NULL && thing2_len > 0' guard plus a D_8004E86C[0] thing2[0] default that the retail ASM omits (retail computes impact_type = imgsnd thing2[rand % len] directly and relies on the compiler div-by-zero 'break 7' / NULL deref), so the two diverge only on data that would crash retail; on all valid data imgsnd is non-NULL and thing2_len>0, making the guard always-true and the default dead. Found by ASM-audit batch 11, queue rank 123, region US. |
| 124 | `sub_GAME_7F0221DC` | src/game/chr.c | `verified-equivalent` | Damage-detail collision-vertex timer marker (glabel chr.c:9374, VERSION_US). Live C body chr.c:9076-9372 (NATIVE_PORT path) traced against retail ASM 9375-9684; the CORE ALGORITHM is instruction-equivalent, the vertex-buffer MEMORY MANAGEMENT is a deliberate NATIVE_PORT helper-based restructuring (behavior-preserving). Verified equivalent: pos_x/y/z = (s32)pos->x/y/z via trunc.w.s (chr.c:9385-9405); min_dist = 0x7FFFFFFF, best_node/best_index = 0, start=cur=node, beqz node -> done (9404). Node-tree walk with opcode = (u16)Opcode & 0xff (lhu + andi 0xff): opcode 8 -> modelApplyDistanceRelations, 18 -> modelApplyToggleRelations, 23 -> modelApplyHeadRelations, 24 -> collision search (beq chain 9408-9420 = C 9129-9135). Collision search (opcode 24): numCollisionVertices (lh +0xe) > 0, loop over 16-byte entries computing dx/dy/dz = pos - read_be16(entry +0/+2/+4) [lh reads N64 big-endian natively; port read_be16 reads the same], dist = dx*dx+dy*dy+dz*dz, slt dist < min_dist updates min_dist/best_node/best_index (9428-9459 = C 9153-9166). Tree traversal: descend to Child (+0x14) when cur==start OR (opcode != 10 && opcode != 17); else walk Next (+0xc) / Parent (+0x8) until start (-> NULL) or a Next is found (9476-9508 = C 9174-9199). done: beqz best_node -> return; rodata = best_node Data (+4), rwdata = modelGetNodeRwData(model, best_node) (9509-9515). timer = randomGetNext() % 50 + 20, + 100 if arg1==8, + 50 if arg1==15 (divu/mfhi + addiu 0x14/0x64/0x32 at 9518-9535 = C 9232-9238). Linked node: best_vtx = rodata CollisionVertices (+0x10) + best_index<<4; retail reads the linked-node pointer at best_vtx+8 directly and linked_vtx_index = read_be16(best_vtx+12) (9538-9551) -- the port calls chrResolveCollisionLinkedNode(model->obj, best_vtx) which performs the equivalent ROM-offset-to-node resolution for the port pointer model, then linked_rodata/linked_rwdata via modelGetNodeRwData. Vertex realloc: retail allocs a writable copy via sub_GAME_7F09BE4C(numVertices, 0xCCCC, 0, 0) + a 16-byte-stride copy loop when rodata.Vertices == rwdata.Vertices (9552-9587, 9588-9625) -- the port replaces this with chrNativeEnsureDlColRuntimeVertices, same intent. Timer write (main + linked): follow the PointUsage (+0x14) linked-index chain starting at read_be16(coll_vtx+6), writing timer into each runtime vertex byte at offset 15, gated on a writable copy existing (rodata.Vertices != rwdata.Vertices) (9627-9672 = C 9314-9337, 9340-9372) -- the port replaces the inline chain with chrNativeApplyDetailTimer(&DisplayListCollisions, runtime_vertices, best_index/linked_vtx_index, timer), same operation. No algorithmic divergence. The port-only additions are behavior-neutral/fail-closed on valid data: the model==NULL/obj==NULL/pos==NULL guard (chr.c:9105), the chrNativeDlColRecordLooksSafe bounds checks and best_index/linked_vtx_index range guards (return/skip), the linked-node MODELNODE_OPCODE_DLCOLLISION opcode check, and GEDBG traces. The chrNative* helper functions encapsulate the N64 vertex-pointer manipulation for the PC memory model and are assumed-correct port infrastructure (their own audit is separate). Found by ASM-audit batch 11, queue rank 124, region US. |
| 125 | `somethingwith_weapon_animation_groups` | src/game/initactorpropstuff.c | `verified-equivalent` | Weapon-animation-group + timing initializer (glabel initactorpropstuff.c:396, VERSION_US). Live C body initactorpropstuff.c:316-389 traced against retail ASM 397-691. Entry loop: entry = D_8002C914 (base), skip whole loop if entry id (offset 0) == -1; per entry, if field_1C (+0x1c) != NULL then field_20 (+0x20) = sub_GAME_7F0004D0(field_1C), if field_24 (+0x24) != NULL then field_28 (+0x28) = sub_GAME_7F0004D0(field_24), entry += 0x2c (stride), continue while next entry id != -1 (ASM 405-427 = C 322-333). Then sub_GAME_7F0004D0(D_8002DEBC); sub_GAME_7F00046C on the 6 firing-anim-group ptrs in order rifle/pistol/doubles/crouched_rifle/crouched_pistol/crouched_doubles; sub_GAME_7F00032C(D_80030078) then (D_80030660) (ASM 428-455 = C 335-343). Then 9 timing floats: for each, base = (deref ptr_animation_table) + OFFSET, count = *(u16*)(base+4), frames = sub_GAME_7F000290(base, 0, count-1), Dxxx = (f32)frames * 0.1 / (f32)count; the (f32)count uses the standard unsigned-to-float 0x4F800000 fixup which is inert for a u16. All 9 (offset -> target) pairs match exactly: 0x4018->D_80030984, 0x40D4->D_80030988, 0x4070->D_8003098C, 0x8204->D_80030990, 0x777C->D_80030994, 0x77D4->D_80030998, 0x84C4->D_8003099C, 0x8520->D_800309A0, 0x77D4->D_800309A4 (ASM 456-691 = C 345-388; the instruction scheduling interleaves each block store with the next block setup, and count is reloaded from ptr_animation_table for the divisor but is the same value). FLAGGED (not filed) sub-ULP constant divergence: the retail 0.1 multiplier is F32_8004ED78 = 0x3dccccce (initactorpropstuff.c:394), which is 1 ULP above what a modern compiler emits for the C literal 0.1f (0x3dcccccd). This is the known IDO-vs-modern float-literal rounding artifact (IDO rounds 0.1f up); the C source 0.1f is faithful to the original intent, and 0x3dccccce appears in only 3 game .c files vs 0x3dcccccd in 7, consistent with a compiler-encoding difference rather than a distinct value. Effect: the 9 D_800309xx weapon-animation average-frame-time floats may differ from N64 in their last 1 to 2 bits (~1e-7 relative), sub-observable and not flipping any integer-tick decision. Recommend tracking as a systematic IDO-float-rounding class in the approximation registry rather than a per-site fix (hardcoding the hex constant). Otherwise fully equivalent. Found by ASM-audit batch 11, queue rank 125, region US. |
| 126 | `draw_current_hand_item_and_ammo` | src/game/watch.c | `verified-equivalent` | LEFTOVERDEBUG copy (glabel watch.c:3194, VERSION_US LEFTOVERDEBUG build) of the same solo-watch weapon-HUD renderer. Same source function as the retail non-LEFTOVERDEBUG copy at glabel watch.c:3512; reviewed together. Identical .late_rodata constants (D_8005850C=0.98, D_80058510=1.0204082, D_80058514=10000.0, D_80058518/1C=6.2831855, watch.c:3173-3192) driving the identical debug-adjust and rotation math, and the same guPerspective / gSPMatrix / matrix / fog / textMeasure+textRender sequence. verified-equivalent per the retail-copy analysis. Found by ASM-audit batch 11, queue ranks 126/127, region US. |
| 127 | `draw_current_hand_item_and_ammo` | src/game/watch.c | `verified-equivalent` | Solo-watch current-weapon item+ammo HUD renderer (retail glabel watch.c:3512, VERSION_US non-LEFTOVERDEBUG). Live C body watch.c:3072-3168 fully traced against retail ASM 3513-3802. dynAllocateMatrix -> mtx; pFontFile=ptrFontBankGothic, pFontChars=ptrFontBankGothicChars, textheight/width=0. All 10 bondinvGet* calls made with curEquippedItem=bondinvGetCurEquippedItem in order: TextbyInvIndex->weapontext, Vpos/Hpos/Depth/Different45Angle/Xrot/Yrot Watch floats, First/SecondTitlebyIndex (ASM 3526-3554 = C 3101-3110). Debug block gated on get_debug_gunwatchpos_flag: gitem = gitem_structs + weaponId*0x38 (getCurrentPlayerWeaponId(0)); joyGetButtons masks 2/1 -> watch_pos_y (+0x18) -/+ 2.0, 4/8 -> watch_pos_x (+0x14) +/- 2.0, 32/16 -> watch_pos_z (+0x1c) *= 0.98 (D_8005850C) / 1.0204082 (D_80058510) (ASM 3555-3638 = C 3112-3133). guPerspective(mtx, &perspNorm, different45angle, 1.2857143=0x3FA45519, 10.0, 10000.0=D_80058514, 1.0); gSPMatrix w0=0x01030040 w1=osVirtualToPhysical(mtx) (ASM 3640-3666 = C 3135-3137). matrix_4x4_set_rotation_around_y(yrot*6.2831855/360.0, &mtxf_combined at sp+0xd0); matrix_4x4_set_rotation_around_z(6.2831855 - xrot*6.2831855/360.0, &mtxf_temp at sp+0x90) [2pi = D_80058518/1C = 0x40c90fdb]; matrix_4x4_multiply_in_place(&mtxf_temp, &mtxf_combined); matrix_4x4_7F059694(&mtxf_temp, depth, vpos, hpos, 0.0, vpos, hpos, 0.0, 1.0, 0.0) with all 10 args register/stack-mapped correctly; matrix_4x4_multiply_in_place again (ASM 3667-3705 = C 3139-3145). sub_GAME_7F0A6EE8. Fog branch: g_WatchBackgroundGreen < 0xE0 (slti 0xe0) -> set_enviro_fog_for_items_in_solo_watch_menu(gdl, weapontext, &mtxf_combined, g_WatchBackgroundGreen+1, 0x64DC6428) else same with 255 (ASM 3706-3732 = C 3149-3153). sub_GAME_7F06A334, microcode_constructor. xpos=96, ypos=188: textMeasure(&textheight, &textwidth, firstTitle, pFontChars, pFontFile, 0) then textRender(gdl, &xpos, &ypos, firstTitle, pFontChars, pFontFile, 0xFF00B0, textwidth, textheight, 0, 0); ypos=198: same for secondTitle (ASM 3733-3797 = C 3158-3165); return gdl. No divergence. The other queue entry (asm 3171-3485) is the LEFTOVERDEBUG copy (glabel watch.c:3194) of the same source fn with identical .late_rodata constant values (D_8005850C..D_80058530), reviewed together. Found by ASM-audit batch 11, queue ranks 126/127, region US. |
| 128 | `sub_GAME_7F03B9C0` | src/game/chrprop.c | `verified-equivalent` | Shot-hit queue insert/evict/penetration manager (glabel chrprop.c:4492, VERSION_US). Live C body is the NATIVE_PORT branch chrprop.c:4150-4240 (clean ShotHitRecord struct); the sibling #else branch chrprop.c:4242-4485 is the raw-offset reference. Both traced against retail ASM 4493-4785; ShotHitRecord stride 0x50 with distance +0x38, prop +0x3c, hit_result +0x40, node +0x44, hitthing +0x48 (0x30=48 bytes), mtx_index +0x78, hit_node +0x7c, model +0x80, do_damage +0x84; ShotData weapon = unk00[6] at +0x18, threshold unk34 at +0x34. do_damage==0 gate (beqz sp+0x54 -> skip damage, ASM 4499 = C 4159). Phase 1: scan 10 entries stride 0x50, skip prop==0 (+0x3c) or do_damage==0 (+0x84), num_valid++, track best_dist=max distance (c.lt.s best_dist < entry distance -> second_best=best_dist, best_dist=distance, best_index=i) (ASM 4507-4527 = C 4165-4178). max_penetration = bondwalkItemGetObjectsShootThrough(weapon); slt num_valid < max_penetration -> path B else eviction (ASM 4528-4541 = C 4180-4182). Eviction: zero prop of shotdata + best_index*0x50 (best_index*5<<4), unk34 = second_best then neg_z if second_best<neg_z, then evict every entry with prop!=0 && do_damage==0 && second_best<distance via unrolled entry0 (+0x3c/+0x84/+0x38), entry1 (+0x8c/+0xd4/+0x88), and the 0xa0..0x320 stride-0x140 group loop over 4 sub-entries each (ASM 4542-4637 = C 4187-4202 / else 4303-4379). Path B (.L7F03BBE0): re-call bondwalkItemGetObjectsShootThrough, if num_valid+1 == max_penetration && neg_z < unk34 -> unk34 = neg_z (ASM 4639-4655 = C 4183-4185). check_has_flag: has_flag==0 (sp+0x58) -> store; weapon==18 or ==20 -> store (ITEM_RUGER=18, ITEM_SILVERWPPK=20 confirmed from bondconstants.h, so == the NATIVE_PORT weapon != ITEM_RUGER && weapon != ITEM_SILVERWPPK); else evict every entry with prop!=0 && neg_z<distance (same entry0/entry1/group-loop shape, no do_damage check) then unk34 = neg_z (ASM 4658-4738 = C 4206-4216 / else 4387-4456). store_entry: first entry with prop(+0x3c)==0 gets distance=neg_z, prop, hit_result, node, a 48-byte 12-word hitthing copy to +0x48, mtx_index, hit_node, model, do_damage, then return (ASM 4741-4780 = C 4218-4239 / else 4460-4485). No behavioral divergence. NATIVE_PORT-only differences are behavior-neutral: (a) best_index init -1 with an 'if best_index >= 0' guard on the eviction, vs retail default 0 with an unconditional evict -- these differ only when the eviction branch is entered (num_valid >= max_penetration >= 1) yet no valid entry has distance > 0.0 so best_index never leaves its default; since stored distances are shot-ray neg_z values that are always positive, the first valid entry always sets best_index >= 0, so the guard is inert and the evicted index matches retail; (b) NATIVE_PORT calls bondwalkItemGetObjectsShootThrough once where retail/path-B calls it twice (redundant, pure function); (c) the GEDBG store trace emits nothing to sim. Found by ASM-audit batch 11, queue rank 128, region US. |
| 129 | `texFindClosestColourIndexRGBA` | src/game/image.c | `verified-equivalent` | Closest RGBA5551 palette-color matcher (glabel image.c:1137, VERSION_US). Live C body image.c:1028-1132 traced against retail ASM 1138-1431; the ASM is compiler-loop-unrolled (exact-match scan by 4, region scan by 2 with Duff-style remainder) but semantically identical. Args: palette=a0/s2, numcolours=a1 (sp+0x1c), r=a2/s0, g=a3/s1, b=sp+0x28/t1, a=sp+0x2c/t2. temp_t6 = ((r<<11)\|(g<<6)\|(b<<1)\|a) & 0xFFFF (ASM 1156-1163 = C 1049). Exact-match linear scan: lhu palette entry (u16 stride, sll idx,1 -- matches the C stride-1 comment), return first index where temp_t6 == entry (ASM 1165-1219 = C 1051-1056). temp_t0 = r*r + g*g + b*b + a*0x3C1, with a*961 built as ((a<<4)-a)<<6 + a = 960a + a (ASM 1230-1244 = C 1059). Binary search while (var_v1 - var_a1) >= 2 (slti 2): temp_a0 = (var_v1+var_a1) signed >>1 (sra), pal_color = palette entry at temp_a0, pal_r/g/b = (pal_color >> 0xB/6/1) & 0x1F, pal_a = pal_color & 1, temp_s4 = pal_r^2+pal_g^2+pal_b^2 + pal_a*961; branch (temp_s4 < temp_t0 -> var_a1 = temp_a0; temp_t0 < temp_s4 -> var_v1 = temp_a0; equal -> var_v1 = var_a1 = temp_a0) exactly reproduced by the beql/beqz delay-slot pair at ASM 1273-1290 = C 1064-1092. loop_start = max(var_v1-4, 0), loop_end = (var_v1+4 < numcolours) ? var_v1+4 : numcolours-1 (ASM 1292-1303 = C 1094-1104). Best-in-region: bestindex=0, bestvalue=999999 (0x000F423F); for each entry in [loop_start, loop_end): pal_r/g/b = ((entry >> 0xB/6/1) & 0x1F) - r/g/b, pal_a = 0x3C1 unless a == (entry & 1) then 0, temp_s4 = pal_r^2+pal_g^2+pal_b^2 + pal_a; track min into bestindex/bestvalue (ASM 1305-1421 = C 1106-1129). Return bestindex, or the exact-match index. No divergence; pure computation, no globals written. Found by ASM-audit batch 11, queue rank 129, region US. |
| 130 | `sub_GAME_7F0A2F30` | src/game/unk_0A1DA0.c | `verified-equivalent` | [LEFTOVERDEBUG variant] Same fn as the fully-audited US variant (queue line 1946): prologue (f32)arg2->0x74 spill, arg3*8, sin/cos*4*130*(6-v)/5 mul/div chains, vertex/color/alpha store layout spot-verified instruction-identical; only lui references = D_80057740 (142.5) / D_80057748 (pi) rodata — no D_800364CC / level-scale reads. Same FID-0080 adjudication: no retail vertex-side scaling. |
| 131 | `sub_GAME_7F0A2F30` | src/game/unk_0A1DA0.c | `verified-equivalent` | [US variant, pull-forward companion for FID-0080] HUD bar vertex-array filler (23 arcs x 2 radii, damage_display_parent stride 32/item 16). ASM unk_0A1DA0.c:1958-2224 vs live C 1581-1662 (NONMATCHING), traced fully: angle=(f32)(s32)(142.5-(f64)sp80)*pi*2/360 (double 142.5 rodata + trunc.w.d, sp80+=5/iter, exact op order); x = (s16)(s32)(((sinf*4)*130*(6-v))/5 * sp74)+1 where sp74=(f32)arg2 — THE ONLY X SCALE IS ARG2 (callers pass +/-1 mirror; ZERO references to D_800364CC / level scale — only lui pair = 142.5/pi rodata); z = -(s32)((cosf*4*130*(6-v))/5) UNSCALED; unk02/06/08/0A=0, default rgb FF; arg2>0 -> r=96-cos*96, g=127-cos*127, b=FF; arg2<0 -> g=127-cos*127, b=32-cos*32 (blez/bgezl senses verified, arg2==0 writes none); alpha lanes i<10 (2*(s32)arg3'-1>=i -> FF / i<(s32)(2*arg3') -> frac*207+0x30 / else 0x30) and i>=10 ((f32)i<=9+(arg3'-5)*4 -> FF / (s32)((arg3'-5)*4+0.5)+9>=i && (s32)(arg3'-5)*2+8<i -> frac*207+0x30 / else 0x30) — all slt/c.le.s/bnezl/beql senses verified, arg3'=arg3*8; item stride 0x10, inner x2, outer x23. arg1 (0x2E from HUD callers) UNUSED in retail — matches C. FID-0080(a) ADJUDICATION: retail has NO vertex-side compensation for the D_800364CC ortho-extent scale (this fn + 7F0A3330 both scale-free) -> on visibility-scale!=1 stages retail renders the bars magnified/clipped by 1/scale; the port's unscaled guOrtho is a REAL intentional parity divergence there (normalizes what retail distorts), pixel-verifiable vs ares per FID-0080 repro. |
| 132 | `lvlPortalDebug7F0BDF10` | src/game/lvl.c | `verified-equivalent` | Debug portal editor, reachable only via debug controller combos. arg0!=0 path: input-buffer shift-by-one (unrolled 4 iters), A-button OR of both pads, two not-equal toggles of D_800483C0, bgRemoved call with ternary CC-or-minus1, returns the saved bgDebugRemoved7F0B9DE4 result (not raw arg0). arg0==0 path: L/R-JPAD dec/inc of g_DebugPortalsD_800483CC with clamp-at-0, then four-way if/else-if chain R_TRIG-and-L_TRIG then R_TRIG then L_TRIG then else, each toggling bg data-portal control bytes with matching bit-1/bit-2 get-then-set senses; D_JPAD in the R_TRIG-and-L_TRIG branch is PLAYER_1-only in both C and ASM (faithful). All button masks, branch senses, bounds equivalent. |
| 133 | `sub_GAME_7F01B6E0` | src/game/unk_01B240.c | `verified-equivalent` | Gradient DL builder (renderer helper). val = (f32)color stored to D_8002BB2C/24/28; divisor 217.0. All three color deltas evaluate to zero (r/g deltas are val-val, b delta is D_8002BB2C-val which just equals val) and the C faithfully reproduces this flat-gradient retail quirk. 217-iteration loop (bound i+1 < 218) emits the same eleven Gfx commands per iteration with identical w0/w1 immediates including 0xFD10, 0xF510 with 0x07080200, 0xE6, 0xF3 with 0x0713F01A, 0xE7, 0xF510A000 with 0x00080200, 0xF2 with 0x004FC000, 0xFA00 plus packed rgba, the 0xE44FF000 tile command using i+13 and i+12 strides, 0xB4, 0xB3 with 0x04000400. rgba packs r shifted left 24 (no mask, matching ASM), g and b masked to 0xFF then shifted 16 and 8, low byte 0xFF. Running texture pointer advances 0x280 per iter. Standard IDO cvt.w.s overflow-clamp idiom for the float-to-u32 casts. Trailer stores val, g_base, r_base to D_8002BB20/1C/18 and returns advanced gdl. Writes only to gdl buffer and scratch globals; no sim state. Equivalent. |
| 134 | `handle_mp_respawn_and_some_things` | src/game/chrprop.c | `finding-filed:FID-0103` | Live NATIVE_PORT body (PORT_FIXME_STUBS else-branch) vs US ASM 7F03C648. Type dispatch matches: PROP_TYPE_CHR=3 (beam tick on chr+0x180 and +0x1AC), OBJ/WEAPON/DOOR=1/4/2 (regen-timer handling), EXPLOSION=7, SMOKE=8, VIEWER=6. Regen path faithfully reproduces was_above_threshold, timetoregen -= g_ClockTimer, sub_GAME_7F03C574 +0x3C readd, state 0x10 runtime_bitflags 0x1000 toggle, maxdamage-zero-and-not-state-0x80 branch, and the autogun beam tick (obj type 13, landed FID fix using the beam field not the raw 0xCC offset). DIVERGENCE FID-0103: the respawn tail at ASM .L7F03C8AC through .L7F03C8C8 is dropped -- the type-21 float copy (object 0x80 into 0x84) and the object-respawn sound (sndPlaySfx index 82 then chrobjSndCreatePostEventDefault) are never emitted, so regenerating objects respawn silently. Two further author-documented but unsubstantiated MP-only skips: the object flags 0x8000 reparent path at .L7F03C7B8 and the whole VIEWER type-6 handler at .L7F03C944; flagged in FID-0103 for oracle confirmation. The added obj-not-NULL and chr-not-NULL guards vs the unconditional ASM derefs are benign port hardening. |
| 135 | `constructor_menu14_mpteams` | src/game/front.c | `verified-equivalent` | MP-teams menu DL constructor (UI/renderer; reads menu state, writes only DL). spCC=get_selected_num_players; viSetFillColor(0,0,0), viFillScreen, frontSetupMenuBackground, microcode_constructor; microcode_constructor_related_to_menus with immediates 38/169/388/171/144, then a spCC>=3 gated call with 212/30/214/310/128. Per-slot loop i in 0..spCC: var_v0=(i>=2)?0x8C:0 giving temp_s6=var_v0+0x1E, second var_v0=(i&1)?0xAF:0, gDPSetScissor at (var_v0+0x2c, temp_s6+5, var_v0+0xcf, temp_s6+0x87) with the *4.0 fixed-point scale, spB4=var_v0+0x7d. i==0 draws the title via langGet 0x9C58 centered by textMeasure. var_s0 is (i==0 \|\| i==teamsize) when scenario==5 else (i!=teamsize); text = var_s0 ? langGet 0x9C59 : langGet 0x9C5A; var_s2 = var_s0 ? -0xFFFF01 (encodes 0xFF0000FF) : 0xFFFF. Second label centered (sp88=spB4-(sp8C>>1), sp84=(temp_s6-(sp90>>1))+0x46) and printed only when (i!=teamsize) or (i==teamsize and D_8002B560<0xA). Trailer combiner_bayer_lod_perspective. viGetX sign-extended via shift pair. C annotated 99.55 percent regalloc-nonmatching but logic is identical. Equivalent. |
| 136 | `sub_GAME_7F0A1DA0` | src/game/unk_0A1DA0.c | `verified-equivalent` | Glass-shatter shard generator (sim; advances RNG). Both arg1/arg2 normalizations via sqrtf and 1.0/len reciprocal, scaledArg4-7 = argN*len, atan2f(arg3[0], arg3[2]) saved as theta in f30, range1=scaledArg5-scaledArg4, range2=scaledArg7-scaledArg6, halfBuf=SHATTERED_WINDOW_PIECES_BUFFER_LEN/2 via the signed round-toward-zero idiom, sqrtResult=sqrtf(range1*range2/halfBuf), gridTotal=trunc(sqrtResult), half=gridTotal>>1 (non-negative so equals /2), gridF=(f32)gridTotal, cols=trunc(range1/gridF), rows=trunc(range2/gridF), and centerX/Y/Z = arg0 plus center1*norm1 plus center2*norm2 all match (FP add order differs only by commutative reorderings). sndPlaySfx(g_musicSfxBufferPtr, 71, NULL) then chrobjSndCreatePostEventDefault called once. Nested loop guarded rows>0 then cols>0, iterating j outer then i inner, calls randomGetNext exactly once per shard in identical order; randF=(f32)(u32)rand * 2^-32 via the standard unsigned-correction, size=(randF*0.7 + 0.1)*sqrtResult using the ordinary 0x3f333333 and 0x3dcccccd literals (not the IDO 1-ULP variant), dispatching sub_GAME_7F0A2160(&pos, theta, size). Only divergence is the behavior-neutral GE007_TRACE_GLASS stderr instrumentation (reads state, writes nothing). Equivalent. |
| 137 | `texWriteLoadToTmemAddr` | src/game/unk_0CC4C0.c | `finding-filed:FID-0104` | NATIVE_PORT NONMATCHING rewrite of the texture-load DL builder vs retail US ASM 7F0CD078. Port adds module-local g_TexPipeSynced (unk_0CC4C0.c:20, in no ASM block) to suppress repeat pipesyncs, adds a depth-16b-and-tmemoffset-0 loadblock fast path retail lacks, and calls texTrySetTileState with leading args 5 and 0 plus tile 5 where retail uses a0=7 with the tex+8 depth field and tile 7 (0xF500 SETTILE). lutmodeindex is bits 30-31 of tex+0xc; TLUT branch tail (gDPLoadTLUT over 0x3ff-minus-tex+0xa) matches. Renderer GBI-stream parity-divergence, sim-neutral; needs ares RDP/pixel confirmation. See FID-0104. |
| 138 | `texWriteLoadToTmemZero` | src/game/unk_0CC4C0.c | `verified-equivalent` | Texture-load-to-TMEM-0 DL builder vs retail US ASM 7F0CD7AC, faithful. dxt=sub_GAME_7F0CCB38(tex); texGetDepthAndSize into depth(0x94)/len(0x90); lutmodeindex is bits 30-31 of tex+0xc. Both branches: gDPSetTextureImage (0xFD00 with fmt from tex+8 bits and depth), texTrySetTileState(7, gbiformat=(tex+8 shifted), depth, 0, 0, ...) gating a gDPSetTile on tile 7 (0xF500 with 0x0700 tile), gDPLoadSync (0xE600), gDPLoadBlock tile 7 with clamped len-1 texels and dxt, then an unconditional gDPPipeSync (0xE700). The lutmodeindex!=0 branch adds the TLUT load: a2 = (0x3ff-tex+0xa) if unsigned-less-than len else 0, tmp = len-a2, texTrySetTileState(7,0,0,1,256,...) gating gDPSetTile(line 1, tmem 256, tile 7 -> 0xF5000300), gDPLoadSync, gDPLoadTLUT07(tmp, a2, tex+0xa+tmp, a2), gDPPipeSync. All branch senses, immediates, tile numbers, and arg registers match. Unlike its sibling texWriteLoadToTmemAddr (FID-0104) this variant uses no g_TexPipeSynced and no 16b fast path -- it is the retail-faithful pattern. Renderer DL builder, sim-neutral. Equivalent. |
| 139 | `sub_GAME_7F01FC10` | src/game/chr.c | `verified-equivalent` | Character move/ground-settle callback vs retail US ASM 7F01FC10, faithful core. stan==NULL early-out to store_ground preserved. ACT_DIE (actiontype byte at chr+7 equals 4) with timeextra>0: accel = playspeed(model+0xa4)*g_GlobalTimerDelta*(timeextra-elapseextra)/timeextra, newpos += extraspeed(chr+0x40/44/48)*accel, elapseextra += delta*playspeed, timeextra zeroed if exceeded. newpos.x/z += fallspeed.x/z(chr+0xb0/0xb8)*delta. sub_GAME_7F01F614(chr, stan, oldpos, newpos, 1) updates stan and sets stanchanged/posptr. LOCK_Y_POS(0x1000) sets yval=chr->ground(0xac) and skips; else stanGetPositionYValue into ground, CHRFLAG_INIT(1) bootstrap (manground=ground, sumground=ground/0.10000002, clears INIT), the sub_GAME_7F057D44(&manground,&fallspeed.y,delta) settle gated on fallspeed.y!=0 or ground<manground, the g_ClockTimer friction loop (fallspeed.x/z *= 0.89999998, sumground = sumground*0.89999998 + ground), manground = sumground*0.10000002, the four fallspeed-in--0.1-to-0.1 goto-update_y guards then zero both, update_y newpos.y += manground-yval. store_ground writes yval to ground_out; if not stanchanged restores newpos.x/z from oldpos; returns 1. All offsets, branch senses, and the 0.10000002/0.1/-0.1/0.89999998 late_rodata constants match. Divergences are documented landed NATIVE_PORT compensations for the stubbed sub_GAME_7F03E27C: stan==NULL recovery via sub_GAME_7F0AF20C, an added rwdata Header.ground = groundval sync in the INIT branch that retail omits (retail only copies rwdata+0x28 into rwdata+0x38), and behavior-neutral debug logging. Core logic equivalent; port additions out-of-scope landed fixes per the solo_char_load precedent. |
| 140 | `init_spectrum_game` | src/game/spectrum.c | `verified-equivalent` | ZX Spectrum emulator init vs retail US ASM 7F0D2FC4, faithful. mempAllocBytesInBank calls for the 256-byte keyboard-parity table, 24576-byte ptr_6000alloc, 768-byte ptr_300alloc, and 65536-byte ptr_spectrum_roms all match sizes and alignment 4. Parity table: for each i in 0..256 count set bits over j in 0..8, store 0 when the count is odd else 4 (ASM count-odd branch writes zero, even writes 4). ptr_300alloc filled with 1. Monitor ROM loaded via _fileNameLoadToBank(aEmDataSpec_rom_seg_rz,1,256,4) then 0x4000 bytes copied into ptr_spectrum_roms. spec_cur_rom_id set to low byte of game, rom_id clamped to 0 when 5 or more. Game snapshot loaded via romnames indexed by rom_id, then 0xC000 bytes copied from source offset 0x1B into roms offset 0x4000 (ASM unrolled by 4, source bytes 0x1B..0x1E to dest 0x4000..0x4003). Snapshot header parse matches byte-for-byte: spec_I from byte 0, the E347 down to E33A bytes from bytes 1..14, spec_IY as byte0x10 shifted left 8 plus byte0x0F, spec_IX from bytes 0x12 and 0x11, IFF2 upper and lower from bit 2 of byte 0x13, spec_R from 0x14, E339 from 0x15, spectrum_header16_15 from 0x16, spec_SP from byte0x18 shifted left 8 plus byte0x17, spec_IM from byte 0x19 with a plus-1 when positive, and finally spec_PC read from ptr_spectrum_roms at the SP value (low byte then high byte shifted 8) with spec_SP advanced by 2. All offsets, shifts, and branch senses match. Mechanical byte copy and header parse, no RNG or float. Equivalent. |
| 141 | `sub_GAME_7F0AEB64` | src/game/textrelated.c | `verified-equivalent` | Text word-wrap builder vs retail US ASM 7F0AEB64, faithful. Outer do-while on continue_flag. Per iteration: measuredwidth/wordlen/wordwidth zeroed, curchar = src byte. When curchar is printable (>=0x21) the inner do-while copies the word into wordbuf(sp+0x4c), advancing src and wordlen, accumulating wordwidth via font1[ch-0x21].width (fontchar stride 24, width field at +12, encoded as the -0x30c displacement after ch*24), with the double-byte path taken when the just-buffered char is >=0x80 (reads ch2, stores it, adds its width), breaking when the next char is <0x21. wordbuf[wordlen] nulled, textMeasure(&measuredheight, &measuredwidth, wordbuf, font1, font2, 0), accumulated_width += measuredwidth, fits_on_line = (maxwidth < accumulated_width) ? 0 : 1. Then the delimiter switch on the current src byte: newline (10) emits a wrap newline plus text_wordwrap spaces when not fitting, zeroes accumulated_width, copies the word, emits newline; space (32) emits the wrap newline plus spaces and sets accumulated_width = text_wordwrap*5 + measuredwidth when not fitting (wordwrap*5 as shift-2 plus self), adds 5, copies the word, emits a space; nul (0) clears continue_flag, emits the wrap if not fitting, copies the word, emits nul; any other byte just advances. src advanced each iteration; loop repeats while continue_flag == 1. All branch senses, the unrolled word-copy, offsets, and the wordwrap arithmetic match. UI/text builder, no sim state, RNG, or float. Equivalent. |
| 142 | `sub_GAME_7F007F30` | src/game/title.c | `finding-filed:FID-0105` | Title Bond gunbarrel renderer, LEFTOVERDEBUG(7F007F30, PAL 137/212/230) and shipping(7F007EB0, US 114/176/191) copies vs the shared NONMATCHING C. Animation/sfx sim logic faithful in both: dword_CODE_bss_80069594 progression, modelSetAnimation(bond_eye_fire) at the start frame, modelSetAnimSpeed at the speed frame, modelTickAnimQuarterSpeed each tick, sndPlaySfx(111) at the trigger frame with sfxPlayed propagated to the gun switch-node Gunfire.visible (sh) and Raw.unk00 (sw). Region constants match the C REFRESH_PAL guard. DIVERGENCE FID-0105: the C writes renderData.envcolour from gBloodColour and fogcolour 0xFFFFFF00 before drawjointlist; neither ASM copy does (they set only PropType 7, zbufferenabled 0, flags 1 then 2, gdl). Also the ifndef NATIVE_PORT fixed-point matrix conversion (matrix_4x4_copy then matrix_4x4_f32_to_s32 over each render_pos) present in both ASM copies is intentionally skipped by the port f32 renderer, and NATIVE_PORT dyn-overflow guards are added -- both documented/expected. See FID-0105 (renderer parity-divergence, ares check pending). |
| 143 | `sub_GAME_7F007F30` | src/game/title.c | `finding-filed:FID-0105` | Title Bond gunbarrel renderer, LEFTOVERDEBUG(7F007F30, PAL 137/212/230) and shipping(7F007EB0, US 114/176/191) copies vs the shared NONMATCHING C. Animation/sfx sim logic faithful in both: dword_CODE_bss_80069594 progression, modelSetAnimation(bond_eye_fire) at the start frame, modelSetAnimSpeed at the speed frame, modelTickAnimQuarterSpeed each tick, sndPlaySfx(111) at the trigger frame with sfxPlayed propagated to the gun switch-node Gunfire.visible (sh) and Raw.unk00 (sw). Region constants match the C REFRESH_PAL guard. DIVERGENCE FID-0105: the C writes renderData.envcolour from gBloodColour and fogcolour 0xFFFFFF00 before drawjointlist; neither ASM copy does (they set only PropType 7, zbufferenabled 0, flags 1 then 2, gdl). Also the ifndef NATIVE_PORT fixed-point matrix conversion (matrix_4x4_copy then matrix_4x4_f32_to_s32 over each render_pos) present in both ASM copies is intentionally skipped by the port f32 renderer, and NATIVE_PORT dyn-overflow guards are added -- both documented/expected. See FID-0105 (renderer parity-divergence, ares check pending). |
| 144 | `texWriteTileLods` | src/game/unk_0CC4C0.c | `verified-equivalent` | Multi-LOD tile-state/tile-size DL builder. Live C 830-890 vs ASM 894-1131. endlod=(tex byte-0xb>>5 maxlod)+startlod, loop-guard startlod<endlod (slt beqz 916) and terminator currentTile!=endlod (bne 1115) both match; lod inits startlod-startlod=0 (subu 918) and increments each iter (addiu s2 1087). texSetLutMode(lutmodeindex<<14) gate emits SETOTHERMODE_H TEXTLUT; texTrySetTileState arg order currentTile,gbiformat(bits2-4),depth(bits0-1),line,tmembase,smode,tmode,masks,maskt,lod,lod verified reg-by-reg; uls/ult/lrs/lrt all (offset==2 && !unk0c_02)?2:0 with (dim-1)<<2 addend (branch senses bne offset,2 + bnezl unk0c_02 correct polarity). tmembase+=sizeInBytes accumulates (addu 1113). Renderer DL builder, sim-neutral. |
| 145 | `update_broken_windows` | src/game/unk_0A1DA0.c | `verified-equivalent` | Shattered-glass-piece integrator. Live C 567-609 vs ASM (LEFTOVERDEBUG variant 629-863). timescale=g_ClockTimer<15?(f32)g_ClockTimer:15.0 (slti 0xf 634); per-piece early-out piece->piece<=0 continue (blezl 660); frames=(s32)timescale via trunc.w.s (662); piece+=frames; five field MACs field_0x10+=0x28*ts, 0x14+=0x2c*ts, 0x18+=0x30*ts, x(4)+=0x1c*ts, z(0xc)+=0x24*ts all offset-verified; inner j<frames loop (4x-unrolled, blez guard) does y(8)+=field_0x20, field_0x20-=0.1 (0x3dcccccd); piece>=150 (slti 0x96) zeroes piece; y<-30000\|\|y>30000 (c.lt.s pair, bc1tl/bc1fl senses correct) zeroes piece; stride 0x68, bound re-read from SHATTERED_WINDOW_PIECES_BUFFER_LEN global each iter. Renderer decor, no sim-hash state. |
| 146 | `update_broken_windows` | src/game/unk_0A1DA0.c | `verified-equivalent` | Same function, non-LEFTOVERDEBUG (retail) variant ASM 879-1113. Byte-identical control flow to the LEFTOVERDEBUG copy; rodata 0.1=0x3dcccccd, +-30000 clamps identical. Equivalent to live C 567-609. |
| 147 | `joyPoll` | src/joy.c | `verified-equivalent` | N64 controller SI poll. Live C 494-588 vs ASM 594-830. Disable-poll / enable-poll / poll branches all map: osRecvMesg NOBLOCK==0 gate senses correct (bnez skips), g_ContBusy block/clear, g_ContPollDisableCount ++/--, (count==0 && g_ContInitDone) short-circuit (bnezl early return then beqz). index=(nextlast+1)%20 with ==curstart -> nextlast fallback; nextsecondlast=(index+19)%20; sample stride 24, pad stride 6, errnum@4; g_ContCheckStatusTimer60++ %120 joyCheckStatus; errnum edge-detect XOR loop (both \|\| clauses + break) verified; bad-reads reset zeroes all four arrays iff any nonzero, 2x-unrolled, bound at g_ContBadReadTimer60 addr. ONE naming note (not a divergence): C uses function-static 'count' where ASM uses global g_ContBadReadTimer60 (joy.c:125, commented 'Static variable?') -- same exclusive storage, referenced nowhere else, so behavior identical. joyPoll live via sched.c:303/316, boss.c. |
| 148 | `spectrum_p1controller_to_kempston` | src/game/spectrum.c | `verified-equivalent` | Spectrum-emulator easter-egg N64->kempston/keyboard input map. Live C 650-757 vs ASM 761-1005. reg map a3=btns, t0=stickX, v0=stickY (joyGetButtons then StickX(-3,3) then StickY(-3,3)). spec_keyboard_buffer[0..8]=0xFF init (byte0 + 8-byte loop). Direction gates verified: Z_TRIG(0x2000)->goAction; (0x202 L_C\|L_JPAD)\|\|stickX<-1->goLeft; (0x101 R)\|\|stickX>=2->goRight; (0x808 U)\|\|stickY>=2->goUp; (0x404 D)\|\|stickY<-1->goDown; all branch senses (bnez/slti) correct. Per-ROM dispatch: every derived ASM id-compare matches spectrum.h enum exactly (JETPAC=2 goUp, {ALIEN8=4,KNIGHTLORE=7} goDown, six-set {0,1,4,6,7,9} kbd[4]&=0xFE, {JETPAC=2,PSSST=8} kbd[3]&=0xEF, GUNFRIGHT=5 kbd[3]&=0xFB, JETMAN=3 kbd[4]&=0xEF/kbd[0]&=0xFD/kbd[7]&=0xFE, UNDER=6 goUp-A/kbd[7]-B, ATIC=1 four jpad masks) with A=0x8000,B=0x4000. L_TRIG(0x20) zeroes ptr_spectrum_roms[0..0x3fff]. Final D_8004EC40 = goAction<<4\|goUp<<3\|goDown<<2\|goLeft<<1\|goRight verified bit-for-bit. Minigame-local, deterministic. |
| 149 | `sub_GAME_7F022648` | src/game/chr.c | `finding-filed:FID-0106` | chr shot hit-scan. Live C 9713-9904 vs ASM 9908-10122. Equivalent: actiontype==ACT_DEAD(5) early-out, prop->flags&2 gate, (zDepth-instsize)<unk34 gate, 2-iter weapons_held[chr+0x160] maxweaponsize=max(getinstsize(wobj->model)*model->scale), instsize+=maxweaponsize, sphere test sub_GAME_7F041074(shot_pos,shot_dir,&submatrix m30,instsize), coarse sub_GAME_7F06C010 then 100\|\|110 poly do-while (sub_GAME_7F04D9B0 break-on-hit else probably_damage_detail_blood_effect_related), hit_result>0 via blezl s1, rotate shot_dir by currentPlayerGetMatrix10D4, nodemtx delta+sqrtf dist, neg_z=-(dir.z*dist+pos.z)<unk34 -> sub_GAME_7F03B9C0, hit_in_range (persistent 0xd4 slot, distinct from reassigned s1 hit_result) -> zDepth<=unk34 sets chrflags\|=4 and numclosearghs++. ONE divergence -> FID-0106: retail directly uses chr->field_20 as list head (no NULL fallback, no free); port adds lazy sub_GAME_7F06B120/7F0523F8 rebuild when field_20==NULL plus 7F06B248 cleanup, decoupling shootability from render-time hit-list build. |
| 150 | `spectrum_draw_screen` | src/game/spectrum.c | `finding-filed:FID-0107` | Spectrum easter-egg screen DL builder. Live C 1382-1514 vs ASM 1519-1723. 10-command prologue (PipeSync, TLUT SETTIMG spec_palette, SETTILE, LoadSync, LOADTLUT 0x0703C000, render-mode BA001301/B900031D, combine FCFFFFFF/FFFCF87C, BA000C02, BB000001/-1) all byte-verified. 3x4 tile loop: yl_bot=(((row+1)*64+24)<<2)&0xFFF, yl_top=((row*64+24)<<2)&0xFFF, dram_offset=tile_row<<11 then +=0x800, tile_row+=4; per-tile 13 commands (SETTIMG ptr_6000alloc+dram_offset, SETTILE, LoadSync, LOADBLOCK 0x073FF200, PipeSync, BA000E02/0x8000, F5400800/0x00018060, SetTileSize 0x007E07E0, TextureRectangle w0=E4000000\|xr\|yl_bot w1=xl\|yl_top with xr/xl=((..*64+32)<<2 &0xFFF)<<12, B4000000, B3000000/0x04000400) all verified. ONE divergence -> FID-0107: SETTILE w1 retail 0x07000000 (tile 7=LOADTILE, matches LOADTLUT/LOADBLOCK tile-7 refs) vs port 0x00070000 (tile 0 + stray cmt/maskt), both palette (1404) and loop (1463) sites; likely digit-shift typo, sibling of FID-0104. |
| 151 | `sub_GAME_7F0AB908` | src/game/watch.c | `verified-equivalent` | Options-menu toggle renderer (real body live; #if 0 PORT_FIXME_STUBS stub correctly disabled). Live C 10858-10992 vs ASM 10998-11212. color_opt1/2/3 default 0x800080; x_pos=j_text_trigger?0xAA:0xB4, option2_x_pos=?0xDC:0xE1 (slot 0x4c); select_state dispatch (beqz/beq 0,1,2) verified: state 0 and 1 set 0xff00b0 by current_value(entry+8), state 2 calls game_option_toggle_input(option_index) then 0xa0ffa0f0 by current_value, other->stay default; entry stride 0xC (option_index*12), text u16[4] at +0/2/4/6; text[3]==0 override x_pos=0xC8/0xBE and option2_x_pos=0xFA both branches (dead 3rd store harmless); three sub_GAME_7F0A9398 calls with (color,0,0xffffffff,1,0,0x3000B0,0) tuples and x_pos/option2_x_pos/0x10e a1 all arg-verified; branch senses correct. Deterministic UI, live via draw_toggle_options. |
| 152 | `chraiFistAttackHandler` | src/game/chrprop.c | `verified-equivalent` | Player melee/fist attack hit-scan. Live C 4820-4936 vs ASM 4940-5143. Reverse walk pp=g_LastOnScreenProp-1 down to g_OnScreenPropList[0] (sltu guards, stride -4). Per prop: NULL skip, zDepth<500 (c.lt.s), type==3(CHR) fall-through else type==6(VIEWER) needs chr!=NULL and getPlayerPointerIndex!=get_cur_playernum else continue. range=50, chr=prop->chr(+4), getCurrentWeaponOrItem==17(SNIPERRIFLE)->range=100. Three sub_GAME_7F03F748(chr->model+0x1c, &max,&min, lane 0/1/2) axis tests: lanes 0,1 require 0<=max && min<=0; lane 2 requires min<=0 && -range<=max (order and c.le.s senses verified). stanTestLineUnobstructed(&tile=posdata->stan+0x14, posdata pos.x+8/pos.z+0x10, prop pos.x+8/pos.z+0x10, 19, dh, dh, 0.0, 1.0) then tile==prop->stan(+0x14). bodypart=15 default, ==200 when crouch==CROUCH_HALF(1), ==201 when crouch==CROUCH_SQUAT(0) -- verified against CROUCH enum SQUAT=0/HALF=1/STAND=2 and the else-if two-call structure. bullet_path_from_screen_center(&pos,&dir,hand)+mtx4RotateVecInPlace(currentPlayerGetMatrix10D4,&dir), handles_shot_actors(chr,bodypart,&dir,item_id,1)->hit=1 + recall_joy2_hits_edit_detail_edit_flag(item_id,prop,-1). No-hit && item_id==ITEM_FIST(1) -> sndPlaySfx(g_musicSfxBufferPtr,105,0). All enum literals (item 1/17, type 3/6, crouch 0/1) confirmed. Sim-critical, fully equivalent. |
| 153 | `textRender` | src/game/textrelated.c | `finding-filed:FID-0108` | Text renderer (ASCII + JP). Live C 1949-2071 (NATIVE_PORT form live) vs US ASM 2077-2279. Verified equivalent: D_80040EA8=1, initial_x=*x/initial_y=*y, prev_char=72, arga default *(sft+0x88c)+*(sft+0x890), j_text_trigger&&arga<14->14, FA000000 header + arg6 (byteswap reassembles to identity), space *x+=5, newline (overlap_correction>=0 && *x==initial_x?*y+=overlap:*y+=arga then *x=initial_x), JP path (24B D_80040EFC copy, jis=((ch&0x7f)<<7\|ptr[1]&0x7f)&0xFFFF, &0x2000->w15/h16, (jis&0x1fff)>=0x3c7->jis=2, index=jis+0x80, pixeldata=langGetJpnCharPixels, ptr+=2), ASCII construct offsets sft+ch*24-0x318 & prev_char*24-0x318, all construct_fontchar_microcode arg tuples (both call sites). ONE divergence -> FID-0108: port adds ch>=0x21 && ch<=0x7e render bound (C 2014) + skips other ch<0x80 (C 2032); retail (ASM only slti 0x80 at 2176) renders control/DEL bytes via out-of-range font indexing. Behavior-neutral on valid printable+JP text. JP/PAL region-variant glabels (2284,2492) share identical logic. |
| 154 | `sub_GAME_7F0A2C44` | src/game/unk_0A1DA0.c | `verified-equivalent` | Falling glass-shards DL builder (un-stubbed for native, A/B via GE007_GLASS_SHARDS; FID-0003/FID-0007 tracked). Live retail-form C 1139-1375 vs ASM 1381-1571. texSelect(&gdl, glassoverlayimage+0xC, 2,1,2); 7 header GBI words byte-verified (BB001001/0D800D80, BA001402/00100000, BA001001/00010000, B6000000/00003000, BA000C02/00002000, B7000000/00060000, field_10E0 base 0x01030040 + osVirtualToPhysical(get_BONDdata_field_10E0)). Per-piece loop (bound SHATTERED_WINDOW_PIECES_BUFFER_LEN, stride 0x68, piece->piece>0 gate): matrix_4x4_set_position_and_rotation_around_xyz(&piece->x=+4, &piece->field_0x10=+0x10, &mtxf@sp+0x90), subtract g_CurrentPlayer->current_model_pos (x+0x38/y+0x3c/z+0x40) from mtxf.m[3][0..2] (mtxf+0x30/34/38) with NO per-piece scalar multiply (matches ASM .L7F0A2DB0 straight into matrix_4x4_f32_to_s32), emit 0x01020040/osVirtualToPhysical(mtx) + 0x04200030/osVirtualToPhysical(piece+0x38) + 0xBF000000/0x00000A14. Footer B6000000/00060000 + 0x01030040/proj + 0x01020040/matrix10C8. dynAllocateMatrix ordering (ASM calls before matrix build, C after) is a no-op reorder. DELTAS all documented port matrix-format/pointer plumbing, not divergences: FID-0003 1/vis scale cancellation (A/B GE007_FIELD_10E0_SCALED, =0 is byte-identical to ASM), FID-0007 float-mtx path + G_MTX_FLOAT_PORT / bondviewGetField10E0GbiFlags tag (0 in fixed config -> retail 0x01030040), osVirtualToPhysical wrapping on footer proj/10C8 pointers (port interpreter needs it; N64 reads raw), plus additive NULL/overflow guards and GE007_TRACE_GLASS diag. |
| 155 | `sub_GAME_7F0A97D0` | src/game/watch.c | `verified-equivalent` | Watch controller-options text renderer. Live C 6545-6612 vs ASM (LEFTOVERDEBUG glabel 6618). pFontFile=ptrFontBankGothic(0x58)/pFontChars=ptrFontBankGothicChars(0x54), textheight/textwidth 0 (0x60/0x5c), buffer[0x7DC]@sp+0x74[0]=0. Loop i<D_800409D8 appends langGet(D_800409DC[i*10]) (u16, stride 0x14). actively_selected!=0 && controller_options_index==0 -> sub_GAME_7F0A611C 9 args verified (player +0x2a60/2a58/2a64/2a5c/2a68, D_800409D8, 0, 1, j_text_trigger?14:10). x=170(0x70)/y=26(0x6c). textMeasure(&th,&tw,buffer,pFontChars,pFontFile,lh) then textRender(gdl,&x,&y,buffer,pFontChars,pFontFile,0x00AA00B0,textwidth,lineheight,neg_vspacing@+0x2a64,lineheight2) all args verified. has_set_control_type_data(+0x2a68)!=0 -> selectedText=langGet(D_800409DC[cur_player_control_type_0(+0x2a58)*10]) recomputed twice, textMeasure, x/y reset, textRender(...,0xA0FFA0F0,textwidth,100,0,lineheight). ONE delta: retail strcat -> port watchAppendText(buffer,0x7DC,...) bounded concat (behavior-neutral for fixed short menu strings). |
| 156 | `sub_GAME_7F0A97D0` | src/game/watch.c | `verified-equivalent` | Same function, non-LEFTOVERDEBUG region variant (glabel 6827). Identical control flow/args to the LEFTOVERDEBUG copy; equivalent to live C 6545-6612 (same strcat->watchAppendText bounded-concat delta, behavior-neutral). |
| 157 | `write_playerrank_to_buffer` | src/game/mp_watch.c | `verified-equivalent` | ASM at glabel write_playerrank_to_buffer matches C: fill indices and get_points_for_mp_player scores, optimized descending bubble sort swapping indices in lockstep on strict less-than, find player_index position, count ties above to derive rank, switch rank 0..3 with scenario-dependent 2v2 or 2v1 (5 or 7) and 3v1 (6) cases mapping to RANK1ST..4TH string ids 0xA011..0xA014. Nuance: retail sprintf vs port snprintf with 256 and a percent-s wrapper, identity for format-free localized rank labels; the not-found player path is unreachable since player_index is always present. |
| 158 | `textRenderGlow` | src/game/textrelated.c | `finding-filed:FID-0109` | Retail US ASM gates only ch less-than 0x80 before the sub_GAME_7F0ADDAC construct call (7F0AE7E4 slti at,v0,0x80 then 7F0AE7E8 beqz); live C at textrelated.c:3771 narrows the render to ch 0x21..0x7e and routes other ch less-than 0x80 to no-render. Exact sibling of FID-0108 on textRender. Rest of textRenderGlow verified equivalent. |
| 159 | `sub_GAME_7F03AB58` | src/game/chrprop.c | `verified-equivalent` | Room-iteration bullet-vs-background test matches ASM: visited-set marking, sub_GAME_7F03A97C and bgTestBulletHitBackground guard calls with correct arg registers, hit-position scale multiply and writeback, a 3-axis strict-between test whose branch-likely spaghetti implements the C boundary inclusivity (src less-equal hit less-than out when src less-equal out; out less-than hit less-equal src otherwise) on all axes, best_room assignment, and the 0x30-byte hit-record copy. |
| 160 | `sub_GAME_7F0AE45C` | src/game/textrelated.c | `verified-equivalent` | Font-glyph RDP texrect emitter matches ASM across all four clip cases: right-edge early-out, top-clipped, bottom-clipped, and unclipped with the text_orientation variant. Every RDP word (0xE4 or 0xE5 texrect, 0xB4, 0xB3), the shift-12 and and-0xFFF and shift-5 bitfield packing, char_data width/y_offset/tex_height at offsets 0xC and 4 and 8, and the gdl-plus-3 return all match. |
| 161 | `sub_GAME_7F09BBBC` | src/game/unk_09B7A0.c | `verified-equivalent` | Free-block coalescing (stride 20) matches ASM across both merge loops, field offsets 0 4 8 0xC 0xE 0x10 0x12, the merge accounting into word_CODE_bss_8007A0F2, and the part-2 linked-list relink. Nuance: the C guards cast s16 of dword_CODE_bss_8007A0D8 shifted right 2 where the ASM compares full 32-bit; benign because the item count is s16-ranged by construction (default 0x1F4, stored via s16 cast at init) so the shifted value never exceeds s16 range. |
| 162 | `sub_GAME_7F0A9398` | src/game/watch.c | `verified-equivalent` | Watch text helper matches ASM: textMeasure call, center-flag signed division-toward-zero for text_width over 2 and right-flag x adjust, g_WatchBackgroundGreen below 0xE0 branch emitting 0xB900031D with 0x0050004B vs 0x005041C8, bg-flag microcode_constructor_related_to_menus arg tuple, and mutually-exclusive glow_yoffset zero picks textRender vs nonzero picks textRenderGlow with matching 11 and 12 arg tuples. The s16 and u32 casts on small text-dimension and color args are identity for the value ranges. |
| 163 | `texFindClosestColourIndexIA` | src/game/image.c | `verified-equivalent` | Closest IA-palette search matches ASM: 4x-unrolled exact-match linear scan returning correct indices, binary search localizing a region by intensity-squared plus alpha-squared magnitude with correct low and loop_end updates including the equal-sum both-assign case, plus-or-minus-4 window clamp, and final window search minimizing squared IA distance. The C sum2 and the if-j statement are inert compiler-coaxing artifacts. |
| 164 | `record_player_input_as_packet` | src/game/ramromreplay.c | `verified-equivalent` | Demo-record packet writer matches ASM: CONTSAMPLE_LEN 20 modulo sample walk, per-pad stick and 16-bit button split into low and high bytes, u8-wrapping checksum, count and speedframes and randseed and check header at offsets 1 0 2 3, and the ALIGN16 romWrite plus even-align address advance. The ASM g_randomSeed plus 4 load is the big-endian low-word access for u64 to u8 truncation, identical to the C randseed assignment. NATIVE_PORT trace calls are pure instrumentation. |
| 165 | `sub_GAME_7F09343C` | src/game/unk_092E50.c | `verified-equivalent` | MipMap2C microcode emitter matches ASM: arg1 nonzero emits gSPDisplayList opcode 0x06000000 to MipMap2C_Something_Setup; arg1 zero emits 12 Gfx commands with a sinf of flt_CODE_bss_80079E88 times 127 plus 128 pulsing alpha in the 0xFA000000 command. Out-of-order scheduled stores land in the correct 12 slots and every RDP word matches. Float-to-u32 is the standard MIPS bias idiom. |
| 166 | `sub_GAME_7F09365C` | src/game/unk_092E50.c | `verified-equivalent` | Sibling of sub_GAME_7F09343C, same structure: arg1 nonzero emits gSPDisplayList to MipMap2C_Something2_Setup; arg1 zero emits 12 Gfx commands with the sinf pulsing alpha. Differs from 7F09343C only in 0xF5480400 vs 0xF5100800 for the first two commands and the Something2 DL, both consistent with its C. Verified equivalent. |
| 167 | `play_sound_for_shot_actor` | src/game/chrlv.c | `verified-equivalent` | Guard-yelp sound picker matches ASM: prop type VIEWER 6 check, bonddead offset 0xd8 early-out, single-player c_item_entries isMale (stride 20 offset 16) vs MP get_player_mp_char_gender, male vs female sound-table rotation via sndPlaySfx with counter wrap at 25 and 3, chrobjSndCreatePostEventDefault with prop pos at offset 8. male_guard_yelps 0x86..0x9E and female_guard_yelps 0xD..0xF literals match the globals; the function-static counters behave identically to the single-user retail globals. |
| 168 | `sub_GAME_7F0A3C08` | src/game/unk_0A1DA0.c | `verified-equivalent` | Effect-entry initializer (LEFTOVERDEBUG rodata variant) matches C body at line 3644: u32-to-f32 random angle times 6.2831855, type switch (4 gives unk04 1 and 1.0 and FLARE, 1 gives 11 and 0.5 and SMOKE, 3 gives 9 and 0.5 and SCATTERED, 6 gives 100 and 0.0 and FLARE, default 11 and 0.5 and SMOKE), D_80040960 RGBA pack stride 4 into bytes 0x28 through 0x2b, pos copy, scale times factor times 1.41412 with cosf and sinf. Port stores a stable EFFECT_IMG handle where retail stores the raw image pointer, a pointer-portability refactor with a 1-to-1 resolver, behaviorally equivalent. |
| 169 | `sub_GAME_7F0A3C08` | src/game/unk_0A1DA0.c | `verified-equivalent` | Non-LEFTOVERDEBUG variant of the same sub_GAME_7F0A3C08; the file comment confirms identical asm differing only in which rodata holds 6.2831855 and 1.41412 (D_8004CE20 and D_8005774C vs D_80057750 and D_80057754). Verifies against the same C body as the LEFTOVERDEBUG variant; same verdict. |
| 170 | `constructor_menu00_legalscreen` | src/game/front.c | `verified-equivalent` | Legal-screen logo + 12-line legal text renderer. ASM front.c:2226-2364 vs live NATIVE_PORT C front.c:2153-2223, traced fully: 0x40-byte spE4 = D_8002AABC struct copy (3-word loop + tail word); insert_imageDL; matrix_4x4_7F059694(&spA0, 0,0,4000, 0,0,0,0,1,0) with stack args at 0x10-0x24 matching arg order; spE4.unk_matrix at offset 0, mtxlist at 0x10 (=dynAllocate of logoinst header halfword-0xe << 6), flags=3 at 0x8, zbuf=0 at 0x4, gdl at 0xc; subdraw then DL=spE4.gdl; microcode_constructor; legal loop legalpage_text_array[0..12] stride 0x14 (terminator address = D_8002AABC by linker adjacency), display_aligned_white_text_to_screen with h_pos/v_pos/halign/valign/txtID at offsets 0/4/8/0xc/0x10 and fonts ptrFontZurichBoldChars/Bold. Only deviations are NATIVE_PORT substrate: the retail float-to-s32 view-matrix conversion loop (matrix_4x4_f32_to_s32 over render_pos) is compiled out on native (matrix-format substrate, float path used), plus additive modelAllocRenderPos null guard and gfx_register_draw_class_dl_range. No sim/behavior divergence. |
| 171 | `sub_GAME_7F09C044` | src/game/unk_09B7A0.c | `verified-equivalent` | Free-list coalescing free (unk_09B7A0 allocator). ASM unk_09B7A0.c:914-1054 vs live C 840-909, traced fully. Pool select by address range: arg0 within pool A base dword_CODE_bss_8007A0E0 span (count dword_CODE_bss_8007A0D0, 16-byte Vertex stride via sll 4) -> entries E8, avail F0; else pool B (E4/D8/EC/F2); else sub_GAME_7F09B7A8 + sub_GAME_7F09B7E4 return. Main do-loop walks entries by idx (a1), 20-byte stride: non-match path idx = entry unk10 with idx==-1 -> found (bnel s0,entry-unk00 delay lh idx); match path found=1, entry unk0E-- and when it reaches 0 does forward-merge (next_idx = unk10 >=0, other unk0E==0 -> absorb unk0C/unk10, other unk0E=-1, fix successor unk12=idx) and backward-merge (prev_idx = unk12 >=0, other unk0E==0 -> other absorbs unk0C/unk10, entry unk0E=-1, fix successor unk12=prev_idx). Every field offset (unk00 ptr, unk0C size at 0xc, unk0E refcount at 0xe, unk10 next at 0x10, unk12 prev at 0x12), branch sense, and *avail_ptr += unk0C match. No jump table; no divergence. |
| 172 | `sub_GAME_7F03AF5C` | src/game/chrprop.c | `verified-equivalent` | Closest-bg-hit-over-rooms selector. ASM chrprop.c:1746-1881 vs live NATIVE_PORT C 1659-1744, traced fully. best_dist init 4294967296.0 (0x4F800000 in f26), best_room=0, scale=get_room_data_float2 (f24); gate getMaxNumRooms>=2 (slti v0,2). Per-room loop room_id 1..getMaxNumRooms with visited_rooms pointer s2 advancing in lockstep: skip when visited_rooms[room_id]!=0 (else set =1 via sb s5), skip when sub_GAME_7F03A97C(room_id, scaled_pos, direction)==0, skip when bgTestBulletHitBackground(src_pos, dest_pos, room_id, &hit_buf)==0. Then dx/dy/dz = hit_buf.pos * scale - src_pos component-wise, dist_sq sum; adj_dist = dist_sq - 4.0 when check_if_imageID_is_light(hit_buf.texturenum at 0x2A)!=0; if adj_dist < best_dist store best_dist/best_room and copy the 0x2C-byte hit record field-by-field into hit_output (f32 at 0/4/8/c/10/14, s32 at 18/1c/20/24, s16 at 28/2a). The bc1fl not-better path folds room_id++ into the delay slot; better path stores then increments at the shared tail. arg registers (src_pos a1, dest_pos at 0xc8(sp), scaled_pos at 0xd0, visited_rooms 0xd4, hit_output 0xd8) all match. Return best_room. NATIVE_PORT typed-struct copy is bit-identical to the retail field copy; no divergence. |
| 173 | `sub_GAME_7F0D2A84` | src/game/spectrum.c | `verified-equivalent` | ZX Spectrum tile-refresh dispatcher (spectrum minigame). ASM spectrum.c:501-642 vs live C 435-499, traced fully. Frame counter D_8004ED08 = (s8)(D_8004ED08+1); every 25th frame ((counter & 0xff) % 25 == 0 via div/mfhi): reset counter=0, set all 768 ptr_300alloc dirty bytes to 1 (unrolled x4 loop, pointer reloaded each store), and flip D_8004ED04 = 1 - D_8004ED04. Then a 3x64 row_block x 4x64 col_block nest (row_block 0..192 step 64, col_block 0..256 step 64): base_col=(row_block>>6)<<2, tile_ptr = arg1 + ((base_col + (col_block>>6))<<11), inner while row<row_end (row+=8, tile_ptr+=0xe0) x while col<col_end (col+=8, tile_ptr+=4): when ptr_300alloc[((row>>3)<<5)\|(col>>3)]!=0 call sub_GAME_7F0D28E0(arg0, col, row, tile_ptr). Finally clear all 768 dirty bytes to 0. The while-loop entry guards (row<row_end via beqz at loop top L7F0D2B70, col<col_end at L7F0D2B94) and increment-tests match the C nested do/while compilation; col_end computed once, col reset per row via move s0,s7; tile_ptr per-row +0x100 total (8*4 inner + 0xe0). All strides, masks and branch senses match; no divergence. |
| 174 | `sub_GAME_7F09BE4C` | src/game/unk_09B7A0.c | `verified-equivalent` | Free-list first-fit alloc + block split (unk_09B7A0 allocator). ASM unk_09B7A0.c:688-832 vs live C 581-686, traced fully. Pool select maps 0xCCCC -> pool A (E8/F0/max D4) and 0x0B0B -> pool B (EC/F2/max DC) else NULL (ASM tests 0x0B0B first then 0xCCCC, C lists CCCC first; distinct constants so identical result). Walk loop by idx: skip when entry unk0E!=0 or unk0C < count (both delay lh idx=unk10), else found=1; tail iter++ then found=-1 when idx==-1 (beql) or max_entries < iter (slt). After loop: max_entries<iter -> call 7F09B7A8+7F09B7E4 return NULL; found!=1 -> NULL. Alloc body: old_count=unk0C, unk04=arg2, unk08=arg3, unk0E++, and when old_count!=count and max_entries>0 scans for a free slot (unk0E==-1) to carve the remainder (new slot unk00 = entry-unk00 + count*16, unk0C = old_count-count, unk0E=0, unk12=idx, unk10=entry-unk10; fixup old successor unk12=j; entry-unk10=j; split_done=1). Then *avail_ptr -= count if split else old_count; return entry-unk00. Every offset and branch sense matches. The live C adds defensive guards absent from retail: top-of-loop idx bounds break, the idx>=max_entries term in the found=-1 test, and the entries/avail null + max_entries<=0 + count<=0 + count > avail early return. These never fire on a well-formed acyclic free-list (retail exits on idx==-1 before re-indexing; unk10 is always a valid index or -1; callers pass count>0) and do not change the split/coalesce math -> behavior-neutral on valid state. |
| 175 | `load_font_tables` | src/game/textrelated.c | `waived:NATIVE_PORT full rewrite, not instruction-diffable. Live compiled body is the NATIVE_PORT branch textrelated.c:468-506 (build defines NATIVE_PORT), which parses the two font blobs (bank-gothic 0x24B0, zurich-bold 0x3540) via pc_convert_font into port-native struct font / struct fontchar. Retail ASM textrelated.c:575-703 (matched by the non-compiled else body 510-572) loads each ROM blob with romCopy and RELOCATES in-place glyph pointers by adding the allocation base (gothic: offset 0x14 per 0x18-byte char across 0..0x8d0; zurich: same field, char0/char1 peeled then 4x-unrolled from 0x30). Verifying equivalence requires auditing pc_convert_font byte-output vs the retail relocated blob and the downstream glyph metrics/render path -- a dedicated deep audit, not an ASM-vs-C instruction diff. The globals zeroed at entry (text_spacing/orientation/wordwrap/x/y/s/t, overlap_correction=-1, bilevel=0) match both; the divergence is entirely in the font-loading mechanism.` | NATIVE_PORT full rewrite, not instruction-diffable. Live compiled body is the NATIVE_PORT branch textrelated.c:468-506 (build defines NATIVE_PORT), which parses the two font blobs (bank-gothic 0x24B0, zurich-bold 0x3540) via pc_convert_font into port-native struct font / struct fontchar. Retail ASM textrelated.c:575-703 (matched by the non-compiled else body 510-572) loads each ROM blob with romCopy and RELOCATES in-place glyph pointers by adding the allocation base (gothic: offset 0x14 per 0x18-byte char across 0..0x8d0; zurich: same field, char0/char1 peeled then 4x-unrolled from 0x30). Verifying equivalence requires auditing pc_convert_font byte-output vs the retail relocated blob and the downstream glyph metrics/render path -- a dedicated deep audit, not an ASM-vs-C instruction diff. The globals zeroed at entry (text_spacing/orientation/wordwrap/x/y/s/t, overlap_correction=-1, bilevel=0) match both; the divergence is entirely in the font-loading mechanism. |
| 176 | `matrix_4x4_f32_to_s32` | src/game/matrixmath.c | `finding-filed:FID-0110` | Float-to-s16.16 matrix packer. ASM matrixmath.c:577-700 vs live NATIVE_PORT C 511-528. The s16.16 bit-packing is byte-identical (traced: top-half int = (e1 & 0xffff0000) \| (e2 >> 16), bottom-half int = (e1 << 16) \| (e2 & 0xffff); ai walks ms row0, af walks ms row2; 8 unrolled iterations indexed as flat float pairs a0 + v0*8). The ONE divergence is the fixed-point scale source: retail multiplies e1 by D_80032310 index 0 (load matrixmath.c:592, mul 598) and e2 by D_80032310 index v0-and-1 (matrixmath.c:600-601); native FTOFIX32 uses constant 65536 for both (matrixmath.c:506). D_80032310 index 0 is runtime-mutated by matrix_4x4_7F058C4C(mCurrentLevelVisibilityScale) at bg.c:5143, so native ignores the per-level visibility scale. Byte-identical at the default scale. See FID-0110. |
| 177 | `textMeasure` | src/game/textrelated.c | `verified-equivalent` | Text width/height measurer. ASM textrelated.c:4475-4609 vs live C 4389-4473, traced fully. Inits prevchar='H'(72), longest=0, *textheight=0, *textwidth=0; lineheight defaults to font1['[' code 0x5B] baseline (offset 4) + height (offset 8) reading font1 index 0x5B at 0x888 (a retail quirk preserved verbatim, no glyph-index adjustment) when lineheight==0; clamped to 14 when j_text_trigger and lineheight<14. Per-char loop: space (0x20) adds 5 unless next is newline and resets prevchar; newline (0x0a) updates longest, zeroes *textwidth, adds lineheight to *textheight; normal printable computes tmp = font2.kerning[prevchar_ki*13 + thischar_ki] + text_spacing - 1 (fontchar 0x18 stride, kerningindex at 0x10, width at 0xc, base offset -0x308/-0x30c compensates the raw-code index) and *textwidth += width - tmp, prevchar=cur; multibyte 0x80..0xBF adds 11 and 0xC0+ adds 15 (both tmp = kerning[0] + text_spacing - 1, text += 2). Final *textwidth = max(*textwidth, longest). Every offset/const/branch sense matches for printable + newline + multibyte input. Documented deviation: the C adds a printable-range guard (cur in 0x21..0x7e) plus a separate control-char branch (prevchar='H', text++) that retail lacks -- retail runs the normal-char kerning path for ALL cur<0x80 after space/newline. Unreachable on valid game text (langGet strings are printable ASCII, newline, or multibyte >=0x80); no divergence on real data. Plus additive NATIVE_PORT font1/font2 null guard. |
| 178 | `sub_GAME_7F058E78` | src/game/matrixmath.c | `finding-filed:FID-0110` | s16.16-to-float matrix unpacker (inverse of matrix_4x4_f32_to_s32). ASM matrixmath.c:731-850 vs live NATIVE_PORT C 708-729. The bit-unpacking is byte-identical (traced: e1 = (*ai & 0xffff0000) \| (*af >> 16), e2 = (*ai << 16) \| (*af & 0xffff); ai = arg0, af = arg0 + 8 ints; 8 unrolled iterations). The ONE divergence is the scale: retail divides e1 by D_80032310 index 0 (load matrixmath.c:748, div 761) and e2 by D_80032310 index v0-and-1 (matrixmath.c:791-797); native divides both by constant 65536 (matrixmath.c:721-722). Same runtime-scale-ignored issue as matrix_4x4_f32_to_s32; byte-identical at default scale. See FID-0110. |
| 179 | `sub_GAME_7F0762E0` | src/game/objecthandler_2.c | `waived:NATIVE_PORT texture/GDL-loader reconstruction at 69.88 percent (decomp.me note in-source), structurally divergent, needs dedicated audit. Live compiled body objecthandler_2.c:28-95 (NONMATCHING, uses NATIVE_PORT texLoadFromGdl/fileSetSize). Function relocates model-file GDL segment pointers using ROM-vs-PC remaining-buffer offsets (get_rom/get_pc_remaining_buffer_for_index) and reloads textures into the port texture pool via texCopyGdls/texLoadFromModelFileHeader/texLoadFromGdl. The C loop diverges from ASM objecthandler_2.c:97-218: the ASM else-branch (gdl==0, L7F0763E8) computes var_s1 = prev_gdl & 0xFFFFFF and var_s2 = sp54 + temp_fp - Switches - var_s1 using a cross-iteration s0 = previous-iteration gdl carry (reloaded in the loop-back delay slot 7F076438), whereas the C sets var_s1=0, var_s2=temp_fp with no prev-gdl term. Confidently classifying this (real defect vs absorbed by the port texture-pool helper semantics) requires deep analysis of the ROM/PC buffer relocation model and the reimplemented tex helpers, not a straight instruction diff.` | NATIVE_PORT texture/GDL-loader reconstruction at 69.88 percent (decomp.me note in-source), structurally divergent, needs dedicated audit. Live compiled body objecthandler_2.c:28-95 (NONMATCHING, uses NATIVE_PORT texLoadFromGdl/fileSetSize). Function relocates model-file GDL segment pointers using ROM-vs-PC remaining-buffer offsets (get_rom/get_pc_remaining_buffer_for_index) and reloads textures into the port texture pool via texCopyGdls/texLoadFromModelFileHeader/texLoadFromGdl. The C loop diverges from ASM objecthandler_2.c:97-218: the ASM else-branch (gdl==0, L7F0763E8) computes var_s1 = prev_gdl & 0xFFFFFF and var_s2 = sp54 + temp_fp - Switches - var_s1 using a cross-iteration s0 = previous-iteration gdl carry (reloaded in the loop-back delay slot 7F076438), whereas the C sets var_s1=0, var_s2=temp_fp with no prev-gdl term. Confidently classifying this (real defect vs absorbed by the port texture-pool helper semantics) requires deep analysis of the ROM/PC buffer relocation model and the reimplemented tex helpers, not a straight instruction diff. |
| 180 | `sub_GAME_7F03F598` | src/game/chrprop.c | `verified-equivalent` | Oriented-bounding-box point-inclusion test (BoundPadRecord). Live compiled body is the PORT_FIXME_STUBS branch chrprop.c:8883-8918 (build defines NONMATCHING+NATIVE_PORT+PORT_FIXME_STUBS per CMakeLists.txt:557; the else node-walker 8920-8949 is dead/never-compiled decomp garbage). ASM chrprop.c:8955-9069 traced fully: rel = pos - boundpads.pos (fields at 0/4/8); side = up cross look where up = fields 0x0c/0x10/0x14 and look = fields 0x18/0x1c/0x20 (all three cross components verified against sp-local side.x/y/z at 0x10/0x14/0x18, mul commutes only); three axis projections rel dot look / rel dot up / rel dot side, each compared as (bbox.max + arg1) < proj \|\| proj < (bbox.min - arg1) -> return 0, with bbox fields xmin 0x2c xmax 0x30 ymin 0x34 ymax 0x38 zmin 0x3c zmax 0x40; look axis uses z-bounds, up axis y-bounds, side axis x-bounds; all pass -> return 1. Every mul/sub/add, c.lt.s and bc1t/bc1fl branch sense, and field offset match. Only deviation = additive pos/boundpads null guard (behavior-neutral on valid pointers). |
| 181 | `sub_GAME_7F02BFE4` | src/game/chrlv.c | `verified-equivalent` | Weapon tracer-sound firing handler. ASM chrlv.c:7183-7302 vs live C 7105-7180, traced fully. prop = chrGetEquippedWeaponProp(self, arg1); temp_v1 = prop-chr (offset 4); attack_item = lb temp_v1 offset 0x80; sp33 = bondwalkItemGetSoundTriggerRate(attack_item) as unsigned byte; sp30 = bondwalkItemGetSound(attack_item) halfword. should_fire logic: arg2!=0 and (sp33<=0 -> fire; else fire only when (self.hidden & 0x80 CHRHIDDEN_FIRE_TRACER)==0 and self.field_178[arg1] < g_GlobalTimer). On fire: pick two sound slots per hand at self + arg1*8 + 0x168/0x16c (hand0 168/16c, hand1 170/174); deactivate each non-null slot whose sndGetPlayingState != AL_STOPPED(0); if sp30!=0 pick target_slot = first null of slot1/slot2; if found sndPlaySfx(g_musicSfxBufferPtr, (s16)sp30, target_slot), chrobjSndCreatePostEventDefault(*target_slot, &self.prop.pos) with self.prop at 0x18 + pos at 8, set self.field_178[arg1] = g_GlobalTimer + sp33 (field_178 offset 0x178 stride 4), self.hidden \|= 0x80. All offsets, blezl/slt/beql branch senses, and slot arithmetic match. Only deviation = additive prop==NULL / prop-chr==NULL null guard (behavior-neutral on an equipped weapon). Header note 'identical instructions/registers: fail' is decomp regalloc/scheduling, not behavior. |
| 182 | `sub_GAME_7F0D28E0` | src/game/spectrum.c | `verified-equivalent` | ZX Spectrum 1bpp-to-4bpp cell pixel expander (spectrum minigame). ASM spectrum.c:299-427 vs live NATIVE_PORT C 113-157 (build takes the elif-NATIVE_PORT branch), traced fully. var_v0 pixel-row address = arg0 + (((arg2<<5)&0x1800)\|((arg2<<8)&0x700)\|((arg2<<2)&0xE0)\|(arg1>>3)); temp_t1 attribute byte = arg0[((arg2<<2)&0x3E0)+(arg1>>3)+0x1800]. Foreground/background nibbles: when (temp_t1 & 0x80) and D_8004ED04 (D_8004EC40+0xC4) !=0 -> var_a0=(t1>>3)&7, var_v1=t1&7 else swapped; when !(temp_t1 & 0x40) both += 8 masked to 0xff (bright bit). 8-row loop reads *var_v0 and emits 4 output bytes, bit pairs 0x80/0x40 -> byte0, 0x20/0x10 -> byte1, 0x08/0x04 -> byte2, 0x02/0x01 -> byte3, each = t0_source \| (a2_source << 4), then arg3 += 0x20, var_v0 += 0x100. Every mask, shift, ternary select (beqz senses) and stride matches; no divergence. |
| 183 | `set_actor_on_path` | src/game/chrlv.c | `finding-filed:FID-0111` | Core patrol-step search and struct writes match ASM 0x7F0290F8; NATIVE_PORT adds non-retail lastvisible60=timer and idle-anim compensations. See FID-0111. |
| 184 | `sub_GAME_7F0A8FEC` | src/game/watch.c | `verified-equivalent` | Matches ASM 0x7F0A8FEC: dyn-alloc 12 verts, sfx volume read plus optional stick adjust, vol over 32767 float, gfx words 0xB900031D and 0x00504240, three setup_watch_rectangles at 600 20 -299 -205 with 7F0A3B40 emits, then 7F0A8D40 of vtx volFloat 30. Only add is a NATIVE_PORT null-guard on dyn-alloc failure, a no-op on the never-NULL faithful path. |
| 185 | `sub_GAME_7F0A8D40` | src/game/watch.c | `verified-equivalent` | Matches ASM 0x7F0A8D40 instruction-for-instruction: some_offset equals s32 of arg2 times 1.2 minus arg1, diff equals halfword 0x20 minus halfword 0x40, entries 0 to 3 RGBA 32 64 32 224, entries 4 to 9 color 48 arg1 plus 64 and 96 arg1 plus 128 with entries 6 to 9 also x-interp clamped to min, entries 10 to 11 x-interp clamped to max, tail copies halfword 0xA0 into halfword 0x10 and halfword 0. The under-10 path float-adds diff and some_offset separately while the 10-and-up path integer-adds them first, exactly as ASM. |
| 186 | `sub_GAME_7F0A91C8` | src/game/watch.c | `verified-equivalent` | Same structure as 7F0A8FEC but for music track 2: reads get_mTrack2Vol, gates on game_options_index equal 0 via bnezl at 0x7F0A9204, calls set_mTrack2Vol, and uses rect y of -275 not -205. All gfx words, 7F0A3B40 emits, and 7F0A8D40 of vtx volFloat 30 tail match ASM 0x7F0A91C8. |
| 187 | `sub_GAME_7F03E27C` | src/game/chrprop.c | `finding-filed:FID-0112` | Retail 0x7F03E27C branches on prop->flags bit 8 and reads projectile+0xCC room source; NATIVE_PORT reimpl restructures the branch and drops that indirection. See FID-0112. |
| 188 | `sub_GAME_7F03ADF4` | src/game/chrprop.c | `verified-equivalent` | Matches ASM 0x7F03ADF4: room flood-fill from start_room with occluded-mark, sub_GAME_7F03A97C visibility then bgTestBulletHitBackground returning current_room on hit, sub_GAME_7F0B8EFC adjacency with dedup insert into room_list. The inner dedup compares the u8 room entry against the full adjacency word while C casts to u8; equivalent because room ids fit in a byte. |
| 189 | `sub_GAME_7F022078` | src/game/chr.c | `verified-equivalent` | Core matches ASM 0x7F022078: linear search of D_8002C914 stride 44 for chr_num giving found_index, entry field_10 gated by random bit 4 producing scaled_dir equal dir times 42 over mag plus 1 transformed by currentPlayerGetMatrix10D4 then 7F0A3E1C with field_10 field_18 room, plus entry field_4 to 7F0A3E1C with hit_pos field_4 field_C room; room read from prop->stan offset 3. C adds two port-hardening guards absent in ASM: a mag under 0.001 div-by-zero early return and a prop->stan NULL guard, both altering only would-crash-on-N64 degenerate paths. |
| 190 | `sub_GAME_7F0A9684` | src/game/watch.c | `verified-equivalent` | Matches ASM 0x7F0A9684: reads joystick X twice to set active-input when at least 10 or less than -9, resets counter to 0 when active and watch selected and controller option 1 else increments counter up to 100, and when counter reaches 100 rotates by minus speed over 10 via 7F0A95C4 with 4.0 else when watch selected and controller option 1 rotates by minus stickX times 0.2 times 2pi over 360. All branch senses and deltas match. |
| 191 | `sub_GAME_7F00032C` | src/game/initactorpropstuff.c | `verified-equivalent` | Matches ASM 0x7F00032C: iterate attackanimconfig entries stride 0x48 while the offset field is nonzero, resolve anim pointer equal offset plus ptr_animation_table, floorFloatToInt of speed into 7F0001F0, angle_units equal low 16 of result, write field plus 8 to angle times 6.2831855 over 65536 divided by speed, subtracting 6.2831855 first when angle is at least 32768, or 0 when speed not positive, return count. Only difference is the port keeps the raw offset in the struct field instead of writing back the resolved pointer, a documented 64-bit pointer-width adaptation; the value passed to 7F0001F0 is identical. |
| 192 | `sub_GAME_7F0A3978` | src/game/unk_0A1DA0.c | `verified-equivalent` | Matches ASM 0x7F0A3978 exactly: arg3 nonzero path emits SPDMA words 0x04300040 and 0x04040010 plus tile-setup 0xB1000003 count 66, then a descending do-loop i from 7 to 0 emitting 0x04300040 to dmem_base plus i shifted left 5, 0x04040010 to original arg1, and 0xB1000042 value 12608, then 0xB1000004 value 16; arg3 zero path emits 8 iterations of 0x04300040 to arg1 and 0xB1000032 value 8464 advancing arg1 by 0x20; both end with 0xB8000000 full sync. uintptr_t versus u32 addressing is cosmetic. |
| 193 | `sub_GAME_7F05B024` | src/game/matrixmath_misc.c | `verified-equivalent` | Matches ASM 0x7F05B024: cubic weights square equals t times t, cube equals square times t, temp_f6 equals cube minus square times u, temp_f16 equals 2 square minus t plus cube times u, temp_f18 equals 2 minus u times cube plus square times u minus 3 plus 1, temp_f8 equals u minus 2 times cube plus square times 3 minus 2u plus t times u, then result x y z equal temp_f16 arg0 plus temp_f18 arg1 plus temp_f8 arg2 plus temp_f6 arg3 per component. Every mul add sub and accumulation order matches; the 74 percent decomp.me note is register ordering only. |
| 194 | `fileGetIndex` | src/game/ob.c | `verified-equivalent` | LEFTOVERDEBUG build variant of fileGetIndex at 0x7F0BD254, structurally identical to the built non-debug variant, differing only in the OBJ_INDEX_MAX cap literal 0x2e0 and symbol relocations, and not the port build target. Index search, cap check, indycommHostCheckFileExists with a pointer to size, and table init all match the live C. |
| 195 | `fileGetIndex` | src/game/ob.c | `verified-equivalent` | Live NATIVE_PORT body matches built ASM 0x7F0BC60C: search file_resource_table entries 1 up to file_entry_max via strcmp returning i on match, cap check file_entry_max at least OBJ_INDEX_MAX returns 0, increment, indycommHostCheckFileExists of resname and a pointer to size (the port passes a pointer matching the ASM addiu a1 sp 0x34, unlike the buggy non-native reference which passes size by value), then init index filename hw_address and the resource_lookup_data_array fields including pc_size equal size plus 0xf or 0xf xor 0xf. The cap is symbolic OBJ_INDEX_MAX in both; the retail literal 0x30c reflects retail OBENDSEG plus 10 while the port value tracks its own object table. |
| 196 | `disable_sounds_attached_to_player_then_something` | src/game/chr.c | `finding-filed:FID-0113` | Rank 196: child-teardown loop type-gates objFreePermanently vs retail unconditional free of child offset-0x4; plus NATIVE-only SEbuffer null-out and model null guards. See FID-0113. |
| 197 | `sub_GAME_7F03AA44` | src/game/chrprop.c | `verified-equivalent` | Rank 197: retail at 0x7F03AA44 computes delta = pos2 minus pos1, scale_factor = bgGetLevelVisibilityScale times get_room_data_float1, scaled = pos1 times scale_factor, then for room index 1 up to getMaxNumRooms (re-read per iteration, guarded by count at least 2) sets occluded_rooms index i to 1 when it is 0 and sub_GAME_7F03A97C(i, scaled ptr at sp+0x4c, delta ptr at sp+0x40) returns 0. C matches branch senses, arg registers, and the dead room_arg. |
| 198 | `spectrum_input_handling` | src/game/spectrum.c | `verified-equivalent` | Rank 198: at 0x7F0D36D8 cmd = arg2 and 0xff, mask = arg1 and 0xff. cmd 0xFE runs two unrolled 4-bit passes over spec_keyboard_buffer indices 0-7, AND-accumulating into accum (init 0xFF) only where the rolling mask bit is 0, shifting mask right 1 and masking 0xff per bit, loop while buf_idx not 8, returns accum. cmd 0x1F returns zero-extended byte D_8004EC40, else 0xFF. C matches bit senses, shifts, and accum persistence across passes. |
| 199 | `sub_GAME_7F09BAC4` | src/game/unk_09B7A0.c | `verified-equivalent` | Rank 199: at 0x7F09BAC4 walks the obj pos list backward via prop offset 0x24 (union prev with nextSibling), and for each PROP_TYPE_OBJ (type at offset 0) walks the model node tree (prop obj 0x4, obj model 0x14, model obj 0x8, RootNode 0x0; ModelNode Opcode 0x0, Parent 0x8, Next 0xc, Child 0x14) to the first node whose opcode low byte is 0x18, then if modelGetNodeRwData offset-0 Vertices equals arg0 writes arg1. Offsets confirmed vs bondtypes.h; descend, sibling, and parent walk senses match. |
| 200 | `sub_GAME_7F059244` | src/game/matrixmath.c | `verified-equivalent` | Rank 200: at 0x7F059244 packs 16.16 split matrix. For src row k in 0-7 at a0 plus k times 8 (w1 word0, w2 word1) writes INT = (w1 and 0xffff0000) or (w2 shift right 16) to a1 words 0-7 and FRAC = (w1 shift left 16) or (w2 and 0xffff) to a1 words 8-15. C nested loop i in 0-1, j in 0-3 writes m i j INT and m i+2 j FRAC with k = 4i+j, giving the identical word layout; shift and mask ops match. |
| 201 | `sub_GAME_7F059334` | src/game/matrixmath.c | `verified-equivalent` | Rank 201: at 0x7F059334 for i in 0-7 int = src i, frac = src i+8 (0x20 bytes), dst at 2i = (int and 0xffff0000) or (frac shift right 16), dst at 2i+1 = (int shift left 16) or (frac and 0xffff). Unrolled by 4 with a3 stride 0x10, loop while v0 not 8. C matches exactly including u32 casts that give logical shifts. |
| 202 | `sub_GAME_7F01D1C0` | src/game/blood_decrypt.c | `verified-equivalent` | Rank 202: at 0x7F01D1C0 RLE decompress. first_byte = one byte from src; per row byte from src; if byte not 0xFF RLE mode: run_offset = (byte and 0x1f) plus first_byte, repeat = (byte shift right 5) plus 1, rows minus repeat, then repeat times write run_offset bytes of 0xFF then width minus run_offset bytes of 0x00; else escape mode reads run lengths with fill toggling 0xFF and 0x00 via xor until a 0xFF terminator, pads to width, rows minus 1. Loop while rows greater than 0, returns src. C matches all senses, blez and bgtz bounds, and the toggle. |
| 203 | `mempAddEntryOfSizeToBank` | src/memp.c | `verified-equivalent` | Rank 203: at 0x70009804 if needmemallocation and ptrdata equals pool 6 prevpos at 0x6C set bank to 6; if pool bank pos at 0x4 is 0 hang; if ptrdata not equal prevpos at 0xc return 2; oldsize = pos minus prevpos; diffsize = newsize minus oldsize; if diffsize at most 0 pos plus diffsize return 1; else if pos greater than end at 0x8 or pos plus diffsize greater than end hang; pos plus diffsize return 1. ASM sltu vs C signed greater-than is harmless because pos and end share sign in one pool region. Non-NATIVE hangs preserved; fields and returns match. |
| 204 | `sub_GAME_7F06BB28` | src/game/objecthandler.c | `verified-equivalent` | Rank 204: at 0x7F06BB28 selection sort by descending prio at 0x8 over a list linked next 0xc and prev 0x10. Two stack sentinels bracket the list; each pass finds the max-prio node in the unsorted remainder (best_val init minus 2 to the 32) and splices it after cursor; returns new head with prev and next nulled. The ASM loads at sp+0x40 and sp+0x30 are sentinel_head next and sentinel_tail prev field accesses; the NATIVE ModelRenderSlot body mirrors every splice and both the best-NULL and scan-equals-sentinel_tail breaks. |
| 205 | `microcode_constructor_related_to_menus` | src/game/textrelated.c | `verified-equivalent` | Rank 205: at 0x7F0AD014 emits 5 display-list words: SetRenderMode 0xB900031D and 0x00504240 (G_RM_XLU_SURF, G_RM_XLU_SURF2), SetCombineMode 0xFCFFFFFF and 0xFFFDF6FB (G_CC_PRIMITIVE), SetPrimColor 0xFA000000 and color where w1 repacks the color bytes to itself with r = color shift right 24 and 0xff and so on, FillRectangle from arg1 arg2 arg3 arg4 as ulx uly lrx lry (stack 0x10 is arg4, 0x14 is color), then raw SetCombineLERP words 0xFCFF97FF and 0xFF2DFEFF; returns gdl plus 5. NATIVE path matches all five commands and the return. |
| 206 | `mempAllocBytesInBank` | src/memp.c | `waived:Rank 206 partial NATIVE_PORT allocator reimpl needing deep audit. Retail at 0x70009720 uses the requested byte count unchanged (move a2 a0 at 0x70009740); the NATIVE_PORT path first rounds the request up to a 16-byte multiple before every pool test and commit, changing allocation stride and thus pool-exhaustion timing. Also intptr_t fields and return-NULL replacing retail infinite-loop error paths. Requires a memp oracle to prove the 16-byte alignment is sim-neutral host-ABI hardening rather than a documented parity-divergence.` | Rank 206 partial NATIVE_PORT allocator reimpl needing deep audit. Retail at 0x70009720 uses the requested byte count unchanged (move a2 a0 at 0x70009740); the NATIVE_PORT path first rounds the request up to a 16-byte multiple before every pool test and commit, changing allocation stride and thus pool-exhaustion timing. Also intptr_t fields and return-NULL replacing retail infinite-loop error paths. Requires a memp oracle to prove the 16-byte alignment is sim-neutral host-ABI hardening rather than a documented parity-divergence. |
| 207 | `sub_GAME_7F0A68D8` | src/game/watch.c | `verified-equivalent` | Rank 207: at 0x7F0A68D8 inits watch tick verts. ystep = truncate to s32 of sqrtf(213444.0 minus float of D_80040B40 squared) using multu low 32, cvt.s.w signed, trunc.w.s. For col in 0 and 4, tick in minus 1 and 1, each 0x10 entry writes s16 at 0 = ystep times tick low 16, s16 at 2 = 0, s16 at 4 = col plus D_80040B40, s16 at 6 8 and 0xa = 0, bytes 0xc = 0, 0xd = 160, 0xe = 0, 0xf = low byte of D_80040B08. C matches offsets, loop bounds, and the little-endian LSB read equals retail sb low byte. |
| 208 | `sub_GAME_7F005450` | src/game/initunk_005450.c | `waived:Rank 208 wholesale NATIVE_PORT rewrite needing deep audit. Retail at 0x7F005450 initializes a 600-entry pool of 80-byte stride-0x50 multi-level skip-list nodes (link fields at 0xc and 0x10, 0x20 and 0x24, 0x34 and 0x38, 0x48 and 0x4c) rooted at D_80036060. The NATIVE_PORT path replaces it with a 600-entry ModelRenderSlot array wired as a single-level doubly-linked free list (model, node, sort_z, next 0xc, prev 0x10) and drops the other skip levels. Different data structure, cannot be instruction-compared. Needs a deep audit of the whole render-slot subsystem, this init plus the sort at 0x7F06BB28 plus consumers, with an oracle to confirm the retail extra skip levels are unused.` | Rank 208 wholesale NATIVE_PORT rewrite needing deep audit. Retail at 0x7F005450 initializes a 600-entry pool of 80-byte stride-0x50 multi-level skip-list nodes (link fields at 0xc and 0x10, 0x20 and 0x24, 0x34 and 0x38, 0x48 and 0x4c) rooted at D_80036060. The NATIVE_PORT path replaces it with a 600-entry ModelRenderSlot array wired as a single-level doubly-linked free list (model, node, sort_z, next 0xc, prev 0x10) and drops the other skip levels. Different data structure, cannot be instruction-compared. Needs a deep audit of the whole render-slot subsystem, this init plus the sort at 0x7F06BB28 plus consumers, with an oracle to confirm the retail extra skip levels are unused. |
| 209 | `draw_blackbox_to_screen` | src/game/textrelated.c | `verified-equivalent` | Rank 209: at 0x7F0ACF4C emits six DL words identical to rank 205's palette: SetRenderMode 0xB900031D and 0x00504240 (G_RM_XLU_SURF, G_RM_XLU_SURF2), SetCombineMode 0xFCFFFFFF and 0xFFFDF6FB (G_CC_PRIMITIVE), SetPrimColor 0xFA000000 and 0, FillRectangle, SetRenderMode again 0xB900031D and 0x00504240, then SetCombineLERP 0xFCFF97FF and 0xFF2DFEFF; returns gdl plus 6. The one structural difference from the live C is that retail DEREFERENCES the four coordinate args as s32 pointers (lw a1, lw a2, lw a3, and lw of the fifth stack arg) to build the FillRectangle: ulx = deref a1, uly = deref a2, lrx = deref a3, lry = deref fifth arg. The port refactored the signature to plain s32 values and updated its NATIVE_PORT call sites to pass values (bondview.c:23134, watch.c:11483, gun.c:31982), while the dead non-NATIVE reference at bondview.c:23136 passes addresses via a (s32) cast of the same locals, i.e. the retail pointer convention. Because the value passed equals what the retail pointer points to, the emitted display list is byte-identical. Verified-equivalent modulo the faithful pointer-to-value signature refactor. |
| 210 | `sub_GAME_7F0A3330` | src/game/unk_0A1DA0.c | `verified-equivalent` | [pull-forward for FID-0080] HUD-bar DL packer, ASM unk_0A1DA0.c:2328-2384 vs live C 2249-2324 (NONMATCHING body, NATIVE_PORT branches), traced fully: half=(arg2/2)-2 signed-div w/ bgez round-toward-zero correction vs C arg2>>1 — differ only for arg2<0 where both yield half<0 and skip the loop (gate bltz/if(half>=0) agrees for all inputs); per-iter G_VTX emit 0x04300040/addr (4 verts, 0x40 bytes, addr stride 0x20) then conditional TRI4 0xB1000032/0x00002110 with cadence i<9: (i&1)==0 (slti/andi/bnez senses verified incl redundant second slti) / i>=9: ((i+3)&3)!=0 (bgez correction dead for i>=9); i s8 wrap via sll/sra 0x18 = C (s8)(i+1); loop bound slt v1,v0 = while(i<=half); addr+=0x20 in loop-back delay slot = per-iteration; tail G_ENDDL 0xB8000000/0 + return gdl+8. NATIVE_PORT words.w0/w1 stores semantically identical to u32 pair stores. CONTAINS NO VERTEX SCALING — never reads/writes vertex data, only emits pointers. FID-0080 implication: in-code rationale 'retail vertex-side scaling in 7F0A3330 unverified' is REFUTED at this fn; any retail compensation for the D_800364CC ortho-extent scale must live in sub_GAME_7F0A2F30's vertex fill (x scale operand = (f32)arg2 with retail callers passing only +/-1 mirror), else FID-0080(a) is a real pixel divergence on 0.2-scale stages. |
| 211 | `sub_GAME_7F020794` | src/game/chr.c | `verified-equivalent` | Rank 211: the LEFTOVERDEBUG variant at 0x7F020794, which IS the port build target (the game defines -DLEFTOVERDEBUG via add_definitions at CMakeLists.txt:1245). Flinch factor from ChrRecord flinchcnt at offset 0x11 (lb, cvt.s.w): if count less than 10.0 returns sinf(count times M_TAU_F times 0.25 over 10.0), else 1.0 minus sinf((count minus 10.0) times M_TAU_F times 0.25 over 20.0). The live NONMATCHING C at chr.c:3591 uses exactly the 10.0 threshold and 20.0 divisor, matching THIS variant. c.lt.s and bc1fl branch sense, the 0.25 constant, and M_TAU_F 0x40c90fdb all match; the header notes only an f0/f2 regalloc swap and a late_rodata offset (9 of 42 instr), not behavior. |
| 212 | `sub_GAME_7F020794` | src/game/chr.c | `verified-equivalent` | Rank 212: the NON-LEFTOVERDEBUG variant of sub_GAME_7F020794 at 0x7F020624, which uses an 8.0 threshold and 16.0 divisor. This is NOT the port build target: the game defines -DLEFTOVERDEBUG (CMakeLists.txt:1237-1248), so the authoritative variant is the LEFTOVERDEBUG one at rank 211 (0x7F020794, 10.0/20.0) which the compiled C matches. This 8.0/16.0 body is the reference for a build the port does not target; verified-equivalent by exclusion (non-target variant), with the faithful match established at rank 211. |
| 213 | `decompressdata` | src/game/decompress.c | `verified-equivalent` | Rank 213: at 0x7F0CE7F0 sets rz_inbuf=arg0, rz_outbuf=arg1, rz_hlist=arg2, then a header sniff that reads arg0 byte 0 vs rz_header_bytes index 0 and arg0 byte 1 vs index 5, and only if not both equal reads arg0 byte -1 vs index 8 and byte -2 vs index 24, but stores nothing (the unused local is optimized away; only conditional reloads of rz_inbuf remain), then unconditionally rz_inbuf += 2, rz_wp=0, rz_inptr=0, jal zlib_inflate, returns rz_wp. C matches every branch sense and the dead header block; every path loads t4=rz_inbuf before the +2. NATIVE_PORT adds a zlib_inflate return check (sets rz_last_error, returns 0 on failure) that is behavior-identical on success where retail ignores the return. |
| 214 | `set_page_rectangle_colors` | src/game/watch.c | `verified-equivalent` | Rank 214: at 0x7F0A70AC first sets all 20 (4 times 5) WatchVertex colors to r=0x20 g=0x70 b=0x20 at struct offsets 0xC/0xD/0xE stride 0x10 via an ascending loop gated by slti 0x14. Then for the selected screen, start i = watch_screen_index times 4, limit i+3, walks those 4 vertices setting r=0x50 g=0xF0 b=0x50, overriding to r=0x30 g=0xA0 b=0x30 when watch_item_is_actively_selected is nonzero (beql zero skip). All colors, offsets, the slt limit guard (dead for valid input), and the sltu loop bound match C. Decomp note match down to regalloc. |
| 215 | `sub_GAME_7F008DE4` | src/game/title.c | `verified-equivalent` | Rank 215: at 0x7F008DE4 sets dword_CODE_bss_8006958C = deref addr, then deref size -= 0x40400 (via add of 0xFFFBFC00), deref addr += 0x40400, dword_CODE_bss_80069588 = new deref addr, romCopy(dest=new addr, src=address of unknown2, length = ALIGN64 of the unknown2 to _ramromDam1SegmentRomStart span using the (v or 0x3f) xor 0x3f idiom), then sub_GAME_7F01B0E0(new addr, old addr). C matches every load/store, order, and the align. NATIVE_PORT swaps the src to ROM_OFFSET(unknown2) with ROM_SPAN for length (standard port ROM-file addressing, copied length identical) and adds a getenv GE007_DUMP_EYE_BUFFER dump inert unless the env var is set. |
| 216 | `obInit` | src/game/ob.c | `finding-filed:FID-0114` | Rank 216: off-by-one init bound. Retail obInit at 0x7F0BCC28 fills resource_lookup_data_array with an ascending pointer loop bounded by a1 = base + (file_entry_max-1) times 20 (computed 0x7F0BCC64-0x7F0BCC7C), so it stops after index file_entry_max-2 and never touches index file_entry_max-1; the NATIVE_PORT C at ob.c:168 uses a descending loop from file_entry_max-1 down to 1 and therefore DOES write index file_entry_max-1. Net: the port initializes one extra entry (rom_size plus three zero fields) that retail leaves at its prior value. See FID-0114. |
| 217 | `sub_GAME_7F01B134` | src/game/unk_01B0E0.c | `verified-equivalent` | Rank 217: at 0x7F01B134 RLE decoder to RGBA5551. remaining = u16 header word 0 times u16 header word 2; ptr = header+0xa; per entry pixel = ((ptr byte3 shr 3) shl 11) or ((ptr byte2 shr 3) shl 6) or ((ptr byte1 shr 3) shl 1) or 1, remaining -= ptr byte0 (count), ptr += 4; inner do-loop writes the 16-bit pixel count times (sh to s0, s0 += 2) via slt-then-decrement; outer bgtzl while remaining greater than 0. C matches the shifts, packing, offsets, count semantics, and the s0 stack save; reads in order c2,c3,c1,count but all from ptr before it advances. |
| 218 | `setup_watch_rectangles` | src/game/unk_0A1DA0.c | `verified-equivalent` | Rank 218: at 0x7F0A3AB8 setup_watch_rectangles. xval = startx + horizontal_offset (stack arg at sp+0x1c after the -8 prologue); outer i loop 2 iters with xval += width in the delay slot; inner j loop 2 iters with zval = startz + vertical_offset (sp+0x20), writing coord1.z at 0x4 then zval += height (sp+0x18), coord1.x at 0, zeros at 0x2/0x6/0x8/0xa, color bytes 0x20/0x70/0x20/0xF0 at 0xc..0xf, vtx += 0x10 stride. Returns final vtx. All offsets, both loop bounds (bne against s0=2), and constants match C; leaf-function regalloc-only NONMATCHING. |
| 219 | `sub_GAME_7F0A3E1C` | src/game/unk_0A1DA0.c | `verified-equivalent` | Rank 219: at 0x7F0A3E1C searches dword_CODE_bss_8007A170 (stride 0x2c, 20 entries, end = base+0x370) for the first entry whose unk04 at offset 0x4 (lh) is 0; on hit calls sub_GAME_7F0A3C08(ptr, arg0, arg1, arg2 as float bits via mfc1, arg3 s16 from stack) and returns ptr, else returns NULL. do-while structure, bnezl skip-if-nonzero, sltu ptr less-than-end bound (examines indices 0..19), and the 5-arg marshaling (a2=arg1 preserved, a3=float bits, 5th on stack) all match C. UNK_8007A170_MAX = 20. |
| 220 | `chrlvGetPatrolStepPad` | src/game/chrlv.c | `verified-equivalent` | Rank 220: at 0x7F0283FC chrlvGetPatrolStepPad. Loads act_patrol.forward (ChrRecord offset 0x34) into a stack local, calls chrlvPatrolCalculateStep(self, and-forward, numsteps) for step, then data = act_patrol.path (offset 0x2c) then deref its data pointer at offset 0 indexed by step; padID = g_CurrentSetup.pathwaypoints (offset 0, stride 0x10) at data, padID field offset 0; returns the address of g_CurrentSetup.pads (offset 0x18) at padID with stride 0x2c synthesized by the shift/sub chain giving padID times 44. All offsets and arg marshaling match C; NONMATCHING is a single-line regalloc swap. |
| 221 | `memaGrow` | src/mema.c | `verified-equivalent` | Rank 221: at 0x70009FA8 memaGrow. curr = address of g_MemoryAllocations.spaces index 2 (offset 0x10, stride 8: addr at 0, size at 4). Walks while curr->addr not equal -1; on curr->addr == addr AND curr->size at least amount (sltu size,amount then beql), does curr->addr += amount, curr->size -= amount, and if the new size == 0 sets curr->addr = 0, returns addr; else returns 0. The bnel short-circuit on the addr compare, the beq/bne sentinel checks, and the unsigned size compare all match C. Decomp note regalloc-only. |
| 222 | `sub_GAME_7F01B1C8` | src/game/unk_01B0E0.c | `verified-equivalent` | Rank 222: at 0x7F01B1C8 RLE decoder to RGBA32. remaining = u16 header word 0 times u16 header word 2; ptr = header+0xa; per entry count = ptr byte0, then inner do-loop writes 4 bytes per pixel: out0 = ptr byte3 (c3), out1 = ptr byte2 (c2), out2 = ptr byte1 (c1), out3 = 255, out += 4, repeated count times via slt-then-decrement; remaining -= count, ptr += 4; outer bgtzl while remaining greater than 0. All byte lanes, the 255 constant, offsets, and count semantics match C. Uses v0 for the output pointer directly (no stack frame), matching this variant. |
| 223 | `boot` | src/boot.c | `waived:asm-only N64 TLB and boot bootstrap at ROM 0x70000450: programs cop0 EntryLo0 EntryHi PageMask Wired via mtc0, runs tlbwi to map the 0x70000000 to 0x70400000 region, then jr to init at 0x70000510. No live C sibling and no port reimplementation; GLOBAL_ASM with no NONMATCHING branch so it is not compiled under NATIVE_PORT. Inherently unportable hardware startup, not sim-reachable (port uses its own host startup). Nothing to instruction-diff. Only revisit if a port TLB emulation layer is ever introduced.` | asm-only N64 TLB and boot bootstrap at ROM 0x70000450: programs cop0 EntryLo0 EntryHi PageMask Wired via mtc0, runs tlbwi to map the 0x70000000 to 0x70400000 region, then jr to init at 0x70000510. No live C sibling and no port reimplementation; GLOBAL_ASM with no NONMATCHING branch so it is not compiled under NATIVE_PORT. Inherently unportable hardware startup, not sim-reachable (port uses its own host startup). Nothing to instruction-diff. Only revisit if a port TLB emulation layer is ever introduced. |
| 224 | `get_ptrpreset_in_table_matching_tile` | src/game/chrlv.c | `verified-equivalent` | Retail 0x7F027BF4 walks g_CurrentSetup.pathwaypoints (field offset 0, waypoint stride 0x10, padID at waypoint offset 0) until padID goes negative; per entry it indexes pads (g_CurrentSetup field offset 0x18, PadRecord stride 44 bytes) and compares pad stan at PadRecord offset 0x28 against the argument, returning the matching waypoint else NULL, with the NULL-pathwaypoints early-out. Live C chrlv.c matches structure, offsets and both early-outs; the branch-likely next-padID preload equals the for-loop advance-then-test. |
| 225 | `sub_GAME_7F06B248` | src/game/objecthandler.c | `verified-equivalent` | Retail 0x7F06B248 prepends arg0 (a node or chain) to the free list D_80036060: null-arg early-out; empty-list stores arg0 and returns; else walks arg0 next-chain (link at offset 0xc) to its tail, sets tail next to the old head, sets old-head prev at offset 0x10 to the tail, then head becomes arg0. This body touches only the 0xc and 0x10 link level, which the NATIVE_PORT single-level ModelRenderSlot free list (next 0xc, prev 0x10) preserves; the dropped higher skip levels flagged at rank 208 are not used here. Live C matches the walk, both early-outs and the old-head prev fixup read before the head store. |
| 226 | `sub_GAME_7F01B0E0` | src/game/unk_01B0E0.c | `verified-equivalent` | Retail 0x7F01B0E0 8-bit indexed RLE decoder: remaining equals u16 width times u16 height (halfword loads then multu), ptr starts at header+0xa, each run reads count and value at ptr and ptr+1 and writes value exactly count times (inner do-while verified for count 1, 2, 3), advancing ptr by 2 and remaining by minus count, looping while remaining stays positive (signed bgtzl). Live C matches. NATIVE_PORT read_be16 reproduces the big-endian halfword loads on little-endian hosts (out of scope). The added null-header-or-output early return has no retail counterpart but only turns a retail null-deref crash into a clean return; identical for every input retail survives. |
| 227 | `sub_GAME_7F06C010` | src/game/objecthandler.c | `verified-equivalent` | Retail 0x7F06C010 walks the next-chain (link at offset 0xc) from the head node to the last node, stores it back to the head slot, zeroes the two output pointers, and tail-calls probably_damage_detail_blood_effect_related passing head, the two unused params, and the fourth output as the fifth stack argument. Live NATIVE_PORT C matches the traversal (ModelRenderSlot next at 0xc), the two zeroing stores and the forwarded call. The added head-null early return (return 0, zero the outputs, no call) has no retail counterpart, but retail would null-deref reading offset 0xc of a null head; identical for every non-null head. Same single-level free list as rank 225. |
| 228 | `chrObjRandomGetNext` | src/game/chrObjRandom.c | `verified-equivalent` | Retail 0x7F09B740 is instruction-for-instruction identical to randomGetNext but on g_chrObjRandomSeed: t equals seed left-63 right-31 or seed left-31 right-32, xor seed left-44 right-32, new seed equals t xor ((t right-20) and 0xfff), returning the low 32 bits sign-extended via dsll32 then dsra32. Live C matches exactly. |
| 229 | `sub_GAME_7F0004D0` | src/game/initactorpropstuff.c | `verified-equivalent` | Retail 0x7F0004D0 relocates a struct animation table array (stride 0x1c, anonymous_0 at offset 0): while anonymous_0 is nonzero it adds ptr_animation_table to it in place, increments count and advances; returns count, guarding on the first element before the loop. Live C matches. NATIVE_PORT uses uintptr_t 64-bit for the relocation add versus the retail 32-bit addu, the correct host pointer-width accommodation, byte-identical for 32-bit offsets. |
| 230 | `randomGetNext` | src/random.c | `verified-equivalent` | Retail 0x7000A450 iterates g_randomSeed: t equals seed left-63 right-31 or seed left-31 right-32, xor seed left-44 right-32, new seed equals t xor ((t right-20) and 0xfff), stored and returned as the low 32 bits sign-extended. Live C core math matches exactly. The surrounding NATIVE_PORT instrumentation (call counting, RNG trace, scripted call-seed injection) is env-gated and a default no-op, leaving the seed math byte-identical. |
| 231 | `tlbRandomGetNext` | src/tlb_random.c | `verified-equivalent` | Retail 0x70001B10 is identical to randomGetNext but on g_tlbRandomSeed and returns s32 via dsll32 then dsra32 sign-extension of the low 32 bits. Live C matches exactly. |
| 232 | `randomGetNextFrom` | src/random.c | `verified-equivalent` | Retail 0x7000A4AC applies the randomGetNext iteration to a caller-supplied u64 pointer instead of the global: loads the value, computes t equals v left-63 right-31 or v left-31 right-32 xor v left-44 right-32, new equals t xor ((t right-20) and 0xfff), stores it back and returns the low 32 bits sign-extended. Live C matches exactly. |
| 233 | `chrObjRandomSetSeed` | src/game/chrObjRandom.c | `verified-equivalent` | Retail 0x7F09B788 sets g_chrObjRandomSeed to arg plus 1 (daddiu, 64-bit) and stores it; the trailing li a0 0 is a dead argument-register clobber. Live C matches. |
| 234 | `randomSetSeed` | src/random.c | `verified-equivalent` | Retail 0x7000A498 sets g_randomSeed to arg plus 1 (daddiu, 64-bit) and stores it; trailing li a0 0 is dead. Live C matches. |
| 235 | `jump_decompressfile` | src/boot.c | `waived:asm-only ROM trampoline at 0x700004F8: loads decompress_entry (0x7020141C) into a3 and jr to it, tail-calling the inflate routine with a0 a1 a2 (source target buffer) unchanged. No live C sibling; GLOBAL_ASM with no NONMATCHING branch so it is not compiled under NATIVE_PORT. The port decompresses through its own inflate library rather than this ROM-address thunk. Nothing to instruction-diff; asm-only with no reimplementation.` | asm-only ROM trampoline at 0x700004F8: loads decompress_entry (0x7020141C) into a3 and jr to it, tail-calling the inflate routine with a0 a1 a2 (source target buffer) unchanged. No live C sibling; GLOBAL_ASM with no NONMATCHING branch so it is not compiled under NATIVE_PORT. The port decompresses through its own inflate library rather than this ROM-address thunk. Nothing to instruction-diff; asm-only with no reimplementation. |
| 236 | `parse_global_vis_command_list` | src/game/bg.c | `verified-equivalent` | LEFTOVERDEBUG variant (build authority; CMakeLists defines LEFTOVERDEBUG). Visibility-command bytecode interpreter, instruction-identical to non-LEFTOVERDEBUG sibling rank 237 (only assembler-comment and label-name cosmetics differ). All handlers match the single C body: jpt_80058C80 high dispatch index opcode-0x50 bound 0xD for opcodes 0x50-0x5C, vis_command_jpt low dispatch index opcode bound 0x28. Stack ops 0x01-0x06, range-push 0x14, force_visible 0x1E, portal-vis 0x1F-0x23, room ops 0x20-0x27, if/endif/toggle control flow all match. NATIVE_PORT vis_read_s32 byte-swap, fprintf traces, and add_visible_room bounds guard plus trace-context hooks are sim-neutral for valid in-range data. |
| 237 | `parse_global_vis_command_list` | src/game/bg.c | `verified-equivalent` | Non-LEFTOVERDEBUG sibling of rank 236. Diff of the two GLOBAL_ASM blocks is 388 vs 388 instructions with only assembler-comment and local-label-name cosmetic differences; both map onto the single C body identically. See rank 236 note for the handler-by-handler match. Authority is the LEFTOVERDEBUG variant (rank 236) since the build defines LEFTOVERDEBUG, but the two are byte-logic-identical so the C is equivalent to both. |
| 238 | `bgRoomVisibilityRelated` | src/game/bg.c | `verified-equivalent` | Portal-chain room resolver. jpt_80058C58 camera-mode switch sets D_800442F8 to 200 for modes 1,2,3,7,9 else 3 (index cam-1 bound 9). Inner portal loop: bg_portal_data_entry stride 8, offset_portal at struct offset 0, connectedRoom1 at 4, connectedRoom2 at 5. Guards flags at D_800443C4 equal 0 and portal_idx not equal last_portal; side test current_room equals connectedRoom1 or connectedRoom2; on sub_GAME_7F0B9F14 hit sets last_portal and current_room = connectedRoom2 xor (current_room xor connectedRoom1). Both break conditions and the 11-iteration outer bound match. NATIVE_PORT block is a comment plus a default-off GE007_FORCE_NEARBY_ROOMS debug override. |
| 239 | `sub_GAME_7F0752FC` | src/game/model.c | `verified-equivalent` | Model-node tree walker. Init picks node from *arg3 (then clears it) else model->obj->RootNode; ModelNode fields Opcode halfword at 0, Data at 4, Parent at 8, Next at 0xc, Child at 0x14. Child-descend then Next/Parent ascent match. type = Opcode and 0xFF; jpt_80054DB4 index type-1 bound 0x18 dispatches cases 8 (modelApplyDistanceRelations), 10 (sub_GAME_7F074C68), 17 (sub_GAME_7F074CAC), 18 (modelApplyToggleRelations), 23 (modelApplyHeadRelations), all else default. Cases 10 and 17 on hit set *arg3=node and return *(s32*)node->Data (offset 4). bboxTrace diagnostics and uintptr_t arg casts are NATIVE_PORT-only, sim-neutral. |
| 240 | `bgTestLineIntersectionInRoom` | src/game/bg.c | `waived:NATIVE_PORT full reimplementation (guard NONMATCHING plus PORT_FIXME_STUBS plus NATIVE_PORT); the decomp else body is mangled question-mark types. Core algorithm verified against retail ASM 7F0B7010: room stride 0x50, coll_index times 8 plus dl_base, coll_dl byte1 and 0xF pool offset, word4 and 0xFFFFFF vertex offset, cmdType checks 4 and 0xB8 and 0xBF and 0xB1, G_TRI1 index cmd bytes 5-7 divided by 10 minus offset, TRI4 0xB1 per-triIndex nibble unpack minus offset, vertex stride 0x10 with big-endian s16 reads, bbox init 0x7FFF and 0xFFFF8000 (D_80044868 and D_80044874), best_dist_sq 0x7FFFFFFF nearest-hit. Waived not verified because the port intentionally diverges structurally: added PC bounds checks (null buffers, coll bounds, vtx range) and a native BgHitRecord output layout replacing the N64 in-place 16-bit metadata overlay. Retest with a room-line collision oracle route.` | NATIVE_PORT full reimplementation (guard NONMATCHING plus PORT_FIXME_STUBS plus NATIVE_PORT); the decomp else body is mangled question-mark types. Core algorithm verified against retail ASM 7F0B7010: room stride 0x50, coll_index times 8 plus dl_base, coll_dl byte1 and 0xF pool offset, word4 and 0xFFFFFF vertex offset, cmdType checks 4 and 0xB8 and 0xBF and 0xB1, G_TRI1 index cmd bytes 5-7 divided by 10 minus offset, TRI4 0xB1 per-triIndex nibble unpack minus offset, vertex stride 0x10 with big-endian s16 reads, bbox init 0x7FFF and 0xFFFF8000 (D_80044868 and D_80044874), best_dist_sq 0x7FFFFFFF nearest-hit. Waived not verified because the port intentionally diverges structurally: added PC bounds checks (null buffers, coll bounds, vtx range) and a native BgHitRecord output layout replacing the N64 in-place 16-bit metadata overlay. Retest with a room-line collision oracle route. |
| 241 | `modelSetAnimFrame2WithChrStuff` | src/game/model.c | `finding-filed:FID-0117` | NATIVE_PORT root-motion animation reimpl. Setup and early-outs match retail ASM 7F0701EC (Opcode and 0xFF not 1 early-out, modelGetNodeRwData, rwdata unk00 not 0 early-out, numFrames from ModelType upper 16 bits, scaledScale = scale times unkb8, forwards floor/ceil frame stepping). One byte-offset divergence filed as FID-0117: retail seeds save_unk01 from rwdata->unk01 (lb 1(v0) at 7F0702EC, struct offset 1) but the port reads rwdata->unk02 (offset 2). anim2 offset-2 accesses correctly match retail lb/sb 2(s0). |
| 242 | `load_bg_file` | src/game/bg.c | `verified-equivalent` | LEFTOVERDEBUG variant and the build authority (CMakeLists defines LEFTOVERDEBUG); the C tail fogRemoved7F0BAA5C then D_800442F8=200 matches this variant, absent in the non-LEFTOVERDEBUG sibling rank 243. NATIVE_PORT loader reimpl verified against ASM 7F0B4124: levelentry_index=0, room-info clear loop stride 0x50 clearing ptr_unique_collision_points, levelinfotable search stride 0x18 levelID at 0, dual obLoadBGFileBytesAtOffset (64-byte header then full), size align (x and 0xFFFFFF) minus 1 then (x or 0xF) plus 1, room-list base plus 0xF1000000, stan load, four levelscale calls (levelscale at 0xc), visibility scale, room-count and portal-processing loops. NATIVE_PORT stub-BG guard and byte-swaps are sim-neutral for valid data. |
| 243 | `load_bg_file` | src/game/bg.c | `waived:Non-LEFTOVERDEBUG variant of load_bg_file (411 instructions) and NOT the build authority: the port build defines LEFTOVERDEBUG so the shipping retail body is the LEFTOVERDEBUG sibling rank 242 (431 instructions). This variant omits the fogRemoved7F0BAA5C plus D_800442F8=200 tail that both the LEFTOVERDEBUG ASM and the compiled C body contain. The C matches rank 242; this variant is a non-shipping build alternate. Waived as non-authoritative; equivalence tracked via rank 242.` | Non-LEFTOVERDEBUG variant of load_bg_file (411 instructions) and NOT the build authority: the port build defines LEFTOVERDEBUG so the shipping retail body is the LEFTOVERDEBUG sibling rank 242 (431 instructions). This variant omits the fogRemoved7F0BAA5C plus D_800442F8=200 tail that both the LEFTOVERDEBUG ASM and the compiled C body contain. The C matches rank 242; this variant is a non-shipping build alternate. Waived as non-authoritative; equivalence tracked via rank 242. |
| 244 | `sub_GAME_7F074CAC` | src/game/model.c | `verified-equivalent` | Sphere/capsule-vs-ray hit test, faithful transcription verified against ASM 7F074CAC. node->Data at 4, BoundingVolumeRadius at Data+4, Op17 pos at Data+8, RelatedNode at Data+0x14, Scale1 at 0x18, Scale2 at 0x1c. opcode = node->Opcode at 0; flag dispatch 0x100 rotated pos, 0x200 child-node midpoint (0.5 average) else plain diff = mtx row3 minus arg2; then 0x400 double scale1/scale2 projections onto mtx rows 0 and 1, else single-axis 0x800/0x1000/0x2000 or skip to final. Final sphere-ray test is division-free cross-multiplication: dot = dir3 dot diff, gated on 0 < dot, then (lendiff minus radius) times lendir less-equal dotsq returns 1. NATIVE_PORT mtx-null early-outs and bboxTrace are sim-neutral. Contrasts sibling rank 245 which was rewritten with division. |
| 245 | `sub_GAME_7F0747D0` | src/game/model.c | `finding-filed:FID-0116` | Ray-vs-OBB slab test. Retail ASM 7F0747D0 is division-free (only mul.s/add.s/sub.s/neg.s/c.lt.s with near/far swaps, zero div.s instructions), carrying axis_len_sq and local_dir as denominators into cross-multiplied comparisons. The port reimplemented it with explicit floating-point division plus a 1e-6f parallel-ray epsilon absent in retail. Filed as FID-0116: bit-exactness at grazing angles and degenerate axes is unproven, and the routine is on the combat path (chrobjhandler.c bullet-vs-object and bullet-vs-door hit tests with shot_pos and shot_dir). |
| 246 | `modelTickAnimQuarterSpeed` | src/game/model.c | `verified-equivalent` | Animation ticker, core verified against ASM 7F070AEC. frame from model unk28, frame2acc from unk58, ticks-less-equal-0 early exit. Per-tick: unkb0 playspeed ramp (playspeed at 0xa4, animrate at 0xa8, unkac at 0xac, unkb0/unkb4 at 0xb0/0xb4) with div.s matching retail; unk88 merge sets anim2 null at 0x54; timespeed interp (speed 0x40, oldspeed 0x44, newspeed 0x48, elapsespeed 0x50); frame += playspeed times speed; anim2 speed2 accumulation; animlooping at 0x26 equal 0 continues; loop-flip calls modelSetAnimation then final modelSetAnimFrame2 or WithChrStuff. Division in the ramps is faithful (retail uses div.s). NATIVE_PORT portChrActionNeedsLooping loop-restore and frame-value guards are documented compat fixes. |
| 247 | `sub_GAME_7F0B7F84` | src/game/bg.c | `waived:NATIVE_PORT portal-descent BFS reimplementation (rank 247 ASM is the LEFTOVERDEBUG variant, the build authority). The C implements the LEFTOVERDEBUG 5-arg gdl-passthrough signature: each early-out returns gdl matching the ASM move v0,s2 returns. Depth guards (>=101, D_8004489C, >=16), controlbytes1 and 1 disabled-portal reject, and player-side dot-vs-scale portal tests structurally match the C. Waived not verified because the full recursion, portal-metric float math, and bbox intersection need dedicated tracing; the two retail variants differ (LEFTOVERDEBUG 252i vs non-LEFTOVERDEBUG rank 252 223i) and the port comment claims decompilation from the non-authoritative variant though the C returns gdl per the authority. Retest with a portal-visibility oracle route.` | NATIVE_PORT portal-descent BFS reimplementation (rank 247 ASM is the LEFTOVERDEBUG variant, the build authority). The C implements the LEFTOVERDEBUG 5-arg gdl-passthrough signature: each early-out returns gdl matching the ASM move v0,s2 returns. Depth guards (>=101, D_8004489C, >=16), controlbytes1 and 1 disabled-portal reject, and player-side dot-vs-scale portal tests structurally match the C. Waived not verified because the full recursion, portal-metric float math, and bbox intersection need dedicated tracing; the two retail variants differ (LEFTOVERDEBUG 252i vs non-LEFTOVERDEBUG rank 252 223i) and the port comment claims decompilation from the non-authoritative variant though the C returns gdl per the authority. Retest with a portal-visibility oracle route. |
| 248 | `modelSetAnimation2` | src/game/model.c | `verified-equivalent` | NATIVE_PORT anim install with root-motion setup, verified against ASM 7F06F8xx and the clean decomp else reference. sp2C = anim was null; anim2 at 0x54 selects unk88/unk84 init; sets anim, gunhand, endframe=-1, speed, timespeed=0, modelSetAnimFrame, animlooping=0. If RootNode Opcode and 0xFF equal 1: rwdata via modelGetNodeRwData, animPos zero-init, scaledScale, sub_GAME_7F06D3F4, cosf/sinf of unk14, merge = unk2c. merge==0 and merge-not-0 root branches match line-for-line including sub_GAME_7F06D0CC angle blend and TAU wraps. Writes rwdata->unk01 = 1 at offset 1 (ASM sb t0,1(s0) at 7F06FC20), corroborating FID-0117. NATIVE_PORT ground-sync from chr->ground and rootMotionReady are documented compat fixes to reach retail-faithful init. |
| 249 | `sub_GAME_7F0B3C8C` | src/game/bg.c | `finding-filed:FID-0118` | Rank 249 LEFTOVERDEBUG build authority, room render pass. Bucket min/max scan, opaque i-ascending 3-phase pass (proj matrix, fogRenderClearFogMode, chrpropsRenderPass 0, field10E0 matrix, fogSetRenderFogColor of bgScissorCurrentPlayerViewF bbox pass 0, sub_GAME_7F0B677C, chrpropsRenderPass 2), mid scorch/bullet-impact, transparent i-descending pass all match ASM 7F0B3C8C. NATIVE_PORT ships two non-retail defaults filed FID-0118: exact_room_scissor default 0 emits bgScissorCurrentPlayerViewDefault where retail emits per-room bgScissorCurrentPlayerViewF (7F0B3DD8 opaque, 7F0B3F4C transparent); transparent rooms via bgSortTransparentRoomOrderForBucket depth sort where retail loops array order (7F0B3F10). |
| 250 | `sub_GAME_7F0B3C8C` | src/game/bg.c | `finding-filed:FID-0118` | Rank 250 non-LEFTOVERDEBUG sibling of rank 249 (0x7F0B2FA4, 234i vs LEFTOVERDEBUG 243i; differ only in stack frame and debug-count instrs). Same shared C body and DL pass structure; port render-default divergence tracked at FID-0118. Authority is the LEFTOVERDEBUG variant rank 249. |
| 251 | `sub_GAME_7F0B4AB4` | src/game/bg.c | `verified-equivalent` | Rank 251 portal flood-fill room-set expansion traced vs ASM 7F0B4AB4. Stack layout confirms retail 16-byte roomSet/newRoomSet/portalRoomSet buffers (0x150/0x140/0x130) matching the C, portalResults 200 at 0x5c. Per-portal sub_GAME_7F0B9F14 result 1 matches connectedRoom1 adds connectedRoom2, result 2 matches connectedRoom2 adds connectedRoom1 via addToByteSetMaxSize15 dedup-insert; roomSet 0xFF terminator walks, output copy max 7 rooms then 0xFF, portalRoomSet to portalOutput s32 stride 4 bounded by maxPortals, portalCountOut all match. NATIVE_PORT read-be reproduction only. |
| 252 | `sub_GAME_7F0B7F84` | src/game/bg.c | `waived:Rank 252 non-LEFTOVERDEBUG sibling of portal-descent sub_GAME_7F0B7F84 (223i); build authority is the LEFTOVERDEBUG variant rank 247 (waived: NATIVE_PORT portal-descent BFS reimplementation needing a portal-visibility oracle). Waived as non-authoritative sibling; equivalence tracked via rank 247.` | Rank 252 non-LEFTOVERDEBUG sibling of portal-descent sub_GAME_7F0B7F84 (223i); build authority is the LEFTOVERDEBUG variant rank 247 (waived: NATIVE_PORT portal-descent BFS reimplementation needing a portal-visibility oracle). Waived as non-authoritative sibling; equivalence tracked via rank 247. |
| 253 | `sub_GAME_7F0B6994` | src/game/bg.c | `verified-equivalent` | Rank 253 per-room collision bbox builder traced vs ASM 7F0B6994. Pass 1 counts G_VTX (type 4, end 0xB8); memaAlloc quadCount times 0x1C aligned 16; writes ptr_unique_collision_points at 0x2c and bitflags3 count at 0x30. Pass 2 per quad: cmdIndex s16 at rec 0, numVerts from cmd byte1 upper nibble plus 1, vtxOffset from big-endian cmd bytes 4-7 masked 0xFFFFFF, per-vertex s16 x/y/z bbox stride 0x10 init 0x7FFF/0xFFFF8000, min==max bump, room-position add from ptr_bgdata_room_fileposition_list pos truncated to s32, 6 bbox s32 at rec 0x04-0x18. All match. Port malloc+memset (zeroes unused pad 0x02), big-endian reassembly and null-checks are sim-neutral. |
| 254 | `sub_GAME_7F0B9F14` | src/game/bg.c | `verified-equivalent` | Rank 254 portal-crossing test traced vs ASM 7F0B9F14. dir=arg2-arg1; dot1/dot2 = arg dot normal times room_data_float1; same-side reject; D_80044900 = midpoint minus metric.min; per-edge cross product of edge and dir with mag_sq==0 reject; vert_dot; arg1_dot with the arg1.z times cross.z operand-order quirk; flag_pos/flag_neg consistency reject; return (dot1<min)?1:2 all match. Sole divergence is latent: at numPoints==0 retail returns (dot1<min)?1:2 (blezl skips loop to 7F0BA238) while NATIVE_PORT returns 0; dead because BG portals always have at least 3 vertices. NATIVE_PORT bgGetPortalEntryPtr null-checks are added hardening. |
| 255 | `sub_GAME_7F0B5528` | src/game/bg.c | `verified-equivalent` | Rank 255 portal-vertex projection and near-plane clip traced vs ASM 7F0B5528. Per vertex: read portal vert offsets 4/8/0xc, optional metric.normal times offset add when offset nonzero (sub_GAME_7F0B96CC), times room_data_float2, mtx4TransformVecInPlace; all-behind test -maxZ times 0.9 (rodata D_80058C7C) less than cur z clears allBehind, maxZ = zrange 1 over mCurrentLevelVisibilityScale. Near-plane clip t = -curZ over (nextZ minus curZ) interpolates x and y with z=0. numPoints<=0 returns 0 via allBehind on both. Match. NATIVE_PORT null-checks added. |
| 256 | `bgTestBulletHitBackground` | src/game/bg.c | `finding-filed:FID-0119` | Rank 256 traced vs ASM 7F0B7A78. Core nearest-hit matches: scale by room_data_float1, per-quad bgTestLineIntersectsBbox cull then bgTestLineIntersectionInRoom, dist_sq of integer-truncated hit minus start, 4-unit dist bias when check_if_imageID_is_light, best-distance field copy to output (0-0x14 f32, 0x18/0x1c/0x20 s32, 0x24, 0x28 s16, 0x2a texturenum). Divergence filed FID-0119: retail tail 7F0B7D2C-7F0B7D5C derives texture_s at output 0x2c and texture_t at 0x2e from a backward DL scan to the 0xF5 command; NATIVE_PORT hardcodes both to -1 because native bgTestLineIntersectionInRoom (rank 240) has no DL pointer at 0x24. |
| 257 | `sub_GAME_7F0B8A6C` | src/game/bg.c | `waived:Rank 257 LEFTOVERDEBUG build authority, room-visibility setup. Core skeleton verified vs ASM 7F0B8A6C: bgUpdateCurrentPlayerScreenMinMax, screen bbox capture from g_CurrentPlayer 0x1118-0x1124, sub_GAME_7F0B3BC4, per-room clear of room_rendered (0), room_neighbor_to_rendered (1), room_loaded_mask (0x34) stride 0x50, D_800442FC clear, D_80044858 = (x+1) mod 10, sub_GAME_7F0B7D94/5168/8A24, levelentry_index==21 special path. The room-admission portal-BFS core carries documented NATIVE_PORT FID-0009 camera-seed-walk and draw-only-room-set additions that intentionally diverge from retail (Silo sky-leak fix). Waived: full admission diff needs a portal-visibility oracle.` | Rank 257 LEFTOVERDEBUG build authority, room-visibility setup. Core skeleton verified vs ASM 7F0B8A6C: bgUpdateCurrentPlayerScreenMinMax, screen bbox capture from g_CurrentPlayer 0x1118-0x1124, sub_GAME_7F0B3BC4, per-room clear of room_rendered (0), room_neighbor_to_rendered (1), room_loaded_mask (0x34) stride 0x50, D_800442FC clear, D_80044858 = (x+1) mod 10, sub_GAME_7F0B7D94/5168/8A24, levelentry_index==21 special path. The room-admission portal-BFS core carries documented NATIVE_PORT FID-0009 camera-seed-walk and draw-only-room-set additions that intentionally diverge from retail (Silo sky-leak fix). Waived: full admission diff needs a portal-visibility oracle. |
| 258 | `sub_GAME_7F0B8A6C` | src/game/bg.c | `waived:Rank 258 non-LEFTOVERDEBUG sibling of sub_GAME_7F0B8A6C (190i vs LEFTOVERDEBUG 195i). Same shared NATIVE_PORT C body; build authority is the LEFTOVERDEBUG variant rank 257 (waived: visibility-setup reimpl with documented FID-0009 divergences, retest with portal-visibility oracle). Waived as non-authoritative sibling.` | Rank 258 non-LEFTOVERDEBUG sibling of sub_GAME_7F0B8A6C (190i vs LEFTOVERDEBUG 195i). Same shared NATIVE_PORT C body; build authority is the LEFTOVERDEBUG variant rank 257 (waived: visibility-setup reimpl with documented FID-0009 divergences, retest with portal-visibility oracle). Waived as non-authoritative sibling. |
| 259 | `sub_GAME_7F0BA2D4` | src/game/bg.c | `verified-equivalent` | Rank 259 portal-bbox room flood-fill traced vs ASM 7F0BA2D4. propbbmin/max = arg bbox times room_data_float1. Per source room, per portal: skip if controlbytes1 bit 0, dest_room = source==room1?room2 : source==room2?room1 : skip, portal bbox init from FLT_MAX globals D_80044904 and -FLT_MAX D_80044910 over all points equals the C point-0 init over the rest, bgIsBboxOverlapping, dedup then arg2 count++ guarded by count<arg4. Two non-obvious equivalences hold: sentinel-init equals point-0 init for real coords; the C do-while reset-to-0 pass equals the ASM monotonic queue walk because the dedup makes reprocessing idempotent and preserves append order. NATIVE_PORT null/bounds/count-clamp are hardening. |
| 260 | `sub_GAME_7F0B5864` | src/game/bg.c | `finding-filed:FID-0118` | Rank 260 LEFTOVERDEBUG build authority, portal screen-bbox. Traced vs ASM 7F0B5864: cache check on entry unk0>=0 returns cached bbox and count; dual sub_GAME_7F0B5528 calls (+scale then -scale when scale>0); per z<=0 vertex transform3Dto2DWithZScaling and min/max bbox left/top/right/bottom at 0x68/0x6c/0x70/0x74; viscount==0 zeroing; right<=left or bottom<=top full-screen fallback from g_CurrentPlayer; store to entry and arg1; return viscount all match. NATIVE_PORT ships portal near-plane clip default ON (bgPortalNearClipEnabled default 1) plus isfinite guard and default-off legacy-clamp; the near-clip default is a non-retail default filed FID-0118. |
| 261 | `sub_GAME_7F0B5864` | src/game/bg.c | `finding-filed:FID-0118` | Rank 261 non-LEFTOVERDEBUG sibling of sub_GAME_7F0B5864 (both 172i, logic-identical to rank 260). Same shared C body; the portal near-plane-clip non-retail default is tracked at FID-0118 via authority rank 260. |
| 262 | `sub_GAME_7F06D8B0` | src/game/model.c | `verified-equivalent` | ModelNode matrix processor (euler-angle variant). parent_mtx via modelFindNodeMtx or arg0 unk_matrix; main set_position_and_rotation_around_xyz then multiply_homogeneous into base index_e pos matches 7F06D8B0-D958. The 0x100 half-quat block (set_rotation_around_xyzf, 7F05BC68 at 0.5, to_transform_matrix) and 0x200 scale block (angle halved about PI, set_rotation_around_y, 7F05A9E0 else 1.5, column-3 mult, set_position, conditional multiply) match 7F06D99C-DB58 branch-for-branch incl delay-slot reuse. g_aimBoneCallback replaces raw D_80036090 hook; portTraceGuardMatrixWrite inert; both NATIVE_PORT only. |
| 263 | `sub_GAME_7F06DB5C` | src/game/model.c | `verified-equivalent` | ModelNode matrix processor (quaternion variant). Main uses quaternion_to_transform_matrix(data, quat, ...) matching 7F06DBDC-DC44; 0x200 block computes angle = acosf(quat0)*2 via jal acosf then add.s f0,f0 (x+x equals x*2.0 exactly in IEEE) matching 7F06DCF0-DCFC. Half-quat 0x100 block and scale block identical to the 7F06D8B0 sibling. g_aimBoneCallback plus portTraceGuard NATIVE_PORT only. |
| 264 | `sub_GAME_7F0B9338` | src/game/bg.c | `finding-filed:FID-0120` | S5 batch 22 rank 264: stan-branch omits retail firststaninroom NULL guard (retail returns early at 7F0B939C when firststaninroom[room] is 0; port bg.c:18130-18144 only gates on room >= dword_CODE_bss_8007B9DC). Stan arithmetic and the geometry BE vertex min/max scan otherwise match instruction-for-instruction. See FID-0120. |
| 265 | `sub_GAME_7F06E2B8` | src/game/model.c | `verified-equivalent` | ModelNode matrix processor (scalar rotation-around-y variant, 4th arg f32 value in f20). parent_mtx select then set_position_and_rotation_around_y into base index_e pos matches 7F06E2B8-E384. The combined has_bit8-or-has_bit9 value-halving block re-reads has_bit9 = opcode and 0x200 inside the if (andi at 7F06E3B4), and set_rotation_around_y receives value through the beqz delay-slot mov.s f12,f20 in both parent branches; both subtleties reproduced. Scale block (>= PI fold, 7F05A9E0 else 1.5, column-3 mult, set_position, conditional multiply) matches 7F06E464-E528. portTraceGuard NATIVE_PORT only. |
| 266 | `sub_GAME_7F0B5208` | src/game/bg.c | `verified-equivalent` | 8-corner 6-plane room AABB frustum cull. NATIVE_PORT body re-derives 7F0B5208 register-for-register: maxZ = zrange1 / mCurrentLevelVisibilityScale, 8 corners via corner_idx bit pattern selecting minbounds when bit set else maxbounds, 7F0B5488 projects (returns 1 if behind). Counters far=s1 left=s2 right=s3 top=s4 bottom=s5 behind=s6. Front branch (5300-538C) bumps far/left/right/top/bottom conditionally plus behind unconditionally; behind branch (5390-5418) uses inverted else-if comparisons and omits behind. Return 0 if any count equals 8 else 1 matches 542C-5460 same short-circuit order. The return-1 stub is dead (FID-0010 refuted); this NONMATCHING plus NATIVE_PORT body is live. |
| 267 | `sub_GAME_7F0B96CC` | src/game/bg.c | `verified-equivalent` | portal plane-normal plus min/max dot. NATIVE_PORT re-derives 7F0B96CC: edge1 = pts0..2 minus pts3..5 and edge2 = pts6..8 minus pts3..5 via two unrolled subtract loops (970C-974C, 9768-97A4), cross-product nx/ny/nz stored to arg1 normal as scratch (97A8-9808), mag = sqrtf(nx2+ny2+nz2) with invmag = 1/mag or mag when zero (983C-985C), normal scaled in place. Dot loop over numPoints (byte at portal 0, pts at portal+4, 12-byte stride) seeds FLT_MAX and -FLT_MAX (D_80058D54/58), writes arg1 min at 0xc and max at 0x10 (98C8-9924). Dot terms differ by IEEE-commutative reassociation only. |
| 268 | `sub_GAME_7F0B6368` | src/game/bg.c | `waived:PC room model loader -- full NATIVE_PORT architectural reimplementation. Retail 7F0B6368 loads compressed room vertex/DL binaries from ROM into fixed N64 memory banks; the port at bg.c:10020-10208 malloc's PC_ROOM_LOAD_BUF_SIZE buffers, decompresses via 7F0B5FAC/609C/61DC, registers big-endian N64 DL regions for the GBI translator, then applies CCRM LUT fog rewrites and light-fixture repopulation. Memory-model divergence is intentional and not instruction-diffable. Retest if a room-load-side-effect oracle is authored.` | PC room model loader -- full NATIVE_PORT architectural reimplementation. Retail 7F0B6368 loads compressed room vertex/DL binaries from ROM into fixed N64 memory banks; the port at bg.c:10020-10208 malloc's PC_ROOM_LOAD_BUF_SIZE buffers, decompresses via 7F0B5FAC/609C/61DC, registers big-endian N64 DL regions for the GBI translator, then applies CCRM LUT fog rewrites and light-fixture repopulation. Memory-model divergence is intentional and not instruction-diffable. Retest if a room-load-side-effect oracle is authored. |
| 269 | `sub_GAME_7F0B6368` | src/game/bg.c | `waived:Same symbol as rank 268 -- the LEFTOVERDEBUG vs non-debug ASM sibling pair both map to the single NATIVE_PORT room-loader reimplementation at bg.c:10020-10208 (malloc buffers plus DL-region registration vs N64 memory-bank ROM load). Memory-model divergence, not instruction-diffable. Retest if a room-load-side-effect oracle is authored.` | Same symbol as rank 268 -- the LEFTOVERDEBUG vs non-debug ASM sibling pair both map to the single NATIVE_PORT room-loader reimplementation at bg.c:10020-10208 (malloc buffers plus DL-region registration vs N64 memory-bank ROM load). Memory-model divergence, not instruction-diffable. Retest if a room-load-side-effect oracle is authored. |
| 270 | `sub_GAME_7F06D490` | src/game/model.c | `verified-equivalent` | ModelRwData header-record position update. modelGetNodeRwData then early-out if unk00 != 0; target = unk34, unk14 = unk30; when model unk2c != 0 and rwdata unk01 != 0 does interpolate3dVectors(&target, unk24, unk2c) and unk14 = 7F06D0CC(unk30, unk20, unk2c); when model anim2 or unk84 nonzero and rwdata unk02 != 0 blends target.y toward unk4c.y (optionally toward unk40.y by unk5c) by unk84. local_pos = target; optional model unka0 callback(model, rwdata pos, local_pos, rwdata ground) early-outs on 0; then delta = local_pos minus target, writes pos (y plus ground), unk34 plus delta, and unk01/unk02-gated unk24/unk4c/unk40 plus delta. Field offsets and branches match 7F06D490-D6A0 exact; no NATIVE_PORT delta. |
| 271 | `process_02_position` | src/game/model.c | `verified-equivalent` | ModelNode type-02 animated position node. data = node Data, skeleton = model obj Skeleton; reads the D_80036094 block into local_pos (native reads the three adjacent globals separately as a PC-redzone contiguity workaround equal to the N64 12-byte memcpy). Calls 7F06DEC0(node 0xC u16, model gunhand, skeleton, model anim, model unk34, local_pos); when unk2c != 0 a second decode into pos2 with unk38 then 7F06D160 blend; branches on unk84 == 0 to 7F06D8B0(local_pos) else decodes pos3 (unk25/anim2/unk64), optional pos4 (unk68) blend by unk5c, quaternion set/ensure-shortest/slerp by unk84, then 7F06DB5C(result_quat). Call args and c.eq.s branches match 7F06E0A0-E2B4. NATIVE_PORT anim/skeleton NULL early-outs guard inputs that would crash retail; default path identical. |
| 272 | `sub_GAME_7F06DEC0` | src/game/model.c | `verified-equivalent` | animation bit-field decoder to 3 euler floats. nbits = info_ptr byte 6; entry_offset from entries + index times 6 plus (flag ? 4 : 2), bit_offset = entry_offset times nbits; three 7F06DE04 bit reads at bit_offset incremented by nbits each. out0 = u16 val0 times 6.2831855 / 65536; when flag != 0, out1/out2 use 0x10000 minus val when nonzero else 0; when flag == 0 they use the u16 val directly. Matches 7F06DEC0-E098 incl the bgez u32-to-f32 fixups (no-op for u16 range). NATIVE_PORT NULL/joint-index/OOB-read guards only trip on inputs that would crash retail; entries via skel Joints equals anim_header plus 4; default path identical. |
| 273 | `get_obj_instance_controller_for_header` | src/game/model.c | `waived:full NATIVE_PORT architectural reimplementation. Retail 7F06C094 manages a fixed 32-byte-per-slot Model pool (ptr_allocation_1): scans for a free slot (slot 8 field is 0) or allocates 32 bytes via mempAllocBytesInBank, allocates rwdata sized numRecords times 4 aligned to 16 from a bank, then modelInit and writes the saved OISAVESIZE index to slot 2. The port at model.c:766-794 instead calloc's an individual 248-byte pointer-expanded Model and a 2x-sized rwdata array with no pool reuse, because the expanded struct cannot fit the N64 32-byte slot. Documented memory/pointer-size divergence, not instruction-diffable. Retest if a model-pool-reuse oracle is authored.` | full NATIVE_PORT architectural reimplementation. Retail 7F06C094 manages a fixed 32-byte-per-slot Model pool (ptr_allocation_1): scans for a free slot (slot 8 field is 0) or allocates 32 bytes via mempAllocBytesInBank, allocates rwdata sized numRecords times 4 aligned to 16 from a bank, then modelInit and writes the saved OISAVESIZE index to slot 2. The port at model.c:766-794 instead calloc's an individual 248-byte pointer-expanded Model and a 2x-sized rwdata array with no pool reuse, because the expanded struct cannot fit the N64 32-byte slot. Documented memory/pointer-size divergence, not instruction-diffable. Retest if a model-pool-reuse oracle is authored. |
| 274 | `sub_GAME_7F0B39BC` | src/game/bg.c | `verified-equivalent` | room-admit bookkeeping (adds or merges a room into the drawn list). Authority is the LEFTOVERDEBUG ASM sibling at 7F0B39BC since the build defines LEFTOVERDEBUG: entry stride 0x1c with roomid s32 at 0, unk1 s32 at 4, bbox at 8, next word at 0x18, and NO entry clamp. The NATIVE_PORT C at bg.c:3716-3818 matches this dword_CODE_bss_8007FFA0 layout: room_rendered=1, return 0 if room_loaded_mask set, merge path does unk1 max then 7F0B5CC0 then overwrites bbox from src and ORs next returning old next, add path appends and bumps g_BgNumberOfRoomsDrawn. The non-debug sibling at 7F0B2C7C uses byte/s16 fields with a 120-entry clamp and is NOT the port authority. Default bbox_src equals the screensize arg matching a2; the GE007_INTRO_FULLSCREEN_BBOX forced bbox and trace logging are default-off NATIVE_PORT diagnostics keeping the default path byte-identical. |
| 275 | `get_aircraft_obj_instance_controller` | src/game/model.c | `waived:Live NATIVE_PORT body at model.c:935-955 replaces the retail fixed-pool model allocator (ptr_allocation_0 slot reuse gated on D_80036070 and D_80036078) with calloc-per-instance and a 2x-sized rw buffer for 64-bit pointer expansion; observable Model-plus-animInit result preserved, pool and unk02 capacity bookkeeping is an N64 memory concern. Partial NATIVE_PORT reimpl.` | Live NATIVE_PORT body at model.c:935-955 replaces the retail fixed-pool model allocator (ptr_allocation_0 slot reuse gated on D_80036070 and D_80036078) with calloc-per-instance and a 2x-sized rw buffer for 64-bit pointer expansion; observable Model-plus-animInit result preserved, pool and unk02 capacity bookkeeping is an N64 memory concern. Partial NATIVE_PORT reimpl. |
| 276 | `process_03_unknown` | src/game/model.c | `verified-equivalent` | C at model.c:5315-5346 matches ASM at glabel model.c:5351 instruction-for-instruction: rodata = node->Data at offset 4, skeleton = model->obj Skeleton (obj at 8, Skeleton at 4), joint_id = Group.JointID at 0xc; first sub_GAME_7F06E540 uses gunhand at 0x24, anim at 0x20, unk34 at 0x34; unk2c-gated interp at 0x2c with unk38 at 0x38; unk84-gated block at 0x84 uses unk25 at 0x25, anim2 at 0x54, unk64 at 0x64, unk5c-gated with unk68 at 0x68; final sub_GAME_7F06E2B8(arg0, model, node, result). Offsets, c.eq.s zero gating, and arg order all match. |
| 277 | `sub_GAME_7F0B39BC` | src/game/bg.c | `verified-equivalent` | Non-debug ASM sibling of the LEFTOVERDEBUG room-admit authority reviewed at rank 274. The compiled NONMATCHING C at bg.c:3684-3818 diffs clean against the LEFTOVERDEBUG ASM at glabel bg.c:3505: room_rendered set to 1 at offset 0, early return 0 when room_loaded_mask at 0x34 is nonzero, linear scan of dword_CODE_bss_8007FFA0 entries at stride 0x1c (roomid at 0, unk1 at 4, bbox at 8, next at 0x18), depth max-merge, bbox overwrite from bbox_src, next OR-in with return of prior next, append at g_BgNumberOfRoomsDrawn. Verified-equivalent to the authority. |
| 278 | `sub_GAME_7F0B609C` | src/game/bg.c | `waived:Live NATIVE_PORT body at bg.c:9563-9572 intentionally skips retail N64 texture processing (texCopyGdls and texLoadFromGdl) and keeps the primary room DL as raw N64 binary for the gfx_pc.c translator, plus a bgDecompress-failure guard; the load and decompress prefix (len from csize_primary at 0x14, size < len+0x20 returns -1, source, obLoadBGFileBytesAtOffset with pPriMappingBin plus 0xf1000000, bgDecompress) matches the LEFTOVERDEBUG ASM at glabel bg.c:9592. Deliberate PC renderer-architecture substitution, partial NATIVE_PORT reimpl.` | Live NATIVE_PORT body at bg.c:9563-9572 intentionally skips retail N64 texture processing (texCopyGdls and texLoadFromGdl) and keeps the primary room DL as raw N64 binary for the gfx_pc.c translator, plus a bgDecompress-failure guard; the load and decompress prefix (len from csize_primary at 0x14, size < len+0x20 returns -1, source, obLoadBGFileBytesAtOffset with pPriMappingBin plus 0xf1000000, bgDecompress) matches the LEFTOVERDEBUG ASM at glabel bg.c:9592. Deliberate PC renderer-architecture substitution, partial NATIVE_PORT reimpl. |
| 279 | `sub_GAME_7F0B609C` | src/game/bg.c | `waived:Byte-identical non-debug sibling of sub_GAME_7F0B609C at the same VRAM 7F0B609C (no debug-specific instructions); same NATIVE_PORT texture-path substitution as rank 278. Partial NATIVE_PORT reimpl.` | Byte-identical non-debug sibling of sub_GAME_7F0B609C at the same VRAM 7F0B609C (no debug-specific instructions); same NATIVE_PORT texture-path substitution as rank 278. Partial NATIVE_PORT reimpl. |
| 280 | `sub_GAME_7F0B61DC` | src/game/bg.c | `waived:Secondary-DL twin of sub_GAME_7F0B609C. Live NATIVE_PORT body at bg.c:9797-9806 skips retail texCopyGdls and texLoadFromGdl and keeps the secondary room DL as raw N64 binary for the gfx_pc.c translator, plus a bgDecompress-failure guard; the prefix (len from csize_secondary at 0x18, size check, source, obLoadBGFileBytesAtOffset with pSecMappingBin plus 0xf1000000, bgDecompress) matches the LEFTOVERDEBUG ASM at glabel bg.c:9825. Deliberate PC renderer-architecture substitution, partial NATIVE_PORT reimpl.` | Secondary-DL twin of sub_GAME_7F0B609C. Live NATIVE_PORT body at bg.c:9797-9806 skips retail texCopyGdls and texLoadFromGdl and keeps the secondary room DL as raw N64 binary for the gfx_pc.c translator, plus a bgDecompress-failure guard; the prefix (len from csize_secondary at 0x18, size check, source, obLoadBGFileBytesAtOffset with pSecMappingBin plus 0xf1000000, bgDecompress) matches the LEFTOVERDEBUG ASM at glabel bg.c:9825. Deliberate PC renderer-architecture substitution, partial NATIVE_PORT reimpl. |
| 281 | `sub_GAME_7F0B61DC` | src/game/bg.c | `waived:Byte-identical non-debug sibling of sub_GAME_7F0B61DC at the same VRAM 7F0B61DC; same NATIVE_PORT secondary-DL texture-path substitution as rank 280. Partial NATIVE_PORT reimpl.` | Byte-identical non-debug sibling of sub_GAME_7F0B61DC at the same VRAM 7F0B61DC; same NATIVE_PORT secondary-DL texture-path substitution as rank 280. Partial NATIVE_PORT reimpl. |
| 282 | `sub_GAME_7F0B9B94` | src/game/bg.c | `verified-equivalent` | Compiled PORT_FIXME_STUBS body at bg.c:19200-19266 matches the bgOrderPortal ASM at glabel bg.c:19339 exactly: entry at g_BgPortals stride 8, room centers from connectedRoom1 at 4 and connectedRoom2 at 5 via sub_GAME_7F0B92B4, metric via sub_GAME_7F0B96CC; room1Dot = normal dot room1Center; if metric.max < room1Dot then bgSwapConnectedRooms, negate normal, min = -max, max = -oldMin, swapped=1; room2Dot = normal dot room2Center; if room2Dot <= metric.min and swapped then bgSwapConnectedRooms; return room2Dot. Only float add operand order differs (commutative, IEEE-identical). NATIVE_PORT additions are faithful-default-inert: bgPortalOrderingEnabled defaults to 1, and the null g_BgPortals, arg0<0, offset_portal==0, numPoints<3 guards never fire for the live caller at bg.c:5444 which only passes portals with offset_portal nonzero. |
| 283 | `sub_GAME_7F06D1CC` | src/game/model.c | `verified-equivalent` | C at model.c:2879-2937 matches ASM at glabel model.c:2942 exactly: entry = entries + index*6, nbits = entry byte at 2, total_offset = entry u16 at 0 plus bit_addend, cur = data pointer at 0x10 plus total_offset>>3, avail = 8 minus low 3 bits; main loop while bits_left >= avail masks low avail bits and shifts left by remaining bits, avail reset to 8; tail masks low bits_left bits of cur shifted right by avail-bits_left; sign-extend when nbits<16 and bit nbits-1 set; result plus entry u16 at 4 masked to 16 bits. srav equals logical shift because cur was lbu zero-extended. Only NATIVE_PORT change is base-plus-offset reconstruction of the entry and data pointers into ptr_animation_table for 64-bit; entry u16 fields pre-swapped at load by animSwapEntryTable. |
| 284 | `sub_GAME_7F06D2E4` | src/game/model.c | `verified-equivalent` | C NATIVE_PORT body at model.c:3029-3105 matches ASM at glabel model.c:3110 in the valid-input path: bit_offset = anim_data u16 at 0xc times frame_mult; joints u16 at index*6+4 when flag nonzero else index*6+2; four sub_GAME_7F06D1CC calls fill out_pos 0..2 and rotation from entry_field plus 0..3 with bit_offset; when flag nonzero out_pos 0 negated unconditionally and rotation = 0x10000 minus rotation only when rotation nonzero; returns rotation. joints via skel Joints struct access replaces the retail anim_header+4 deref for 64-bit layout. Added defensive early-returns (null out_pos, null anim_header or anim_data, anim_data below 0x1000, bad skeleton or index) and the out_pos zeroing are crash-avoidance on N64-UB inputs and never fire for valid callers. |
| 285 | `sub_GAME_7F06E540` | src/game/model.c | `verified-equivalent` | C NATIVE_PORT body at model.c:5184-5225 matches ASM at glabel model.c:5234 exactly: nbits = keyframe_info byte at 6; raw_val = keyframes u16 at index*6+4 when flag nonzero else index*6+2; bitoffset = raw_val times nbits; extracted = sub_GAME_7F06DE04(data_ptr, nbits, bitoffset); position mode result = (0x10000 minus extracted) times 6.2831855 over 65536 only when extracted nonzero else 0; rotation mode result = extracted times 6.2831855 over 65536. extracted is u16 so always non-negative and the retail bgez unsigned-correction is never taken. Only NATIVE_PORT change is keyframes = skel Joints struct access replacing the retail anim_header+4 deref. |
| 286 | `sub_GAME_7F0B38B4` | src/game/bg.c | `finding-filed:FID-0121` | LEFTOVERDEBUG authority variant at VRAM 7F0B38B4, glabel bg.c:3505. Retail resets s0 to pair start a0 on a room match (move s0,a0 at 7F0B3908) so the entire first visibility string is processed; the compiled C at bg.c:3459-3483 processes from the matched position instead. Currently latent (no caller). See FID-0121. |
| 287 | `sub_GAME_7F0B38B4` | src/game/bg.c | `finding-filed:FID-0121` | Byte-different non-debug sibling at VRAM 7F0B2B74, glabel bg.c:3587, carries the identical divergence (reset move s0,a0 at 7F0B2BC8). Same C body, same FID-0121. |
| 288 | `sub_GAME_7F0B7DE4` | src/game/bg.c | `verified-equivalent` | LEFTOVERDEBUG authority variant (glabel 7F0B7DE4; port defines LEFTOVERDEBUG, CMakeLists:1355). NONMATCHING C bg.c:13709-13758 matches: 32-byte entry stride t6<<5 into dword_CODE_bss_8007C100; depth-gate arg3>=2; dest_room=(g_BgPortals[arg2].connectedRoom2^connectedRoom1)^arg1; sub_GAME_7F0B7DA8 portal-count>=9 drop; entry[0..3]=arg0..arg3 words + entry[4..7]=arg4[0..3] floats at 0x10-0x1c; 500-ring wrap + D_800448A4 tail-collision backoff (new_idx-1). 6/64 diffs = ido_recomp register allocation (in-source note); 58 byte-identical. NATIVE_PORT bgShouldTracePortal traces default-off. |
| 289 | `sub_GAME_7F0B7DE4` | src/game/bg.c | `verified-equivalent` | Non-debug sibling of the LEFTOVERDEBUG authority (rank recorded at line 13731). This variant (VRAM 7F0B7120) is structurally different: 20-byte byte/halfword-packed entry (stride t6*20, sb/sh writes), 250-entry ring, gating on arg2 with arg0/1/2 portal order. It is a different build variant and NOT the port authority (the port defines LEFTOVERDEBUG); the port C is verified-equivalent to the LEFTOVERDEBUG variant, per the sibling convention (cf. ranks 274/277). |
