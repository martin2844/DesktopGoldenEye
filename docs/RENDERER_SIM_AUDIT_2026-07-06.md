# MGB64 Renderer + Simulation Audit — 2026-07-06

A no-quarter "fishing expedition" audit of the native port's Metal backend, Fast3D display-list
interpreter, OpenGL parity, and game simulation. Produced by an 18-lane parallel finder sweep
(Metal lifecycle/sync/render-pass/shader-gen/post-FX, Fast3D texture-cache/T&L/tri-assembly/XLU,
GL↔Metal parity, sim timing/objects/weapons/physics/effects, memory-safety, port-correctness,
player-experience) followed by per-finding adversarial verification against the real code.

**Framing:** this is a *native port* — the goal is excellent gameplay for players, not preserving
original-game bugs. Each finding is classified `port-defect` (a bug the port introduced),
`parity-divergence` (Metal behaves differently from OpenGL), or `n64-quirk-hurting-players`
(faithful behavior that degrades the modern experience — fix behind an opt-in flag so the
deterministic/`sim_state_hash` path stays byte-identical).

## Summary

- **67 findings**: 5 high · 20 medium · 42 low
- **Verification** (adversarial, 67/67 findings re-checked before the run was stopped): 47 CONFIRMED · 14 REFUTED · 6 UNCERTAIN
- Verdicts below are heuristically matched to findings by file/symbol/title overlap in the verifier's reasoning; unmatched findings carry the finder's own confidence. Raw per-agent detail lives in the workflow journal (`wf_d4bd97e4-1bd/journal.jsonl`).
- **Independently cross-checked and found CORRECT (not bugs):** Metal CPU/GPU triple-buffering (semaphore signal in the completion handler, `gfx_metal.mm:2649`); GL→Metal NDC-z remap (`gfx_pc.c:19154`, gated by `mtl_z_is_from_0_to_1`); PSO/depth/sampler cache-key completeness (`mtl_pso_for:1066`); decal depth-bias presence; scissor/viewport Y-flip; mid-frame vs between-frame readback source selection. The hard Metal traps are handled.

Severity: 🔴 high · 🟠 medium · 🟡 low. Verdict: ✅ CONFIRMED · ❌ REFUTED · ❓ UNCERTAIN · ▫️ not re-checked.

---

## Fast3D interpreter (gfx_pc.c)

### 1. 🔴 ▫️ Unclamped current_num_lights (G_MW_NUMLIGHT) drives an OOB read+write in the lighting loop

- **File:** `src/platform/fast3d/gfx_pc.c:20111` · `gfx_sp_moveword / gfx_sp_vertex`
- **Severity:** high · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** high
- **What's wrong:** G_MW_NUMLIGHT stores the light count straight from the DL word with no clamp: `rsp.current_num_lights = ((uint32_t)data - 0x80000000U) / 32;` (line 20111). `data` is the raw command word `cmd->words.w1` (call sites 22160 PC path, 23415 N64 path), so a corrupt/mis-resolved DL, or any numlights payload below 0x80000000 (the subtraction underflows to a huge value before the uint8_t truncation), yields an arbitrary 0-255 count. The lit-vertex path in gfx_sp_vertex then iterates that count over arrays sized for only MAX_LIGHTS(=2): `for (int li = 0; li < rsp.current_num_lights - 1; li++) calculate_normal_dir(&rsp.current_lights[li], rsp.current_lights_coeffs[li]);` (16947-16948) and the shading loop at 16959-16968, plus `rsp.current_lights[rsp.current_num_lights - 1]` at 16955. current_lights is [MAX_LIGHTS+1]=3 and current_lights_coeffs is [MAX_LIGHTS]=2 (decl 3416/3418). calculate_normal_dir WRITES its 2nd argument (15460-15461 via gfx_transposed_matrix_mul/gfx_normalize_vector), so any li>=2 is a 12-byte OOB WRITE into adjacent rsp state, and current_lights[li] for li>=3 is an OOB read. The safe bound MAX_LIGHTS+1 is already known-untrusted elsewhere (line 10646 guards `current_num_lights <= (MAX_LIGHTS + 1)`), but the hot vertex path has no such guard.
- **Evidence:**

  ```
  20111: `rsp.current_num_lights = ((uint32_t)data - 0x80000000U) / 32;` (no clamp). 3418: `float current_lights_coeffs[MAX_LIGHTS][3];` with `#define MAX_LIGHTS 2` (line 210). 16948: `calculate_normal_dir(&rsp.current_lights[li], rsp.current_lights_coeffs[li]);` inside `for (int li = 0; li < rsp.current_num_lights - 1; li++)`. 15460-15461 calculate_normal_dir writes coeffs. Contrast guard at 10646: `rsp.current_num_lights <= (MAX_LIGHTS + 1)`.
  ```
- **Player impact:** A malformed/mis-decoded numlights command followed by any lit geometry corrupts neighbouring renderer state (or crashes) via an out-of-bounds write into the rsp struct; manifests as flicker/garbage lighting or a hard crash rather than a skipped draw.
- **Suggested fix:** Clamp at the set site: `rsp.current_num_lights = min(((uint32_t)data - 0x80000000U) / 32, MAX_LIGHTS + 1);` (and reject values < 0x80000000). Also bound the loop counters in gfx_sp_vertex to MAX_LIGHTS, matching the existing guard at line 10646.

### 2. 🔴 ❌ modelview_matrix_stack_size can underflow to 0, making [size-1] indexing OOB (index 255 / -1)

- **File:** `src/platform/fast3d/gfx_pc.c:16610` · `gfx_sp_pop_matrix / gfx_sp_vertex / gfx_sp_matrix`
- **Severity:** high · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** high
- **What's wrong:** gfx_sp_pop_matrix guards only against wrapping the counter, allowing it to legitimately reach 0: `if (rsp.modelview_matrix_stack_size > 0) { --rsp.modelview_matrix_stack_size; ... }` (16128). Downstream code then unconditionally indexes `[modelview_matrix_stack_size - 1]` assuming >=1. In gfx_sp_vertex the index is first narrowed to uint8_t: `uint8_t mtx_stack_pos = rsp.modelview_matrix_stack_size - 1;` (16610) -> 255 when size==0, so `rsp.modelview_matrix_stack[255]` / `modelview_cmd_addr[255]` etc. read ~14KB past the 11-entry arrays. In gfx_sp_matrix the modelview LOAD path does `memcpy(rsp.modelview_matrix_stack[rsp.modelview_matrix_stack_size - 1], matrix, sizeof(matrix));` (16025) where the int-promoted `0 - 1` = -1 -> a 64-byte OOB WRITE below the array; calculate_normal_dir (15460) also reads [size-1]. Reachable two ways: (a) an unbalanced G_POPMTX (the N64/PC dispatchers call gfx_sp_pop_matrix(1) at 22144/23005) with more pops than pushes; (b) the silent push-drop at line 16005 (`(parameters & G_MTX_PUSH) && rsp.modelview_matrix_stack_size < 11` -> push dropped when full) which unbalances pushes vs pops so later pops reach 0. Other accessors DO guard (`size > 0` at 3446, ternary at 10954), confirming the invariant is known-fragile but unenforced on the hot paths.
- **Evidence:**

  ```
  16128: `if (rsp.modelview_matrix_stack_size > 0) { --rsp.modelview_matrix_stack_size;` (allows 0). 16610: `uint8_t mtx_stack_pos = rsp.modelview_matrix_stack_size - 1;` then 16615 `mv = rsp.modelview_matrix_stack[mtx_stack_pos];` (array `modelview_matrix_stack[11]`, decl 3402). 16025: `memcpy(rsp.modelview_matrix_stack[rsp.modelview_matrix_stack_size - 1], matrix, sizeof(matrix));`. 16005: push silently dropped when `size >= 11`.
  ```
- **Player impact:** After an unbalanced matrix pop (or a scene that nests matrices past 11), the next vertex or matrix-load command reads/writes far outside the modelview stack, corrupting renderer state or crashing with SIGSEGV.
- **Suggested fix:** Never let the stack fall below 1 (clamp pop floor to 1, since index base is 1), or guard every `[modelview_matrix_stack_size - 1]` access with `size > 0`. Also account for dropped pushes at line 16005 so pop/push stay balanced (e.g., track an overflow depth counter to swallow the matching pops).
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited code substantially exists but the defect is not reachable with the data this port actually processes, and the cited line numbers are partly wrong.

What matches: the pop guard at 16128 is verbatim (`if (rsp.modelview_matrix_stack_size > 0) { --...; }`), permitting the counter to reach exactly 0. The unguarded `[modelview_matrix_stack_size - 1]` indexing exists (gfx_sp_vertex, the LOAD memcpy at 16025, calculate_normal_dir at 15460), and would compute index 255 (uint8_t) or -1 (int) if size were 0. Array decl is [11] at 3402.

What's wrong/unsubstantiated: (1) The cited line 16610 is NOT the vertex index — it's a diagnostic printf inside a reject-logger. The real `uint8_t mtx_stack_pos = rsp.modelview_matrix_stack_size - 1;` is at 16642. (2) Reachability is not substantiated. gfx_sp_reset (23722) initializes the stack to size=1. Both G_POPMTX dispatch sites (22144, 23005) call gfx_sp_pop_matrix(1) — always a single decrement, never a bulk count. So reaching 0 requires strictly more POPMTX commands than PUSH commands since reset, i.e. a malformed display list. This port replays fixed, authored retail GoldenEye display lists, which are well-formed and balance push/pop; the base level-1 matrix is never popped by well-formed DLs. (3) The push-drop path at 16005 only drops when the stack is already 11 deep — GoldenEye's matrix nesting (rooms/bones) is only a few levels deep and never approaches 11, so this path is effectively unreachable and cannot create the imbalance described. (4) This structure is faithful to upstream fast3d (SM64 PC port), where the identical pattern has run for years without this firing, precisely because it relies on well-formed DL input. Project history notes the engine runs across all 18+ levels without crashes.

So it's a latent, upstream-shared fragility gated behind an externally-guaranteed invariant (balanced push/pop from fixed retail data), not a demonstrated reachable defect against the inputs the port receives.
  - **Correction:** The described unguarded indexing is real but the vertex-path line is 16642, not 16610 (16610 is a diagnostic printf). More importantly, the underflow-to-0 is only reachable via a malformed display list (more POPMTX than PUSHMTX, or 11-deep nesting) — neither occurs in the fixed, well-formed retail GoldenEye display lists this port replays. Both G_POPMTX sites call pop(1), reset sets size=1, and the code is faithful to battle-tested upstream fast3d. It is a latent hardening opportunity (clamp the pop floor to 1), not a defect that affects players with the game's actual data.

### 3. 🟠 ❌ Coverage-memory XLU strips are excluded from the per-group deferred re-snapshot, forcing overlapping backdrop tris to share one stale per-batch framebuffer snapshot

- **File:** `src/platform/fast3d/gfx_pc.c:18376` · `gfx_emit_loaded_triangle`
- **Severity:** medium · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** medium
- **What's wrong:** The RDP coverage-memory emulation reads the framebuffer 'memory' color from a snapshot taken ONCE per draw_triangles batch (gfx_opengl_draw_triangles per-batch path, gfx_opengl.c:2266-2285; mtl_draw_triangles single snapshot, gfx_metal.mm:2980-3009). The per-batch snapshot is only correct if same-material tris in the batch are coplanar/non-overlapping, since every fragment reads the pre-batch framebuffer. The room-XLU sort/defer machinery WOULD fix overlap by splitting each cmd_addr group into its own draw_triangles call (each triggering a fresh snapshot in gfx_room_xlu_deferred_draw_pending, line 13977-13983). But coverage-memory tris are structurally excluded from that path: gfx_api_blend_mode_for_rdp_memory_class sets api_blend_mode to GFX_BLEND_ALPHA_RDP_CVG_MEMORY (line 18355-18357), while room_secondary_xlu_sort requires 'api_blend_mode == GFX_BLEND_ALPHA' (line 18376). So sortable=0 for every coverage tri (line 19542-19543: buf_vbo_tri_sortable = room_secondary_xlu_sort ? 1 : 0), and gfx_room_xlu_defer_buffer bails if ANY tri is non-sortable (line 14012-14016). Result: all coverage backdrop tris land in one batch with one snapshot. Where a secondary-room backdrop shell overlaps in screen space (e.g. near+far faces of an unculled fog shell, or layered haze strips), the second-drawn tri reads pre-batch memory instead of the first tri's blended+coverage-accumulated result, so the RDP 3-bit coverage-wrap term and color are wrong (banding / under-darkened fog vs N64).
- **Evidence:**

  ```
  room_secondary_xlu_sort at line 18370-18382 requires `blend_mode == GFX_BLEND_ALPHA && api_blend_mode == GFX_BLEND_ALPHA`; api_blend_mode was just set for the coverage class at 18355: `gfx_api_blend_mode_for_rdp_memory_class(blend_mode, rdp_memory_blend_class)` -> GFX_BLEND_ALPHA_RDP_CVG_MEMORY. Emission: line 19542 `buf_vbo_tri_sortable[buf_vbo_num_tris] = room_secondary_xlu_sort ? 1 : 0;`. Defer gate: line 14012 `for (...) if (!buf_vbo_tri_sortable[i]) return false;`
  ```
- **Player impact:** On secondary-room translucent/fog backdrops whose strips overlap in screen space, the coverage-wrap blend is under-accumulated: subtly wrong tint/darkness or faint banding on those backdrops. Single-layer non-overlapping shells are unaffected.
- **Suggested fix:** Either allow coverage-memory tris into the defer/sort path (relax the api_blend_mode==GFX_BLEND_ALPHA gate to also accept GFX_BLEND_ALPHA_RDP_CVG_MEMORY so each cmd_addr group re-snapshots), or explicitly force per-tri snapshot for the coverage class when the batch's tri bboxes overlap.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The structural claim is real: coverage-class tris get api_blend_mode=GFX_BLEND_ALPHA_RDP_CVG_MEMORY (gfx_pc.c:5116, 18387-18389), room_secondary_xlu_sort requires api_blend_mode==GFX_BLEND_ALPHA (18402-18414, NOT the cited 18376), so coverage tris are non-sortable (19580) and the defer path bails on them (14017-14021). But the finder's causal mechanism is a misread. The framebuffer snapshot exists ONLY for the memory/coverage-memory classes — gfx_opengl.c:2245 gates it on g_blend_alpha_rdp_memory||g_blend_alpha_rdp_cvg_memory. The plain GFX_BLEND_ALPHA tris that the sort/defer path actually operates on take NO snapshot and use native fixed-function per-fragment alpha blending against the live framebuffer, correct regardless of batching. So the sort/defer machinery is a back-to-front depth sort, not the 'per-group re-snapshot overlap fix' the finder describes; coverage tris being excluded from it does not remove any snapshot they would otherwise get. The genuine intra-batch overlap approximation for CVG_MEMORY is explicitly documented at gfx_opengl.c:2266-2274 AND already has an opt-in exact path, GFX_XLU_SNAPSHOT_PER_TRI / GE007_XLU_SNAPSHOT_MODE=pertri (2248-2264), that re-snapshots per triangle and is 'Correct for intra-batch overlap.' The suggested fix (route coverage tris into the sort path) would not produce per-tri snapshots and so would not fix intra-group overlap anyway. Additionally the one known secondary-room coverage-shell case (Frigate, gfx_room_water_alpha_suppress_needed at 5125+) has its alpha suppressed so it does not visibly shade the framebuffer. No evidence of an actual observed artifact. This is a documented approximation with an existing correct-path hatch, not an unhandled defect.
  - **Correction:** Coverage-class secondary-room XLU tris are indeed excluded from the room-XLU sort/defer gate (real code at gfx_pc.c:18402-18414, 19580, 14017-14021; the finder's line numbers are ~26 lines low). However, that path is a depth-ordering sort for plain fixed-function alpha blending, not a per-group framebuffer-snapshot mechanism — plain GFX_BLEND_ALPHA tris take no snapshot (gfx_opengl.c:2245). The real intra-batch overlap approximation applies only to the CVG_MEMORY class and is already a documented performance tradeoff (gfx_opengl.c:2266-2274) with an existing exact opt-in path GFX_XLU_SNAPSHOT_PER_TRI/GE007_XLU_SNAPSHOT_MODE=pertri (2248-2264). The proposed gate relaxation would not fix intra-group overlap and is not the intended remedy.

### 4. 🟠 ✅ 64-bit pointer hash table keys on low 32 bits only — aliasing returns the wrong buffer and window-overflow silently drops mappings

- **File:** `src/platform/gfx_ptr.h:29` · `gfx_ptr_store / gfx_ptr_resolve`
- **Severity:** medium · **Class:** port-defect · **Category:** correctness · **Finder confidence:** medium
- **What's wrong:** The table that recovers full 64-bit pointers from GBI-truncated 32-bit display-list addresses is keyed purely on the low 32 bits of the pointer (`uint32_t key = (uint32_t)full;`). Two distinct live allocations whose addresses differ only in the high 32 bits (i.e. 4GB apart — plausible with a large malloc'd ROM buffer plus mmap'd texture-pack pages plus the gfx arena) collapse to the same key; the second gfx_ptr_store() overwrites the first (key-match branch), so gfx_ptr_resolve() then returns the WRONG buffer for whichever DL referenced the evicted one — reading foreign/short data as vertices/matrices. Separately, the store uses a fixed 4-slot linear-probe window and, when all four slots hold *other* keys, unconditionally evicts base_idx (`gfx_ptr_keys[base_idx] = key;`). The evicted mapping is lost: a later gfx_ptr_resolve() of the evicted key walks the 4 slots, matches none, and returns NULL. Because the table is only cleared at renderer init (gfx_pc.c:23775 memset), not per frame, distinct keys accumulate across a whole level, and in a dense scene some 4-window will overflow (birthday effect over the effective bucket count) → dropped draw.
- **Evidence:**

  ```
  gfx_ptr.h:31 `uint32_t key = (uint32_t)full;`  gfx_ptr.h:37-41 store overwrites on key match `if (gfx_ptr_keys[idx] == key || gfx_ptr_keys[idx] == 0) { gfx_ptr_keys[idx] = key; gfx_ptr_vals[idx] = full; return; }`  gfx_ptr.h:43-45 `/* All probe slots occupied — evict first slot */ gfx_ptr_keys[base_idx] = key; gfx_ptr_vals[base_idx] = full;`  gfx_ptr.h:51-57 resolve returns NULL once the 4-probe window is exhausted. Populated via platform_os.h:513 `gfx_ptr_store(addr)` in osVirtualToPhysical_pc.
  ```
- **Player impact:** Intermittent wrong/missing geometry (a mesh drawn with another object's vertices, or a draw silently skipped) in heavy scenes or long sessions, hard to reproduce and not tied to any single asset.
- **Suggested fix:** Store the high bits too (compare full uintptr_t, not just the 32-bit key) so an aliased low-32 key can't shadow a live buffer, and on window overflow grow/relocate rather than evicting a possibly-live mapping (or at least log + fail closed). Given the 64K-slot table this is cheap.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited code matches the evidence verbatim (gfx_ptr.h:31,37-45,66-76) and the table is cleared only at init (gfx_pc.c:23775), so distinct keys do accumulate per level. The eviction sub-claim is a genuine defect: on a full 4-probe window, gfx_ptr_store overwrites base_idx with no tombstone, and a later gfx_ptr_resolve of the evicted key walks all 4 slots, matches none, encounters no zero slot, and returns NULL — a silently dropped mapping. gfx_resolve_addr then returns NULL and callers null-check, so the outcome is a dropped draw, not corruption or a crash. However the headline aliasing mechanism is mischaracterized: resolve is fundamentally handed only a 32-bit token (GBI truncates pointers to 32-bit; gfx_ptr_resolve(uint32_t key) / gfx_resolve_addr(uint32_t addr)), so two live buffers sharing identical low-32 bits are indistinguishable at resolve time no matter what is stored. The finder's suggested fix ('store the high bits, compare full uintptr_t') cannot work for resolution because there are no high bits available in the resolve key — this is an inherent limitation of the port's truncation scheme, not a fixable keying bug, and it requires two live allocations with byte-identical low-32 bits (very low probability). So the real, code-substantiated issue is the silent no-tombstone eviction on window overflow, a low-probability dropped-draw robustness gap.
  - **Correction:** The actionable defect is the silent eviction on a full 4-slot probe window (gfx_ptr.h:43-45): it drops a possibly-live mapping without a tombstone, so a later resolve returns NULL and the draw is skipped. Impact is an intermittent dropped draw (callers null-check gfx_resolve_addr), not a wrong-buffer read or crash, and it is low-probability given the 65536-slot table. The finding's primary 'low-32 aliasing returns the wrong buffer' framing and its suggested fix are incorrect: gfx_ptr_resolve only ever receives a 32-bit truncated token, so storing/comparing the high 32 bits cannot disambiguate aliased pointers — low-32 keying is inherent to the GBI truncation, not a fixable table defect. Severity is low, not medium.

### 5. 🟠 ✅ N64 display-list recursion has no depth cap (unlike the PC path) -> C-stack overflow on cyclic/deep DL

- **File:** `src/platform/fast3d/gfx_pc.c:22705` · `gfx_process_n64_dl`
- **Severity:** medium · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** gfx_process_n64_dl handles a push-type G_DL by recursing into itself via native C recursion: `gfx_process_n64_dl((const uint8_t *)dl_addr);` (23267). It increments the shared dl_depth (22705) but never checks it against any maximum. The sibling PC interpreter gfx_run_dl_pc DOES cap recursion: `dl_depth++; if (dl_depth > 32) { ... dl_depth--; return; }` (22028-22033). The only N64-side bounds are the per-invocation 50000-command counter (22761) and the g_n64_dl_stack[128] write guard (22707) — neither limits recursion DEPTH. A corrupt or mis-resolved N64 DL that forms a cycle (A->B->A push-type) or nests very deeply passes the plausibility/segment checks and recurses until the native stack overflows (SIGSEGV), instead of the intended graceful skip. gfx_process_n64_dl frames are large (many locals + fprintf temporaries), so the overflow arrives quickly.
- **Evidence:**

  ```
  22705: `dl_depth++;` with no subsequent `if (dl_depth > N)` in gfx_process_n64_dl. 23267: `gfx_process_n64_dl((const uint8_t *)dl_addr);` (recursion). Contrast gfx_run_dl_pc 22029: `if (dl_depth > 32) { g_effect_pending_child_label = NULL; dl_depth--; return; }`.
  ```
- **Player impact:** A corrupt asset or a display-list pointer that mis-resolves into a self-referential region crashes the process via stack overflow rather than being skipped like every other malformed-DL case the interpreter defends against.
- **Suggested fix:** Add the same depth guard as gfx_run_dl_pc at the top of gfx_process_n64_dl (e.g. `if (dl_depth > 32) { ...restore state...; return; }`) before the recursive descent, reusing the ABORT_N64_DL cleanup macro.
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). Re-read gfx_process_n64_dl and gfx_run_dl_pc in src/platform/fast3d/gfx_pc.c. The asymmetry is real: gfx_run_dl_pc caps recursion at 22066-22071 (dl_depth++; if (dl_depth > 32) { dl_depth--; return; }), but gfx_process_n64_dl increments the shared dl_depth at 22743 with no depth check anywhere before recursing into itself at 23305 for a push-type G_DL. I confirmed the only N64-side bounds are per-invocation and non-depth: n64_cmd_count is reset to 0 at each entry (22701) and only trips at 22799 (>50000) within a single invocation, so it cannot limit recursion depth; the g_n64_dl_stack write is guarded at 22745 only to avoid an OOB array store, not to stop recursion (depth continues past the 128-entry array). A corrupt/mis-resolved DL forming a cycle (or deep nest) that passes gfx_addr_is_n64_data + gfx_n64_dl_start_is_plausible would recurse on the native C stack until SIGSEGV, whereas the function is otherwise heavily instrumented to skip malformed DLs gracefully. The finder's cited line numbers are each off by a few lines (22705 is 'int entry_depth = dl_depth;' not the increment; 23267 is a boot-logo fprintf not the recursion at 23305; 22029 is seg_addr not the PC cap at 22067), but every cited construct exists nearby and the described defect is accurate and verifiable.
  - **Correction:** Line numbers are imprecise: the uncapped increment is at gfx_pc.c:22743 (not 22705); the self-recursion is at 23305 (not 23267); the sibling PC-path cap is at 22066-22071 (not 22029-22033); the per-invocation 50000-command counter is at 22799 (not 22761) and is reset per call so it cannot bound recursion depth. Substance of the finding — N64 DL recursion lacks the depth cap the PC path has, so a cyclic/deep DL overflows the native stack instead of being skipped — is correct.

### 6. 🟠 ✅ Default-on minimap silently disappears on the Metal / --remaster path (backend feature divergence)

- **File:** `src/platform/fast3d/gfx_pc.c:24137` · `gfx_run_dl`
- **Severity:** medium · **Class:** parity-divergence · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The tactical minimap overlay (Input.MinimapEnabled default = 1) is drawn with direct OpenGL calls (minimap_overlay.c: glCreateShader/glBindFramebuffer/glDrawArrays) and is unconditionally skipped whenever the Metal backend is active. gfx_metal.mm contains no minimap implementation. --remaster (the flagship visual mode) forces GE007_RENDERER=metal (main_pc.c:687) but its preset does NOT disable Input.MinimapEnabled, so the setting still reads ON while nothing renders.
- **Evidence:**

  ```
  src/platform/fast3d/gfx_pc.c:24136-24139 `#ifdef __APPLE__\n    if (!gfx_backend_use_metal())\n#endif\n        minimap_overlay_draw_queued_frames();` — comment: "it would crash on the Metal path (no GL context). Skip it there." grep of gfx_metal.mm for minimap returns nothing.
  ```
- **Player impact:** A player who selects --remaster (or GE007_RENDERER=metal) gets no minimap even though the settings menu shows it enabled — the same install renders differently on the two backends, and the confusing part is the toggle appears to do nothing.
- **Suggested fix:** Port the minimap overlay to the gfx_rapi backend vtable (or add a Metal overlay draw), or at minimum have the Metal path clear/ignore Input.MinimapEnabled and surface a one-time warning so the UI does not misrepresent state.
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). Every cited fact checks out against the real code. gfx_pc.c:24136-24139 skips minimap_overlay_draw_queued_frames() whenever gfx_backend_use_metal() is true, with the exact comment quoted. minimap_overlay.c uses direct GL (glCreateShader:180, glDrawArrays:307, glGetIntegerv:1608+) and gfx_metal.mm has no minimap. Input.MinimapEnabled defaults to 1 (platform_sdl.c:1124/2006). main_pc.c:687 forces GE007_RENDERER=metal under --remaster, and the s_remasterPreset[] table (platform_sdl.c:2117-2141) does NOT pin Input.MinimapEnabled to 0 — unlike the --faithful (line 2088) and --faithful-hd (line 2184) presets which both explicitly set it to 0. So on the flagship --remaster mode (on Apple, where Metal is active) the setting reads ON and the settings menu shows the toggle enabled, yet the overlay never renders. Genuine setting/backend parity divergence with a confusing dead toggle. Severity medium is fair — real but low-stakes (a remaster-only HUD overlay silently missing, no crash/data loss).
  - **Correction:** The divergence is Apple-specific, not a general cross-platform split: the Metal backend exists only on __APPLE__, so the skip only triggers there. On Windows/Linux the GL path always draws the minimap. The finding's "same install renders differently on the two backends" framing is accurate only within a macOS install (GL default vs Metal/--remaster).

### 7. 🟡 ❌ Coverage-memory backdrop classification falls back to a material-only signature with no geometric ownership, risking misclassification and frame-to-frame flicker

- **File:** `src/platform/fast3d/gfx_pc.c:5243` · `gfx_room_xlu_cvg_memory_gate_reason`
- **Severity:** low · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** medium
- **What's wrong:** gfx_room_xlu_cvg_memory_gate_reason decides whether a room XLU draw gets RDP coverage-memory emulation. The primary 'ok' branch requires recoverable command-address/room-matrix ownership (dl_room>=0 && dl_which=="secondary" && room_matrix). When that ownership is not recoverable (the code's own comment at 5197-5201 admits backdrop strips are emitted through 'generated or locally transformed room paths where command-address and room-matrix ownership are no longer recoverable'), it falls to a purely MATERIAL signature: `dl_room < 0 && dl_which == NULL && !room_matrix && rdp.prim_color.a == 0` -> 'ok_generated_room'. This has no spatial/geometric key — any DRAWCLASS_ROOM XLU draw that happens to match (envA==255, primA==0, fog on, XLU coverage-wrap mode) is treated as coverage-memory backdrop. Because whether a given strip resolves via the geometric 'ok' path or drops to (or misses) the material fallback depends on fragile per-command address attribution that can change frame-to-frame, the same surface can switch between coverage-memory blend and plain alpha+depth-sort across frames, producing pop/flicker; unrelated water/glass matching the signature can be wrongly coverage-blended.
- **Evidence:**

  ```
  Lines 5243-5246: `if (dl_room < 0 && dl_which == NULL && !room_matrix && rdp.prim_color.a == 0) { return "ok_generated_room"; }` preceded by the comment (5197-5201) acknowledging command-address/room-matrix ownership 'no longer recoverable' for 'some backdrop strips'. gfx_room_xlu_cvg_memory_needed (5261-5266) returns true for both "ok" and "ok_generated_room".
  ```
- **Player impact:** Possible shimmer/flicker on fog/water backdrops when attribution flips, or occasional wrong tint on room translucent surfaces that coincidentally match the material signature. Narrow and content-dependent.
- **Suggested fix:** Tighten the material fallback with an additional stable discriminator (e.g. the settex texture number / tile dims already used by gfx_room_water_alpha_suppress at 5142-5146, or a per-command sticky classification cache) so the coverage-memory decision cannot toggle across frames for the same surface.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The quoted code is accurate: the fallback at 5248-5251 (`dl_room < 0 && dl_which == NULL && !room_matrix && rdp.prim_color.a == 0` -> "ok_generated_room") is material-only, and gfx_room_xlu_cvg_memory_needed (5266-5271) returns true for both "ok" and "ok_generated_room". However, the finding's central player-impact mechanism — frame-to-frame flicker when attribution "flips" between the geometric path and the material fallback — does not hold up. Both "ok" (5246) and "ok_generated_room" (5250) drive the SAME downstream decision (coverage-memory needed = true). So if a surface's per-command attribution wobbles between the geometric-ownership state and the generated-room state, the rendered result is IDENTICAL; no flicker. For flicker to occur, the same surface would have to oscillate between a needed=true branch and an outright miss ("owner"/"dl"/"roommtx"/"dl_room"), which requires attribution to degrade to a specifically wrong partial state (e.g. dl_room>=0 with dl_which != "secondary") — a hypothetical the finder asserts but never demonstrates with a repro or content instance. The remaining conditions gating both branches are extremely narrow (DRAWCLASS_ROOM + settex_active + use_fog + GFX_BLEND_ALPHA + depth_test + depth_compare + !depth_update + ZMODE_XLU + !texture_edge + !g_sky_tri_mode + envA==255 + xlu_wrap_color_on_coverage, plus primA==0), and glass/non-fog shells are explicitly excluded by design (comment 5199-5200; use_fog gate at 5214). The whole feature is an intentional RDP coverage-memory emulation heuristic with an env kill switch (GE007_DISABLE_ROOM_XLU_CVG_MEMORY, 5161-5177); a material+mode signature is inherent to that approach, and the comment (5200-5206) already documents that primitive alpha is unstable and that the material fallback is a deliberate, narrowed accommodation. The finder self-rates this "low / narrow / content-dependent" and offers no evidence of an actual visible artifact. This is a speculative fragility observation about an intentional heuristic, not a substantiated player-facing defect.
  - **Correction:** The code is as quoted, but the described flicker mechanism is undercut by the fact that both the geometric "ok" and the material "ok_generated_room" branches yield needed=true, so switching between them produces no visible change. Any real flicker would require oscillation to a distinct miss state, which is not demonstrated. This is an intentional, env-gated, narrowly-conditioned coverage-memory emulation heuristic, not a demonstrated port defect.

### 8. 🟡 ❌ Default-on room XLU depth sort reorders whole secondary-room translucent display lists by camera depth, which the N64 RDP does not do (draws in DL order)

- **File:** `src/platform/fast3d/gfx_pc.c:13704` · `gfx_room_xlu_sort_enabled`
- **Severity:** low · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** low
- **What's wrong:** gfx_room_xlu_sort_enabled() defaults to 1, and gfx_room_xlu_sort_flush_segment / gfx_room_xlu_deferred_draw_pending reorder secondary-room XLU cmd_addr groups back-to-front by average view-space depth (key = mean vertex w, gfx_room_xlu_tri_sort_key line 13741-13772; groups sorted at 14136-14148 and 13916-13928). The N64 draws translucent room geometry in display-list order with sequential RDP framebuffer-memory semantics; level geometry was authored around that order. Re-sorting whole display lists by camera depth is a port-introduced behavior (shared by both GL and Metal, so not a backend split) that can change the visible layering of intentionally-ordered translucent surfaces. It generally reduces painter artifacts but is not faithful, and is not tied to a remaster/quality gate — it is simply on by default with only GE007_DISABLE_ROOM_XLU_SORT to opt out.
- **Evidence:**

  ```
  Line 13710 `enabled = 1;` with opt-out only via GE007_DISABLE_ROOM_XLU_SORT / GE007_SORT_ROOM_XLU=0. Sort keying by depth: gfx_room_xlu_sort_group_precedes (13774-13786) `if (a->key > b->key + epsilon) return true;` (larger w / farther first). Reorder memcpy at 14150-14160.
  ```
- **Player impact:** On the affected secondary-room translucent surfaces, layering order can differ from the N64 original; usually an improvement but a deviation from faithful rendering that is not surfaced as a faithful-mode toggle.
- **Suggested fix:** Decide policy: if the faithful path must be byte/order-identical to N64, gate the room XLU sort under the remaster/quality flag (default off in faithful mode) rather than default-on; otherwise document it as an intentional quality improvement in VISUAL_MODES.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited code is accurately described: gfx_room_xlu_sort_enabled() (line 13709-13723) defaults enabled=1 and is only disabled via GE007_SORT_ROOM_XLU=0 / GE007_DISABLE_ROOM_XLU_SORT; gfx_room_xlu_tri_sort_key (13746-13777) keys on mean vertex w (view-space depth); gfx_room_xlu_sort_group_precedes (13779-13791) orders larger-w (farther) first; gfx_room_xlu_sort_flush_segment groups by cmd_addr and insertion-sorts back-to-front (14104-14153) then memcpys the reordered VBO back (14155-14165). So the finder correctly established the mechanism, the default-on state, and that it is shared by both backends (it operates on the VBO before either GL/Metal draw). That part is real. What it is NOT is a defect or player-facing problem. This is one piece of a large, deliberately-built room-translucency subsystem (draw-class tagging around 454/528, room-XLU coverage-memory emulation at 5161+, deferred-draw batching, and this depth sort) engineered specifically to combat translucency painter artifacts. The finder itself concedes it "generally reduces painter artifacts" and is "usually an improvement," and its suggested fix is a faithfulness-policy/documentation decision ("gate under quality flag or document in VISUAL_MODES"), not a bug fix. Reordering here is intended behavior with an explicit opt-out, not a misread bug nor a regression that harms players. It is a stylistic/policy nit about whether a beneficial default should be behind a faithful-mode toggle.
  - **Correction:** The mechanism and line references are accurate, but this is intended, deliberately-engineered, env-toggleable quality behavior (an anti-painter's-artifact depth sort) that the finder itself notes usually improves rendering — not a defect. It is at most a faithfulness-policy/documentation preference about whether a beneficial default should be surfaced as a faithful-mode toggle, which does not rise to a player-facing bug.

### 9. 🟡 ✅ Footprint-reject path evicts the previously-bound unrelated texture from the cache

- **File:** `src/platform/fast3d/gfx_pc.c:15261` · `import_texture`
- **Severity:** low · **Class:** port-defect · **Category:** performance · **Finder confidence:** medium
- **What's wrong:** When a texture change is processed, import_texture() runs the plausibility gate BEFORE the cache lookup. At that point rendering_state.textures[slot] still holds the node bound by the PREVIOUS successful import for this texture unit (a different, still-valid, still-cached texture). On an implausible footprint it calls gfx_texture_cache_discard_node(rendering_state.textures[slot]), which fully removes that unrelated node from the hashmap/LRU and deletes its GL texture. So a malformed tile for material B causes the still-good cached texture for the previously-drawn material A to be evicted and re-decoded on its next use. Correctness is preserved (re-decode reproduces it), so this is a spurious eviction / re-decode churn, not visible corruption. Contrast the upload-fail path at ~15400 where discarding rendering_state.textures[slot] is correct because the cache lookup already pointed it at the freshly-allocated (failed) node.
- **Evidence:**

  ```
  gfx_texture_cache_discard_node(rendering_state.textures[slot]);
          rendering_state.textures[slot] = NULL;
          gfx_bind_texture(slot, 0);
          return false;   // reached before gfx_texture_cache_lookup() runs, so textures[slot] is the PREVIOUS material's node
  ```
- **Player impact:** On the rare frames where a malformed/implausible tile descriptor is seen (the [TEX-REJECT] path), an unrelated in-use texture is dropped and re-decoded, a brief hitch/redundant upload. No visible artifact.
- **Suggested fix:** On the footprint-reject path, only unbind the slot (rendering_state.textures[slot]=NULL; gfx_bind_texture(slot,0)) without discarding the node, or explicitly null the pointer before the discard so the previously-cached texture survives. Discarding should be reserved for the post-lookup upload-fail path where the node genuinely belongs to this import.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code matches the finding. In import_texture (line 15255), the footprint-plausibility gate (15276) runs before gfx_texture_cache_lookup (15390). Lookup is the only thing in this function that repoints &rendering_state.textures[slot] at a node belonging to the current import; the successful path returns at 15402 after setting it to the cached node, and the failed-lookup path leaves it pointing at the freshly-allocated node before the 15432 discard. Therefore, at line 15293 rendering_state.textures[slot] still holds whatever the PREVIOUS import left there — either NULL (discard is a no-op, guarded at 2880) or a valid, still-cached node for a previously-drawn material. gfx_texture_cache_discard_node (2879) fully evicts that node: tex_lru_remove, tex_hash_remove, delete_texture, and returns it to the free list. So an implausible footprint for material B spuriously evicts and forces re-decode of the unrelated cached texture A. The upload-fail path at 15432 uses the same three lines correctly because the lookup has already repointed the slot at this import's own node. Correctness is preserved (re-decode reproduces the texture); this is redundant eviction/re-upload churn, not corruption. The mechanism, line, symbol, and quoted evidence are all accurate. Note gfx_invalidate_evicted_texture_node (4416 area) prevents dangling-pointer issues if the node had been LRU-reassigned, so the worst case is still just a spurious eviction, never a crash.
  - **Correction:** The defect is real as described. However the player impact is negligible: it only triggers on the rare [TEX-REJECT] implausible-footprint error path, produces no visible artifact, and costs at most one redundant re-decode/re-upload of an unrelated texture on its next use. It is a genuine logic error (wrong node discarded, a copy-paste of the upload-fail cleanup into a pre-lookup path) worth the suggested one-line fix, but it does not meaningfully affect gameplay or stability.

### 10. 🟡 ✅ settex texture-by-number cache uses FIFO eviction with an O(n) full-array memmove

- **File:** `src/platform/fast3d/gfx_pc.c:21921` · `gfx_handle_settex`
- **Severity:** low · **Class:** port-defect · **Category:** performance · **Finder confidence:** low
- **What's wrong:** The Rare G_SETTEX (0xC0) texture-by-number cache (settex_cache[2048], linear-searched) evicts strictly the oldest slot (index 0) when full, then memmove()s the entire remaining array down one slot (~2047 * sizeof(entry) bytes, entry ~64B => ~130KB copy) for every new load once full. This is FIFO, not LRU, so a hot texture that happens to sit near slot 0 is evicted before rarely-used newer entries, forcing a full texLoadFromTextureNum + RGBA32 decode + GL upload of a still-in-use material. The main LRU texture cache (TEXTURE_CACHE_MAX_SIZE 1024) is better behaved but similarly bounded. In a scene cycling through more than 2048 (settex) or 1024 (main) distinct textures, either can thrash into per-frame re-decode. GE levels are believed to stay under these bounds, so this is a latent robustness/perf risk rather than an observed defect.
- **Evidence:**

  ```
  } else {
              /* Cache full - evict oldest entry (slot 0), shift down. */
              gfx_flush();
              ... delete_texture(settex_cache[0].gl_tex_id); free(settex_cache[0].rgba);
              memmove(&settex_cache[0], &settex_cache[1],
                      (SETTEX_CACHE_SIZE - 1) * sizeof(settex_cache[0]));
              slot = SETTEX_CACHE_SIZE - 1;
          }
  ```
- **Player impact:** None under normal play (levels stay under capacity). Under pathological texture churn (very texture-heavy custom scenes), evicting hot entries FIFO-style plus repeated 130KB memmoves would cause frame-time spikes and redundant decode/upload.
- **Suggested fix:** Give settex_cache an intrusive LRU (or an approximate second-chance/clock policy) and evict the least-recently-used entry in place instead of memmoving the whole array; keep the slot index stable so cache hits do not depend on array position. Optionally raise/parameterize the capacities.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code at gfx_pc.c:21957-21972 matches the quoted evidence. settex_cache is a flat array of SETTEX_CACHE_SIZE=2048 entries (defs at lines 943/955), looked up by linear scan on texturenum (line 21514). When full, the code deletes slot 0, frees its rgba, memmoves the remaining ~2047 entries down one slot, and inserts at the last index — a genuine FIFO-by-insertion policy with an O(n) full-array shift per load once saturated. So the mechanism the finder describes is real code, not a misread or gated path. However, the eviction branch only runs after 2048 distinct texture-by-number materials are simultaneously resident; GE levels stay far below that, so it is never reached in practice (the finder concedes this). One detail is overstated: because hits are keyed on texturenum, cache-hit correctness does not depend on array position — the memmove is only a cost. Net: an accurate, honestly-scoped low-severity latent perf note, but with no real player/stability impact under actual play.
  - **Correction:** The suggested-fix rationale that "cache hits depend on array position" is inaccurate: lookups are by texturenum (line 21514), so the FIFO memmove affects only cost, not hit correctness. The eviction/memmove path is unreachable under real GE workloads (never approaches 2048 distinct settex textures), making this a latent perf nit rather than an observed defect.

### 11. 🟡 ❓ Eye-intro I8 strip bakes prim-color modulation into the cached texture without prim_color in the cache identity

- **File:** `src/platform/fast3d/gfx_pc.c:15129` · `import_texture_i8`
- **Severity:** low · **Class:** port-defect · **Category:** correctness · **Finder confidence:** low
- **What's wrong:** import_texture_i8 special-cases the boot-intro 'eye' strip (gfx_is_eye_intro_strip_material) and multiplies decoded intensity by rdp.prim_color.r ('intensity = intensity * prim.r / 255') so the fade is baked into the uploaded RGBA. But prim_color is NOT part of the texture cache identity (gfx_texture_cache_lookup keys on addr/source key, fmt, siz, sizes, line sizes, tile w/h, palette state only). If this strip is ever served from a persistent (non-invalidated) cache entry across frames with a changing prim.r, the fade would freeze at the first frame's baked brightness. In practice the eye strip appears to be a per-frame DRAM composition, so each LOADBLOCK invalidates the entry (gfx_dp_load_block -> gfx_texture_cache_delete_by_texture_key for non-static textures) and it is re-decoded with the current prim, masking the gap. It remains a latent architectural hazard: a decode-affecting per-draw uniform is embedded in a cacheable texture whose key omits it.
- **Evidence:**

  ```
  if (is_eye_intro_strip) {
                  intensity = (uint8_t)((intensity * rdp.prim_color.r) / 255U);
              }
              rgba32_buf[4*out + 0] = intensity; ...   // prim.r baked into cached texels; prim not in cache key
  ```
- **Player impact:** None observed under the normal intro (per-frame reload re-applies the current fade value). If the strip ever became a cached/static texture, the intro eye fade would visibly freeze at one brightness.
- **Suggested fix:** Apply the prim modulation in the color-combiner/shader (as PRIM * TEXEL, matching the N64 CC) rather than baking it into the decoded texture; or, if it must be baked, fold prim_color.r into the cache identity / bypass the cache for this material so a changing prim always re-decodes.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). The quoted code is accurate: import_texture_i8 (gfx_pc.c:15126-15130, 15161-15163) does gate on the eye-intro strip and bakes intensity*prim.r/255 into the decoded RGBA, and prim_color is genuinely absent from the texture cache identity. So the finder did not misread the code. However, the described defect is not reachable in practice. gfx_is_eye_intro_strip_material (line 20784) matches only a highly specific per-frame-composited I8 strip (combine_mode 0x007ffe3f..., fmt I / 8b / height 1 / line_size 440 / decode size 440), and gfx_dp_load_block (line 20832-20834) unconditionally deletes the cache entry via gfx_texture_cache_delete_by_texture_key for every non-static-game-texture LOADBLOCK. A per-frame DRAM-composed strip is by definition non-static, so it is invalidated and re-decoded with the current prim every frame — the exact masking the finder acknowledges. There is no demonstrated path by which this material becomes a static/persistent cache entry, so the freeze scenario is hypothetical. The finding is a truthful description of a latent architectural smell, not a confirmed defect or player-facing bug; the finder self-rated it low severity with no observed impact, which is correct.
  - **Correction:** All technical claims are accurate, but the hazard is purely latent: the eye-intro strip is only ever a non-static per-frame texture, which gfx_dp_load_block always cache-invalidates at LOADBLOCK, so it is re-decoded with the current prim.r each frame. No reachable code path makes it a static/persistent cache entry, so the fade cannot actually freeze. This is a maintainability/architecture nit, not a current defect.

### 12. 🟡 ✅ Base-GBI light load (G_MV_L0..L7) is unbounded; MAX_LIGHTS=2 makes light index >=3 an OOB struct write

- **File:** `src/platform/fast3d/gfx_pc.c:20065` · `gfx_sp_movemem`
- **Severity:** low · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** medium
- **What's wrong:** The port reduced MAX_LIGHTS to 2, so rsp.current_lights is Light_t[3] (line 3416) and rsp.current_lights_coeffs is float[2][3] (line 3417). The F3DEX2 light path was given an explicit bound check (line 20048: 'lightidx < sizeof(current_lights)/sizeof(current_lights[0])'), but the base-GBI path that GoldenEye actually uses was NOT: the G_MV_L0..L7 case does 'memcpy(rsp.current_lights + (index - G_MV_L0)/2, data, sizeof(Light_t))' with no bound. G_MV_L0=0x86..G_MV_L7=0x94 give slot indices 0..7, so loading light slot >=3 memcpys 16 bytes past the 48-byte array, clobbering current_lookat / current_lights_coeffs / current_num_lights. Symmetrically, G_MW_NUMLIGHT decodes NUML(n)=((n)+1)*32+0x80000000 to num_lights=n+1; for gSPNumLights(NUMLIGHTS_3) num_lights=4, so the lighting loop (line 16959) reads current_lights_coeffs[2] (OOB, size 2) and the ambient read (line 16955) reads current_lights[3] (OOB, size 3).
- **Evidence:**

  ```
  memcpy(rsp.current_lights + (index - G_MV_L0) / 2, data, sizeof(Light_t)); // no bound, array size MAX_LIGHTS+1 = 3
  ```
- **Player impact:** None in normal play: every GE DL uses gdSPDefLights1/gSPSetLights1/NUMLIGHTS_1 (one diffuse + ambient => slots 0..1). But any model/level DL that loads 3+ lights would silently corrupt adjacent RSP render state (wrong lighting/texgen, potential subsequent bad reads) with no clamp. It is a latent memory-safety gap the port opened by tightening MAX_LIGHTS without guarding the base-GBI path.
- **Suggested fix:** Bound the base-GBI case: compute idx=(index-G_MV_L0)/2 and skip if idx>=MAX_LIGHTS+1; clamp/reject current_num_lights to <=MAX_LIGHTS+1 in G_MW_NUMLIGHT; or raise MAX_LIGHTS to the F3D max (7/8) to match upstream and the F3DEX2 path.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). Every cited code fact matches the source. MAX_LIGHTS is 2 (gfx_pc.c:210), so current_lights is Light_t[3] (line 3416) and current_lights_coeffs is float[2][3] (line 3418). The F3DEX2 light-load path carries an explicit bound check (line 20048), but the base-GBI G_MV_L0..L7 case at line 20065 does `memcpy(rsp.current_lights + (index - G_MV_L0)/2, data, sizeof(Light_t))` with no bound. G_MV_L0=0x86 with stride 2 (include/PR/gbi.h:952/2049) yields slot indices 0..7, so loading light slot >=3 writes 16 bytes past the 48-byte array into current_lookat/current_lights_coeffs. Symmetrically G_MW_NUMLIGHT (line 20111) decodes num_lights unbounded and the lighting loop at 16947/16955/16959 indexes current_lights[num_lights-1] and current_lights_coeffs[li] with no clamp. The defect and the port-introduced asymmetry (F3DEX2 path guarded, base path not) are real. It is latent: the finder's own analysis and my read confirm GE's DLs load a single diffuse+ambient (slots 0..1), so no known input reaches slot >=3 in normal play. Severity low is accurate.

### 13. 🟡 ❓ gfx_sp_geometry_mode re-asserts G_FOG on world geometry, overriding an explicit gSPClearGeometryMode(G_FOG)

- **File:** `src/platform/fast3d/gfx_pc.c:19961` · `gfx_sp_geometry_mode`
- **Severity:** low · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** low
- **What's wrong:** After applying the DL's clear/set masks, the handler force-enables fog for any non-suppressed, non-texgen geometry-mode change whose 'clear' mask does not include G_TEXTURE_GEN: 'else if ((clear & G_TEXTURE_GEN) == 0) rsp.geometry_mode |= G_FOG;'. Because the condition keys on the clear mask (not on whether the DL wanted fog), a display list that explicitly does gSPClearGeometryMode(G_FOG) for a world-space draw has G_FOG immediately turned back on. This makes it impossible for non-room/non-frontend world geometry to actually disable fog. It is faithful/desirable for opaque world surfaces (GE fogs the world), but it also overrides the DL's intent for in-world self-lit/additive effects.
- **Evidence:**

  ```
  rsp.geometry_mode &= ~clear; rsp.geometry_mode |= set; ... } else if ((clear & G_TEXTURE_GEN) == 0) { rsp.geometry_mode |= G_FOG; }
  ```
- **Player impact:** Bounded: at typical near-camera combat distances the forced fog factor is ~0, so muzzle flashes/tracers/sparks/explosion billboards are largely unaffected. But an in-world effect the game deliberately renders fog-free at distance would be tinted toward the fog color, which the N64 would not do.
- **Suggested fix:** Gate the forced |= G_FOG on draw class / room-matrix membership only (i.e. force fog for room/model surfaces, but honor an explicit G_FOG clear for effect/prop draw classes) instead of on the clear-mask heuristic.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). The quoted code is accurate — src/platform/fast3d/gfx_pc.c:19960-19962 does force `rsp.geometry_mode |= G_FOG` whenever the clear mask lacks G_TEXTURE_GEN, which mechanically overrides an explicit gSPClearGeometryMode(G_FOG). So the finder read the code correctly. But this is clearly intended, documented behavior: a comment explains the rationale (force GE world fog on legacy room/model paths, spare texgen reflection passes), and it is guarded by an A/B env hatch (GE007_KEEP_TEXTURE_GEN_FOG). It is also fronted by gfx_current_draw_suppresses_room_fog() (gfx_pc.c:3433), which already clears G_FOG for FRONTEND, ROOM class, and any draw NOT under a room matrix — so ordinary in-world effect/prop draws (own matrix) are already exempt. The only case that reaches the forced re-fog is an effect drawn while a room matrix is on top that also explicitly cleared G_FOG, and the finder supplies no evidence GE ever does this. The finder's own player-impact note concedes the effect is bounded and near-camera fog is ~0. So the mechanism is real but it is intended/faithful behavior with only a speculative, undemonstrated player-visible consequence.
  - **Correction:** The code and mechanism are quoted correctly, but this is an intentional, env-gated heuristic (with a comment and GE007_KEEP_TEXTURE_GEN_FOG hatch) rather than a defect. In-world effects rendered on their own matrix are already fog-suppressed by gfx_current_draw_suppresses_room_fog(); only a room-matrix draw that explicitly clears G_FOG is overridden, and no concrete GE display list demonstrating that case is shown. Player impact is hypothetical and, by the finder's own admission, negligible.

### 14. 🟡 ✅ Matrix stack overflow (push at depth 11) is silently swallowed and then clobbers the current top

- **File:** `src/platform/fast3d/gfx_pc.c:16005` · `gfx_sp_matrix`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** medium
- **What's wrong:** The modelview stack arrays are sized [11] (rsp.modelview_matrix_stack[11]). A G_MTX_PUSH is only honored when 'rsp.modelview_matrix_stack_size < 11'. When the stack is already full, the push is skipped but execution falls through to the LOAD/MUL below, which writes into modelview_matrix_stack[stack_size-1] = the existing top slot. The pushed matrix is lost and the parent frame at the top is destroyed, so the matching G_POPMTX later restores a wrong parent. Conversely gfx_sp_pop_matrix can drive stack_size to 0, after which gfx_sp_vertex computes 'mtx_stack_pos = modelview_matrix_stack_size - 1' = 255 (uint8_t) and indexes modelview_* arrays OOB.
- **Evidence:**

  ```
  if ((parameters & G_MTX_PUSH) && rsp.modelview_matrix_stack_size < 11) { ++rsp.modelview_matrix_stack_size; ... } // full-stack push is dropped, LOAD/MUL below still overwrites the top
  ```
- **Player impact:** None in normal play — GE display lists keep the modelview stack shallow and balanced (base F3D depth <=10). It is a latent corruption/OOB path only reachable via a malformed or unusually deep DL, where it would mis-transform subsequent geometry rather than fail cleanly.
- **Suggested fix:** On overflow, skip the whole matrix op (early-return) rather than falling through to overwrite the top; guard gfx_update_mp_matrix / gfx_sp_vertex against stack_size==0.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). Both described mechanisms are present in the real code. gfx_pc.c:16005 gates the G_MTX_PUSH block on `rsp.modelview_matrix_stack_size < 11`; on a full stack the increment/copy is skipped and execution falls through to the G_MTX_LOAD memcpy (16025) or the MUL branch (16043), each writing into `modelview_matrix_stack[stack_size-1]` = the current top, so the push is dropped and the parent frame is clobbered — matching the quoted evidence. For underflow: gfx_sp_pop_matrix (16126) clamps size at 0 and guards its own gfx_update_mp_matrix call, but gfx_sp_vertex line 16642 computes `uint8_t mtx_stack_pos = rsp.modelview_matrix_stack_size - 1` with no guard, so size==0 yields 255 and lines 16643-16647 read the parallel arrays (all sized [11]) OOB; gfx_update_mp_matrix at 15579/15594 has the same unguarded `size-1`. Lines 3446 and 10954 guard identically-shaped reads with `> 0`, confirming these are real gaps. However, both paths require an unbalanced or >11-deep display list; the game's shipped DLs are fixed, balanced, and shallow, so neither triggers in normal play. It is a genuine latent robustness/OOB defect, not a player-facing one.
  - **Correction:** Minor: the finding implies gfx_update_mp_matrix is a live OOB entry on underflow, but within gfx_sp_pop_matrix that call is guarded by `if (...size > 0)`. The genuine unguarded OOB entry point on a 0-sized stack is gfx_sp_vertex line 16642 (and gfx_update_mp_matrix at 15579/15594 if reached with size==0 from elsewhere). The push-overflow clobber at 16005 is also faithful to upstream Fast3D, not port-introduced; only the added parallel tracking arrays and unguarded vertex-path reads are port-specific.

### 15. 🟡 ✅ Texgen texture coordinate can overflow the 16-bit `short U/V` for texture scale > 0x7FFF

- **File:** `src/platform/fast3d/gfx_pc.c:17004` · `gfx_sp_vertex`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** low
- **What's wrong:** In the G_TEXTURE_GEN path, U and V are the loop-scoped `short` variables declared at the top of gfx_sp_vertex ('short U = v->tc[0] * rsp.texture_scaling_factor.s >> 16;'). Texgen computes, for the common (non-linear) branch, 'U = (int32_t)((dotx + 1.0f) * (s_scale * 0.5f))' with dotx in [-1,1] and s_scale = texture_scaling_factor.s (uint16, up to 0xFFFF), yielding a value up to s_scale (up to 65535), then 'U = (int32_t)s_scale - U'. Storing a value up to 65535 into a signed 16-bit `short` wraps to negative garbage for any texgen surface whose gSPTexture S/T scale exceeds 0x7FFF (0xFFFF is the standard 1:1 scale). Note this maps to [0, s_scale], i.e. 2x the reference n64-fast3d formula ('(dot/127+1)/4 * scale' => [0, s_scale/2]) which is exactly sized to fit int16.
- **Evidence:**

  ```
  U = (int32_t)((dotx + 1.0f) * (s_scale * 0.5f)); ... U = (int32_t)s_scale - U;   // stored back into `short U`
  ```
- **Player impact:** Not triggered by the one confirmed texgen user (title.c gunbarrel/logo uses gSPTexture scale 0x0800 => U<=2048, safe). But any environment-mapped/chrome surface authored with a 0x8000-0xFFFF texgen scale would show wrapped/garbage reflection UVs. It also silently diverges 2x from the reference texgen scale.
- **Suggested fix:** Compute U/V texgen in int32_t and either match the reference (scale*0.25) or clamp to int16 range before storing; widen the U/V staging to int32_t to remove the overflow class entirely.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code matches the finding. `U`/`V` are declared as plain (signed 16-bit) `short` at gfx_pc.c:16901-16902. In the G_TEXTURE_GEN non-linear branch (16999-17000), `U = (int32_t)((dotx + 1.0f) * (s_scale * 0.5f))` with dotx in [-1,1] yields a range [0, s_scale], and `s_scale` derives from `texture_scaling_factor.s`, a `uint16_t` (struct at 3427) reaching 0xFFFF. Line 17004 then does `U = (int32_t)s_scale - U`, still ranging [0, s_scale]. Both results are assigned back into the `short U`, so any texgen surface with S/T scale > 0x7FFF truncates/wraps to a negative value. The value is only widened to float at `d->u = U` (17076; LoadedVertex.u is float at 2435), which is AFTER the short truncation, so nothing masks it. The overflow class is genuine. It is latent: the finder honestly notes the one confirmed texgen user (title logo, scale 0x0800) stays well under the limit, and there is no crash — worst case is wrapped/garbage reflection UVs on an environment-mapped surface authored with a 0x8000-0xFFFF scale (0xFFFF being the common 1:1 default). The 'diverges 2x from reference' remark is a real numeric difference but the in-code comments show the full-scale multiply is a deliberate choice to match the triangle path's 5.5 coordinate domain, so that part is intended, not the defect; the defect is strictly the short storage.
  - **Correction:** The core mechanism is accurate. One clarification: the 2x-vs-reference scaling is a deliberate domain match (per the code comments targeting the triangle path's 5.5 texel units), not a bug in itself; the genuine defect is only the truncation of a value up to s_scale (max 0xFFFF) into the signed 16-bit `short U`/`short V`. No shipping content triggers it (confirmed texgen user uses scale 0x0800), so it is a latent robustness issue with no crash — cosmetic wrapped UVs at worst.
  - **RETRACTION (2026-07-20):** ⛔ The Correction above is **wrong**, and its error is the interesting part of this entry. It dismissed the 2x divergence by deferring to the in-code comment ("deliberate domain match") instead of testing pixels. The 2x **was** the defect, and it was player-visible the whole time: it is the washed-out grey top half of the boot "GoldenEye" logo. Retail renders that logo gold throughout.
    - **Mechanism:** the reference coordinate spans **half** of `texture_scaling_factor`, not all of it — proven from the F3DEX2 microcode (`Mr-Wiseguy/f3dex2` f3dex2.s): `ST = 0x4000 * (1 + dot)` then `vmudm` (a `>>16`), giving `coord = (1 + dot) * scale / 4`. GE authors env materials with `scale == tex_width * 64` while one texture span is `tex_width * 32`, so the correct mapping covers the map exactly once and a camera-facing normal samples its **centre**. At 2x, a camera-facing normal lands on the **border** — and these tiles are `G_TX_CLAMP` (logos) or `G_TX_WRAP` (in-world), so that is a pin/wrap, not a harmless overrun.
    - **Measured:** fraction of texgen coordinates landing outside the env map — GoldenEye logo S 42%/T 39%, Nintendo logo S 49%/T 48% — all going to 0% after the fix.
    - **Console (ares) agreement after the fix:** GoldenEye logo mean saturation 0.82 vs console 0.82 (was 0.06); Rare logo hue 38.9° vs 36.8° (was 141°); Nintendo logo median luma 83 vs 86 (was 114, with 10% of pixels blown to pure white that console never reaches).
    - **Two further divergences** were found in the same block and are also fixed: the `U = s_scale - U` S mirror (no counterpart in the microcode or in any of nine reference implementations; invisible on the GoldenEye logo only because that env map is column-constant), and the `[-1,1]` dot clamp sitting inside the `G_TEXTURE_GEN_LINEAR` branch — i.e. on the one path GE never takes — leaving the live path unclamped.
    - **The overflow limb this entry was reduced to is now closed by construction:** with `|dot| <= 1` and the `/4` divisor, U and V are bounded by `scale/2 <= 32767` and always fit the `short` staging.
    - **Root cause of the miss:** there was no test, baseline, or golden image covering texgen anywhere in the repo. Now pinned by `tests/test_texgen_coords.c` (ctest `texgen_coords`) against `src/platform/fast3d/gfx_texgen.h`. A/B revert: `GE007_TEXGEN_LEGACY=1` (verified byte-identical to pre-fix output).

### 16. 🟡 ❌ depth_test gated on Z_CMP disables depth writes for Z_UPD-only (depth-priming) surfaces, diverging from N64/PD

- **File:** `src/platform/fast3d/gfx_pc.c:17535` · `gfx_emit_loaded_triangle`
- **Severity:** low · **Class:** port-defect · **Category:** correctness · **Finder confidence:** medium
- **What's wrong:** mgb64 requires Z_CMP for depth_test to be enabled, whereas the N64/PD reference enables depth whenever G_ZBUFFER (or G_ZS_PRIM) is set, independent of Z_CMP. Because both backends gate the depth write on (depth_test && depth_update) (GL disables depth entirely when depth_test is false; Metal sets depthWriteEnabled=(test&&update)), a surface with G_ZBUFFER + Z_UPD set but Z_CMP clear will NOT write depth in this port. On real N64 / the PD reference such a surface writes depth unconditionally (glDepthFunc(GL_ALWAYS), depth mask on) — i.e. depth priming. The change is deliberate (documented comment) but is a genuine behavioral divergence from faithful rendering.
- **Evidence:**

  ```
  gfx_pc.c:17535-17538 `bool depth_test = ((rsp.geometry_mode & G_ZBUFFER) != 0 || sky_backdrop_depth) && (rdp.other_mode_l & Z_CMP) != 0; bool depth_update = !sky_backdrop_depth && (rdp.other_mode_l & Z_UPD) == Z_UPD;` — PD reference gfx_pc.cpp:1240 `bool depth_test = ((rsp.geometry_mode & G_ZBUFFER) == G_ZBUFFER || (rdp.other_mode_l & G_ZS_PRIM) == G_ZS_PRIM) && (...cycle...);` (no Z_CMP term).
  ```
- **Player impact:** Any content whose baked DL primes the depth buffer with a Z_UPD-only surface (G_ZBUFFER on, Z_CMP off) will fail to occlude later geometry. GoldenEye's C-built display lists do not appear to use this combination (AA_ZB_* modes all include Z_CMP), so real-world impact is small, but a level's ROM-baked geometry using it would render incorrectly.
- **Suggested fix:** Only require Z_CMP for the compare, not for the write/enable: enable depth when G_ZBUFFER is set (matching PD), pass depth_compare=Z_CMP separately, and let depth_update=Z_UPD drive writes even when compare is off (depthFunc ALWAYS). Keep the current behavior behind a GE007_ flag if the Z_UPD-occlusion regression it fixed is real.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The finder accurately describes the code mechanism: gfx_pc.c:17567-17568 gates depth_test on both G_ZBUFFER and Z_CMP, and both backends (gfx_opengl.c:1809-1811 disable depth entirely when !depth_test; gfx_metal.mm:1138 depthWriteEnabled=(test&&update)) suppress depth writes for a Z_UPD-only surface — a genuine divergence from PD, which would enable depth on G_ZBUFFER alone and prime via GL_ALWAYS. However this is not a defect: (1) it is deliberate and explicitly documented at gfx_pc.c:17562-17564; (2) the write-only priming path (depth_compare false, depth_test true) is implemented in both backends but is effectively unreachable because depth_test itself requires Z_CMP, i.e. the coupling is by design; (3) the finder concedes no GoldenEye content uses G_ZBUFFER+Z_UPD without Z_CMP (all AA_ZB_* modes include Z_CMP), so the divergent path is never exercised by real content. Player impact is purely hypothetical ("a level's ROM-baked geometry would..."). Also the cited line 17535 is wrong — it lands in an unrelated G_CULL_BOTH logging block; the real code is at 17567-17571. This is an intended, documented faithfulness trade-off with no demonstrated player impact, not a real defect.
  - **Correction:** The actual depth-gating code is at gfx_pc.c:17567-17571, not line 17535 (which is a G_CULL_BOTH diagnostic block). The divergence from PD is real but deliberate/documented, and the write-only depth-prime path is intentionally unreachable because depth_test requires Z_CMP; no GoldenEye content uses the G_ZBUFFER+Z_UPD-without-Z_CMP combination, so there is no demonstrated player-facing impact.

### 17. 🟡 ✅ G_SETBLENDCOLOR (0xF9) is silently dropped in both DL interpreters; no blend-color state and no alpha-threshold compare

- **File:** `src/platform/fast3d/gfx_pc.c:23630` · `gfx_process_n64_dl / gfx_run_dl_pc`
- **Severity:** low · **Class:** port-defect · **Category:** correctness · **Finder confidence:** medium
- **What's wrong:** Both the PC-native interpreter (gfx_run_dl_pc, case 0xF9 at ~22644) and the N64 interpreter (gfx_process_n64_dl, line 23630) treat G_SETBLENDCOLOR as a NOOP, and there is no rdp.blend_color field anywhere. Consequently (a) the RDP blender's fixed-color input (G_BL_CLR_BL) is unavailable, and (b) alpha-compare in G_AC_THRESHOLD mode — which the RDP evaluates as fragment.a >= blend_color.a — cannot be emulated. The port only handles G_AC_DITHER (`use_noise = (rdp.other_mode_l & G_AC_DITHER) == G_AC_DITHER`) and coverage/CVG_X_ALPHA cutouts; the threshold alpha-compare path is absent.
- **Evidence:**

  ```
  gfx_pc.c:23630 `case 0xF9: /* SETBLENDCOLOR */` grouped with sync NOOPs; gfx_pc.c ~22644 `/* G_SETBLENDCOLOR */ case 0xF9: break;`. No `blend_color` member exists in the rdp state. Alpha compare only decodes dither: gfx_pc.c:17677 `bool use_noise = (rdp.other_mode_l & G_AC_DITHER) == G_AC_DITHER;`.
  ```
- **Player impact:** Surfaces relying on threshold alpha-compare against the blend color, or blends that source the fixed blend color, render without the intended cutout/blend. GoldenEye mostly issues gDPSetAlphaCompare(G_AC_NONE) and sets blend color only in isolated spots (boss.c/debugmenu, always full white), so player impact is minimal, but the feature is a latent faithfulness gap for any baked asset that uses it.
- **Suggested fix:** Add rdp.blend_color, decode G_SETBLENDCOLOR in both interpreters, and implement G_AC_THRESHOLD by discarding fragments with alpha < blend_color.a in the generated shader (a new SHADER_OPT plumbed from the alpha-compare field).
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The technical claims all check out against the real code. (1) G_SETBLENDCOLOR (0xF9) is a bare NOOP in both interpreters: gfx_pc.c:22644-22645 (PC-native) is an empty `break;`, and gfx_pc.c:23668 (N64 interpreter) folds 0xF9 into the sync/NOOP case group. (2) `grep` for `blend_color` across the file returns zero hits, confirming no rdp.blend_color state exists. (3) The alpha-compare decode only handles dither (`use_noise = (rdp.other_mode_l & G_AC_DITHER) == G_AC_DITHER;` at 17709) and the coverage cutout (`texture_edge = (rdp.other_mode_l & CVG_X_ALPHA) == CVG_X_ALPHA;` at 17708); there is no G_AC_THRESHOLD path comparing fragment alpha against a blend color. So the described defect (dropped SETBLENDCOLOR, missing fixed-color blender input, unimplemented threshold alpha-compare) is real as described. Line-number nits: the cited N64-interpreter line 23630 is actually 23668, and the cited alpha-compare line 17677 is actually 17709 — the described code exists a few lines off from the citations. Player impact is genuinely marginal: the finder itself concedes GoldenEye overwhelmingly issues gDPSetAlphaCompare(G_AC_NONE) and sets blend color only in isolated spots (always full white), so no shipped GE surface visibly depends on this path. It is an accurate but latent faithfulness gap, not a defect that materially degrades gameplay or stability.
  - **Correction:** The code is accurately described but two cited line numbers are off: the N64-interpreter 0xF9 NOOP is at gfx_pc.c:23668 (not 23630), and the dither-only alpha-compare decode is at gfx_pc.c:17709 (not 17677). Impact is a latent faithfulness gap with no observed effect on actual GoldenEye assets, so it does not rise to a genuine player-facing issue.

### 18. 🟡 ✅ Primitive depth source (G_ZS_PRIM / G_SETPRIMDEPTH) unimplemented; depth_source_prim computed but ignored by both backends

- **File:** `src/platform/fast3d/gfx_pc.c:22648` · `gfx_run_dl_pc / gfx_process_n64_dl (0xEE) + gfx_emit_loaded_triangle`
- **Severity:** low · **Class:** port-defect · **Category:** correctness · **Finder confidence:** medium
- **What's wrong:** G_SETPRIMDEPTH (0xEE) is a NOOP in both interpreters, and although gfx_emit_loaded_triangle computes `depth_source_prim = (rdp.other_mode_l & G_ZS_PRIM) == G_ZS_PRIM` and passes it into set_depth_mode, neither backend uses that parameter (gfx_opengl_set_depth_mode / mtl_depth_state_for key only on test/update/compare/zmode). A surface that sets gDPSetDepthSource(G_ZS_PRIM) therefore renders with interpolated per-vertex depth instead of the constant primitive depth the game requested.
- **Evidence:**

  ```
  gfx_pc.c ~22648 `/* G_SETPRIMDEPTH */ case 0xEE: break;` and gfx_pc.c:23631 `case 0xEE: /* SETPRIMDEPTH */` (NOOP). gfx_pc.c:17540 `bool depth_source_prim = (rdp.other_mode_l & G_ZS_PRIM) == G_ZS_PRIM;` — the value is threaded through set_depth_mode but the GL body (gfx_opengl.c:1760-1811) never references depth_source_prim.
  ```
- **Player impact:** Effects that force a constant primitive depth (in GoldenEye this is used in boss.c:634-635 for a boss-specific effect) will depth-sort by interpolated geometry depth instead, potentially z-fighting or being occluded incorrectly. Very narrow usage in GE, so low impact.
- **Suggested fix:** Store prim depth from G_SETPRIMDEPTH and, when depth_source_prim is set, emit that constant depth for all three vertices (or set a shader/pipeline constant-depth path) so G_ZS_PRIM surfaces occlude as the game intends.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The code substantiates the described gap. gfx_pc.c:22648 (`case 0xEE: break;`) and the N64-DL interpreter case at ~23631 both make G_SETPRIMDEPTH a NOOP. gfx_pc.c:17572 computes `depth_source_prim = (rdp.other_mode_l & G_ZS_PRIM) == G_ZS_PRIM` and passes it into set_depth_mode (17583), but gfx_opengl_set_depth_mode (gfx_opengl.c:1760-1811) never references the parameter and mtl_set_depth_mode (gfx_metal.mm:2838) explicitly discards it with `(void)depth_source_prim;`. So primitive-depth is truly unimplemented and the plumbed flag is ignored — the mechanism is real. However, the finder's player-impact rationale is inaccurate: the sole occurrence of G_ZS_PRIM/gDPSetPrimDepth in the whole source tree is src/boss.c:647-648, and it is NOT a boss gameplay effect — it is inside `if (get_debug_VisCVG_flag())`, a debug coverage/anti-aliasing (VisCVG, G_RM_VISCVG) visualizer gated behind the debug-menu flag debug_VisCVG_flag, which is off during normal play. No gameplay code path ever sets G_ZS_PRIM. The defect is genuine at the code level but its only trigger is a debug visualization, so it does not affect shipping players.
  - **Correction:** The unimplemented-primdepth gap is real (0xEE NOOP in both interpreters; depth_source_prim threaded but ignored by GL and Metal backends). But the claimed player impact is wrong: the only G_ZS_PRIM user in the codebase is src/boss.c:647-648, which is a debug coverage-visualization mode (VisCVG / G_RM_VISCVG) gated behind get_debug_VisCVG_flag() (debug_VisCVG_flag in debugmenu_handler.c), not a boss-specific gameplay effect. That debug flag is off in normal play, so no player-facing rendering is affected. This is an inert/informational port gap, not a player or robustness issue.

### 19. 🟡 ❌ settex_rgba_pixels becomes a dangling pointer when its cache slot is evicted (use-after-free)

- **File:** `src/platform/fast3d/gfx_pc.c:21968` · `gfx_settex cache eviction / gfx_settex_fetch_rgba*`
- **Severity:** low · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** medium
- **What's wrong:** On a G_SETTEX cache hit the global sampler pointer is aliased onto a cache slot's heap buffer: `settex_rgba_pixels = settex_cache[i].rgba;` (21522). When the 2048-entry cache later fills, eviction frees slot 0 and shifts: `free(settex_cache[0].rgba); memmove(&settex_cache[0], &settex_cache[1], ...);` (21968-21970) without checking whether settex_rgba_pixels currently points at that freed buffer. Subsequent CPU texel fetches dereference it: `const uint8_t *px = settex_rgba_pixels + ((iy * (int)settex_rgba_w + ix) * 4);` (8506/8537, also 8823/8918) -> use-after-free. This is only reachable when sample_rgba is retained, which requires a diagnostic flag (gfx_trace_settex_material_cc_samples/gfx_diag_dump_settex_texture/gfx_trace_settex_pixel_texture) AND 2048+ distinct settex textures to force eviction, so default play never populates rgba (it is freed at 21951 and stored as NULL).
- **Evidence:**

  ```
  21522: `settex_rgba_pixels = settex_cache[i].rgba;`. 21968: `free(settex_cache[0].rgba);` on eviction, no invalidation of settex_rgba_pixels. 8506: `const uint8_t *px = settex_rgba_pixels + ((iy * (int)settex_rgba_w + ix) * 4);`.
  ```
- **Player impact:** None in default builds (rgba retained only under diagnostic env vars); under those flags with a very large texture working set it can crash or sample freed memory.
- **Suggested fix:** On eviction, if `settex_rgba_pixels == settex_cache[0].rgba` reset settex_rgba_pixels/_w/_h to NULL/0 before freeing, or re-point it after the memmove.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The quoted evidence matches the real code exactly (21522 alias, 21968 free + memmove, 8506/8537 deref, fetch gated only by a NULL check at 8823/8918). But the described use-after-free is not reachable. The eviction path in gfx_handle_settex (a) calls gfx_flush() at 21963 BEFORE free(settex_cache[0].rgba) at 21968, draining any pending draws that CPU-sample the currently-bound texture while its buffer is still live, and (b) unconditionally reassigns settex_rgba_pixels = sample_rgba at 21996 with no early return between the free and the reassignment. settex_rgba_pixels is only mutated, and cache buffers only freed, inside gfx_handle_settex (plus the two teardown functions at 24909/24935, which also NULL the pointer). Texel fetches occur only during rendering, strictly between handler calls. Therefore, at every point a fetch runs, settex_rgba_pixels points to a live buffer or NULL (which the fetch guards reject). No fetch ever dereferences the freed slot-0 buffer. The finder correctly spotted the aliasing and the absence of an explicit invalidation check but missed the flush-before-free ordering and the unconditional repoint that together prevent the dangling read.
  - **Correction:** The eviction does not leave settex_rgba_pixels dangling: gfx_flush() at line 21963 drains draws before the free at 21968, and settex_rgba_pixels is unconditionally reassigned at line 21996 before the handler returns, so no subsequent fetch can dereference the freed buffer. The suggested fix (invalidate the pointer on eviction) is therefore unnecessary.


## Platform / input (platform_sdl.c, stubs.c)

### 20. 🔴 ✅ Video.FrameCap=display runs the whole sim at the display refresh rate (game too fast on 75/90/120 Hz panels)

- **File:** `src/platform/platform_sdl.c:1547` · `platformFrameCapPeriodMs`
- **Severity:** high · **Class:** port-defect · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** With Video.FrameCap=display and VSync on/adaptive (both user-reachable via the in-game settings menu, pc_settings_menu.c:50), platformFrameCapPeriodMs returns 0.0, which makes platformFrameSync skip the entire deadline pacer (`if (period_ms > 0.0)` is false at platform_sdl.c:2890). The only remaining limiter is vsync + the game's own sim gate waitForNextFrame(). But that gate does NOT clamp to 60: it computes nextFrameTime = floor((elapsed_counts + 387937) / 775875) (unk_0C0A70.c:204) and busy-waits only while nextFrameTime < frameDelay(=1). For any present period between ~8.28 ms and ~16.55 ms (i.e. display rates 61-120.8 Hz) this rounds to exactly 1 EVERY frame, so speedgraphframes=1, g_ClockTimer=1 (lvl.c:2113), and the world advances one full 1/60 s tick per PRESENT. On a 90 Hz Steam Deck OLED that is 90 ticks/s = 1.5x game speed; a 75 Hz monitor = 1.25x; a 120 Hz monitor = 2.0x. Movement, gravity, AI cooldowns, animation, objective/cutscene timers and g_GlobalTimer-driven audio all speed up together. FRAME_TIMING_ARCHITECTURE.md sec.3 claims 'gate #1 still holds the sim at 60' — that is only true above ~121 Hz; for the common 61-120 Hz range the wall-clock gate rounds one present to one tick and nothing caps the sim.
- **Evidence:**

  ```
  platform_sdl.c:1547-1550: `case PLATFORM_FRAME_CAP_DISPLAY: if (!g_forceNoVsync && g_vsyncMode != PLATFORM_VSYNC_OFF) { return 0.0; }` ; platform_sdl.c:2890 `if (period_ms > 0.0) {` (pacer skipped); unk_0C0A70.c:204 `nextFrameTime = ((currentCount - copy_of_osgetcount_value_1) + 387937) / 775875;` with :212 `} while (nextFrameTime < frameDelay);` and :214 `frameDelay = 1;`
  ```
- **Player impact:** Selecting the documented 'display' frame-cap on any 61-120 Hz display (75/90/100/120 Hz monitors, 90 Hz Steam Deck OLED) makes the entire game run 1.25x-2.0x too fast — unplayable speed-up of movement, enemies, timers and audio, not the intended higher frame rate.
- **Suggested fix:** Do not disable the pacer under FrameCap=display: always pace to at least the 16.667 ms (1/60 s) floor so the sim can never advance faster than 60 Hz, OR make waitForNextFrame authoritative by busy/sleep-waiting to a true 1/60 s regardless of present rate. Simplest: in platformFrameCapPeriodMs, for DISPLAY mode return max(displayPeriodMs, 1000/60) instead of 0.0 so present may be capped by vsync but the sim floor is preserved. Also correct FRAME_TIMING_ARCHITECTURE.md sec.3.
- **Verification:** CONFIRMED (adj. severity high, real-player-issue=True). All cited code substantiates the defect. platform_sdl.c:1547-1551 returns 0.0 for FrameCap=display when vsync != OFF (default vsync is ADAPTIVE, so one setting change triggers it). platform_sdl.c:2890 gates the entire deadline pacer behind period_ms > 0.0, so at 0.0 the sim is paced only by the vsync present. stubs.c:250-261 confirms osGetCount() is real CLOCK_MONOTONIC wall time in normal play. unk_0C0A70.c:199-218 confirms nextFrameTime = (Δcount + 387937)/775875 with a busy-wait only while < 1; the +387937 is a half-tick offset so the loop exits at ~8.28 ms and rounds Δ to the nearest whole tick — it does NOT wait a full 16.55 ms. lvl.c:2113-2128 sets g_ClockTimer = speedgraphframes with only high-spike clamps (1 on first frame, else 4), nothing clamping steady-state to 60. Consequently every vsync present in the ~40-121 Hz range yields nextFrameTime=1, advancing one 1/60 s tick per present, so the whole sim (movement, gravity, AI cooldowns, animation, g_GlobalTimer-driven timers/audio) runs at the display refresh rate: 90 Hz = 1.5x, 120 Hz ≈ 2.0x, 75 Hz = 1.25x. The project doc (FRAME_TIMING_ARCHITECTURE.md §3) actively and incorrectly claims gate #1 busy-waits ~16.55 ms and holds the loop to 60, which the half-tick rounding disproves. This is a genuine, game-breaking speedup on high-refresh displays, gated only by an opt-in but documented menu setting.
  - **Correction:** Mechanism confirmed. One imprecision: the finding says the doc's "gate holds sim at 60" is "only true above ~121 Hz." In reality, above ~121 Hz the ~8.28 ms half-tick busy-wait floor caps the sim at ~120.8 ticks/s (~2.01x), not 60 — so the doc's claim is never true above 60 Hz; the sim tracks display rate up to ~121 Hz then plateaus at ~2x. Also note the bug is opt-in: default Video.FrameCap=60 keeps the pacer active and the sim at 60; the defect requires the user to select FrameCap=display (with vsync at its default on/adaptive) on a >60 Hz panel.

### 21. 🟠 ✅ Gamepad movement stick uses a large square deadzone with no rescale, killing slow-walk

- **File:** `src/platform/stubs.c:6326` · `osContGetReadData`
- **Severity:** medium · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The left (movement) stick is deadzoned with a fixed per-axis square threshold GAMEPAD_DEADZONE=8000 (0.244 of range) and then mapped LINEARLY with NO deadzone-edge rescale: `mapped = (lx*80)/32767`. The first value the game can ever see is lx=8001 -> mapped=19, so the analog range 1..18 (out of 80) is unreachable and appears as an instant step. The N64 stick GoldenEye was authored for had a near-zero deadzone, so this is a regression AND it is inconsistent with the LOOK stick, which the port gave a modern radial deadzone WITH rescale-from-edge (lvl.c ~5889: `rescaled=(mag-dz)/(1-dz)`). The per-pad path (pcFillPadFromController, lines 5889-5900) is identically deficient. There is no config knob to change it (unlike the look stick's Input.GamepadDeadzone / Input.GamepadRadialDeadzone).
- **Evidence:**

  ```
  src/platform/stubs.c:6326 `if (lx > GAMEPAD_DEADZONE || lx < -GAMEPAD_DEADZONE) { int mapped = (lx * 80) / 32767; ...}` with `#define GAMEPAD_DEADZONE 8000` (stubs.c:634). Contrast lvl.c:5889 look-stick rescale `f32 rescaled = (mag - dz) / (1.0f - dz);`.
  ```
- **Player impact:** Gamepad players cannot walk/creep slowly (minimum walk speed jumps to ~24%), and diagonal input has an inconsistent (corner) deadzone. In a stealth-heavy game precise slow movement matters. Feels stiff and dated compared to the polished look stick.
- **Suggested fix:** Apply the same radial deadzone + rescale-from-edge used for the look stick to the movement stick (compute magnitude, subtract deadzone, rescale (mag-dz)/(1-dz), scale to +-80), and expose an Input.MovementDeadzone knob. Keep the exact 8000 square path selectable for the --faithful preset so sim-hash/determinism is unaffected.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited code matches the finding exactly. stubs.c:634 defines `#define GAMEPAD_DEADZONE 8000`, and stubs.c:6326-6337 (osContGetReadData) gates the left/movement stick per-axis with `if (lx > GAMEPAD_DEADZONE || lx < -GAMEPAD_DEADZONE)` then maps linearly `int mapped = (lx * 80) / 32767;` with NO deadzone-edge rescale. The math holds: the smallest non-zero input lx=8001 yields mapped=(8001*80)/32767=19 (truncated), so N64 stick outputs 1..18 (of 80) are unreachable and movement snaps to ~24% of walk speed. The per-pad path pcFillPadFromController (stubs.c:5889-5900) is byte-for-byte identical and equally deficient. The contrast is real: the look stick (lvl.c:5882-5894) does implement rescale-from-edge `f32 rescaled = (mag - dz) / (1.0f - dz);` and has config knobs (Input.GamepadDeadzone / GamepadRadialDeadzone, platform_sdl.c:1893), whereas the movement stick has a hardcoded square threshold with no exposed knob (I confirmed g_pcGamepadDeadzone is consumed only by the lvl.c look path). This is a genuine gamepad-feel regression: no continuous slow-walk / creep, plus a corner (per-axis square) deadzone shape on diagonals.
  - **Correction:** Two nuances that don't change the core defect: (1) 8000/32767 = 0.244 is essentially the Xbox-recommended left-stick inner deadzone, so the deadzone SIZE is standard; the real defect is the missing rescale-from-edge causing a hard step to ~24%, not the deadzone magnitude itself. (2) The look-stick radial deadzone + rescale is OPT-IN (guarded by g_pcGamepadRadialDeadzone, default path is also linear), so the "inconsistency" is about knob/capability availability rather than default runtime behavior. Severity adjusted medium->low: this is a real but modest player-feel/polish issue (loss of precise slow movement, corner deadzone on diagonals), not a correctness or stability bug. The suggested fix (mirror the look-stick radial deadzone+rescale, add an Input.MovementDeadzone knob, keep the 8000 square path for --faithful/sim-hash determinism) is sound.

### 22. 🟠 ❓ No raw-input / acceleration hint for relative-mode mouse aim

- **File:** `src/platform/platform_sdl.c:2649` · `platformPollEvents`
- **Severity:** medium · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** low
- **What's wrong:** Mouse-look enters SDL relative mode via SDL_SetRelativeMouseMode(SDL_TRUE) and consumes event.motion.xrel/yrel directly (platform_sdl.c:2679-2680). No SDL hints are set anywhere in the tree (grep for SDL_SetHint in src/platform returns nothing), so whether the deltas are raw/unaccelerated is left entirely to SDL's per-platform defaults. On macOS (the primary platform) older/default SDL relative-mode paths feed OS-accelerated NSEvent deltas, which makes flick-to-turn distance non-linear and inconsistent — the classic 'mouse feels off' port complaint.
- **Evidence:**

  ```
  src/platform/platform_sdl.c:2649 `SDL_SetRelativeMouseMode(SDL_TRUE);` and :2679 `g_mouseDeltaX += event.motion.xrel;`. `grep -rn SDL_SetHint src/platform` -> 0 matches.
  ```
- **Player impact:** Aim may carry OS pointer acceleration on some platforms/SDL versions, so the same physical mouse motion produces different in-game turn amounts depending on speed — degrading precision aiming and muscle memory.
- **Suggested fix:** Explicitly configure relative-mode input: set SDL_HINT_MOUSE_RELATIVE_MODE_WARP='0' (prefer raw) and, on SDL builds that support it, ensure raw/unaccelerated deltas; optionally expose an Input.RawMouse toggle. Verify on macOS that turn distance is speed-independent.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). The literal code claims are all correct: platform_sdl.c:2649 calls SDL_SetRelativeMouseMode(SDL_TRUE) on MOUSEBUTTONDOWN, lines 2679-2680 do `g_mouseDeltaX += event.motion.xrel` (and yrel), and grep confirms no SDL_SetHint / SDL_HINT_MOUSE_* call anywhere in src/. So the "no raw-input hint is set" observation is factually true. But the finding graduates from that fact to a concrete player defect ("aim carries OS pointer acceleration on macOS") that the code does NOT substantiate — it's an assumption about SDL runtime behavior, not something visible in the tree. In practice SDL2 relative-mouse mode defaults to SDL_HINT_MOUSE_RELATIVE_SYSTEM_SCALE="0" (system acceleration NOT applied), so modern SDL2 already delivers largely unaccelerated deltas on macOS; the premise that "default SDL relative-mode paths feed OS-accelerated NSEvent deltas" is version-dependent and unverified here. The finder also missed that the port already exposes tunable sensitivity (Input.MouseSensitivity=0.15 and Input.MouseSensitivityAim, registered at platform_sdl.c:1868/1873, consumed in lvl.c:5930) — which addresses overall scale even if not non-linearity. Net: the observation is real, but whether it is an actual player-facing defect is speculative and likely a non-issue given SDL defaults. Explicitly setting a raw-input hint would be a reasonable defensive nicety, not a demonstrated bug.
  - **Correction:** Code facts are accurate (2649 relative mode, 2679 raw xrel, no SDL_SetHint). But the acceleration/"mouse feels off" impact is unproven: SDL2 relative mode defaults to no system-scale acceleration (SDL_HINT_MOUSE_RELATIVE_SYSTEM_SCALE=0), and the port already exposes configurable Input.MouseSensitivity / MouseSensitivityAim. This is a defensive hardening suggestion, not a substantiated defect.

### 23. 🟠 ✅ HiDPI off by default double-resamples the image to a soft result on Retina

- **File:** `src/platform/platform_sdl.c:1587` · `platformRegisterConfig`
- **Severity:** medium · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** medium
- **What's wrong:** Video.HiDPI defaults to 0, so on a Retina display the SDL drawable is created at window POINT size (e.g. 1440x810) rather than backing-pixel size. Combined with the default Video.RenderScale=2 (platform_sdl.c:198), the scene is supersampled to 2880x1620, downsampled to the 1440x810 drawable, and then the OS bilinearly upscales that backing store to the physical 2880x1620 Retina panel. The final on-screen image is a resample of a resample — softer than either rendering natively at the panel resolution or presenting 1:1.
- **Evidence:**

  ```
  src/platform/platform_sdl.c:1587 `settingsRegisterInt("Video.HiDPI", &g_cfgHiDpi, 0, 0, 1, ... "Off keeps rendering at the configured window size for steadier performance.")` together with :198 `f32 g_pcRenderScale = 2.0f;`.
  ```
- **Player impact:** macOS (the primary/dev platform, and the whole point of the remaster is fidelity) shows a noticeably soft image out of the box; the 2x SSAA cost is partly wasted to an OS upscale blur. Users must know to enable Video.HiDPI to get a crisp picture.
- **Suggested fix:** Default Video.HiDPI on (and let RenderScale default to 1 when HiDPI is on to hold the pixel budget), or auto-detect the display backing scale and pick a sensible RenderScale so the composited output maps 1:1 to physical pixels. Keep the low-DPI path available for min-spec.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited evidence matches the real code exactly: platform_sdl.c:1587 registers Video.HiDPI default 0, and :198 sets g_pcRenderScale=2.0f. The mechanism holds end-to-end. At :2397 enable_highdpi requires g_cfgHiDpi!=0, and :2407-2408 only sets SDL_WINDOW_ALLOW_HIGHDPI when enabled, so on Retina the drawable is created at point size (e.g. 1440x810). gfx_pc.c:4455-4469 (gfx_sync_current_dimensions_from_window) reads SDL_GL_GetDrawableSize and multiplies by RenderScale (2), so the scene FBO is 2880x1620, resolved down to the 1440x810 backing, which macOS then bilinearly upscales to the physical 2880x1620 panel — a genuine downsample-then-upsample chain. The bare default (no faithful/remaster preset) does carry RenderScale=2 + HiDPI=0, so this is the actual out-of-box state on the primary macOS platform. The finder's technical claim is accurate and grounded. Tempering: HiDPI=0 is an intentional, documented performance default; the practical softness is modest because the 1440 backing is itself 2x-supersampled (clean 1440 stretched, not blurry mush); and an explicit user setting corrects it. So it is a real but subtle default-tuning/quality tradeoff, not a bug or robustness defect.
  - **Correction:** The mechanism is correct, but severity is lower than claimed. HiDPI=0 is an intentional, documented performance default (not a defect), and the resulting softness is mild rather than "noticeably soft" — the 1440x810 backing is already 2x-supersampled before the OS upscale, so it is a clean image being stretched, not a blur of a blur. The genuinely actionable point is the incoherent default pairing (paying for 2x SSAA while presenting through a non-1:1 point-size backing on the primary fidelity-focused platform), which wastes part of the SSAA cost. The scaling base is confirmed in gfx_pc.c:4448-4469 (SDL_GL_GetDrawableSize * RenderScale), which the finder cited only obliquely.

### 24. 🟡 ✅ Stub PI-DMA bounds check `devAddr + size <= g_romSize` overflows in u32 — the hardened sibling doRomCopy already avoids this

- **File:** `src/platform/stubs.c:395` · `osPiStartDma / osPiRawStartDma`
- **Severity:** low · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** high
- **What's wrong:** Both stub DMA entry points validate the ROM source range with `devAddr + size <= g_romSize`, computed in 32-bit unsigned arithmetic. If `devAddr + size` wraps past 2^32 (devAddr near UINT32_MAX, which the osPiStartDma comment explicitly warns can happen — 'devAddr arrives truncated from void* on 64-bit'), the sum becomes small, the check passes, and `memcpy(dramAddr, g_romData + devAddr, size)` reads far out of bounds → crash/UB. The codebase already recognized exactly this hazard on the *main* DMA path: ramrom.c:66-70 doRomCopy uses the overflow-safe form `if (size <= g_romSize && offset <= g_romSize - size)` with a comment explaining the wrap. These two stubs (reached by real callers: audi.c:664 / audi_port.c:275 audio streaming, init.c:152-154, usb.c:336-338) were left on the unsafe pattern.
- **Evidence:**

  ```
  stubs.c:395 `if (direction == OS_READ && g_romData && devAddr + size <= g_romSize && dramAddr) { memcpy(dramAddr, g_romData + devAddr, size); }` and identical at stubs.c:7074 in osPiRawStartDma. Contrast ramrom.c:70 `if (size <= g_romSize && offset <= g_romSize - size)` with its comment 'offset+size can wrap (both are u32...)'.
  ```
- **Player impact:** In normal play devAddr is always a valid sub-12MB ROM offset so this never triggers; it is a latent OOB-read/crash if a caller ever passes a large/garbage truncated address, and an avoidable inconsistency with the already-fixed main path.
- **Suggested fix:** Mirror doRomCopy's check in both stubs: `if (direction == OS_READ && g_romData && dramAddr && size <= g_romSize && devAddr <= g_romSize - size)`.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The quoted code matches exactly. stubs.c:387 declares `u32 devAddr` and `u32 size`, and line 395 checks `devAddr + size <= g_romSize` then `memcpy(dramAddr, g_romData + devAddr, size)`. The identical wrappable check is duplicated at line 7074 in osPiRawStartDma. Because both operands are u32, `devAddr + size` wraps modulo 2^32: e.g. devAddr=0xFFFFF000, size=0x2000 computes 0x1000, which passes `<= g_romSize`, and memcpy then reads from g_romData + 0xFFFFF000 — far outside the ROM buffer. A non-wrapping check (devAddr <= g_romSize - size) would correctly reject this. The codebase itself acknowledged this exact hazard: doRomCopy (ramrom.c:70) was hardened to `size <= g_romSize && offset <= g_romSize - size` with a comment 'offset+size can wrap (both are u32...)'. The two stubs retained the wrappable form. The stub comment at line 391 explicitly notes devAddr may arrive truncated from a void* on 64-bit, making a large devAddr plausible on the audio-streaming path this stub serves. This is a genuine memory-safety/robustness gap, consistent with the finder's low severity (practical trigger requires malformed/crafted ROM or audio-stream data; normal ROM offsets are < 12MB). Suggested fix mirrors the already-proven doRomCopy form.

### 25. 🟡 ✅ ROM-DMA stub bounds check `devAddr + size <= g_romSize` can integer-overflow (main path already hardened)

- **File:** `src/platform/stubs.c:395` · `osPiStartDma / osPiRawStartDma`
- **Severity:** low · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** high
- **What's wrong:** The two direct-DMA stubs validate with `if (direction == OS_READ && g_romData && devAddr + size <= g_romSize && dramAddr)` (stubs.c:395 and identically at 7074) then `memcpy(dramAddr, g_romData + devAddr, size)`. Both devAddr and size are u32, so `devAddr + size` can wrap: a large devAddr (the comment at 391 notes devAddr 'arrives truncated from void* on 64-bit', i.e. it may be a truncated pointer, not a small ROM offset) plus size can wrap below g_romSize, passing the check and memcpy'ing from `g_romData + devAddr` far outside the 12MB buffer. The main DMA path was explicitly hardened against this exact wrap (ramrom.c doRomCopy:70 uses `size <= g_romSize && offset <= g_romSize - size` with a comment 'offset+size can wrap'), but these two stubs kept the wrappable form.
- **Evidence:**

  ```
  stubs.c:395: `if (direction == OS_READ && g_romData && devAddr + size <= g_romSize && dramAddr) { memcpy(dramAddr, g_romData + devAddr, size); }` (duplicated at 7074). Hardened sibling ramrom.c:70: `if (size <= g_romSize && offset <= g_romSize - size)`.
  ```
- **Player impact:** A malformed audio/stream DMA request (or a truncated-pointer devAddr) can read arbitrary memory into the audio buffer instead of being rejected; low practical trigger frequency because real ROM offsets are < 12MB.
- **Suggested fix:** Mirror doRomCopy's overflow-safe form: `if (g_romData && size <= g_romSize && devAddr <= g_romSize - size && dramAddr)` in both stubs.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The quoted code is accurate on both counts. stubs.c:395 (osPiStartDma) and stubs.c:7074 (osPiRawStartDma) both gate the copy with `direction == OS_READ && g_romData && devAddr + size <= g_romSize && dramAddr`, where `devAddr` and `size` are both u32 — so `devAddr + size` can wrap past 2^32, pass the check, and drive `memcpy(dramAddr, g_romData + devAddr, size)` out of bounds. The osPiStartDma comment (line 391) explicitly acknowledges `devAddr arrives truncated from void* on 64-bit`, which is the exact vector that could produce a large devAddr. The contrast is also real: ramrom.c:70 `if (size <= g_romSize && offset <= g_romSize - size)` uses the overflow-safe form with a comment (66-69) spelling out that `offset+size can wrap` and that a crafted image could slip a copy past the buffer. So the two stubs are genuinely left on the unsafe pattern the codebase already recognized and fixed on the main path. The mechanism is a real, consistent memory-safety hazard and the suggested fix mirrors the proven doRomCopy check.
  - **Correction:** The defect is real and correctly described, but it is purely latent defense-in-depth. In normal play devAddr is always a valid sub-12MB ROM offset, so devAddr+size cannot approach 2^32 and the check never mis-passes; triggering the OOB read requires a garbage/truncated devAddr near UINT32_MAX that no real caller produces. It is an avoidable inconsistency with the already-hardened main path and a cheap, correct fix, but not something that affects real players or current stability — hence low severity and not a practical player-facing issue.

### 26. 🟡 ✅ siglongjmp out of a SIGSEGV handler is UB under Windows SEH (recovery path)

- **File:** `src/platform/main_pc.c:387` · `crashHandler`
- **Severity:** low · **Class:** port-defect · **Category:** undefined-behavior · **Finder confidence:** medium
- **What's wrong:** The crash-recovery path does `siglongjmp(g_gfxRecoveryJmp, 1);` from inside the SIGSEGV/SIGBUS signal handler (main_pc.c:387). On Windows, platform.h:38-43 macro-maps `sigsetjmp/siglongjmp` onto plain `setjmp/longjmp`, and Windows delivers a segfault as a translated SEH exception; longjmp'ing out of that handler unwinds through the SEH frame, which is undefined behavior (the runtime may abort or leave the stack in an inconsistent state). It is gated behind GE007_ENABLE_RECOVERY (armed at gfx_pc.c:23909-23910), so default builds never take the longjmp — the default handler path just prints diagnostics and _exit(1). The recovery design is also POSIX-shaped (sigaltstack + SA_SIGINFO is #ifndef _WIN32 only, main_pc.c:418-455), so the Windows recovery path is the weakest.
- **Evidence:**

  ```
  main_pc.c:387: `siglongjmp(g_gfxRecoveryJmp, 1);` inside crashHandler for SIGSEGV/SIGBUS/SIGABRT (installed 650-660). platform.h:41-42: `#define sigsetjmp(env, savemask) setjmp(env)` / `#define siglongjmp(env, val) longjmp(env, val)`.
  ```
- **Player impact:** None by default (recovery off). If a user enables GE007_ENABLE_RECOVERY on Windows to investigate a crash, the recovery attempt itself can behave undefinedly instead of resuming.
- **Suggested fix:** Document/keep recovery POSIX-only, or on Windows implement recovery via a vectored/SEH __try/__except handler around the frame render instead of longjmp-from-signal; at minimum compile-guard the arming so GE007_ENABLE_RECOVERY is a no-op on _WIN32.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code matches the finding precisely. main_pc.c:387 executes `siglongjmp(g_gfxRecoveryJmp, 1);` inside crashHandler(), reachable for SIGSEGV/SIGBUS/SIGABRT. platform.h:38-43 macro-maps sigsetjmp/siglongjmp to plain setjmp/longjmp under _WIN32. The Windows handler-install path (main_pc.c:659-660) uses bare signal(SIGSEGV/SIGABRT, crashHandler); the POSIX sigaltstack + SA_SIGINFO machinery (418-455) is #ifndef _WIN32. On Windows/MinGW, a hardware access violation is delivered to a signal handler via runtime-translated SEH exception dispatch, and longjmp'ing out of that handler unwinds through the SEH exception frame — a genuine, well-known UB/robustness hazard that can leave the stack inconsistent or abort. So the mechanism holds. However, the path is fully gated: GE007_ENABLE_RECOVERY (gfx_pc.c:23945) must be set for g_gfxRecoveryActive to become 1; unset (the default) leaves it 0, so the longjmp branch (guarded by `if (g_gfxRecoveryActive ...)` at main_pc.c:368) is never taken and default builds only print diagnostics and _exit(1). This is a real but low-severity, opt-in, developer-diagnostic-only defect. Minor factual slips in the finding: the arming site is src/platform/fast3d/gfx_pc.c:23945/23950-23951 (not src/game/gfx_pc.c:23909-23910), and SIGBUS is not installed on Windows (only SIGSEGV/SIGABRT via the #else signal() path). Neither slip changes the conclusion.
  - **Correction:** The recovery-arming code is at src/platform/fast3d/gfx_pc.c:23945 (getenv gate) and 23950-23951 (g_gfxRecoveryActive=1 / sigsetjmp), not src/game/gfx_pc.c:23909-23910. On Windows only SIGSEGV and SIGABRT are hooked (main_pc.c:659-660); SIGBUS is POSIX-only. The defect is real but affects no players — it is off by default and, even when enabled via GE007_ENABLE_RECOVERY, is a developer crash-investigation path, so player/stability impact is nil in shipping builds.

### 27. 🟡 ✅ Mouse-wheel weapon cycling collapses multiple notches per frame into one switch

- **File:** `src/platform/stubs.c:6268` · `osContGetReadData`
- **Severity:** low · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** platformGetMouseWheel() returns and clears the accumulated wheel delta, but the consumer only tests its SIGN and sets a single one-shot flag, discarding magnitude. If a fast scroll accumulates several notches within one 60Hz poll, only one weapon-cycle step is applied.
- **Evidence:**

  ```
  src/platform/stubs.c:6267-6269 `int wheel = platformGetMouseWheel(); if (wheel > 0) g_pcWeaponCycleForward = 1; else if (wheel < 0) g_pcWeaponCycleBack = 1;`
  ```
- **Player impact:** Fast flick-scrolling to a weapon several slots away only advances one slot per frame, so quick weapon selection feels unresponsive / drops inputs under fast scrolling.
- **Suggested fix:** Track the wheel magnitude (e.g. an accumulator or repeat count) and apply that many cycle steps, or queue N cycle requests, so every notch is honored.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited evidence matches the real code exactly (stubs.c:6267-6269). I traced the full data path: platformGetMouseWheel() (platform_sdl.c:2217-2221) returns and zeroes g_mouseWheelY, which is an accumulator incremented per SDL_MOUSEWHEEL event (`g_mouseWheelY += wy` at line 2657). The consumer keeps only the sign and sets a boolean one-shot flag g_pcWeaponCycleForward/Back = 1. bondviewApplyNativeWeaponCycle (bondview.c:11526-11534) then sets moveData->weaponForwardOffset = 1, and the terminal consumer at bondview.c:13085-13088 treats it strictly as a boolean: `if (moveData.weaponForwardOffset) advance_through_inventory();` — exactly one inventory step regardless of how many notches were accumulated. So the finder's mechanism holds: if a fast flick scrolls 2+ detents within one 16.6ms poll, the extra notches are silently dropped and only one weapon-cycle step is applied. The magnitude truly is discarded end to end. This is a genuine (if minor) input-fidelity/responsiveness defect for mouse users; severity low is appropriate. Note: the natural fix is more than a one-liner, since weaponForwardOffset is a boolean at every downstream site and would need to become a repeat count with advance_through_inventory() looped, but that does not undermine the finding that the defect exists.

### 28. 🟡 ❌ Menus have no mouse support on a PC port (keyboard/stick only)

- **File:** `src/platform/stubs.c:6182` · `osContGetReadData`
- **Severity:** low · **Class:** n64-quirk-hurting-players · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The frontend/menu input branch maps only keyboard directions + Enter/Tab/Backspace (and stick) to navigate menus; there is no mouse cursor, hover, or click-to-select. In gameplay the mouse buttons are used for fire/aim and any click merely re-grabs the pointer. A mouse-driven player must drop to the keyboard/D-pad to operate menus.
- **Evidence:**

  ```
  src/platform/stubs.c:6182 `if (frontend_input) { ... pcApplyMenuDirection(&buttons,&stick_x,&stick_y, keys[SDL_SCANCODE_UP]||keys[SDL_SCANCODE_W], ...); }` — no SDL_GetMouseState hit-testing / cursor path in the frontend branch.
  ```
- **Player impact:** PC players instinctively reach for the mouse in menus and find it inert; navigation feels archaic relative to modern expectations, and mixing mouse-aim gameplay with keyboard-only menus is jarring.
- **Suggested fix:** Add optional pointer-driven menu navigation (show the OS cursor when a menu is active, map click to A/select and hover to the highlighted item via hit-testing of the menu rects). Gate behind a flag if needed and keep the keyboard/stick path for the faithful/deterministic runs.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited code at src/platform/stubs.c:6182 matches the finder's quote: the `if (frontend_input)` (menu) branch maps only keyboard Enter/Space (A), Tab (START), Backspace (B), WASD/arrows via pcApplyMenuDirection, and IJKL jpad — plus scripted stick. There is no SDL_GetMouseState hit-testing or cursor path in that branch. Mouse buttons are only read in the gameplay `else` branch (6244-6249) for fire/aim. So the factual claim is accurate. But this is not a defect or robustness issue: menus are fully and reliably navigable via keyboard and controller stick. The original N64 title had no mouse; keyboard/stick-only menus are a faithful design choice for this port, and under --deterministic the input path is intentionally scripted. What the finder describes is a missing modern-convenience feature (optional pointer navigation), framed as an N64 quirk 'hurting players.' Nothing is broken and no player is blocked — it is a low-value UX preference, not a genuine player-harming or stability issue, so it does not meet the bar for CONFIRMED.
  - **Correction:** Code quote is accurate, but the classification (n64-quirk-hurting-players, defect) is wrong: keyboard/stick menu navigation is fully functional and faithful to the mouse-less N64 original. This is an optional feature request (add gated pointer-driven menu nav), not a defect or robustness problem.

### 29. 🟡 ✅ Debug FPS/frame-time overlay is enabled by default in shipping builds

- **File:** `src/platform/platform_sdl.c:1628` · `platformRegisterConfig`
- **Severity:** low · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** Video.FpsOverlay registers with default 1, so every normal play session renders an FPS / frame-time / 1%-low widget in the top-right corner. It is a developer diagnostic, not part of the game HUD, and is not in any faithful/remaster preset — it is simply on for everyone by default.
- **Evidence:**

  ```
  src/platform/platform_sdl.c:240 `s32 g_pcFpsOverlay = 1;` and :1628 `settingsRegisterInt("Video.FpsOverlay", &g_pcFpsOverlay, 1, 0, 1, ...)`.
  ```
- **Player impact:** A persistent developer FPS counter sits over the game for players who never asked for one, which reads as unfinished/debug and clutters the corner of the screen.
- **Suggested fix:** Default Video.FpsOverlay to 0 for shipping and let users opt in (or bind a toggle key). No determinism impact — it is already force-suppressed under --deterministic/--screenshot sessions.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Both cited lines match the real code verbatim. src/platform/platform_sdl.c:240 declares `s32 g_pcFpsOverlay = 1;` and :1628 registers `settingsRegisterInt("Video.FpsOverlay", &g_pcFpsOverlay, 1, 0, 1, ...)` with default 1. The author's own comment on line 240 calls it "a HUD debug widget, not part of the original LOOK," notes it is deliberately excluded from the --faithful/--faithful-hd preset tables, and confirms it is force-suppressed only under --deterministic / GE007_BACKGROUND / --screenshot-frame. So in any ordinary play session the FPS/frame-time/1%-low overlay renders in the top-right for every player, and no preset turns it off. This is not a misread of a gated path — the mechanism holds exactly as claimed. The only nuance is that it is a deliberate, documented shipping decision (flipped default-ON at T11), so it is "intended" in the taste-choice sense rather than an accidental bug; it is explicitly not a faithfulness feature. The finder's facts, severity (low), and no-determinism-impact assessment are all correct.


## Character AI / collision (chr*.c, stan.c)

### 30. 🔴 ✅ Guard auto-aim targetability AND movement-AI mode are driven by RENDER visibility (room_rendered/fog), not the N64 camera-frustum sim test

- **File:** `src/game/chr.c:5232` · `chrTickBeams`
- **Severity:** high · **Class:** port-defect · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** The N64 gates a guard's PROPFLAG_ONSCREEN on the camera-frustum sim test sub_GAME_7F054D6C (camIsPosInScreen — a deterministic camera computation). The port replaces it, by default, with a room-render bypass: `visible = getROOMID_isRendered(room_ids[room_index])`. `visible` then sets/clears PROPFLAG_ONSCREEN (chr.c:5347 `prop->flags |= 2`; chr.c:5342 clear on the not-visible path). PROPFLAG_ONSCREEN builds g_OnScreenPropList (chrprop.c:536) which is the ONLY list chrpropUpdateAutoaimTarget iterates (chrprop.c:6628), and it also drives guard movement: chrlvTickGoPos/chrlvTickPatrol read `onscreen = self_prop->flags & PROPFLAG_ONSCREEN` to decide whether a guard walks normally or teleports via WAYMODE_MAGIC (chrlv.c:11058, chrlv.c:11247). room_rendered is a render-pass quantity set one frame earlier in the render/DL build (bgRoomVisibilityRelated, called from lvlRender in lvl.c:1725), is affected by port render overrides, and — as the port's own comment admits — fog interacts with it. This makes core combat (what you can auto-aim) and guard locomotion depend on what the renderer drew, at the render's FOV/resolution/fog, rather than on sim state.
- **Evidence:**

  ```
  chr.c:5225-5237: `s_frustum_gate = ge_env_bool("GE007_CHRBEAMS_FRUSTUM", 0);` ... `} else { visible = 0; ... if (... getROOMID_isRendered(room_ids[room_index])) { visible = 1; ...`. chrlv.c:11225-11236 (chrlvTickPatrol): "On NATIVE_PORT, fog aggressively culls guards to alpha=0 even when their room IS rendered. This prevents PROPFLAG_ONSCREEN from being set, so lastvisible60 never updates, and guards perpetually re-enter WAYMODE_MAGIC ... This blocks the normal walk path (chrlvTravelTick)" guarded by `&& !chrlvPropHasRenderedRoom(self_prop)`.
  ```
- **Player impact:** Guards can teleport/stutter (WAYMODE_MAGIC) or fail to walk when the port's fog/culling suppresses their on-screen flag even in a rendered room; auto-aim cannot lock onto a guard the renderer culled, and CAN behave differently at different FOV/resolution/fog than on N64. The faithful path exists but is default-OFF behind GE007_CHRBEAMS_FRUSTUM.
- **Suggested fix:** Source PROPFLAG_ONSCREEN from a resolution/FOV/fog-independent sim visibility test (the camera-frustum sub_GAME_7F054D6C, matching N64) rather than getROOMID_isRendered; never let fog alpha influence PROPFLAG_ONSCREEN. Validate and flip GE007_CHRBEAMS_FRUSTUM ON by default, removing the room_rendered bypass and its downstream chrlv.c workarounds.
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). Every cited line matches the real code. chr.c:5215-5238 (in chrTickBeams) gates the faithful N64 frustum test sub_GAME_7F054D6C behind GE007_CHRBEAMS_FRUSTUM, which ge_env_bool defaults to 0; the default path sets visible = getROOMID_isRendered(room), a render-pass quantity. That `visible` sets (5347 `prop->flags |= 2`) or clears (5342) PROPFLAG_ONSCREEN. chrprop.c:536 builds g_OnScreenPropList exclusively from ENABLED|ONSCREEN props, and chrpropUpdateAutoaimTarget (chrprop.c:6628) iterates only that list, so auto-aim targetability tracks render visibility. chrlv.c:11058/11247 read the same flag to decide WAYMODE_MAGIC vs normal walk, and the NATIVE_PORT comment at chrlv.c:11225-11236 explicitly documents the exact failure the finding describes: fog culls guards to alpha=0 even in rendered rooms, suppressing PROPFLAG_ONSCREEN and forcing perpetual WAYMODE_MAGIC re-entry — mitigated by the chrlvPropHasRenderedRoom workaround. The divergence from N64 sim visibility is real and player-facing (auto-aim/locomotion can behave differently by FOV/resolution/fog). The finding overstates in two respects: (1) it is a deliberate, documented, gated tradeoff kept OFF pending the split-screen per-player-camera union (M1b), not an accidental bug; (2) the guard-teleport symptom is already partially worked around in chrlv.c (chrlvPropHasRenderedRoom guard + chrlvMagicTravelShouldUseRenderedPathExit). The auto-aim half is not mitigated. Given the deliberate gating and existing movement mitigations, high overstates it; medium is honest.
  - **Correction:** The mechanism, files, lines and symbol are all accurate. Correction on framing/severity: this is a deliberate, author-documented, env-gated divergence (GE007_CHRBEAMS_FRUSTUM, default OFF) kept off intentionally because the frustum path uses the current player's camera and is unsafe in split-screen until the per-player union lands (chr.c:5211-5213). The guard-locomotion/WAYMODE_MAGIC symptom is already partially mitigated in the port (chrlv.c:11236 `!chrlvPropHasRenderedRoom(self_prop)` guard and chrlvMagicTravelShouldUseRenderedPathExit), so guards in rendered rooms are steered back to the normal walk path. The unmitigated portion is auto-aim targetability, which still depends on render visibility (fog/FOV/resolution) rather than the N64 sim frustum test. Not a crash/corruption; it is a faithfulness/gameplay-consistency defect. Severity medium, not high.

### 31. 🟡 ❌ Guard floor-recovery searches ALL rooms (roomset=0), can bind a stan-less guard to a wrong-room floor and rescans the whole navmesh every tick

- **File:** `src/game/chr.c:2010` · `sub_GAME_7F01FC10`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** medium
- **What's wrong:** Two NATIVE_PORT recovery sites call the floor-finder with roomset=0, which makes sub_GAME_7F0AF20C scan EVERY room for the highest floor tile at/below the guard's Y, ignoring the guard's room membership (the roomset filter that would constrain it is bypassed). In vertically-stacked geometry a stan-less guard can be bound to a floor tile belonging to a different room that happens to sit below its Y, then chr->ground is derived from that tile and newpos->y is pulled to it. It also runs a full O(rooms×tiles) navmesh scan every frame while prop->stan stays NULL.
- **Evidence:**

  ```
  chr.c:2007-2022 (move callback): `f32 pos3[3] = {...}; ... StandTile *found = sub_GAME_7F0AF20C(pos3, 0, &dist); ... if (found) { chr->prop->stan = found; }`. chr.c:5137-5141 (chrTickBeams): `prop->stan = sub_GAME_7F0AF20C(pos3, 0, &dist);` — both pass roomset=0. The canonical decomp (stan.c:895-931) uses the roomset argument to restrict the room list.
  ```
- **Player impact:** A guard that loses its stan tile can snap/sink to a floor in a wrong (stacked) room instead of its own, and repeatedly triggers a full navmesh scan per tick until a tile is found.
- **Suggested fix:** Pass the guard's registered room list as the roomset instead of 0 so recovery is constrained to the guard's own rooms, and cache/limit the recovery so it does not full-scan every frame while stan is NULL.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). Code claims are literally accurate: chr.c:2010 and chr.c:5140 both pass roomset=0, and stan.c:757-764 confirms roomset=0 bypasses the room filter and scans all rooms (bbox-gated). But the defect framing fails. Both are NATIVE_PORT-only recovery paths firing only when prop->stan==NULL, a degenerate state from the stubbed room-intersection setup (sub_GAME_7F03E27C); the guard's room binding is exactly what is unavailable, so there is no reliable room list to pass. The wrong-room bind is self-mitigating: stanConsiderFloorTile (stan.c:700-706) picks the highest tile with Y<=pos.y, i.e. the floor immediately beneath the guard, which is physically correct even across stacked rooms; a roomset filter would find nothing and fall forever in the sink case, making it worse. The per-frame scan is bounded (O(rooms x tiles), few guards) and only runs while stan is NULL, stopping once a tile is bound. This is a defensible intentional band-aid, not a robustness bug, and the suggested fix would regress recovery.</parameter>
<parameter name="correction">roomset=0 is an intended global-search choice for a stan==NULL port recovery path where room membership is unavailable; highest-floor-at-or-below-Y selection picks the nearest floor beneath, and the suggested roomset filter would make recovery worse.

### 32. 🟡 ❓ Patrol tick unconditionally force-loops any non-looping guard animation every frame as a rendering-bug workaround

- **File:** `src/game/chrlv.c:11216` · `chrlvTickPatrol`
- **Severity:** low · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** low
- **What's wrong:** To compensate for the port's fog/visibility breaking WAYMODE_MAGIC walking (Finding 1), chrlvTickPatrol forces animation looping on every patrolling guard whose animlooping flag is 0, on every tick. The guard rule is only `animlooping == 0 && anim != NULL`, not restricted to walk cycles, so any patrolling guard momentarily in a legitimately non-looping animation (a one-shot transition/pose) would be forced to loop it. This is a NATIVE_PORT-only behavior with no ASM equivalent.
- **Evidence:**

  ```
  chrlv.c:11216-11218: `if (self->model && self->model->animlooping == 0 && self->model->anim != NULL) { modelSetAnimLooping(self->model, 0.0f, 16.0f); }` with the preceding comment: "On NATIVE_PORT, fog aggressively zeros alpha, preventing visibility, which causes WAYMODE_MAGIC entry, which bypasses chrlvTravelTick entirely. This per-tick enforcement breaks the circular dependency."
  ```
- **Player impact:** A patrolling guard's non-looping animation can be visibly forced to loop; the workaround also masks the root render->AI coupling rather than fixing it.
- **Suggested fix:** Remove the per-tick force-loop once PROPFLAG_ONSCREEN is decoupled from fog/render (Finding 1); if kept as interim, gate it strictly to the intended walk/travel animation so it cannot override one-shot poses.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). The cited code is accurate: chrlv.c:11216-11218 inside chrlvTickPatrol contains exactly the quoted `if (self->model && self->model->animlooping == 0 && self->model->anim != NULL) { modelSetAnimLooping(self->model, 0.0f, 16.0f); }`, gated by #ifdef NATIVE_PORT with the quoted comment, and has no ASM equivalent. modelSetAnimLooping (model.c:7111) sets animlooping=1 with loopframe 0.0. The finder is right that the condition is unrestricted (no animation-ID check), so in principle any non-looping animation active on the model during a patrol tick would be forced to loop. BUT: (1) this is deliberate, explicitly-documented gated workaround behavior, not an unintended defect — the finder itself acknowledges this and rates it low; (2) the claimed player-visible harm (a legitimate one-shot pose/transition being forced to loop) is unsubstantiated — chrlvTickPatrol only runs in ACT_PATROL where the guard is walking between pads; one-shot animations like surrender/spotting/reaction transitions live in other action states, not ACT_PATROL, so no concrete failure case is demonstrated. The finding's real thrust ("masks root render->AI coupling rather than fixing it") is an architecture/code-quality critique of an intentional interim workaround, not a proven runtime bug. The mechanism (over-broad guard) is real code but the player impact is speculative.
  - **Correction:** The code is described accurately, but this is intended, documented NATIVE_PORT workaround behavior rather than a defect, and the claimed harm is speculative: chrlvTickPatrol only executes in the ACT_PATROL state where guards play looping walk/travel cycles, while the game's one-shot poses (surrender, spotting_bond, look_around, hit reactions) are set in other action states, so no demonstrated case exists where a legitimate one-shot pose is force-looped by this path. The valid residual point is a minor code-quality one: the guard should ideally be scoped to the walk/travel animation ID rather than any non-looping animation.


## Object/prop interaction (chrobjhandler.c)

### 33. 🔴 ✅ Magazine pickup reads PROPDEF type (0x3) instead of ammoType (0x80): every ammo clip grants remote mines

- **File:** `src/game/chrobjhandler.c:40607` · `collect_or_interact_object`
- **Severity:** high · **Class:** port-defect · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** In the PROPDEF_MAGAZINE collect branch, `add_ammo_to_inventory(mag->type, get_ammo_in_magazine(mag), 1, showstring)` passes `mag->type`. `mag` is an `AmmoCrateRecord*`; `type` is the inherited `PropDefHeaderRecord.type` at struct offset 0x3, promoted via the `inherits`(=`struct`, MS/plan9 anonymous member) trick. To even reach this `case PROPDEF_MAGAZINE`, `propobj->type == PROPDEF_MAGAZINE == 7`, so `mag->type` is ALWAYS the constant 7. AMMOTYPE value 7 is AMMO_REMOTEMINE. The real ammo type lives in `AmmoCrateRecord.ammoType` at offset 0x80. Net effect: picking up any placed/dropped magazine calls `add_ammo_to_inventory(AMMO_REMOTEMINE, qty)` where qty is correctly computed from the real ammoType by get_ammo_in_magazine — so the player NEVER receives the clip's intended ammo, and instead receives remote-mine ammo plus (via add_ammo_to_inventory's AMMO_REMOTEMINE arm) bondinvAddInvItem(ITEM_REMOTEMINE) and bondinvAddInvItem(ITEM_TRIGGER). This is a field-offset/lane confusion exactly like the ammo-crate stride bug. It also creates a gate/collect divergence: the collectability gate object_collectability_routines reads the correct field (`ammo->ammoType`), so a magazine is gated collectable based on the real ammo type but then handed remote mines. This diverges sim state from N64 (determinism) and is identical on both renderer backends (not a parity issue).
- **Evidence:**

  ```
  collect (line 40607): `add_ammo_to_inventory(mag->type, get_ammo_in_magazine(mag), 1, showstring);` where get_ammo_in_magazine(40296) reads `crate->ammoType`. Retail asm `interact_magazine_object` (7F05031C): `lw $a0, 0x80($s0)` loads ammoType (0x80) as arg0, then `jal add_ammo_to_inventory`. Struct: PropDefHeaderRecord.type is u8 at 0x3 (bondtypes.h:2365); AmmoCrateRecord.ammoType is at 0x80 (bondtypes.h:3170). Enum: AMMO_REMOTEMINE == 7 (bondconstants.h:3295), PROPDEF_MAGAZINE == 7. Gate reads correct field (line 42052): `check_cur_player_ammo_amount_in_inventory(ammo->ammoType)`.
  ```
- **Player impact:** Walking over any ammo-clip pickup gives the wrong ammunition entirely: you never gain the intended ammo (9mm/rifle/etc.), and instead silently receive remote-mine ammo plus the remote-mine weapon and detonator in your inventory. Breaks the ammo economy, hands out free explosives, and (where a magazine is common in a level) is game-breaking; magazines are loaded via load_proptype(PROPDEF_MAGAZINE) so this triggers wherever they are placed.
- **Suggested fix:** Change `mag->type` to `mag->ammoType` to match the retail asm (arg0 = *(obj+0x80)).
- **Verification:** CONFIRMED (adj. severity high, real-player-issue=True). Verified in-code. chrobjhandler.c:40607 passes mag->type to add_ammo_to_inventory. mag is (AmmoCrateRecord*)propobj; the case is only entered when propobj->type==PROPDEF_MAGAZINE, so mag->type (PropDefHeaderRecord.type at 0x3, bondtypes.h:2365, inherited via ObjectRecord which has no shadowing type) is always the constant 7. AMMO_REMOTEMINE==7 (bondconstants.h enum: NONE=0..REMOTEMINE=7) and PROPDEF_MAGAZINE==7 collide. The correct field AmmoCrateRecord.ammoType is at 0x80 (bondtypes.h:3170) and IS read for quantity by get_ammo_in_magazine (40296, switches on crate->ammoType) — proving the author knew ammoType is the right field, so mag->type is an unambiguous misread, not faithful behavior. Downstream, add_ammo_to_inventory's AMMO_REMOTEMINE arm (40244-40248) grants ITEM_REMOTEMINE + ITEM_TRIGGER. The collectability gate at 42051-42053 correctly reads ammo->ammoType, so the object is gated on real ammo but handed remote mines on collect — a genuine gate/collect divergence and determinism break. Live pickup path, not dead code. Fix mag->type -> mag->ammoType is correct.

### 34. 🟡 ✅ Destroying a multi-ammo crate spawns no pickup magazines (retail behavior dropped in native reimpl)

- **File:** `src/game/chrobjhandler.c:37568` · `maybe_detonate_object`
- **Severity:** low · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The native-port reimplementation of maybe_detonate_object (the compiled NATIVE_PORT branch, 37386-37628) omits the retail behavior of scattering collectable magazines when a multi-ammo crate (PROPDEF_AMMO) is destroyed. Its post-damage switch handles only PROPDEF_AUTOGUN/CCTV/MONITOR/MULTI_MONITOR/GAS_RELEASING/ARMOUR and has no PROPDEF_AMMO arm, and there is no pre-switch OBJECT_UNTOUCHED ammo-spawn loop. The retail routine (asm at 7F04E3C8+) does randomGetNext -> ammocrateAllocate -> objInitWithModelDef(PitemZ_entries) -> chrpropReparent to spawn magazines. In the native build `ammocrateAllocate` is referenced only from dead code (line 37855, inside the non-NATIVE_PORT `#else` of the NONMATCHING block, which is not compiled), so the spawn path never runs. Consequence: shooting/destroying an ammo crate yields no droppable ammo, whereas retail lets you collect the scattered magazines.
- **Evidence:**

  ```
  Native switch (37568-37617) cases: PROPDEF_AUTOGUN(37570), PROPDEF_CCTV(37576), PROPDEF_MONITOR(37581), PROPDEF_MULTI_MONITOR(37589), PROPDEF_GAS_RELEASING(37602), PROPDEF_ARMOUR(37607), default — no PROPDEF_AMMO. Retail asm ref (region 37962-38419) shows `jal randomGetNext` (7F04E3C8), `jal ammocrateAllocate` (7F04E410), `jal objInitWithModelDef` (7F04E4F0), `jal chrpropReparent` (7F04E518). Only live `ammocrateAllocate(` call is at 37855, inside the dead `#else` of `#ifdef NONMATCHING` (NATIVE_PORT is defined, so this branch is excluded).
  ```
- **Player impact:** When a player destroys a multi-ammo crate by shooting it (rather than collecting it), no magazines drop to pick up, so the ammo is lost. Minor and only affects the destroy-instead-of-collect case, but it is a real divergence from N64 pickup behavior.
- **Suggested fix:** Port the retail PROPDEF_AMMO OBJECT_UNTOUCHED spawn loop into the native maybe_detonate_object: on first destruction, walk slots[i].quantity (pair stride) and spawn magazines via ammocrateAllocate/objInitWithModelDef/chrpropReparent as the asm does. Optionally gate behind a GE007_* flag if strict determinism parity with the current build is required.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited code matches the finding exactly. The compiled branch (NONMATCHING && NATIVE_PORT, function at 37386-37628) has a post-damage switch at line 37568 covering only PROPDEF_AUTOGUN(37570), CCTV(37576), MONITOR(37581), MULTI_MONITOR(37589), GAS_RELEASING(37602), ARMOUR(37607), default — no PROPDEF_AMMO arm — and there is no pre-switch OBJECT_UNTOUCHED magazine-spawn loop. The only post-destruction extra is the child propobjSetDropped loop (37619-37627), which marks pre-existing children dropped, not spawns new magazines. Retail behavior is corroborated not only by the speculative 44%-match reference #else (37841-37886) but by the authoritative disassembly at `glabel maybe_detonate_object` (37990): 7F04E3AC gates on obj->type==20 (PROPDEF_AMMO, verified =20 in bondconstants.h:3458 / bondtypes.h:3370) and destroyed-level, then jal randomGetNext (38186), a 4-byte pair-stride slot loop reading 0x82 (quantity) and 0x80 (modelnum), jal ammocrateAllocate (38205), objInitWithModelDef, chrpropReparent — i.e., it spawns and reparents collectable magazines. In the native build ammocrateAllocate (defined at 44550) has no live call site; the only references are the non-compiled reference C (37855) and the ASM. So the native reimpl genuinely drops the retail ammo-crate magazine-scatter behavior. This is a real behavioral divergence, though niche (only fires when an ammo crate is destroyed rather than collected, at a specific destroyed-level), consistent with the claimed low severity.


## Metal backend (gfx_metal.mm)

### 35. 🟠 ✅ Metal backend is hardcoded single-sample: Video.MSAA and alpha-to-coverage cutout smoothing are silently ignored

- **File:** `src/platform/fast3d/gfx_metal.mm:3013` · `mtl_draw_triangles / mtl_pso_for`
- **Severity:** medium · **Class:** parity-divergence · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** Every Metal PSO is built with rasterSampleCount=1: mtl_draw_triangles always calls mtl_pso_for(ms, s_blend, 1, ...) and all post/shadow PSOs pass 1 as well (gfx_metal.mm:1083 d.rasterSampleCount=samples; 1951/2242/2256/2572 all =1; comment at 2001-2002 'Metal is single-sample here'). The GL backend, by contrast, builds a multisample scene target (GL_DEPTH24_STENCIL8 / GL_RGBA8 renderbuffers at gfx_opengl.c:2616-2621 when gfx_opengl_effective_msaa_samples()>0) and drives GL_SAMPLE_ALPHA_TO_COVERAGE for alpha-tested cutouts via gfx_opengl_update_a2c_state() (glEnable(GL_SAMPLE_ALPHA_TO_COVERAGE) at gfx_opengl.c:747). There is no MSAA and no alpha-to-coverage anywhere in the Metal path, and no warning that Video.MSAA is being dropped. Cutouts on Metal are hard alpha-tested only (discard_fragment at gfx_metal.mm:621).
- **Evidence:**

  ```
  gfx_metal.mm:3013  `id<MTLRenderPipelineState> pso = mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha);`  (samples arg is the literal 1); gfx_metal.mm:1083 `d.rasterSampleCount = samples;`; vs gfx_opengl.c:747 `glEnable(GL_SAMPLE_ALPHA_TO_COVERAGE);` and gfx_opengl.c:2616-2621 multisample renderbuffers.
  ```
- **Player impact:** A macOS user running the Metal backend (GE007_RENDERER=metal / --remaster) who sets Video.MSAA>0 gets no multisampling and no smoothed alpha-cutout edges (foliage, fences, grates) — the setting silently does nothing, unlike on GL. The default --remaster preset pins Video.MSAA=0 (uses 2x SSAA+FXAA), so the default look is unaffected; the divergence only bites when MSAA is explicitly enabled.
- **Suggested fix:** Either (a) honor Video.MSAA on Metal: allocate a multisampled scene color/depth (sampleCount from gfx_opengl_effective_msaa_samples()), set rasterSampleCount to match on scene PSOs, enable ca.alphaToCoverageEnabled for cutout/GFX_BLEND_ALPHA_COVERAGE draws, and add a resolve; or (b) at minimum log a one-time 'Video.MSAA ignored on Metal backend (use SSAA/SMAA)' so the knob isn't a silent no-op.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Re-reading the cited code confirms every claim. gfx_metal.mm:3013 calls mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha); the function's third parameter is `int samples` (1064-1065) which flows to d.rasterSampleCount = samples (1083), so scene PSOs are hardcoded single-sample. mtl_ensure_targets (969-983) creates the scene color and depth textures with no sampleCount (default 1), and all four other PSO build sites also pin rasterSampleCount=1 (1951, 2242, 2256, 2572). The Metal source itself states at 2001-2002 "No MSAA clause — Metal is single-sample here." The GL backend, by contrast, builds a real multisample scene FBO (g_scene_msaa_fbo/g_scene_msaa_color_rb at 690-691, 2601-2631, resolved in gfx_opengl_resolve_scene_target at 4070) keyed off g_pcMsaaSamples/Video.MSAA (2419-2444) and enables GL_SAMPLE_ALPHA_TO_COVERAGE (747, 3687). GL even warns when the setting interacts poorly (1854, 2482); Metal has no path and no warning, so Video.MSAA>0 is silently ignored only on Metal. The mechanism holds; this is a genuine backend parity divergence, not a misread gated path. Severity is honestly low: MSAA defaults to 0 and the remaster AA stack (RenderScale/SSAA + FXAA/SMAA) covers anti-aliasing, so real-world impact is marginal, but a player who enables Video.MSAA on macOS/Metal does get a different, quietly worse result than on GL.

### 36. 🟠 ✅ Metal sun-shadow depth pass omits depth-clamp, diverging from GL's global GL_DEPTH_CLAMP — casters at the shadow-frustum boundary are clipped instead of clamped

- **File:** `src/platform/fast3d/gfx_metal.mm:1461` · `mtl_shadow_encode / mtl_render_shadow_map`
- **Severity:** medium · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** The main scene encoder sets [s_enc setDepthClipMode:MTLDepthClipModeClamp] (gfx_metal.mm:1184), matching GL's globally-enabled GL_DEPTH_CLAMP (gfx_opengl.c:3734) and the CPU clipper that forces g_depth_clamp_enabled=true on Metal. But the shadow-map encoder (mtl_shadow_encode, used by mtl_render_shadow_map:1579 and mtl_dump_shadow_pgm:1498) never calls setDepthClipMode, so it uses Metal's default MTLDepthClipModeClip. GL's GL_DEPTH_CLAMP is a global enable that is never disabled, so it is still active during gfx_opengl_render_shadow_map. Result: shadow-caster triangles whose depth exceeds the shadow orthographic near/far range (after the shadow VS remap o.pos.z=(z+w)*0.5 at gfx_metal.mm:1402) are hardware-clipped/dropped on Metal but rasterized at clamped depth on GL.
- **Evidence:**

  ```
  gfx_metal.mm:1184 (scene) `[s_enc setDepthClipMode:MTLDepthClipModeClamp];` — present; mtl_shadow_encode (1461-1479) sets pipeline/winding/cull/bias but has NO setDepthClipMode call, so the shadow encoder defaults to Clip; vs gfx_opengl.c:3734 `glEnable(GL_DEPTH_CLAMP);` (global, covers the shadow pass).
  ```
- **Player impact:** With Video.SunShadow enabled on the Metal backend, tall/near or slightly-out-of-fit shadow casters can lose the portion of their shadow that extends past the shadow frustum's near/far plane (shadow truncation/flicker), whereas GL keeps them clamped and casts the full shadow. SunShadow is not in the default remaster preset (off by default), so this affects users who opt into it.
- **Suggested fix:** Add `[enc setDepthClipMode:MTLDepthClipModeClamp];` inside mtl_shadow_encode so the shadow pass clamps depth like the scene pass and like GL's global GL_DEPTH_CLAMP.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Re-read all cited code and it matches the finding precisely. gfx_metal.mm:1184 sets MTLDepthClipModeClamp on the scene encoder and it is the only setDepthClipMode call in the whole Metal backend (grep-verified). mtl_shadow_encode (1461-1479) sets pipeline/winding/cull/bias/buffers but never sets a depth clip mode, so both shadow call sites (mtl_render_shadow_map:1580, mtl_dump_shadow_pgm:1499) rasterize at Metal's default MTLDepthClipModeClip. On the GL side, gfx_opengl.c:3734 enables GL_DEPTH_CLAMP globally and it is never disabled anywhere (grep-verified); gfx_opengl_render_shadow_map (3936) never touches it, so depth clamping is active during the GL shadow pass. The shadow VS remap at 1402 is present as quoted, and the backend even forces g_depth_clamp_enabled=true on Metal (1237), making the missing shadow-pass clamp an internal inconsistency. Mechanism holds: GL clamps shadow-caster depth outside the ortho near/far, Metal clips it — a genuine parity divergence.
  - **Correction:** Directionally and mechanically correct as written; only severity is overstated. SunShadow is opt-in (off in the default preset) and the divergence manifests only for casters that extend past the shadow orthographic near/far boundary, so real player impact is a bounded edge case rather than a medium-severity default-path issue. Downgrade to low. The suggested fix (add [enc setDepthClipMode:MTLDepthClipModeClamp] in mtl_shadow_encode) is the correct remedy.

### 37. 🟠 ✅ Sun-shadow depth pass omits depthClipMode:Clamp — casters outside the light frustum are clipped on Metal but clamped on GL

- **File:** `src/platform/fast3d/gfx_metal.mm:1472` · `mtl_shadow_encode`
- **Severity:** medium · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** The scene encoder explicitly mirrors GL's global GL_DEPTH_CLAMP by calling `[s_enc setDepthClipMode:MTLDepthClipModeClamp]` in mtl_open_scene_encoder (line 1184). The sun-shadow depth pass does NOT: mtl_render_shadow_map creates its encoder at line 1579 and mtl_shadow_encode sets winding, cull and depth-bias but never calls setDepthClipMode, so it stays at Metal's default MTLDepthClipModeClip. GL's shadow pass (gfx_opengl_render_shadow_map) runs with GL_DEPTH_CLAMP still enabled (set once in gfx_opengl_init:3734, never disabled by the pass). Net effect: a shadow caster whose remapped depth lands outside the light's [near,far] (e.g. geometry between the sun and the shadow near plane, or past the far plane) is dropped by Metal's clip but kept and rasterized at the clamped 0/1 depth by GL. The same omission exists on the GE007_DUMP_SHADOW_MAP path (mtl_dump_shadow_pgm, encoder at line 1498).
- **Evidence:**

  ```
  mtl_shadow_encode sets `[enc setFrontFacingWinding:...]; [enc setCullMode:MTLCullModeFront]; [enc setDepthBias:4.0f slopeScale:2.0f clamp:0.0f];` (lines 1472-1474) with no setDepthClipMode; contrast mtl_open_scene_encoder line 1184 `[s_enc setDepthClipMode:MTLDepthClipModeClamp];`. GL keeps GL_DEPTH_CLAMP on globally (gfx_opengl.c:3734) and never toggles it in gfx_opengl_render_shadow_map (gfx_opengl.c:3992-4008).
  ```
- **Player impact:** On the native Metal backend (used for --remaster), directional sun shadows show holes / light-leaks under tall or light-proximate casters that the GL backend renders solid. Gated behind Video.SunShadow (remaster feature, default off), so no impact on the faithful/default path.
- **Suggested fix:** Add `[enc setDepthClipMode:MTLDepthClipModeClamp];` at the top of mtl_shadow_encode (and in mtl_dump_shadow_pgm's encoder) so caster depth is clamped rather than clipped, matching GL's GL_DEPTH_CLAMP.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Every cited fact matches the real code. mtl_shadow_encode (gfx_metal.mm:1461-1479) sets front-facing winding (CCW), MTLCullModeFront, and setDepthBias:4.0 slopeScale:2.0, but never calls setDepthClipMode — so its encoder keeps Metal's default MTLDepthClipModeClip. Both shadow encoders (mtl_render_shadow_map's at line 1579, and mtl_dump_shadow_pgm's at 1498) route through mtl_shadow_encode, so both clip. By contrast mtl_open_scene_encoder explicitly sets [s_enc setDepthClipMode:MTLDepthClipModeClamp] at line 1184, proving the codebase deliberately mirrors GL_DEPTH_CLAMP for the scene pass and the shadow pass is the sole omission. On the GL side, GL_DEPTH_CLAMP is enabled once globally at gfx_opengl.c:3734 (gated on GL>=3.2 and GE007_NO_DEPTH_CLAMP), and gfx_opengl_render_shadow_map (3936-~4030) never disables it in the pass or its state save/restore block, so the GL shadow pass rasterizes casters with depth clamping active. Net: on the common macOS GL 4.1 path a caster straddling the light near/far plane is clamped/pancaked and retained by GL but clipped and dropped by Metal — a genuine cross-backend shadow-map parity divergence (the classic near-plane pancaking case). The mechanism holds.
  - **Correction:** Finding is accurate. Only adjustment is severity: it is double-gated (native Metal backend, used for --remaster, AND Video.SunShadow which is default off) and only manifests for casters whose remapped depth falls outside the light frustum, so impact is low/niche rather than medium. The suggested fix (add [enc setDepthClipMode:MTLDepthClipModeClamp] at the top of mtl_shadow_encode, which covers both the render and dump encoders since both go through it) is correct.

### 38. 🟠 ✅ SSAO v2 hemisphere reprojection divides by view-Z (sp.z) with no positive/near-zero guard

- **File:** `src/platform/fast3d/gfx_metal.mm:2194` · `mtl_ensure_ssao_program (ssaoFragment MSL)`
- **Severity:** medium · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** In the hemisphere AO pass (SsaoMode=2, Metal-only), each of the 12 kernel samples is reprojected to screen space via `float2 suv = float2(sp.x * u.projX / sp.z, sp.y * u.projY / sp.z);` where sp = P + (TBN*kernel)*radius and P.z is the positive view distance. The kernel z-components point along +N (toward the camera, i.e. decreasing z), and radius = g_pcSsaoRadius*80 = 40 view-units by default. For surface points with P.z < ~40 (close geometry past nearCut: corridor walls, door frames, crates, props right in front of the player — well within the farCutoff=800 world scale), a sample can reach sp.z <= 0, producing a division by ~0/negative. That yields an inf/NaN or wildly wrong `suv`, so `dep.sample(ds, suv)` fetches an unrelated (clamped/garbage) depth, S is reconstructed nonsensically, and the occlusion term flickers. There is no guard (e.g. `if (sp.z <= near) skip;` or clamping sp.z) as most reference SSAO implementations use.
- **Evidence:**

  ```
  Line 2193-2196: `float3 sp = P + (TBN * kKernel[i]) * u.radius;` then `float2 suv = float2(sp.x * u.projX / sp.z, sp.y * u.projY / sp.z);` — sp.z used as a divisor unguarded. radius set in mtl_fill_ssao_uniforms:2274 `u->radius = g_pcSsaoRadius * 80.0f;`. Early-outs only reject far (P.z>=farCutoff) / sky / viewmodel, never the sp.z<=0 case.
  ```
- **Player impact:** With hemisphere AO enabled (Video.SsaoMode=hemisphere), flickering dark/bright speckle and unstable occlusion on surfaces close to the camera. Reachability is limited: --remaster deliberately keeps planar v1, so this only triggers when a user explicitly selects hemisphere mode.
- **Suggested fix:** Reject or clamp near-plane samples before the divide, e.g. skip the sample when `sp.z <= u.nearPlaneZ` (or a small epsilon like the near-cut view distance), matching standard hemisphere-SSAO reprojection guards.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The quoted code is exactly present. gfx_metal.mm:2193-2194 (inside the ssaoFragment MSL string built by mtl_ensure_ssao_program) reads `float3 sp = P + (TBN * kKernel[i]) * u.radius;` then `float2 suv = float2(sp.x * u.projX / sp.z, sp.y * u.projY / sp.z);` with no positive/near guard on sp.z. radius = g_pcSsaoRadius*80 = 40 view-units (line 2274), kernel magnitudes reach ~0.86, so the offset's z-component spans roughly ±34 view-units. The only early-outs are near-cut/sky (d<=nearCut||d>=skyCut) and far (P.z>=farCutoff=800); none reject small/degenerate sp.z. So for close geometry (P.z in the single digits to low tens, reachable when the player is near a wall/crate; nearCut=0.02 window-depth only culls the viewmodel) a sample's sp.z can reach ~0 or negative, so the reprojection is degenerate. That much is real: standard hemisphere-SSAO does guard this divide.

However the claimed impact is overstated. (1) The impact is bounded, not a NaN/inf that corrupts output: the PASS-A depth sampler s_depth_sampler (bound at 2328) is ClampToEdge (created 1231-1233), so a huge/inf suv clamps to an edge texel and returns a finite depth; the resulting S is then range-rejected by `rangeChk = smoothstep(0,1, radius/len)` (→0 for far samples) and the output is `pow(saturate(1.0 - occ/12.0), power)` — saturated, no NaN persists. Worst case is a single mis-weighted 1/12 sample → minor speckle, further attenuated by the optional bilateral blur. (2) The finder's stated mechanism ("kernel z points toward camera, i.e. decreasing z") is imprecise: in this positive-view-distance convention the depth-reconstructed normal tends to point into-screen (+z), so the dominant N·kz term ADDS to sp.z; a zero-crossing actually requires tilted/grazing surfaces where the lateral TBN terms dominate, not a uniform march toward the camera. (3) Reachability is genuinely narrow: g_pcSsaoMode defaults to 1 (planar, all backends); mode 2 hemisphere is Metal-only and explicitly excluded from --remaster (platform_sdl.c:2120), requiring the user to opt in via Video.SsaoMode=hemisphere.
  - **Correction:** The unguarded divide by sp.z is real, but it does not produce persistent NaN/inf or garbage occlusion of the severity described: the depth sampler is ClampToEdge and the range check + saturate bound an errant sample to at most a minor 1/12-weight speckle. It is a legitimate robustness gap (a near-plane skip/clamp of sp.z would be correct) but only manifests as subtle flicker on close/grazing surfaces, only in the non-default, opt-in, Metal-only hemisphere mode (Video.SsaoMode=hemisphere, not enabled by --remaster). The mechanism is tilted-surface/grazing driven, not the uniform toward-camera march claimed; severity is low, not medium.

### 39. 🟠 ❓ ZMODE_DEC (decal) depth bias differs materially between GL and Metal backends

- **File:** `src/platform/fast3d/gfx_metal.mm:3018` · `mtl_open_scene_encoder (decal polygon-offset apply)`
- **Severity:** medium · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** medium
- **What's wrong:** gfx_pc.c decodes the N64 decal z-mode correctly (gfx_emit_loaded_triangle: `uint16_t zmode = rdp.other_mode_l & ZMODE_DEC;`) and passes the identical zmode (0xc00) to both backends. The OpenGL backend applies `glPolygonOffset(factor=-2.0, units=-2.0)` (gfx_opengl.c:1796-1797 with defaults at gfx_opengl.c:154-155); for a 24-bit depth buffer the constant term is units*r ≈ -2 * 5.96e-8 ≈ -1.2e-7. The Metal backend instead applies `setDepthBias:-7.5e-6f slopeScale:-2.0f`. The slope term matches (-2.0) but the constant depth bias (-7.5e-6) is ~60x larger than GL's. The inline comment even claims it mirrors GL's 'factor/units -2', which it does not for the constant term. Decal surfaces are pushed toward the camera much more aggressively on Metal than on GL.
- **Evidence:**

  ```
  gfx_metal.mm:3016-3021 `/* ZMODE_DEC decal polygon offset (gfx_opengl.c:1582-1595, factor/units -2). */\n    if (s_zmode == 0xc00 && s_depth_test && s_depth_compare) {\n        [s_enc setDepthBias:-7.5e-6f slopeScale:-2.0f clamp:0.0f];` vs gfx_opengl.c:1795-1797 `gfx_diag_zmode_dec_offset_values(&factor, &units); glEnable(GL_POLYGON_OFFSET_FILL); glPolygonOffset(factor, units);` with defaults `g_diag_zmode_dec_offset_factor = -2.0f; g_diag_zmode_dec_offset_units = -2.0f;`
  ```
- **Player impact:** Decals (bullet holes, blood splats, floor grates, posters — GE uses AA_ZB_XLU_DECAL2/AA_ZB_OPA_DECAL2) can z-fight or bleed through nearby geometry at different distances depending on whether the player is running the GL or Metal backend; the two renderers are not visually identical for decal-heavy scenes.
- **Suggested fix:** Derive the Metal constant depthBias from the same r-scaled value GL uses for the active depth format (or measure and match on hardware), and make GL/Metal share one decal-bias constant so both backends resolve decals identically. Reconcile the misleading comment.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). Every cited line is accurate: GL allocates GL_DEPTH_COMPONENT24 / GL_DEPTH24_STENCIL8 (gfx_opengl.c:2559-2563) with glPolygonOffset(-2,-2) for ZMODE_DEC (1786-1797), while Metal allocates MTLPixelFormatDepth32Float (gfx_metal.mm:979) and uses setDepthBias:-7.5e-6 slopeScale:-2 for the same s_zmode==0xc00 decal path (3018). So the descriptive facts hold. But this is not a genuine defect. The depth format choice is deliberate and hardware-forced — the in-code comment (975-977) states Depth32Float is required because native depth sampling op-hangs GL-over-Metal, and Apple GPUs have no 24-bit unorm depth. The Metal decal bias is intentionally per-backend-tuned with an explicit source cross-reference to the GL path (comment at 3016). The finder concedes the item is unavoidable, the correct choice, and offered only for parity awareness. The claimed player impact (decals 'can z-fight or shimmer slightly differently') is unsubstantiated speculation with no repro or evidence of a perceptible difference; it does not rise to a real defect.
  - **Correction:** The code facts are correct, but the item is an intended, hardware-mandated tradeoff with a deliberately per-backend-tuned decal bias, not a bug. Any player-visible z-fighting divergence is unsubstantiated. Appropriate as a documentation/awareness note only, not a finding.

### 40. 🟡 ✅ Semaphore permit leaks when [s_queue commandBuffer] returns nil after the throttle wait, eventually deadlocking the render thread

- **File:** `src/platform/fast3d/gfx_metal.mm:1670` · `mtl_start_frame / mtl_end_frame`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** start_frame decrements s_frame_sem at line 1639 (`dispatch_semaphore_wait`) and THEN unconditionally creates the command buffer at line 1670 (`s_cmdbuf = [s_queue commandBuffer];`) with no nil check. The only place the permit is ever returned is the completion handler added in mtl_end_frame, which is guarded by `if (s_cmdbuf == nil) return;` (line 2641) BEFORE the handler is attached (lines 2648-2656). If commandBuffer returns nil (device removal/loss, or extreme memory pressure), the wait already consumed a permit but end_frame bails at 2641 without ever calling `dispatch_semaphore_signal`. After MTL_VBUF_SLOTS (3) such frames the semaphore reaches 0 and the next start_frame blocks forever at line 1639 (DISPATCH_TIME_FOREVER) — a permanent render-thread hang. The pairing comment at 1638 ("exactly paired with the signal in end_frame") assumes cmdbuf creation always succeeds; the nil path breaks that invariant.
- **Evidence:**

  ```
  L1639 `if (s_frame_sem != nil) dispatch_semaphore_wait(s_frame_sem, DISPATCH_TIME_FOREVER);`  L1670 `s_cmdbuf = [s_queue commandBuffer];` (unchecked)  L2641 `if (s_cmdbuf == nil) return;` (skips the signal handler at L2648-2656)
  ```
- **Player impact:** On eGPU unplug / GPU reset / severe VRAM pressure the game hard-freezes (black or last-frame) instead of dropping a frame and recovering; requires force-quit.
- **Suggested fix:** After line 1670, if `s_cmdbuf == nil` signal the semaphore back (or move the wait to only run once a non-nil command buffer is obtained) so every wait is matched by exactly one signal on all paths. Alternatively add the completion handler + signal unconditionally when a wait occurred, tracking 'wait happened this frame' independently of s_cmdbuf.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). All cited code matches exactly. s_frame_sem is created with count MTL_VBUF_SLOTS=3 (line 1200). start_frame waits at line 1639 (DISPATCH_TIME_FOREVER), consuming a permit, then unconditionally does s_cmdbuf = [s_queue commandBuffer] at line 1670 with no nil check. The only signal site is the completion handler in end_frame (lines 2648-2656), which is reached only if execution passes line 2641's `if (s_cmdbuf == nil) return;`. So on the nil-cmdbuf path the wait's permit is never returned; after 3 such frames the semaphore hits 0 and the next start_frame blocks forever — a permanent render-thread hang. The pairing comment at line 1638 ("exactly paired with the signal in end_frame") is genuinely violated by this path. The mechanism is sound as a code-level invariant defect. Caveat on likelihood: the retained-reference `[s_queue commandBuffer]` almost never returns nil in practice (its documented nil condition is resource/uncommitted-buffer exhaustion; GPU/device loss instead surfaces as an errored completion where the handler still fires and signals), so the finder's headline trigger of 'device removal/loss' is somewhat off — device loss does NOT deadlock here because the handler runs. The real (rare) trigger is command-buffer allocation failure under extreme pressure. The leak itself is real either way.
  - **Correction:** The deadlock triggers only when [s_queue commandBuffer] itself returns nil (documented for command-buffer allocation exhaustion), NOT on ordinary GPU/device loss — with the retained-reference commandBuffer, device loss produces a non-nil buffer whose completion handler still fires and signals the semaphore, so it recovers. The invariant violation and eventual hang-after-3-leaked-frames are real; the specific "eGPU unplug / GPU reset" scenarios in player_impact largely do not hit this path.

### 41. 🟡 ❌ Frame silently dropped (no present) when nextDrawable returns nil or size-mismatches, causing Metal-only freeze/stutter during live resize that GL never exhibits

- **File:** `src/platform/fast3d/gfx_metal.mm:2695` · `mtl_end_frame`
- **Severity:** low · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** end_frame acquires `s_drawable = [s_layer nextDrawable]` (line 2695) and only blits+presents when `s_drawable != nil && present_src != nil && drawable.texture.width==s_fb_w && height==s_fb_h` (lines 2696-2706). If nextDrawable returns nil (transient, or up to a ~1s timeout when the surface is occluded) OR the drawable size does not match the render target (which happens during a live window resize / display-scale change while CAMetalLayer.drawableSize is being reconciled asynchronously — the code even re-asserts drawableSize at 2660-2662 precisely because this races), the whole rendered frame is committed (line 2756) but never presented. The screen holds the previously presented image. The GL backend presents unconditionally via SDL_GL_SwapWindow every frame, so this frozen-frame behavior is Metal-specific. The acquired-but-unpresented drawable is dropped (nil'd at 2759), briefly starving the 3-deep drawable pool during the stall.
- **Evidence:**

  ```
  L2695 `s_drawable = [s_layer nextDrawable];`  L2696-2697 `if (s_drawable != nil && present_src != nil && (int)s_drawable.texture.width == s_fb_w && (int)s_drawable.texture.height == s_fb_h) {`  ... else no `presentDrawable`; L2756 `[s_cmdbuf commit];` still runs; L2759 `s_drawable = nil;`
  ```
- **Player impact:** Visible image freeze / stutter for a handful of frames while dragging or resizing the window or moving it between displays of differing backing scale; not seen on the GL backend.
- **Suggested fix:** On size mismatch, still present by scaling/letterboxing or by re-creating targets to the drawable size before the blit; on transient nil, retry once. At minimum log-rate-limit and avoid acquiring the drawable until sizes are confirmed to reduce pool churn.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited lines match the real code (2695 nextDrawable; 2696-2706 conditional blit+present; 2756 commit; 2759 nil). However the core mechanism is mischaracterized. The size-mismatch/resize scenario the finder describes is explicitly defended against by lines 2660-2662, which the finder cites as evidence of an unhandled race but is actually the mitigation: drawableSize is re-asserted to s_fb_w/s_fb_h synchronously on the same thread immediately before nextDrawable, and the vended drawable inherits that size, so the equal-size guard at 2696-2697 passes. The code comment states this purpose verbatim ("so the equal-size check below doesn't drop the present and freeze the screen"). The finder inverted the meaning of the code it quoted. The only residual path is a transient nextDrawable==nil, which is standard, unavoidable Metal behavior — you cannot present without a drawable, holding the prior frame for one frame is correct, and it self-corrects on the next frame (no multi-frame freeze). The pool-starvation claim is speculative and the drawable is released at frame end. This is not a genuine defect or player-visible freeze.
  - **Correction:** The resize/size-mismatch freeze is already handled at lines 2660-2662 (synchronous drawableSize re-assertion before nextDrawable), which the finder mis-cited as a race rather than the mitigation. The only remaining behavior is dropping a single present when nextDrawable transiently returns nil, which is correct and unavoidable Metal semantics, not a defect.

### 42. 🟡 ❌ Post-FX intermediate targets are single-buffered (not ring-buffered), so Metal auto-hazard tracking serializes the GPU across frames when RemasterFX/SSAO/SMAA is enabled, defeating the frame-overlap the vbuf ring provides

- **File:** `src/platform/fast3d/gfx_metal.mm:1019` · `mtl_ensure_targets / mtl_run_ssao / mtl_run_smaa / mtl_run_output_filter`
- **Severity:** low · **Class:** port-defect · **Category:** performance · **Finder confidence:** high
- **What's wrong:** The geometry vertex arena is triple-buffered (s_vbuf[MTL_VBUF_SLOTS]) and semaphore-throttled so up to 3 frames overlap on the GPU. But every post-FX intermediate is a single texture reused every frame: s_final_color / s_filter_low (allocated once at lines 1019-1020, read+written by mtl_run_output_filter passes 0/1 at 2052-2054), s_ssao_raw / s_ssao_tmp (2110-2111, ping-ponged in mtl_run_ssao 2333-2334 and read by the composite at 2075), and s_smaa_edges/blend/out (read/written in mtl_run_smaa 2613-2617). None sets MTLHazardTrackingModeUntracked, so they are default-tracked private textures. When frame N+1's SSAO/filter WRITES one of these while frame N's output-filter READ of the same texture has not completed, Metal inserts an automatic cross-command-buffer WAR dependency, forcing frame N+1's post chain to wait for frame N to finish. Correctness is preserved (no corruption), but it collapses the triple-buffer overlap specifically for the post passes whenever any RemasterFX post-FX is active. Default path (RemasterFX off) never touches these, so impact is confined to remaster/quality modes.
- **Evidence:**

  ```
  L1019-1020 `s_final_color = finalc; s_filter_low = low;` (single instances)  L2053-2054 `id<MTLTexture> passes_src[2] = { pass0_src, s_filter_low }; id<MTLTexture> passes_dst[2] = { s_filter_low, s_final_color };`  L2333-2334 ping-pong `mtl_ssao_blur_pass(cmdbuf,&u,s_ssao_raw,s_ssao_tmp,0); ...s_ssao_tmp,s_ssao_raw,1);`  no `hazardTrackingMode` set anywhere in the file.
  ```
- **Player impact:** When SSAO/SMAA/output-filter (RemasterFX) is enabled, the GPU cannot overlap consecutive frames' post work, reducing GPU headroom on borderline scenes; no effect on the default faithful path.
- **Suggested fix:** Ring-buffer the post-FX intermediates by s_vbuf_slot (as s_vbuf/s_shadow_vbuf already are), or mark them and drive the dependency explicitly, so frame N+1's post writes target a different instance than frame N's still-in-flight reads.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The literal code facts are correct: s_final_color/s_filter_low (1019-1020), s_ssao_raw/s_ssao_tmp (2110-2111), and s_smaa_edges/blend/out (2391) are single static private textures, not ring-indexed by s_vbuf_slot, and no hazardTrackingMode is set. However, the performance mechanism is a misunderstanding. The primary scene render targets s_scene_color/s_scene_depth/s_snapshot_tex (1016-1018) are ALSO single-buffered private textures written every frame on ALL paths, including the default faithful path. Frame N+1's geometry pass writes s_scene_color and cannot start until frame N's last read of s_scene_color completes, so the single scene-color target is already the dominant, unconditional frame-to-frame GPU serialization point. The vbuf/shadow_vbuf ring exists to let the CPU encode ahead without stalling on reuse of CPU-written StorageModeShared vertex buffers (semaphore-throttled) — it does not provide GPU-side overlap of consecutive frames' fragment work into shared render targets, and cannot given the single-buffered scene color. Therefore the single-buffered post-FX intermediates add no serialization beyond what s_scene_color already imposes: frame N+1 is already gated on frame N's scene-color read before the post chain runs. The claim that this 'defeats the frame-overlap the vbuf ring provides' misattributes the ring's purpose and describes a bottleneck already present on every path. Correctness is fine (finder concedes this), and the incremental perf cost is essentially nil.
  - **Correction:** The post-FX intermediates are indeed single-buffered as described, but so is the always-used s_scene_color/s_scene_depth render target on every path. Because the single scene-color target already serializes consecutive frames' GPU render work (and the vbuf ring only provides CPU-ahead encoding, not GPU frame overlap of render targets), the single-buffered post-FX targets introduce no additional serialization. There is no measurable player-facing performance regression.

### 43. 🟡 ✅ MSL shader compilation runs outside any autoreleasepool, leaking the compiler's transient allocations

- **File:** `src/platform/fast3d/gfx_metal.mm:716` · `mtl_create_and_load_new_shader`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** mtl_create_and_load_new_shader has no @autoreleasepool of its own. It is invoked from gfx_pc.c's DL interpreter (gfx_lookup_or_create_shader_program) during the window BETWEEN start_frame and end_frame — i.e. after start_frame's @autoreleasepool block has already drained and returned (the pool spans only lines 1588-1681). The file's own comment at line 1588 confirms there is NO outer pool: 'without it, the C game loop never drains and RSS/drawable pool grow.' So the autoreleased NSString of the full MSL source (line 716, `[NSString stringWithUTF8String:src.p]`, backed by up to the 65536-byte TB) plus every transient object Metal's runtime shader compiler autoreleases inside `newLibraryWithSource:` (line 717) are orphaned with no pool to drain them. Unlike mtl_draw_triangles and mtl_upload_texture, which both self-wrap in @autoreleasepool, this hot-for-startup path does not. The leak is bounded (once per distinct combiner, a few hundred over a session) and one-time, not per-frame, so it is a footprint bump at level-load rather than unbounded growth.
- **Evidence:**

  ```
  Line 716: `NSString *nssrc = [NSString stringWithUTF8String:src.p];` then line 717 `[s_device newLibraryWithSource:nssrc options:nil error:&err];` — the function body (695-752) contains no @autoreleasepool, while the file comment at 1588 states the C game loop never drains autoreleased temporaries on its own.
  ```
- **Player impact:** A modest, bounded one-time RSS increase (roughly the MSL source size plus compiler scratch, times the number of unique combiners ~ a few MB) accrued as shaders compile on first encounter during level loads; not observable in gameplay and never grows per frame.
- **Suggested fix:** Wrap the body of mtl_create_and_load_new_shader in `@autoreleasepool { ... }` exactly as mtl_draw_triangles/mtl_upload_texture already do, so the NSString and the compiler's transient autoreleased objects are reclaimed immediately after each shader builds.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). Re-read gfx_metal.mm 695-752: mtl_create_and_load_new_shader has no @autoreleasepool. Lines 716-717 match the quoted evidence exactly (autoreleased NSString from stringWithUTF8String, then synchronous newLibraryWithSource:). grep confirms the file's @autoreleasepool blocks are only at 1588/2636/2806/2950/3131 — start_frame, end_frame, upload_texture, draw_triangles, and one more — so the shader-build path is genuinely the exception among the hot paths. The 1588 comment confirms the custom C game loop drains no autoreleased temporaries on its own, and gfx_pc.c:14480 calls gfx_lookup_or_create_shader_program (fn-ptr wired at 3211) during DL interpretation between start_frame and end_frame, after start_frame's pool has already drained. So the mechanism holds: the MSL NSString plus the runtime shader compiler's transient autoreleased objects have no pool in scope and leak. However, as the finder honestly states, this is bounded and one-time — once per distinct combiner (a few hundred per session, ~a few MB), never per-frame, never observable in gameplay. It is a real but negligible robustness/cleanliness defect.

### 44. 🟡 ❌ Vertex-arena high-water target (s_vbuf_want) ratchets up permanently and never decays

- **File:** `src/platform/fast3d/gfx_metal.mm:3049` · `mtl_draw_triangles`
- **Severity:** low · **Class:** port-defect · **Category:** performance · **Finder confidence:** high
- **What's wrong:** The triple-buffered vertex arena sizes its three StorageModeShared ring buffers to s_vbuf_want, which is a monotonic high-water mark that only ever increases. In mtl_draw_triangles the cursor bump path raises it (`if (s_vbuf_cursor > s_vbuf_want) s_vbuf_want = s_vbuf_cursor;`, line 3049) and the overflow-fallback path raises it further (`if (aligned + need > s_vbuf_want) s_vbuf_want = aligned + need;`, line 3051). start_frame then (re)allocates each slot up to s_vbuf_want (lines 1643-1648). Because s_vbuf_want is set to the cumulative per-frame vertex bytes and is never lowered, a single pathological or transient heavy-geometry frame (or a temporary overflow batch) permanently inflates ALL THREE ring buffers to that worst case for the remainder of the session, even after the game returns to light scenes (menus/HUD). This is bounded by the largest single-frame vertex payload (not a leak), but the memory is held forever rather than tracking the current working set.
- **Evidence:**

  ```
  Line 915 seeds `static size_t s_vbuf_want = (2u << 20);`; line 3049 `if (s_vbuf_cursor > s_vbuf_want) s_vbuf_want = s_vbuf_cursor;  /* grow target for next (re)alloc */`; line 3051 `if (aligned + need > s_vbuf_want) s_vbuf_want = aligned + need;`; start_frame lines 1643-1648 reallocate the slot whenever `s_vbuf_cap[slot] < s_vbuf_want`. There is no path that ever decreases s_vbuf_want.
  ```
- **Player impact:** Slightly elevated steady-state memory: after the heaviest scene visited, ~3x the peak per-frame vertex bytes stays resident for the rest of the session even in light scenes. No visual or gameplay effect.
- **Suggested fix:** Track the arena target with hysteresis/decay (e.g. keep a per-frame max and let s_vbuf_want relax toward a rolling average of recent frames, only shrinking slots when the observed working set stays low for N frames), rather than latching the all-time maximum permanently.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The code-level claims are all accurate: s_vbuf_want is seeded at 2MB (line 915), raised at lines 3049 and 3051, consumed by start_frame's per-slot (re)alloc (lines 1643-1648), and never decreased anywhere. So the described monotonic high-water mechanism is real. But it is not a defect. It is the documented, intended behavior of the triple-buffered StorageModeShared vertex arena (comment at 900-904 explicitly calls out growing the high-water for next realloc). Memory is strictly bounded by the largest legitimate single-frame vertex payload — the game's heaviest scenes are a normal, revisited part of the working set, not a pathological/attacker-controlled input, so steady state naturally sits at the working-set peak anyway. There is no leak and no unbounded growth. The finder itself concedes no visual or gameplay effect and only a few MB of extra resident memory. Not shrinking mid-session is also a reasonable tradeoff, since reallocating shared buffers during play risks hitches that matter more than the memory. This is a micro-optimization opportunity, not a player- or stability-relevant defect.
  - **Correction:** The mechanism is accurately described, but the effect is bounded by the largest legitimate frame's vertex bytes (times three slots), which the game reaches in normal play regardless; it is intended triple-buffer design, not a defect, and has no player-visible impact.

### 45. 🟡 ✅ Game textures allocated MTLStorageModeShared rather than Private (VRAM-locality divergence from GL on discrete-GPU Macs)

- **File:** `src/platform/fast3d/gfx_metal.mm:2817` · `mtl_upload_texture`
- **Severity:** low · **Class:** parity-divergence · **Category:** performance · **Finder confidence:** medium
- **What's wrong:** Every game texture is created with storageMode MTLStorageModeShared and populated via replaceRegion (lines 2817-2821). On Apple Silicon this is optimal (unified memory, Shared == Private for sampling). On an Intel Mac with a discrete GPU, however, Shared keeps the texture in system RAM and each GPU sample crosses the PCIe boundary, whereas the OpenGL backend's glTexImage2D lets the driver place the texture in VRAM (Private-equivalent). The offscreen render targets in this file correctly use MTLStorageModePrivate (mtl_ensure_targets, lines 973/982/990/998), so only the sampled game-texture set diverges. Because the Metal backend is opt-in and primarily targets Apple Silicon, real-world impact is minimal, but it is a genuine backend divergence: on discrete GPUs textured draws sample from slower memory than the GL path would.
- **Evidence:**

  ```
  Lines 2816-2818 in mtl_upload_texture: `d.usage = MTLTextureUsageShaderRead; d.storageMode = MTLStorageModeShared; id<MTLTexture> tex = [s_device newTextureWithDescriptor:d];` followed by `[tex replaceRegion:...]`. Contrast the scene targets which set `cd.storageMode = MTLStorageModePrivate;` (line 973).
  ```
- **Player impact:** On discrete-GPU (Intel) Macs, reduced texture-sampling throughput versus the GL backend under heavy texturing; on Apple Silicon (the primary target) there is no measurable difference since Shared and Private share unified memory.
- **Suggested fix:** On non-unified-memory devices (query s_device.hasUnifiedMemory == NO), upload via a Private texture using a staging Shared buffer + blit (or a temporary Shared texture blitted to Private), matching the driver-managed VRAM residency GL provides; keep the current Shared+replaceRegion fast path when hasUnifiedMemory == YES.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code at gfx_metal.mm:2813-2821 (mtl_upload_texture) matches the finding verbatim: `d.usage = MTLTextureUsageShaderRead; d.storageMode = MTLStorageModeShared;` then `newTextureWithDescriptor` and `replaceRegion`. The contrast is also accurate: the offscreen scene targets in mtl_ensure_targets (lines 973/982/990/998) all use `MTLStorageModePrivate`. So the factual/quoted evidence is correct, and the mechanism is technically sound: on a discrete-GPU Intel Mac, MTLStorageModeShared keeps the resource in system RAM and the GPU samples it across PCIe, whereas GL's glTexImage2D lets the driver place it in VRAM. That is a genuine, real backend divergence in memory residency. However, the practical significance is very small and the finder honestly says so: the Metal backend is opt-in and explicitly targets Apple Silicon (the ensure_targets comments confirm the whole Metal path exists to fix GL-over-Metal op-hangs on Apple GPUs), where hasUnifiedMemory==YES makes Shared and Private equivalent — zero impact. On Intel discrete GPUs the effect is a marginal, largely GPU-cache-mitigated sampling-throughput difference on a legacy/secondary platform, not a correctness, stability, or perceptible-gameplay problem. Also note replaceRegion cannot target a Private texture directly, so Shared is the simple portable choice; the suggested hasUnifiedMemory-gated staging-blit is a valid micro-optimization, not a bug fix. Verdict is CONFIRMED on the facts (the divergence is real and the code says what's claimed), severity honestly low, but it does not rise to a genuine player-affecting issue.

### 46. 🟡 ❌ Metal backend has no per-tri XLU snapshot mode; GE007_XLU_SNAPSHOT_MODE=pertri is silently ignored, so overlapping coverage XLU cannot be made N64-exact on Metal

- **File:** `src/platform/fast3d/gfx_metal.mm:2980` · `mtl_draw_triangles`
- **Severity:** low · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** The OpenGL backend supports two snapshot granularities via GE007_XLU_SNAPSHOT_MODE: default per-batch and legacy 'pertri' (gfx_opengl.c:2182-2195, 2248-2265), where pertri re-copies the framebuffer before EVERY triangle and is the exact-parity fallback that correctly handles intra-batch overlapping translucent tris. mtl_draw_triangles implements only the single per-batch snapshot (one endEncoding/blit/reopen before one draw, lines 2999-3009) and never queries GE007_XLU_SNAPSHOT_MODE (Metal only reads GE007_DISABLE_ROOM_XLU_CVG_MEMORY at gfx_metal.mm:2872 and GE007_DIAG_XLU_RDP_CVG_MEMORY_BLEND_CC at 2883). So on Metal there is no way to obtain the exact-parity per-tri behavior that GL offers, and A/B parity testing between the two snapshot modes is impossible on Metal. This compounds the previous finding: the very content that the per-batch assumption mishandles has a GL escape hatch but no Metal one.
- **Evidence:**

  ```
  gfx_metal.mm mtl_draw_triangles: `if (rdp_mem && s_snapshot_tex != nil) { ... [s_enc endEncoding]; ... copyFromTexture:s_scene_color ... mtl_open_scene_encoder(false); }` then a single draw — no per-tri loop. GL counterpart gfx_opengl.c:2248 `if (gfx_opengl_xlu_snapshot_mode() == GFX_XLU_SNAPSHOT_PER_TRI) { for (size_t tri = 0; ...) { ...copy_framebuffer_snapshot...; glDrawArrays(GL_TRIANGLES, tri*3, 3); } }`. grep confirms GE007_XLU_SNAPSHOT_MODE appears only in gfx_opengl.c, never in gfx_metal.mm.
  ```
- **Player impact:** Default per-batch visuals match GL. But on any level content with overlapping intra-batch coverage translucency, GL users can force exact rendering with GE007_XLU_SNAPSHOT_MODE=pertri while Metal users are stuck with the approximate per-batch result; also blocks A/B parity validation on macOS.
- **Suggested fix:** Port the pertri branch into mtl_draw_triangles gated on the same env (loop: blit batch/tri rect, reopen encoder, draw 3 verts), or at minimum honor the env by falling back to a per-tri snapshot loop so parity/A-B is achievable on Metal.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The code is described accurately: mtl_draw_triangles (gfx_metal.mm:2949-3009) does only a single per-batch snapshot and never reads GE007_XLU_SNAPSHOT_MODE, while GL (gfx_opengl.c:2182-2265) offers both per-batch (default) and pertri (legacy). Grep confirms the env var lives only in gfx_opengl.c. However this is not a real player or robustness issue. The GL source itself labels pertri as the "legacy exact-parity A/B" path and treats per-batch as the intended faithful default; both backends render identically by default. The per-batch/pertri divergence only affects triangles overlapping within one same-material batch, which the GL comment identifies as coplanar/non-overlapping glass panes and foliage cards — i.e. effectively no visual difference in real content. pertri is a developer A/B diagnostic hatch that no player ever enables; its absence on Metal is a testing-convenience/tooling-parity gap, not a rendering defect or player-facing divergence. The finder even concedes default per-batch visuals match GL.
  - **Correction:** The mechanical/code claims are correct, but the framing overstates impact: pertri is a legacy developer A/B diagnostic, and per-batch (identical on GL and Metal) is the intended faithful default. The only real gap is that this specific A/B diagnostic cannot be run on macOS; default rendering is already at parity, so there is no player-facing "cannot make XLU N64-exact on Metal" defect.

### 47. 🟡 ❌ Scene depth buffer is 24-bit fixed on GL but 32-bit float on Metal, with a differently-tuned ZMODE_DEC decal bias — z-fighting/decal behavior can differ between backends

- **File:** `src/platform/fast3d/gfx_metal.mm:979` · `mtl_ensure_targets / mtl_draw_triangles`
- **Severity:** low · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** GL allocates the scene depth attachment as GL_DEPTH_COMPONENT24 (or GL_DEPTH24_STENCIL8) at gfx_opengl.c:2559-2563 — a 24-bit fixed-point buffer with uniform window-space precision. Metal allocates MTLPixelFormatDepth32Float (gfx_metal.mm:979), whose precision is non-uniform (more near the near plane, ~2x less than 24-bit fixed near the far plane). Because the effective minimum-resolvable-depth differs, the ZMODE_DEC decal polygon offset also diverges: GL uses glPolygonOffset with factor/units default (-2,-2) scaled by the 24-bit r, while Metal uses a hand-tuned constant setDepthBias:-7.5e-6 slopeScale:-2 (gfx_metal.mm:3018). The slope term matches but the constant/format-dependent term is a different regime.
- **Evidence:**

  ```
  gfx_opengl.c:2562 `glTexImage2D(GL_TEXTURE_2D, 0, GL_DEPTH_COMPONENT24, ...)`; gfx_metal.mm:979 `texture2DDescriptorWithPixelFormat:MTLPixelFormatDepth32Float`; decal bias gfx_metal.mm:3018 `[s_enc setDepthBias:-7.5e-6f slopeScale:-2.0f clamp:0.0f];` vs GL glPolygonOffset default (-2,-2) at gfx_opengl.c:154-155 / 1786-1795.
  ```
- **Player impact:** Coplanar surfaces and decals (ZMODE_DEC: bullet marks, signage, terrain overlays) can z-fight or shimmer slightly differently on the Metal backend than on GL — most visible on distant surfaces where 32-bit float depth has less precision than GL's 24-bit fixed. Largely unavoidable: Apple GPUs do not support a 24-bit unorm depth format, so Depth32Float is the correct choice; the item is noted for parity awareness and to justify keeping the decal bias tuned per-backend.
- **Suggested fix:** Keep Depth32Float (no native 24-bit unorm on Apple GPUs) but validate the ZMODE_DEC decal bias on-device against GL, and consider deriving the Metal constant bias from the float format's per-primitive resolvable delta rather than a fixed magic number so it tracks depth range changes.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited code matches the quotes verbatim: gfx_metal.mm:3016-3021 applies setDepthBias:-7.5e-6 slopeScale:-2.0 for s_zmode==0xc00, and gfx_opengl.c:1786-1797/154-155 applies glPolygonOffset(-2.0,-2.0). So the quotes are real. But the claimed defect rests on a flawed same-format arithmetic comparison. The finder treats -7.5e-6 (Metal) and units*r=-1.2e-7 (GL, assuming 24-bit) as directly comparable normalized-depth offsets on the same buffer. They are not: Metal explicitly renders to MTLPixelFormatDepth32Float (gfx_metal.mm:1084, 979 comment 'native Depth32Float sample is the whole point'), while GL uses a fixed-point ~24-bit depth attachment. A float depth buffer has a highly non-uniform precision distribution versus fixed-point's uniform r, and Metal's depthBias interpretation for float depth is itself format-dependent, so the constant that resolves decals without z-fighting is necessarily different per format. Reproducing GL's raw numeric constant on Metal's float depth would NOT yield visual parity — it would likely under-bias and z-fight. The -7.5e-6 reads as a deliberate per-format tuning to achieve the SAME visual result GL gets, which is precisely the goal. The finder demonstrates no actual z-fighting divergence and concedes in the suggested fix 'or measure and match on hardware,' i.e. it was never verified. Under skepticism, the code does not substantiate a genuine player-visible parity bug. At most, the inline comment 'factor/units -2' is slightly imprecise (it strictly describes only the slopeScale term), a minor doc nit, not a player/robustness issue.
  - **Correction:** The two backends use different depth formats (Metal Depth32Float vs GL fixed-point ~24-bit), so a constant depth-bias term must differ between them to achieve the same visual decal resolution; the numeric 60x comparison is apples-to-oranges and does not establish a parity divergence. The only substantiable nit is that the inline comment's 'factor/units -2' phrasing accurately describes only the slopeScale (-2.0), not the constant depthBias term.

### 48. 🟡 ✅ Shadow-receiver fallback binds the live scene depth attachment as a sampled texture (read-while-write hazard)

- **File:** `src/platform/fast3d/gfx_metal.mm:3115` · `mtl_draw_triangles`
- **Severity:** low · **Class:** port-defect · **Category:** undefined-behavior · **Finder confidence:** medium
- **What's wrong:** When a draw uses the opt_sun_shadow shader variant, the receiver samples the shadow map at fragment texture index 5. If s_shadow_depth is still nil (shadow-resource allocation failed, or the feature was toggled on mid-frame before mtl_render_shadow_map ran), the code falls back to binding s_scene_depth. But s_scene_depth is the CURRENT depth render-target of the open scene encoder s_enc, and those draws run with depthWriteEnabled potentially true (mtl_depth_state_for). Binding a color/depth attachment that is simultaneously being written as a sampled texture is a Metal read/write hazard — undefined sampled values and a possible validation-layer abort under Metal API validation. It is transient (self-heals the next frame once the shadow map exists) and only reachable when g_pcSunShadow is enabled.
- **Evidence:**

  ```
  `id<MTLTexture> smap = (s_shadow_depth != nil) ? s_shadow_depth : s_scene_depth;` then `[s_enc setFragmentTexture:smap atIndex:5];` (lines 3115-3117) — s_scene_depth is the depth attachment of the still-open s_enc (bound in mtl_open_scene_encoder line 1176).
  ```
- **Player impact:** Rare, remaster-only: a one-frame garbage/undefined shadow term (flicker) on the frame sun-shadows are enabled or when shadow-map allocation fails; under Metal API validation it can hard-abort. No impact on the default path.
- **Suggested fix:** On the nil fallback, bind a neutral resource that is not the active attachment (e.g. s_white_tex or a dedicated 1x1 'no-shadow' depth) instead of s_scene_depth, or skip the shadow term for that frame (treat as fully lit).
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited evidence matches the code exactly. Line 3115: `id<MTLTexture> smap = (s_shadow_depth != nil) ? s_shadow_depth : s_scene_depth;` then line 3117 binds `smap` at fragment texture index 5 on the encoder `s_enc`. Line 1176 confirms `rpd.depthAttachment.texture = s_scene_depth` — i.e. `s_scene_depth` is the depth render-target of the still-open scene encoder that these very draws (line 3122 `[s_enc drawPrimitives...]`) run on. Sampling a texture that is simultaneously an attachment of the active render pass is a real Metal hazard: undefined sampled values without API validation, and a hard abort under Metal API validation. So the mechanism is genuine. That said, the finder overstates reachability: `mtl_ensure_shadow_resources(g_pcSunShadowRes)` is called synchronously at line 3114 immediately before the fallback and allocates `s_shadow_depth` unconditionally (lines 1447-1454). So the "feature toggled on mid-frame before mtl_render_shadow_map ran" case does NOT hit the fallback — the texture exists (holding stale/uninitialized depth → one frame of wrong shadows, which is a visual glitch, not a hazard). The fallback to `s_scene_depth` is only actually reached when `newTextureWithDescriptor` returns nil (allocation failure / OOM). That is a much rarer path than described. The defect is real but latent and low-severity, matching the claimed severity. Note the suggested fix's `s_white_tex` is a color texture that would likely mis-bind against the comparison sampler at unit 5; the 'dedicated 1x1 no-shadow depth' or skip-the-term alternative is the sound remedy.
  - **Correction:** The code substantiates the read-while-write hazard, but the primary reachable trigger is shadow-depth texture allocation failure, not the mid-frame-toggle case. Because mtl_ensure_shadow_resources() runs synchronously at line 3114 and allocates s_shadow_depth before the fallback check, the mid-frame-enable scenario yields a valid-but-stale texture (one frame of wrong shadows), not the s_scene_depth fallback. Only nil allocation (OOM) actually binds the live depth attachment as a sampled texture. Severity low is correct; the hazard itself (binding the active render-pass depth attachment at fragment texture unit 5) is genuine.

### 49. 🟡 ✅ MSAA is silently ignored on the Metal backend (Video.MSAA has no effect under --remaster)

- **File:** `src/platform/fast3d/gfx_metal.mm:3013` · `mtl_draw_triangles`
- **Severity:** low · **Class:** parity-divergence · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The GL backend implements a full multisample scene target (g_scene_msaa_fbo / g_scene_msaa_color_rb, resolved in gfx_opengl_resolve_scene_target) honoring Video.MSAA, plus GL_SAMPLE_ALPHA_TO_COVERAGE for cutout edges. The Metal backend has no MSAA path at all: the scene color/depth targets are created single-sample (mtl_ensure_targets, no sampleCount), and every scene PSO is requested with rasterSampleCount hardcoded to 1 (`mtl_pso_for(ms, s_blend, 1, ...)`). So a user who sets Video.MSAA>0 gets geometry MSAA + alpha-to-coverage on GL but nothing on Metal. This is largely by design (the remaster AA stack is RenderScale/SSAA + FXAA/SMAA, and g_pcMsaaSamples defaults to 0), but the setting is accepted and then silently dropped only on Metal.
- **Evidence:**

  ```
  `id<MTLRenderPipelineState> pso = mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha);` (line 3013) always passes samples=1; scene targets in mtl_ensure_targets (lines 969-983) set no sampleCount. GL by contrast builds g_scene_msaa_fbo at gfx_opengl.c:2601-2643 and enables GL_SAMPLE_ALPHA_TO_COVERAGE (gfx_opengl.c:747).
  ```
- **Player impact:** On macOS --remaster (Metal), enabling Video.MSAA produces no anti-aliasing and no alpha-to-coverage cutout feathering, unlike the GL backend — a quiet, surprising difference. Minor because MSAA is off by default and RenderScale/FXAA/SMAA cover AA.
- **Suggested fix:** Either implement an MSAA scene target + MultisampleResolve on Metal keyed off g_pcMsaaSamples (and add rasterSampleCount to the PSO key, which currently already carries a samples field but is always 1), or explicitly document/warn that Video.MSAA is unsupported on the Metal backend so the setting isn't misleading.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Re-read the cited code. gfx_metal.mm:3013 does call mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha) with a literal 1; line 1083 assigns d.rasterSampleCount = samples; and every post/shadow PSO (1951, 2242, 2256, 2572) hardcodes rasterSampleCount = 1. The comment at 2001-2002 confirms "Metal is single-sample here." A grep for msaa/multisample/alphaToCoverage in gfx_metal.mm returns only those two comment lines — there is no MSAA scene target and no alpha-to-coverage in the Metal path, and no warning that Video.MSAA is dropped. The GL contrast is real: gfx_opengl.c drives GL_SAMPLE_ALPHA_TO_COVERAGE (739-749) and allocates a genuine multisample target gated on gfx_opengl_effective_msaa_samples() (2414, 2559/2620 GL_DEPTH24_STENCIL8). So Video.MSAA>0 on the Metal backend is a genuine silent no-op and cutouts are hard alpha-tested only. All quoted evidence matches the actual code.
  - **Correction:** Finding is accurate as written; only the severity is overstated. It is a real parity divergence but narrow: the default --remaster preset pins Video.MSAA=0 and uses SSAA+FXAA, so defaults are unaffected. It only bites a user who explicitly sets Video.MSAA>0 on the Metal backend, and that user still has functioning SSAA/FXAA anti-aliasing — the knob is a silent no-op rather than a loss of all AA. That makes it low, not medium. The suggested fix (option b: a one-time "Video.MSAA ignored on Metal backend" log) is the proportionate remedy.

### 50. 🟡 ✅ Half-res SSAO composite upsamples with plain bilinear, not the depth-aware filter it built

- **File:** `src/platform/fast3d/gfx_metal.mm:1896` · `mtl_ensure_filter_program (filterFragment MSL, ssaoMode==2 composite)`
- **Severity:** low · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** When Video.SsaoHalfRes=1 the AO buffer (s_ssao_raw) is allocated at half scene resolution (mtl_ensure_ssao_targets:2098) and the PASS-B bilateral (depth-weighted) blur runs at half res, correctly preserving AO edges within that resolution. But the final composite in the output filter upsamples the half-res AO to full res with a plain linear sampler (`uSsaoTex.sample(ssaoSmp, in.vTexCoord)`, ssaoSmp = s_filter_smp linear clamp) with no depth-aware/joint-bilateral upsample. At object silhouettes the bilinear tap blends AO across the depth discontinuity, so the carefully depth-preserved half-res AO bleeds a ~1px halo back over the edge on upscale — defeating the point of the bilateral blur.
- **Evidence:**

  ```
  Composite line 1896: `if (u.ssaoMode == 2) { float aoV = uSsaoTex.sample(ssaoSmp, in.vTexCoord).r; ... }`; sampler bound at mtl_run_output_filter:2075-2076 is s_filter_smp (linear). Half-res alloc at mtl_ensure_ssao_targets:2097-2104; the depth-aware blur it pairs with is ssaoBlurFragment:2209-2221.
  ```
- **Player impact:** Faint AO halo/bleed at object silhouettes when the half-res SSAO performance option is enabled (off by default). Full-res AO is unaffected.
- **Suggested fix:** Use a depth-guided (joint-bilateral) upsample in the composite for the half-res case (reuse the scene depth already bound at texture(1)), or only offer half-res AO with a bilateral upsample.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Re-read the cited code and every supporting site. Line 1896 matches the quoted composite verbatim and samples the AO buffer with `ssaoSmp`, bound at index 2 (line 2076) to `s_filter_smp`, which is created with MTLSamplerMinMagFilterLinear + ClampToEdge (lines 1960-1966) — a plain linear sampler, no depth input. The AO buffer is allocated at half scene resolution when g_pcSsaoHalfRes is set (mtl_ensure_ssao_targets, line 2098: aw = s_fb_w >> hr), and the depth-weighted (bilateral) blur ssaoBlurFragment (2209-2221) runs at AO/half res. The composite therefore upsamples half-res AO to full res with plain bilinear interpolation and no joint-bilateral/depth-guided upsample, so taps straddling a silhouette blend AO across the depth discontinuity, producing the described ~1px halo — a well-known half-res-SSAO limitation. Correctly scoped: at full res (default) the AO tex matches drawable res and linear taps land on texel centers (no cross-edge bleed), so only the non-default Video.SsaoHalfRes path is affected, and only under the non-default SSAO v2 (ssaoMode==2) remaster feature. Not a misread, gated-path confusion, or dead code; the defect is real but faint and double-gated behind non-default options.

### 51. 🟡 ❓ Sun-shadow constant depth-bias value reused verbatim across GL 24-bit fixed depth and Metal Depth32Float

- **File:** `src/platform/fast3d/gfx_metal.mm:1474` · `mtl_shadow_encode`
- **Severity:** low · **Class:** parity-divergence · **Category:** visual-artifact · **Finder confidence:** medium
- **What's wrong:** The Metal shadow depth pass sets `[enc setDepthBias:4.0f slopeScale:2.0f clamp:0.0f]` to mirror GL's `glPolygonOffset(2.0f, 4.0f)` (slope=2.0, units=4.0). The slope terms match, but the constant term's units differ by depth format: GL renders the shadow map to GL_DEPTH_COMPONENT24 (fixed-point, well-defined minimum-resolvable-difference r), while Metal renders to MTLPixelFormatDepth32Float (mtl_ensure_shadow_resources:1449), where the polygon-offset constant `r` is magnitude-dependent per the primitive's depth range. The same numeric constant (4) therefore produces a different effective bias on each backend, so the acne-vs-peter-panning balance is not actually at parity between GL and Metal even though the numbers are copied.
- **Evidence:**

  ```
  Metal: mtl_shadow_encode:1474 `[enc setDepthBias:4.0f slopeScale:2.0f clamp:0.0f];` with target format Depth32Float (mtl_ensure_shadow_resources:1449). GL: gfx_opengl_render_shadow_map:4003 `glPolygonOffset(2.0f, 4.0f);` with GL_DEPTH_COMPONENT24 (gfx_opengl.c:2562/3868).
  ```
- **Player impact:** With the opt-in sun shadow map enabled (Video.SunShadow, off by default), shadow acne or peter-panning can differ between the OpenGL and Metal backends. Practical impact is small because slopeScale dominates and the receiver adds a separate tunable normalized bias (g_pcSunShadowBias=0.0015).
- **Suggested fix:** Re-tune the Metal constant depthBias for the float depth target (or express the bias in the receiver's normalized-depth domain only) and validate acne/peter-panning against GL on the same scene rather than copying GL's fixed-point units value.
- **Verification:** UNCERTAIN (adj. severity low, real-player-issue=False). All quoted code is accurate: gfx_metal.mm:1474 sets depthBias 4.0/slopeScale 2.0 on a Depth32Float shadow target (line 1449), and gfx_opengl.c:4003 sets glPolygonOffset(2.0,4.0) on GL_DEPTH_COMPONENT24 (line 3868). The underlying mechanism is also real: the polygon-offset constant (units) term's minimum-resolvable-difference r is a fixed constant for fixed-point depth but magnitude-dependent for float depth, so copying the numeric constant does not guarantee identical effective bias. So the finder is technically/directionally correct that true bias parity is not achieved. But the finding does not substantiate that this yields any observable acne/peter-panning difference: it is an off-by-default feature (Video.SunShadow), slopeScale dominates, and the receiver applies a separate tunable normalized-domain bias (g_pcSunShadowBias) in the same [0,1] depth space that largely compensates. No visible artifact is demonstrated. This reads as a theoretical parity/tuning nit rather than a proven defect.
  - **Correction:** The code and the float-vs-fixed depth-bias mechanism are accurately described. The overstatement is in impact: there is no evidence the theoretical bias-units mismatch produces a visible shadow-acne/peter-panning difference. The feature is off by default and a separate normalized-domain receiver bias (g_pcSunShadowBias) largely absorbs any constant-term discrepancy, making this a low-value parity/tuning nit rather than a substantiated player-facing defect.

### 52. 🟡 ✅ Video.MSAA setting is silently a no-op on the Metal backend (rasterSampleCount hardcoded to 1)

- **File:** `src/platform/fast3d/gfx_metal.mm:3013` · `mtl_draw_triangles`
- **Severity:** low · **Class:** parity-divergence · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The GL backend renders the scene into a multisample target sized by the user setting Video.MSAA (gfx_opengl.c:2419 `int requested = g_pcMsaaSamples;` -> gfx_opengl_effective_msaa_samples() -> 2/4/8x MSAA renderbuffer at gfx_opengl.c:2616, and additionally enables GL_SAMPLE_ALPHA_TO_COVERAGE for alpha-cutout draws in gfx_opengl_update_a2c_state). The Metal backend NEVER reads g_pcMsaaSamples (the only occurrence in gfx_metal.mm is the comment at 2001-2002 'No MSAA clause — Metal is single-sample here') and every scene PSO is built with a hardcoded sample count of 1. Result: a user who raises Video.MSAA gets multisample anti-aliasing on the GL build and NOTHING on the Metal build, with no warning. Metal offers a separate, Metal-only Video.Smaa (platform_sdl.c:223, default 0) as its AA path, so the two backends expose different, non-interchangeable AA controls.
- **Evidence:**

  ```
  gfx_metal.mm:3013 `id<MTLRenderPipelineState> pso = mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha);` (3rd arg = samples, always 1) and gfx_metal.mm:1083 `d.rasterSampleCount = samples;`. Contrast gfx_opengl.c:2419 `int requested = g_pcMsaaSamples;`. grep confirms gfx_metal.mm has no other g_pcMsaaSamples reference.
  ```
- **Player impact:** On macOS Metal, enabling MSAA in settings produces no anti-aliasing and no feedback; edges stay aliased unless the user separately discovers the Metal-only SMAA toggle. Both backends are AA-off by default, so this only bites users who deliberately enable MSAA.
- **Suggested fix:** Either honor Video.MSAA on Metal by allocating a multisampled scene color/depth target and resolve (note: SSAO reads single-sample depth, so this needs a resolve step), or at minimum log a one-time warning that Video.MSAA is unsupported on the Metal backend and surface Video.Smaa as the equivalent, so the setting is not silently inert.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Re-read the cited code and it matches the finding verbatim. gfx_metal.mm:3013 calls mtl_pso_for(ms, s_blend, 1, !s_preserve_cov_alpha) with samples hardcoded to 1; mtl_pso_for (line 1064) forwards that int to d.rasterSampleCount = samples (line 1083). Every rasterSampleCount in the file is a literal 1 (1951, 2242, 2256, 2572), and g_pcMsaaSamples is never referenced in gfx_metal.mm — the only MSAA mention is the comment at 2001-2002 confirming "Metal is single-sample here." The GL backend, by contrast, reads g_pcMsaaSamples (gfx_opengl.c:2419), builds real MSAA renderbuffers, enables alpha-to-coverage, and even logs clamp/SSAO warnings (2444, 2482). Video.MSAA is a live user-facing enum setting (platform_sdl.c:1829, pc_settings_menu.c:52). So enabling Video.MSAA has full effect on GL and silently zero effect (no warning) on Metal, while Metal ships a separate Video.Smaa control. This is a genuine parity divergence, not a gated/dead path. Impact is low: both backends default MSAA off and Metal offers SMAA, so only users who deliberately enable MSAA are affected, and they get a silently inert control with no feedback.

### 53. 🟡 ✅ GE007_DIAG_QUANTIZE_COMBINER combiner-banding diagnostic is not emitted in MSL (Metal ignores it)

- **File:** `src/platform/fast3d/gfx_metal.mm:572` · `mtl_generate_msl`
- **Severity:** low · **Class:** parity-divergence · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** The GL generator emits a per-cycle 8-bit quantization of the combiner result when GE007_DIAG_QUANTIZE_COMBINER is set (gfx_diag_quantize_combiner_enabled), reproducing the RDP's 8-bit color pipeline banding for A/B analysis. mtl_generate_msl has no `quantize_combiner` path at all: its 2-cycle combiner loop emits only the inter-cycle clamp and the final clamp, never the quantize line. When the diagnostic is enabled, GL and Metal produce different combiner output (Metal stays full-precision), so the diagnostic cannot be used to compare backends and the two shaders diverge.
- **Evidence:**

  ```
  GL gfx_opengl.c:1270-1273 `if (quantize_combiner) { append_line(fs_buf, &fs_len, "texel = floor(clamp(texel, 0.0, 1.0) * 255.0 + 0.5) / 255.0;"); }`. Metal gfx_metal.mm:559-574 combiner loop emits `texel = clamp(texel, -1.01, 1.01);` between cycles and `texel = clamp(texel, 0.0, 1.0);` after, with no quantization branch anywhere in the function.
  ```
- **Player impact:** None in normal play (GE007_DIAG_QUANTIZE_COMBINER is a developer diagnostic, default off). Impact is limited to breaking GL-vs-Metal parity work with that flag.
- **Suggested fix:** Either wire an equivalent GE007_DIAG_QUANTIZE_COMBINER read into mtl_generate_msl and emit `texel = floor(clamp(texel, 0.0, 1.0) * 255.0 + 0.5) / 255.0;` per cycle, or document explicitly that the flag is GL-only.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). Verified against the real code. gfx_opengl.c:1270-1273 emits `texel = floor(clamp(texel, 0.0, 1.0) * 255.0 + 0.5) / 255.0;` gated by `quantize_combiner`, which is set from `gfx_diag_quantize_combiner_enabled()` reading GE007_DIAG_QUANTIZE_COMBINER (gfx_opengl.c:406-417, 885). mtl_generate_msl (gfx_metal.mm:268) has the matching 2-cycle combiner loop at 559-574, emitting only the inter-cycle clamp (line 573) and the post-loop clamp (line 575) — no quantization branch exists anywhere in the Metal generator. A grep confirms the flag and any quantize-combiner logic appear only in gfx_opengl.c, never in gfx_metal.mm. So when the diagnostic is on, GL banding does not reproduce on Metal and the two backends diverge, defeating the A/B purpose of the flag. The finder's line/symbol/quoted evidence all match. However, GE007_DIAG_QUANTIZE_COMBINER is a dev-only diagnostic that is default-off, so there is zero normal gameplay or stability impact; the harm is limited to GL-vs-Metal parity tooling with that flag set.

### 54. 🟡 ✅ Metal hardcodes the diag SETTEX/coverage scale values instead of honoring the GE007_DIAG_*_VALUE overrides used by GL

- **File:** `src/platform/fast3d/gfx_metal.mm:628` · `mtl_generate_msl`
- **Severity:** low · **Class:** parity-divergence · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** Several diagnostic scale blocks are gated by shader_id1 feature bits that GL fills with a configurable runtime value, but Metal bakes in GL's DEFAULT constant. When the corresponding *_VALUE env var is set (alongside the enable env that sets the feature bit), GL uses the override while Metal keeps the default, so the two backends diverge. Affected: diag_color_scale (Metal hardcodes 1.02 vs GL gfx_diag_settex_cc_color_scale_value(), default 1.02), diag_alpha_scale (Metal 1.0 vs configurable), diag_alpha_from_tex_intensity mix (Metal 1.0 vs gfx_diag_alpha_from_tex_intensity_mix()), and diag_xlu_coverage_wrap_thin rate (Metal 0.25 vs gfx_diag_xlu_coverage_wrap_thin_rate()).
- **Evidence:**

  ```
  Metal gfx_metal.mm:628-631 `if (cc.diag_color_scale) { if (cc.opt_alpha) tb_str(s, "    texel = float4(clamp(texel.rgb * 1.02, 0.0, 1.0), texel.a);\n"); ...}` and :638 `float rate = 0.25f;`, :552 `float mix = 1.0f;`. GL gfx_opengl.c:1332-1343 reads `float scale = gfx_diag_settex_cc_color_scale_value();` and formats it with `%.9f`. The feature bits (SHADER_OPT_DIAG_COLOR_SCALE etc.) are only set when the enable env is on (gfx_pc.c:18344), so the blocks match on defaults but diverge when the value env is also set.
  ```
- **Player impact:** None in normal play (all paths require GE007_DIAG_* envs no player sets). Impact is limited to diagnostic A/B parity when a non-default *_VALUE override is used.
- **Suggested fix:** Have mtl_generate_msl read the same GE007_DIAG_*_VALUE overrides (or pass the resolved floats from the frontend) rather than baking constants, or document these as GL-only diagnostic knobs.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). Re-read both cited files. Metal gfx_metal.mm bakes constants: line 552 `float mix = 1.0f`, lines 628-630 hardcode `1.02` for diag_color_scale, line 633 `* 1.0` for diag_alpha_scale, line 638 `float rate = 0.25f` for diag_xlu_coverage_wrap_thin. GL gfx_opengl.c instead reads runtime values: line 1228 `gfx_diag_alpha_from_tex_intensity_mix()`, 1333 `gfx_diag_settex_cc_color_scale_value()`, 1346 `gfx_diag_settex_cc_alpha_scale_value()`, 1356 `gfx_diag_xlu_coverage_wrap_thin_rate()`, each formatted with `%.9f` from a `GE007_DIAG_*` env var (getenv at lines 235, 252, 422, 442). GL default initializers (137=1.02f, 139=1.0f, 160=0.25f, 175=1.0f) exactly equal Metal's baked constants, so backends match on defaults and diverge only when the *_VALUE/_RATE/_MIX override env is set. The evidence quotes and line numbers all match the real code, and the mechanism holds. This is a real parity divergence, but it lives entirely inside a diagnostic A/B path gated behind GE007_DIAG_* env vars that no player sets, so it has no gameplay or stability impact.


## Frame timing / level sim (lvl.c)

### 55. 🟠 ✅ speedgraphframes is left unclamped while g_ClockTimer is capped, so watch/HUD timers jump on PC frame spikes and alt-tab

- **File:** `src/game/lvl.c:2124` · `lvlManageMpGame`
- **Severity:** medium · **Class:** port-defect · **Category:** visual-artifact · **Finder confidence:** high
- **What's wrong:** The NATIVE_PORT catch-up clamp only limits g_ClockTimer (the world-sim tick multiplier) to <=4 (or 1 on the first frame after load). speedgraphframes — set by updateFrameCounters(nextFrameTime) in unk_0C0A70.c:151 — is never clamped. Several watch/HUD animation timers advance by speedgraphframes DIRECTLY instead of g_ClockTimer: the watch raise/zoom (bondview.c:9201, 9209), the watch face rotation angle (watch.c:2472 `D_80040B14 += D_80040B1C * speedgraphframes * M_TAU / 360`), and watch sub-animations (watch.c:9158, 9202). On a PC hitch or window-focus change the clamp comment itself cites an 'observed: 13' spike (lvl.c:2117); after an alt-tab/background stall speedgraphframes can be far larger. When that happens the world correctly advances only 4 ticks, but these UI timers jump by the full unclamped spike, desyncing the watch from the sim clock. This inconsistency does not exist on N64, where g_ClockTimer==speedgraphframes always.
- **Evidence:**

  ```
  lvl.c:2113 `g_ClockTimer = speedgraphframes;` then :2124-2128 clamp ONLY g_ClockTimer (`else if (D_80048380 == 0 && g_ClockTimer > 1) { g_ClockTimer = 1; } else if (g_ClockTimer > 4) { g_ClockTimer = 4; }`); speedgraphframes untouched. unk_0C0A70.c:151 `speedgraphframes = deltaFrames;` (no clamp). Consumers: bondview.c:9209 `zoomintime + (speedgraphframes * watch_transition_time)`; watch.c:2472 `D_80040B14 += ((D_80040B1C * speedgraphframes * M_TAU_F) / 360.0f)`.
  ```
- **Player impact:** After any frame stall or alt-tab, the watch second-hand/face spins and the watch raise/zoom snaps instantly instead of animating smoothly — a visible desync between the HUD/watch and the (correctly clamped) game world.
- **Suggested fix:** Under NATIVE_PORT, clamp speedgraphframes with the same cap applied to g_ClockTimer (or feed the watch/HUD timers from g_ClockTimer / g_GlobalTimerDelta so they share the clamped sim clock). Must stay behind the existing RAMROM exemption so replay timing is unaffected.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). All cited code matches exactly. lvl.c:2113-2128 clamps only g_ClockTimer (to 1 first-frame, else 4) behind the RAMROM exemption, leaving speedgraphframes untouched. unk_0C0A70.c:151 sets speedgraphframes = deltaFrames with no clamp, and deltaFrames = nextFrameTime is an osGetCount-derived delta (waitForNextFrame:218) that genuinely can spike after a PC stall/alt-tab. The watch/HUD consumers advance directly by unclamped speedgraphframes: watch.c:2472 (US face rotation), watch.c:9158/9202 (z-rotation sub-anims), bondview.c:9201/9209 (watch raise/zoom, non-BUGFIX_R1 path). Grep confirms speedgraphframes is never clamped anywhere else. So the inconsistency (sim clock capped, watch animation inputs not) is real and does not exist on N64 where the two are identical. However the player impact is over-stated as medium: the effect is confined to watch UI, is transient (one frame after a stall), the zoom clamps to zoomintimemax immediately and the rotations pass through watchWrapAroundPI, so the visible artifact is a minor cosmetic jump only when a stall coincides with the watch being on-screen. Honest severity is low.
  - **Correction:** The code and mechanism are accurate as reported, but severity is low rather than medium: the desync is a transient one-frame cosmetic glitch limited to watch/HUD animation (zoom clamps to zoomintimemax, rotations wrap through watchWrapAroundPI), only visible when a stall/alt-tab coincides with the watch being displayed — not a sustained or gameplay-affecting artifact.

### 56. 🟠 ✅ No render interpolation: presentation is locked to 60 unique frames even on 120/144 Hz displays; integer-tick model gives no motion smoothing

- **File:** `src/game/lvl.c:2113` · `lvlManageMpGame`
- **Severity:** medium · **Class:** n64-quirk-hurting-players · **Category:** player-quality · **Finder confidence:** medium
- **What's wrong:** Path A (sim-60 + render-interpolated presentation) from FRAME_TIMING_ARCHITECTURE.md sec.5 is not implemented — there is no snapshot/lerp layer for camera, viewmodel, characters, or projectiles anywhere in the render path (grep for interpolation/prevpos/render-alpha finds only per-vertex clip lerp in gfx_pc.c and audio resampling, no per-frame transform interpolation). Because the sim is a fixed integer-tick model (g_ClockTimer = speedgraphframes, one 1/60 s step per render frame) and rendering reads live game state, the game can present at most 60 unique frames/s. On a 120/144 Hz display the default (FrameCap=60) simply repeats each frame twice with no added smoothness; the only path that produces more presents (FrameCap=display) is the broken one in finding #1. This is faithful to the N64's 60 Hz sim, but on modern high-refresh displays it caps perceived smoothness well below what the hardware could deliver.
- **Evidence:**

  ```
  lvl.c:2113 `g_ClockTimer = speedgraphframes;` (one integer tick per frame) and lvl.c:2134-2135 `g_GlobalTimerDelta = (f32) temp_v0; g_GlobalTimer += temp_v0;`; docs/FRAME_TIMING_ARCHITECTURE.md sec.5 lists 'Path A' interpolation as unbuilt; no interpolation snapshot code exists (only gfx_loaded_vertex_lerp clip-space lerp in gfx_pc.c:2977).
  ```
- **Player impact:** On 120/144/240 Hz monitors the game feels no smoother than on a 60 Hz panel — motion, camera and viewmodel are all quantized to 60 fps presentation, which reads as slightly juddery to players used to high-refresh play.
- **Suggested fix:** This is a design limitation, not a defect — the candidate improvement is Path A: snapshot camera/viewmodel/entity transforms each 60 Hz sim tick and lerp between the two latest for display only, behind an opt-in GE007_* flag so the deterministic sim/RAMROM/sim_state_hash path stays byte-identical (interpolation is display-only and must not feed back into the hashed sim state).
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited evidence matches the real code exactly. lvl.c:2113 in lvlManageMpGame reads `g_ClockTimer = speedgraphframes;` (integer-tick model), and lvl.c:2134-2135 reads `g_GlobalTimerDelta = (f32) temp_v0; g_GlobalTimer += temp_v0;` — a fixed integer 1/60s-tick sim advance. The surrounding NATIVE_PORT block caps tick spikes but does nothing to add render-time interpolation. The grep claim also holds: the only lerp in the render path is gfx_pc.c:2973 `gfx_loaded_vertex_lerp`, which is clip-space/affine vertex interpolation for geometry crossing clip planes (§3.7 fix), NOT temporal per-frame transform interpolation between sim ticks; gfx_lerp_u8 is a color helper. `prevpos` hits are memp allocator bookkeeping and bondprevpos movement history, not a render snapshot/lerp layer. So there is genuinely no snapshot+lerp presentation layer for camera/viewmodel/entities. The mechanism is correctly described: with a fixed integer-tick sim and live-state rendering, presentation is capped to at most 60 unique frames/s, so high-refresh displays gain no added motion smoothness at the default cap.

Caveat on framing: the finder itself concedes this is "faithful to the N64's 60 Hz sim" and "a design limitation, not a defect." This is not a bug, robustness hole, or gated-path misread — it is an unbuilt enhancement (Path A from FRAME_TIMING_ARCHITECTURE.md sec.5). The player impact is real but mild and only manifests on 120/144/240 Hz panels as slightly reduced perceived smoothness. Because it is an accurate architectural observation rather than a correctness defect, and self-classified as faithful/quirk, the honest severity is low, not medium.
  - **Correction:** Factually accurate as written; the only adjustment is severity (medium -> low) and framing: this is a design/faithfulness limitation and unbuilt enhancement, not a defect. The lerp at gfx_pc.c:2973 is clip-space vertex interpolation, correctly excluded by the finder as not a temporal render-interpolation layer, so the "no interpolation snapshot code exists" claim stands.

### 57. 🟠 ✅ Port fallback marks EVERY loaded room rendered when the portal walk yields zero, making all guards auto-aim-targetable/rendered-through-walls that frame

- **File:** `src/game/lvl.c:1742` · `lvlRender`
- **Severity:** medium · **Class:** port-defect · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** This NATIVE_PORT-only fallback fires when the portal-visibility walk produced zero rendered rooms but rooms are loaded. It force-sets room_rendered=1 for every loaded room. Because room_rendered feeds PROPFLAG_ONSCREEN (Finding 1), on the next tick every guard in every loaded room becomes on-screen: auto-aim-targetable (chrprop.c:6628) and eligible to exit WAYMODE_MAGIC. It also renders all those rooms, i.e. through walls. A pure render-pass anomaly thus perturbs sim targeting and guard AI, and diverges from N64 which has no such fallback.
- **Evidence:**

  ```
  lvl.c:1737-1743: `if (rendered_rooms == 0 && pc_num_loaded_rooms > 0) { ... for (ri = 0; ri < pc_num_loaded_rooms; ri++) { int rm = pc_room_allocs[ri].roomID; g_BgRoomInfo[rm].room_rendered = 1; } ... g_portRoomRenderFallbackTotal++;`
  ```
- **Player impact:** During portal-ambiguity frames (e.g. startup/room seams), Bond can auto-aim onto guards in adjacent rooms through walls, and those rooms/guards briefly render through geometry.
- **Suggested fix:** Do not let the render fallback write room_rendered that gameplay reads; either decouple the sim's on-screen test from room_rendered (Finding 1) or maintain a separate render-only visibility mask for the fallback so sim targeting/AI never observe the forced-rendered rooms.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited code matches the finding verbatim (lvl.c:1737-1747): a NATIVE_PORT-only fallback that sets room_rendered=1 for every loaded room when the portal walk produced zero rendered rooms. The sim-coupling is real and default-active: chrTickBeams (chr.c:5225-5238) derives guard visibility from getROOMID_isRendered() on the default path (GE007_CHRBEAMS_FRUSTUM defaults OFF), and on visible it sets PROPFLAG_ONSCREEN (0x2, chr.c:5347 / bondconstants.h:356). That flag feeds g_OnScreenPropList (chrprop.c:536) which auto-aim iterates (chrprop.c:6628). Critically, the fallback executes at lvl.c:1737 BEFORE determing_type_of_object_and_detection (1761), chraiUpdateOnscreenPropCount (1762), and chrpropUpdateAutoaimTarget (1763) — so the forced room_rendered is observed by sim the SAME frame (the finding's "next tick" wording is slightly off). Thus a render-only defensive fallback can spuriously make guards in loaded-but-not-portal-visible rooms on-screen/auto-aim-targetable and render them, with no N64 equivalent. Two accuracy caveats keep this from being higher severity: (1) it only fires in the degenerate case rendered_rooms==0 while rooms are loaded — in normal play the current room is always marked rendered (bg.c:3656), so the fallback does not fire per-frame; it is a rare startup/room-seam anomaly (tracked by g_portRoomRenderFallbackTotal and an 8-shot log). (2) The separate 1585 all-rooms mark is the fly-camera debug path, not this one. The mechanism is genuine but the exposure window is narrow.
  - **Correction:** The perturbation is same-frame, not "next tick": the fallback at lvl.c:1737 runs before determing_type_of_object_and_detection/chraiUpdateOnscreenPropCount/chrpropUpdateAutoaimTarget at lvl.c:1761-1763. It only triggers on the degenerate frame where the portal walk yielded zero rendered rooms while rooms are loaded (normally the current room is always rendered, so this does not fire in routine gameplay) — it is an edge-case defensive fallback, not a per-frame defect, which is why severity is low rather than medium.

### 58. 🟡 ❌ waitForNextFrame() sim gate is a hard busy-spin with no yield; pegs a CPU core when the pacer is disabled (FrameCap=display on >120 Hz displays)

- **File:** `src/game/unk_0C0A70.c:199` · `waitForNextFrame`
- **Severity:** low · **Class:** port-defect · **Category:** performance · **Finder confidence:** high
- **What's wrong:** waitForNextFrame() spins `do { currentCount = osGetCount(); ... } while (nextFrameTime < frameDelay);` with no SDL_Delay/sched_yield in the loop; osGetCount() is a clock_gettime() syscall so the loop pegs one core at 100%. In the default config this is harmless because the platform deadline pacer (platform_sdl.c:2905) has already slept ~16.6 ms before the next waitForNextFrame, so the elapsed test passes on the first iteration and the loop exits immediately. But when the pacer is disabled — Video.FrameCap=display with vsync on (platformFrameCapPeriodMs returns 0.0) — and the display refresh exceeds ~120.8 Hz (144/165/240 Hz), each present is < 8.28 ms so nextFrameTime rounds to 0 and this loop hot-spins ~8-10 ms every frame to hold the sim at 60, delivering NO frame-rate benefit while burning a full core (extra heat, fan, and battery drain on laptops/handhelds).
- **Evidence:**

  ```
  unk_0C0A70.c:199-212: `do { currentCount = osGetCount(); ... nextFrameTime = ((currentCount - copy_of_osgetcount_value_1) + 387937) / 775875; ... } while (nextFrameTime < frameDelay);` — no yield/sleep. Trigger: platform_sdl.c:1547-1550 returns 0.0 (pacer off) for DISPLAY+vsync; at >120.8 Hz elapsed<387938 counts so nextFrameTime==0.
  ```
- **Player impact:** On a 144/165/240 Hz display with the 'display' frame cap, the game is still locked to 60 fps (as documented) but now also pins a CPU core at 100%, causing extra heat/fan noise and battery drain for zero benefit.
- **Suggested fix:** Add a coarse yield (SDL_Delay(0) or a short nanosleep) inside the wait loop, and/or fix the root cause per finding #1 by keeping the pacer's 1/60 s floor active under FrameCap=display so the loop never has to spin.
- **Verification:** REFUTED (adj. severity low, real-player-issue=False). The cited loop (unk_0C0A70.c:199-212) is quoted accurately — a yield-free do-while over osGetCount() (clock_gettime in the non-deterministic path), and the pacer-disable trigger (platformFrameCapPeriodMs()==0.0 for display+vsync) is real. But the finding's mechanism ("waitForNextFrame hot-spins ~8-10 ms every frame, pegging a core") does not hold. The per-frame caller (boss.c:569) is pre-gated by boss.c:549: waitForNextFrame is only entered once mainTickElapsed >= MAIN_LOOP_TICK_INTERVAL (0x5eb61 = 387937 counts, ~half frame). waitForNextFrame's own exit condition is (elapsed+387937)/775875 >= 1, i.e. elapsed >= 387938 — the SAME threshold, measured against the SAME copy_of_osgetcount_value_1 reference (reset per-frame in updateFrameCounters at line 147). So by the time the loop is entered, its exit condition is essentially already satisfied and it exits within ~1 count (~21 ns), not after 8-10 ms. The only unguarded call is the one-time init at boss.c:511, which spins once, not per frame. Any real busy-spin under this config would be in the retrace-drain path (stubs.c:177-181 / platformFrameSync with period_ms==0), a different location and mechanism than cited.
  - **Correction:** The do-while at unk_0C0A70.c:199 does not busy-spin per frame. Its per-frame caller (boss.c:569) pre-gates entry with MAIN_LOOP_TICK_INTERVAL (boss.c:79, 0x5eb61=387937), the same half-frame threshold as the loop's own exit condition (elapsed>=387938) and using the same osGetCount reference, so the loop exits within ~1 count of entry. If a CPU-pegging busy-spin exists under FrameCap=display+vsync on high-refresh displays, it would be in the retrace-drain loop (stubs.c:177-181 calling platformFrameSync, which does no pacer sleep when period_ms==0 and no vsync swap occurs on skipped retraces), not in waitForNextFrame.


## Weapons (gun.c / watch.c)

### 59. 🟠 ✅ Magnum-class weapon grouping swapped: Cougar Magnum + Grenade Launcher lose slow-fire gate; Sniper Rifle + Watch Laser gain it

- **File:** `src/game/gun.c:18102` · `handle_weapon_id_values_possibly_1st_person_animation (STATE 1 equip switch, weapon_id-1)`
- **Severity:** medium · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The STATE-1 equip switch that mirrors retail jump table jpt_80053F24 puts the WRONG items in the 'magnum' bucket. The magnum bucket imposes a ~6-tick minimum in state 1 before a shot fires (`if (hand_ptr->field_890 < 6) { field_92C = 0; break; }`), which floors the weapon's fire cadence. The reimpl assigns this gate to ITEM_SNIPERRIFLE and ITEM_WATCHLASER (lines 18102-18103) and puts ITEM_RUGER (Cougar Magnum, line 18076) and ITEM_GRENADELAUNCH (line 18081) in the ungated 'guns' bucket. The authoritative ASM table is the exact opposite: jpt_80053F24[idx17]=RUGER=Weapon_function_magnum (gun.c:19472) and [idx23]=GRENADELAUNCH=Weapon_function_magnum (gun.c:19478), while [idx16]=SNIPERRIFLE=Weapon_function_guns (gun.c:19471) and [idx22]=WATCHLASER=Weapon_function_guns (gun.c:19477). Index = weapon_id-1, verified: FIST(1)->idx0=slapper, and the sibling table jpt_80054084 cross-validates the enum-to-ROM mapping (KNIFE=noammo, SKORPION..FNP90=machinegun, CAMERA=watch_magnet all line up). This is a clean off-by-one pair swap that was introduced in the original launch reimplementation (commit 8de295b) and left untouched by later gun.c fixes.
- **Evidence:**

  ```
  reimpl magnum bucket (gun.c:18102): `case ITEM_SNIPERRIFLE - 1: // magnum (sniper + watchlaser)` / `case ITEM_WATCHLASER - 1:` then `if (hand_ptr->field_890 < 6) { hand_ptr->field_92C = 0; break; }`. reimpl guns bucket (gun.c:18076,18081): `case ITEM_RUGER - 1:` ... `case ITEM_GRENADELAUNCH - 1:`. Authoritative ASM jpt_80053F24: line 19472 `.word Weapon_function_magnum` (idx17=RUGER), line 19478 `.word Weapon_function_magnum` (idx23=GRENADELAUNCH), line 19471 `.word Weapon_function_guns` (idx16=SNIPERRIFLE), line 19477 `.word Weapon_function_guns` (idx22=WATCHLASER).
  ```
- **Player impact:** In the port the Cougar Magnum (RUGER) and Grenade Launcher can be trigger-spammed faster than on N64 because they no longer have the ~6-tick per-shot floor (a real MP balance change for the one-shot-kill Magnum). Conversely the Sniper Rifle and Watch Laser gain an unintended firing delay, making them feel slightly less responsive than the original.
- **Suggested fix:** Swap the magnum/guns membership to match jpt_80053F24: move ITEM_RUGER and ITEM_GRENADELAUNCH into the magnum (field_890<6) case, and move ITEM_SNIPERRIFLE and ITEM_WATCHLASER into the 'guns' fall-through group. Re-verify against the ASM table entries at gun.c:19471-19478.
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). I re-read the cited code and the authoritative ASM table, plus the enum, and all the finder's claims hold exactly.

Enum (src/bondconstants.h:3344+): ITEM_UNARMED=0, FIST=1, KNIFE=2, THROWKNIFE=3, WPPK=4 ... AUTOSHOT=16, SNIPERRIFLE=17, RUGER=18, GOLDENGUN=19, SILVERWPPK=20, GOLDWPPK=21, LASER=22, WATCHLASER=23, GRENADELAUNCH=24.

ASM jpt_80053F24 (src/game/gun.c, line 19455 = index 0): index N is on line 19455+N. idx16 (line 19471)=Weapon_function_guns → SNIPERRIFLE(17); idx17 (line 19472)=Weapon_function_magnum → RUGER(18); idx22 (line 19477)=guns → WATCHLASER(23); idx23 (line 19478)=magnum → GRENADELAUNCH(24). So retail routes RUGER and GRENADELAUNCH through magnum, SNIPERRIFLE and WATCHLASER through guns.

Reimpl STATE-1 switch: at gun.c:18032 `if (state == 1)` then 18033 `switch (weapon_id - 1)`. In the guns fall-through group are `case ITEM_RUGER - 1` (18076) and `case ITEM_GRENADELAUNCH - 1` (18081), both ungated. The magnum bucket is `case ITEM_SNIPERRIFLE - 1` (18102) / `case ITEM_WATCHLASER - 1` (18103) with `if (hand_ptr->field_890 < 6) { hand_ptr->field_92C = 0; break; }` (18104-18106). This is the precise inverse of the ASM table — a clean off-by-nothing pair swap of the magnum/guns membership for these four items. Each case appears exactly once in the C switch, so RUGER/GRENADELAUNCH cannot also be gated elsewhere. The switch dispatches per jpt_80053F24 (the ITEM_TRIGGER comment at 18087-18094 confirms this switch mirrors that table). Behavior diverges from retail for all four weapons: RUGER and GRENADELAUNCH lose the field_890<6 state-1 gate, SNIPERRIFLE and WATCHLASER gain it.

One minor framing nuance: the gate lives in STATE 1 (the equip/ready path, per the 18031 comment "Initial weapon equip"), so it is more precisely a ready-to-fire timing gate than strictly a per-shot floor, but it is a genuine, retail-divergent behavior change affecting weapon cadence/feel and MP balance (RUGER = the one-shot-kill Cougar Magnum). The core defect and the suggested fix (swap the two groups) are correct.
  - **Correction:** The gate is in the STATE-1 (weapon equip / ready) branch (`if (state == 1)` at gun.c:18032), so it is precisely a state-1 ready-timing gate rather than strictly a per-shot fire-rate floor as worded; the net effect on cadence/feel and the swapped-grouping defect are otherwise exactly as described, and the suggested fix is correct.

### 60. 🟠 ✅ Tank cannon (ITEM_TANKSHELLS) fires full-auto instead of single-shot-per-press (STATE 2)

- **File:** `src/game/gun.c:18306` · `handle_weapon_id_values_possibly_1st_person_animation (STATE 2 shooting switch, weapon_id-2)`
- **Severity:** medium · **Class:** port-defect · **Category:** correctness · **Finder confidence:** high
- **What's wrong:** The STATE-2 shooting switch (mirror of jpt_80054084) groups ITEM_TANKSHELLS into the Weapon_shooting_machinegun case (gun.c:18306), which keeps firing while the trigger is held (`if (hand_ptr->field_88C % fire_rate != 0) break;` cyclic auto-fire). The authoritative ASM routes it to Weapon_shooting_pistol: jpt_80054084[idx30] (weapon_id 32 = TANKSHELLS) = `.word Weapon_shooting_pistol` (gun.c:19576), which latches a single shot per trigger press and transitions to state 3. This item is reachable: STATE 1 routes TANKSHELLS through the 'guns' group to state 2 in both reimpl and ASM, and TANKSHELLS is a live player weapon (the tank cannon: bondview.c:11980 gates on `in_tank_flag == 1 && getCurrentPlayerWeaponId(GUNRIGHT) == ITEM_TANKSHELLS`, and bondview_r.c:56 spawns PROJECTILES_TYPE_ROCKET_ROUND2 for it). (Note ITEM_BOMBCASE is also mis-placed in this machinegun case but is harmless because STATE 1 routes BOMBCASE to the throwable state 28 and it never reaches STATE 2.)
- **Evidence:**

  ```
  reimpl machinegun group (gun.c:18306): `case ITEM_TANKSHELLS - 2:` immediately above `fire_rate = bondwalkItemGetAutomaticFiringRate(weapon_id);` cyclic-fire body. Authoritative ASM jpt_80054084 line 19576: `.word Weapon_shooting_pistol` (index 30 = weapon_id 32 = ITEM_TANKSHELLS). Reachability: bondview.c:11980 `if (getCurrentPlayerWeaponId(GUNRIGHT) == ITEM_TANKSHELLS)` under `in_tank_flag == 1`; bondview_r.c:56 `case ITEM_TANKSHELLS: model = PROJECTILES_TYPE_ROCKET_ROUND2;`.
  ```
- **Player impact:** While driving a tank, holding the fire button in the port fires shells continuously (auto-fire, throttled only by the weapon's AutomaticFiringRate) instead of the retail behavior of one shell per discrete trigger press, changing the feel and rate of the tank cannon in tank sequences.
- **Suggested fix:** Remove ITEM_TANKSHELLS from the machinegun case block and add it to the Weapon_shooting_pistol case group (with the other pistol/single-fire weapons), matching jpt_80054084[idx30]. Optionally also drop the dead ITEM_BOMBCASE label from the machinegun group for clarity.
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). All claims verified against the actual code. (1) gun.c:18306 `case ITEM_TANKSHELLS - 2:` sits in the machinegun case group (18293-18340) whose body does cyclic auto-fire via `hand_ptr->field_88C % fire_rate`. The group's justifying comment only covers M16/FNP90, not TANKSHELLS. (2) The authoritative ASM jpt_80054084 (starts line 19546=idx0) is indexed by weapon_id-2; ITEM_TANKSHELLS=32 (bondconstants.h enum) → idx30 → line 19576 = Weapon_shooting_pistol. Cross-checks confirm the indexing: TASER=31→idx29→tazer (19575), BOMBCASE=33→idx31→throwable (19577). So retail routes the tank shell to the single-shot pistol path. (3) The pistol body (18273-18291) fires once then transitions to state 3 (single latch per press) versus the machinegun body's held-trigger cyclic fire — a genuine behavioral difference. (4) Reachability confirmed: STATE 1 routes TANKSHELLS to guns→state 2 in both reimpl (gun.c:18083) and ASM (jpt_80053F24 idx31=line 19486=guns), and TANKSHELLS is a live tank-cannon weapon (bondview.c:11980 under in_tank_flag==1, with full ammo/inventory plumbing). (5) The BOMBCASE-is-harmless caveat is also correct: STATE 1 sends BOMBCASE to state 28 (gun.c:18151), so it never reaches STATE 2. Line numbers and symbols all match.

### 61. 🟡 ✅ Weapon equip/reload sound-cue switches diverge from jpt_80054194 / jpt_80054294 for several items

- **File:** `src/game/gun.c:18816` · `handle_weapon_id_values_possibly_1st_person_animation (STATE 8 switch-sound / STATE 11 reload-sound)`
- **Severity:** low · **Class:** port-defect · **Category:** player-quality · **Finder confidence:** high
- **What's wrong:** The two weapon_id sound-selection switches do not match their ASM jump tables. STATE 8 (switch/equip sound, gun.c:18816, mirror of jpt_80054194): the reimpl plays sound 235 for ITEM_GRENADE (grouped with TIMEDMINE/PROXIMITYMINE at gun.c:18827-18830), but jpt_80054194[26]=weapon_switchstyle_NONE (gun.c:19654) means retail plays nothing when equipping a plain grenade. Conversely the reimpl `break`s (silent) for ITEM_REMOTEMINE (gun.c:18833) whereas jpt_80054194[29]=weapon_playsfx_mine (gun.c:19657) plays the mine cue. Other reimpl cases also fall to the `default` gun-rack sound 232 for items the table marks NONE (e.g. ITEM_WATCHLASER, ITEM_CAMERA). STATE 11 (reload sound, gun.c:18959, mirror of jpt_80054294) has the same class of mismatch (e.g. TANKSHELLS/PLASTIQUE/CAMERA fall to the default reload sound 50 where jpt_80054294 marks them weapon_reload_none_sfx). These are audible on commonly-equipped items (grenades, mines) but are purely cosmetic SFX cues.
- **Evidence:**

  ```
  reimpl STATE 8 (gun.c:18827): `case ITEM_GRENADE:` / `case ITEM_TIMEDMINE:` / `case ITEM_PROXIMITYMINE:` -> `sndPlaySfx(g_musicSfxBufferPtr, 235, NULL);`, and (gun.c:18833) `case ITEM_REMOTEMINE:` inside the `break` (silent) group. ASM jpt_80054194 line 19654 `.word weapon_switchstyle_NONE` (idx26=GRENADE) vs line 19657 `.word weapon_playsfx_mine` (idx29=REMOTEMINE). Index = weapon_id (idx0=UNARMED=NONE confirmed at line 19628).
  ```
- **Player impact:** When switching to / equipping certain items the wrong audio cue plays: a plain grenade emits the mine-arming sound it should not, a remote mine is silent when it should play the mine cue, and the watch laser / camera play a gun-rack sound. No gameplay effect, only incorrect SFX feedback.
- **Suggested fix:** Reconcile the STATE 8 and STATE 11 case groupings entry-for-entry with jpt_80054194 (gun.c:19628-19689) and jpt_80054294 (gun.c:19699-19760): move ITEM_GRENADE into the silent group, ITEM_REMOTEMINE into the mine-sound group, and ensure WATCHLASER/CAMERA/TANKSHELLS/PLASTIQUE map to NONE where the tables specify weapon_switchstyle_NONE / weapon_reload_none_sfx.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). I re-read gun.c STATE 8 (18816-18844) and STATE 11 (18959-18980) switches, the two ASM jump tables jpt_80054194 (19628-19690) and jpt_80054294 (19699-19760), the sound-function bodies (21127-21160: F2=242, knife=233, mine=235, gun=232, NONE=silent, reload_gun=50), and the ITEM enum in bondconstants.h:3344+ (0-indexed, matching table index). Every specific mismatch the finder cited checks out: STATE 8 plays mine sound 235 for ITEM_GRENADE (idx26) where table entry line 19654 = weapon_switchstyle_NONE (silent); STATE 8 is silent for ITEM_REMOTEMINE (idx29) where table line 19657 = weapon_playsfx_mine; ITEM_WATCHLASER (idx23, table NONE) and ITEM_CAMERA (idx40, table NONE) fall to the default gun sound 232. STATE 11 sends TANKSHELLS (idx32), PLASTIQUE (idx34), and CAMERA (idx40) to default reload sound 50 where jpt_80054294 lines 19731/19733/19739 mark them weapon_reload_none_sfx. The reimpl comments explicitly declare these switches mirror the named jump tables, so the divergences are genuine faithfulness defects, not a gated flag path or intended behavior. I also found additional uncited mismatches (ROCKETLAUNCH, BOMBDEFUSER, EXPLOSIVEFLOPPY silent where table = gun), which only strengthen the finding. Effect is purely cosmetic audio cues but audible on commonly-equipped grenades/mines.
  - **Correction:** The finding is accurate as stated. Minor amplification: the switches also diverge for items the finder did not name — STATE 8 is silent for ITEM_ROCKETLAUNCH (idx25), ITEM_BOMBDEFUSER (idx39), and ITEM_EXPLOSIVEFLOPPY (idx50) where jpt_80054194 (lines 19653/19667/19678) marks them weapon_playsfx_gun. So the correct fix is a full entry-for-entry reconciliation of both switches against the two tables, not just the four/three cited cases.


## Effects / allocators (dyn.c)

### 62. 🟠 ✅ Effect vertex allocator dynAllocate7F0BD6C4 lacks the overflow guard its siblings have (OOB heap write on VTX-pool exhaustion)

- **File:** `src/game/dyn.c:129` · `dynAllocate7F0BD6C4`
- **Severity:** medium · **Class:** port-defect · **Category:** memory-safety · **Finder confidence:** high
- **What's wrong:** dynAllocate7F0BD6C4 is the bump allocator every billboard effect uses to reserve its per-quad Vtx array, but unlike its two siblings it performs NO bounds check before advancing the shared VTX pool cursor g_GfxMemPos. The port deliberately added NATIVE_PORT overflow guards to dynAllocateMatrix (dyn.c:144, returns a scratch Mtx on overflow) and dynAllocate (dyn.c:170, returns without advancing) precisely because a heavy frame can push g_GfxMemPos past g_VtxBuffers[active+1] (see the SEGV note at dyn.c:138-142). dynAllocate7F0BD6C4 was left unguarded: it unconditionally does `g_GfxMemPos += count * 0x10; return ptr;`. When the pool is already near-full (brute-force room rendering with no portal culling consumes the bulk of the 512KB pool per the dyn.c:44 comment, plus per-shard matrices), an effect call returns a pointer at/after the pool end and the caller then writes count*16 bytes (64 for a 4-vtx quad, 144 for the 9-vtx native fireball ring) straight past the buffer into adjacent heap. The effect callers use fixed small counts so they are the straw, not the whole load; note data-driven callers outside this lane (front.c:4411, chrobjhandler.c:45947 pass model->numVertices) make the same allocator overflow even easier.
- **Evidence:**

  ```
  dyn.c:129-133 `void *dynAllocate7F0BD6C4(s32 count) {\n    void *ptr = g_GfxMemPos;\n\tg_GfxMemPos += count * 0x10/*sizeof(Vtx)?*/;\n\treturn ptr;\n}` — compare dynAllocate (dyn.c:170) `if (g_GfxMemPos + size > g_VtxBuffers[g_GfxActiveBufferIndex + 1]) { ... return ptr; /* ...don't advance past end */ }` and dynAllocateMatrix (dyn.c:144) `if (g_GfxMemPos + sizeof(Mtx) > g_VtxBuffers[...]) { ... return &s_overflowScratch; }`. Effect callers: explosions.c:2117 `vertices = dynAllocate7F0BD6C4(4)` (explosionRenderPart), explosions.c:2244 (explosionSmokeRenderPart), explosions.c:1965 `dynAllocate7F0BD6C4(9)` (explosionRenderPartNativeFallback), unk_0A1DA0.c:4157 (sub_GAME_7F0A3F04 spark render) — each immediately does `vertices[0..N] = template; vertices[k].v.ob[...] = ...`.
  ```
- **Player impact:** On the largest levels during a heavy firefight (many simultaneous explosions/smoke/sparks layered on top of brute-force room geometry), the VTX pool can exhaust mid-frame and an explosion/smoke/spark quad write corrupts adjacent heap — manifesting as a crash or intermittent corruption that is hard to reproduce, exactly the failure class the sibling guards were added to prevent.
- **Suggested fix:** Mirror the dynAllocate guard in dynAllocate7F0BD6C4: if `g_GfxMemPos + count*0x10 > g_VtxBuffers[g_GfxActiveBufferIndex + 1]`, log once and return without advancing (callers already tolerate a stale/clamped pointer far better than an OOB write; ideally return a small static scratch so the write lands in-bounds).
- **Verification:** CONFIRMED (adj. severity medium, real-player-issue=True). The cited code matches the evidence exactly. dynAllocate7F0BD6C4 (dyn.c:129-133) unconditionally does `void *ptr = g_GfxMemPos; g_GfxMemPos += count * 0x10; return ptr;` with no bounds check, while its siblings dynAllocateMatrix (dyn.c:144) and dynAllocate (dyn.c:170) both carry NATIVE_PORT overflow guards. The dyn.c:138-142 comment explicitly documents the OOB-write/SEGV failure class the guards were added to stop, and notes the shared bump pool exhausts on heavy frames (glass shatter ~200 matrices). Because matrices, sub-DLs, and vertices all advance the same g_GfxMemPos, the exhaustion condition that empirically produced the matrix SEGV also applies to this allocator. All cited callers (explosions.c:1965/2117/2244, unk_0A1DA0.c:4157) immediately write count*16 bytes into the returned pointer; data-driven callers (front.c:4411/4426, chrobjhandler.c:45947) pass model->numVertices, making overflow easier. On overflow the write either clobbers the other frame buffer (active=0) or runs past the malloc'd region entirely (active=1). This is a genuine, non-stylistic memory-safety gap of exactly the class the port authors already guarded elsewhere.
  - **Correction:** The finder says only "two siblings" have the guard; that is correct, but there is a third sibling with the identical gap — dynAllocate7F0BD6F8 (the Light allocator, dyn.c:160-164) is also unguarded. The fix should mirror the dynAllocate guard in both unguarded bump allocators, not just dynAllocate7F0BD6C4.

### 63. 🟡 ✅ dynAllocate overflow returns a non-advancing pointer, so consecutive allocations alias (blood overlay / glass-shard matrices collide under pool pressure)

- **File:** `src/game/dyn.c:170` · `dynAllocate`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** medium
- **What's wrong:** The NATIVE_PORT overflow branch in dynAllocate avoids an OOB write, but it returns the current cursor and does NOT advance g_GfxMemPos. Consequently, when the pool is full, two successive dynAllocate calls hand back the SAME address, so two logically distinct live allocations alias the same memory for the rest of the frame. die_blood_image_routine relies on two independent dynAllocate(BLOOD_IMAGE_BYTES) buffers per frame (the decoded 'next' scratch and the stored bloodImgBufPtrArray slot); under overflow they collapse onto one another and the gunbarrel/gameplay blood mask decodes into garbage. The glass-shard float-matrix path (sub_GAME_7F0A2C44 -> dynAllocate(sizeof(Mtxf))) has the same exposure: all overflowed shards would share one modelview and snap to a single transform. This is a deliberate fail-safe (aliasing beats OOB) but it silently produces visible corruption rather than dropping the effect.
- **Evidence:**

  ```
  dyn.c:166-181 `void *dynAllocate(s32 size) { void *ptr = g_GfxMemPos; size = ALIGN16_a(size); if (g_GfxMemPos + size > g_VtxBuffers[g_GfxActiveBufferIndex + 1]) { ... return ptr; /* return current pos but don't advance past end */ } g_GfxMemPos += size; return ptr; }`. Aliasing callers: blood_animation.c:408-409 `g_CurrentPlayer->bloodImgBufPtrArray[...] = dynAllocate(BLOOD_IMAGE_BYTES); temp_v0_2 = dynAllocate(BLOOD_IMAGE_BYTES);` and unk_0A1DA0.c:1321 `Mtxf *mtxf_dyn = (Mtxf *)dynAllocate(sizeof(Mtxf)); memcpy(mtxf_dyn, &mtxf, sizeof(Mtxf));`.
  ```
- **Player impact:** Under the same rare VTX-pool exhaustion, the full-screen death/gunbarrel blood overlay renders as scrambled data, and a heavy glass shatter collapses all shards onto one spot, instead of the effect simply being skipped.
- **Suggested fix:** On overflow, return a distinct per-call scratch (or a tiny rotating scratch pool) rather than the shared non-advancing cursor, or have callers null-check an explicit overflow return and skip the effect; at minimum document that the returned pointer is unsafe to hold across another dynAllocate.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). The cited code matches the evidence verbatim. dynAllocate (dyn.c:166-181) sets ptr=g_GfxMemPos, and on the NATIVE_PORT overflow branch (line 170) returns ptr at line 176 without advancing g_GfxMemPos, explicitly commented "return current pos but don't advance past end". Two consecutive overflow calls therefore return the same address. The aliasing callers are real: blood_animation.c:408-409 performs two back-to-back dynAllocate(BLOOD_IMAGE_BYTES) — one stored in bloodImgBufPtrArray, one used as scratch, both live the same frame — and unk_0A1DA0.c:1321 uses dynAllocate(sizeof(Mtxf)) in the use_float_mtx glass-shard path. Under overflow these collapse to one buffer, producing decoded garbage / all shards sharing one matrix. The mechanism is corroborated by the sibling dynAllocateMatrix (dyn.c:135-158), which was specifically hardened to return a dedicated s_overflowScratch on overflow (with a SEGV comment) — that safety was not extended to dynAllocate, confirming this is a genuine oversight rather than intended design. It is not a gated flag misread or dead code. Severity is genuinely low because it only manifests under rare VTX-pool exhaustion, but within that path it is a real robustness/correctness defect.


## ROM loading (rom_*.c)

### 64. 🟠 ✅ ROM is accepted by size alone — no SHA-1/region/CRC check, so PAL/JP or corrupt 12MB ROMs are silently mis-patched

- **File:** `src/platform/rom_offsets.c:12` · `platformPatchFileTable`
- **Severity:** medium · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** The only gate on a user-supplied ROM is an exact-size check (rom_io.c:46 `if (size != GE007_ROM_SIZE_BYTES)`). GoldenEye's US (NTSC), European (PAL) and Japanese ROMs are ALL exactly 12MB but are three distinct images with different internal byte offsets. platformPatchFileTable() then blindly writes ~726 hardcoded absolute offsets (e.g. `file_resource_table[14].hw_address = romData + 0x005ffc50`) that were generated from ONE specific revision (the US decomp base). Loading any other 12MB N64 image — a PAL/JP GoldenEye, a bit-flipped/bad-CRC dump, or an entirely different N64 game — passes the size check, gets these US offsets applied, and prints the reassuring `[ROM] File table patched with 12582912-byte ROM` before every subsequent resource read lands on the wrong bytes. I grep-confirmed the whole tree has NO sha1/crc32/region/header-country validation anywhere in the boot path (rom_io.c:52 only *mentions* ge007.u/e/j.sha1 in a comment; nothing computes or checks them).
- **Evidence:**

  ```
  rom_io.c:46 `if (size != GE007_ROM_SIZE_BYTES) { ... return -1; }` is the sole validation; rom_offsets.c:14-34 hardcode single-revision offsets `file_resource_table[1].hw_address = romData + 0x00438660;` ... `[14] romData + 0x005ffc50`; main_pc.c:1137 `platformPatchFileTable(g_romData);` then main_pc.c:1138 `printf("[ROM] File table patched with %u-byte ROM\n", g_romSize);`. Tree-wide grep for sha1|crc32|region|country|gamecode returns only comments.
  ```
- **Player impact:** European (PAL) and Japanese players — a large share of the world — own legally-dumped 12MB ROMs that are NOT the US revision. They will hit a crash or garbage geometry/text on level load with a misleading success message, and no hint that the ROM revision is the problem. Same for any subtly corrupt US dump.
- **Suggested fix:** After loading, compute the ROM's SHA-1 (or at least read the internal name / CRC1+CRC2 from the header at 0x10-0x18) and require it to match the exact revision rom_offsets.c was generated from; reject (or clearly warn) on mismatch instead of patching wrong offsets. The .u/.e/.j.sha1 files referenced in the existing comment already list the expected hashes.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Every cited fact matches the real code. rom_io.c:46 is the sole ROM gate (`if (size != GE007_ROM_SIZE_BYTES)`), an exact-size check with no hash/CRC/region validation. rom_offsets.c:12-14 confirms platformPatchFileTable() writes hardcoded absolute offsets (romData + 0x00438660 ... [14] romData + 0x005ffc50) generated from a single N64 ELF symbol table (the US decomp base). main_pc.c:1137-1138 calls it unconditionally and prints the reassuring "File table patched" message. Tree-wide grep confirms no sha1/crc32/region/country/gamecode computation exists anywhere in the boot path — only the comment at rom_io.c:52 naming the .sha1 files. All three GoldenEye regions (U/E/J) are exactly 12MB but distinct images, so a PAL/JP ROM or a same-size corrupt US dump passes the size gate and gets US offsets applied, then reads land on wrong bytes -> crash/garbage with a misleading success message. Note the finder undersold one partial mitigation: the size check does reject wrong-size, headered, and truncated files with an explicit message that even points at the .sha1 files. The residual gap is specifically same-size wrong-region/corrupt input.
  - **Correction:** Severity is closer to low than medium: the intended US-ROM path works correctly and the existing size check already catches wrong-size/headered/truncated files while pointing users at the expected SHA-1s. The genuine residual is same-size wrong-region (PAL/JP) or bit-corrupt-but-right-size dumps, which silently get US offsets and mis-read. Real but a graceful-failure/UX-hint improvement, not a defect in normal operation.


## Determinism rail (sim_state_hash)

### 65. 🟠 ✅ Determinism rail (sim_state_hash) does not cover the BSS prop table — movement/collision position drift is invisible to the invariance check

- **File:** `src/platform/sim_state_hash_registry.c:26` · `simHashRegistryBuild`
- **Severity:** medium · **Class:** port-defect · **Category:** robustness · **Finder confidence:** medium
- **What's wrong:** The determinism rail hashes only three regions: the malloc'd N64-heap emulation pool (bossGetPcPoolBase/Size = s_pcPool, boss.c:246), g_ClockTimer, and g_GlobalTimer. But the entire prop table `PropRecord pos_data_entry[POS_DATA_ENTRY_LEN]` is a C BSS global (chrprop.c:152), used directly (e.g. bondview.c:242 `&pos_data_entry[*lookup_index]`), NOT inside the malloc'd pool. Every prop's pos/flags/rooms — the authoritative output of movement and collision — therefore lies outside the hashed region. A movement/collision divergence that manifests as prop position drift produces no change in the sim hash, so the 'sim invariance' rail cannot detect it.
- **Evidence:**

  ```
  sim_state_hash_registry.c:26-38 registers only `out[i].name="pool"; out[i].base=bossGetPcPoolBase(); ...` plus g_ClockTimer and g_GlobalTimer, then `*n = i;` (3 regions). boss.c:246 `s_pcPool = (u8*)malloc(PC_POOL_SIZE);` — a separate heap block from the BSS array `PropRecord pos_data_entry[POS_DATA_ENTRY_LEN] = {0};` (chrprop.c:152).
  ```
- **Player impact:** No direct player-visible symptom, but the guarantee that movement/collision remains byte-identical to N64 is unenforced — regressions in this lane (fall-through-floor, drift, stuck) can ship without tripping the determinism rail.
- **Suggested fix:** Add pos_data_entry (and other mutable BSS sim state such as chr/waydata tables not living in s_pcPool) as hashed regions in simHashRegistryBuild, using the existing pointer-canonicalization so ASLR'd links stay invariant.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=True). Every cited fact checks out. simHashRegistryBuild (sim_state_hash_registry.c:24-38) registers only three regions: the malloc'd pool via bossGetPcPoolBase/Size, g_ClockTimer, and g_GlobalTimer. s_pcPool is a separate 8MB malloc block (boss.c:70,246). pos_data_entry is a file-scope BSS array `PropRecord pos_data_entry[POS_DATA_ENTRY_LEN] = {0};` (chrprop.c:152), demonstrably outside the pool — chrobjhandler.c:142-151 validates prop pointers against the array's own [first,last) address range, distinct from the pool. PropRecord (bondtypes.h:2289,2276,2303) holds pos, flags, and rooms, and this is the authoritative position even for characters: chr movement/collision reads and writes chr->prop->pos (chr.c:2008, 2044). Therefore the primary output of the movement+collision lane lies entirely outside the hashed set, and a divergence expressed as prop position drift would leave the sim hash unchanged. The mechanism is real and correctly described.
  - **Correction:** Details are accurate; only the severity is overstated. This is an off-by-default developer/CI diagnostic (--sim-state-hash-out) with no runtime malfunction and, as the finder concedes, no player-visible symptom. It is a genuine coverage blind spot — the invariance rail cannot detect regressions in the very lane (prop/chr position, flags, rooms) it is meant to guard — but the concrete impact is weakened test assurance, not a shipping bug. Severity low (medium is defensible since the unhashed region is the lane's primary output). The suggested fix (register pos_data_entry with the existing pointer canonicalization) is sound.


## Config / env flags

### 66. 🟡 ✅ Enum config values from ini/env bypass range clamping — unmatched token is stored raw via strtol with no validation

- **File:** `src/platform/config_pc.c:228` · `setFromString (CFG_ENUM)`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** high
- **What's wrong:** For CFG_S32/CFG_U32/CFG_F32, setFromString clamps the parsed value to the registered [min,max] range. For CFG_ENUM it does not: if the ini/env string matches no enum token, it falls back to `*(s32 *)e->ptr = (s32)strtol(val, NULL, 0)` and stores that integer verbatim, even though settings.c:130-131 registers the enum's valid range as [0, option_count-1]. A user (or a `--config-set`/env override, since settingsApplyEnvOverrides routes through the same setFromString) writing e.g. `SsaoMode = 999` or `WindowMode = -5` lands an out-of-range value in the backing global. Today's consumers happen to be switch statements with default cases so they degrade gracefully, but the setting layer is silently storing values it advertises as invalid, so any future consumer that uses an enum as an array index inherits an OOB.
- **Evidence:**

  ```
  config_pc.c:189-193 CFG_S32 clamps `if (e->min_s32 < e->max_s32) v = clampInt(v, e->min_s32, e->max_s32);` — but config_pc.c:227-229 CFG_ENUM has no clamp: `if (!found) { *(s32 *)e->ptr = (s32)strtol(val, NULL, 0); }`. settings.c:130-131 sets `setting->min.s32_value = 0; setting->max.s32_value = option_count > 0 ? option_count - 1 : 0;`.
  ```
- **Player impact:** A typo or stale value in ge007.ini for an enum setting is accepted rather than clamped/reset; benign for current consumers but a latent OOB landmine and an inconsistency with the numeric settings' hardening.
- **Suggested fix:** In the CFG_ENUM fallback, validate the parsed integer against the registered enum values/range and either reject (keep previous) or clamp to a valid option, matching the CFG_S32 path.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code matches the finding exactly. In setFromString (config_pc.c:183), the CFG_S32 (189-192), CFG_F32 (194-210), and CFG_U32 (212-215) branches all clamp the parsed value against the registered range, but the CFG_ENUM branch (217-230) only clamps by matching a token; on no-match it falls through to `*(s32 *)e->ptr = (s32)strtol(val, NULL, 0)` with zero validation. settings.c:130-131 does advertise the enum range as [0, option_count-1], and both the ini-load path (config_pc.c:298) and the env-override path (settingsApplyEnvOverrides -> configSetValue at 591 -> setFromString) route through this same function, so an out-of-range integer for an enum key is stored verbatim in the backing global. The behavior described is real. However, the finder itself notes all current consumers use switch statements with default cases and degrade gracefully, so there is no present OOB — this is a latent robustness inconsistency, correctly classified as low severity.
  - **Correction:** The behavior and line/symbol are accurate, but the suggested fix is imprecise: enum option `value` fields are arbitrary (e->enum_options[i].value), not guaranteed to equal contiguous indices 0..count-1, and the ConfigEntry layer stores no min/max (only the Setting layer at settings.c:130-131 does). A correct fix must validate the strtol result against the set of registered enum_options[i].value entries and reject/keep-previous on no match, not clamp to a numeric [0, count-1] range. Impact is latent only — no current consumer indexes an array by the enum, so there is no live player/stability issue today.

### 67. 🟡 ✅ port_env registry ignores the Kind of an already-registered flag — a name reused with two accessors type-puns the value union

- **File:** `src/platform/port_env.c:41` · `get_or_create`
- **Severity:** low · **Class:** port-defect · **Category:** robustness · **Finder confidence:** medium
- **What's wrong:** get_or_create() returns any existing entry by name without checking that the caller's Kind matches the one it was first registered with, and the cached value lives in a `union { int b; int i; float f; }`. If the same GE007_* name is ever read once via port_env_bool and once via port_env_int/port_env_float (or vice-versa), the second accessor sees e->parsed==1 and returns e->cur through a different union member than was written — reading a bool 1 as int 1 is harmless, but reading a float default (e.g. 1.5f, bit pattern 0x3FC00000) back through e->cur.i, or an int through e->cur.f, yields garbage. There is also no assert/warning to surface the misuse. I did not find a live occurrence in the current tree, so this is latent, but the registry is explicitly designed as the single funnel for all GE007_* flags (port_env.h) and offers no guardrail against the mistake.
- **Evidence:**

  ```
  port_env.c:41-55 `get_or_create` returns `find_entry(name)` with no kind comparison; the entry stores `union { int b; int i; float f; } cur;` (port_env.c:25). port_env_bool reads `e->cur.b` (line 78), port_env_int reads `e->cur.i` (line 106), port_env_float reads `e->cur.f` (line 134) — all aliasing the same 4 bytes.
  ```
- **Player impact:** None today (no mismatched reuse exists), but a future flag accidentally read as two types would silently return a nonsense value with no diagnostic — the kind of bug that surfaces as an inexplicable gate misbehavior.
- **Suggested fix:** In get_or_create, if an existing entry's kind != requested kind, log and treat as a fresh direct parse (or assert in debug builds) rather than returning the type-punned cache.
- **Verification:** CONFIRMED (adj. severity low, real-player-issue=False). The cited code matches the evidence precisely. get_or_create (port_env.c:41) returns find_entry(name) (lines 42-44) with no kind comparison; the entry caches into union { int b; int i; float f; } cur; (line 25). port_env_bool/int/float read e->cur.b/i/f (lines 78/106/134) — same 4 bytes. The `parsed` guards (67/94/122) mean a second accessor on an already-registered name skips reparsing and returns through a different union member. So a name first registered via port_env_float and later read via port_env_int (or vice-versa) reinterprets a float bit pattern as int / int as float — genuine type-pun garbage. The `kind` field is stored but only used for dump formatting, never enforced; no assert/warning exists. The mechanism is real and fully substantiated by the code. That said, the finder is candid that no mismatched reuse exists in the tree, so this is strictly latent with no current player or stability impact — a low-severity robustness gap / missing guardrail, not a live bug.

