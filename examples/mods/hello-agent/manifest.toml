manifest_version = 1
id = "example.hello-agent"
name = "Hello, Agent"
version = "0.1.0"
author = "GoldenEye Mod Platform"
description = "Minimal Lua mod that unlocks every solo mission for the current session."
entrypoint = "main.lua"
