# FID-0074..FID-0073 — ASM-audit batch-3 derivations

Authority: retail ASM (`GLOBAL_ASM` bodies + `glabel jpt_*`) inlined in
`src/game/*.c`, per `docs/fidelity/CHARTER.md` rule 1 (retail ASM > oracle >
decomp C; the `#else` reference bodies are not consulted as authority).

This batch adjudicated 11 risk-ranked GLOBAL_ASM entries (12 queue rows —
`maybe_mp_interface` has two region variants, both audited): 3
verified-equivalent, 8 findings filed. Skipped as out-of-batch:
`object_interaction` (4641 instrs > 2500 cap) and both `playerTickBeams`
rows (known-deferred giants). Full instruction-level divergence statements
live in the ledger entries; this file records the batch method and the
load-bearing anchors so a fixer can re-derive without re-reading the whole
function.

Method note: each entry was reviewed by an isolated read-only agent against
the review contract (CHARTER.md §audit), then every claimed divergence was
independently re-derived instruction-by-instruction by the batch auditor
before filing (branch senses, likely-branch delay-slot semantics, jump-table
groupings, load widths, fixed-point shifts, `.late_rodata` constants,
call-argument stack maps). Verified-equivalent verdicts were spot-checked on
their highest-risk anchors.

---

## Findings filed

### FID-0074 — `maybe_detonate_object` (P1, sim)

ASM `src/game/chrobjhandler.c:38027-38457` vs live C `37425-37667`. Five
divergences, each re-derived:

- **D1 missing PROPDEF_AMMO salvage spawn**: retail type==20 &&
  destroyedLevel==1 block (38215-38326): `randomGetNext()%13` (`divu`
  38227-38229), circular 13-slot scan (`lhu 0x82` quantity `blezl`-skip
  38235-38237; `lhu 0x80` modelnum != 0xFFFF 38238-38241),
  `ammocrateAllocate` (38244), magazine stamp + `chrpropReparent`. The live
  C post-damage switch (37607-37656) has no case; `ammocrateAllocate` is
  referenced only from the dead `PORT_FIXME_STUBS` branch (37894). Also an
  RNG-stream desync (retail draw skipped).
- **D2 armour inversion**: retail type 21 (38412-38431): destroyed →
  `bnezl` 38418 with `mtc1 $zero` delay → `amount(0x84) = 0.0`; not
  destroyed → `[0x80]*([0x74]-[0x70])/[0x74]`. C 37646-37652 is exactly
  backwards.
- **D3 unarmed gate inversion**: retail proceeds iff
  `(flags & 0x01000000)==0` (`sll $t9,$t7,7` + `bgezl` 38070-38072); C
  37467 proceeds iff the bit is SET. Collectable side (bit 0x00800000,
  38063-38066 = C 37453) is correct.
- **D4 dropped armed-path INVINCIBLE early-out**: `sll $t8,$v1,0xe` +
  `bltzl` 38077-38079 returns on `flags & 0x20000` before type dispatch; C
  has no equivalent, so invincible explosives still get `timer=0` (37486)
  / chain flag (37503).
- **D5 type-7/8 fall-through**: retail non-matching weaponnum/ammoType
  returns unconditionally (`bnel` 38102 / `bne` 38128 → bare epilogue,
  labels verified at 38450-38456); C falls through to
  `objIsMortal`+damage (37481-37519).

### FID-0067 — `microcode_generation_ammo_related` (P2, renderer)

ASM `src/game/gun.c:31418-31716` vs NATIVE_PORT rewrite `31196-31242`.
`position[]` is the rect **center** in the shared consumer
(`draw_textured_rectangle`, bondwalk2.c:33-37). Retail y>=0:
`position[1] = y - h*0.5` (`c.le.s` 31508; `f0=0.5` from `li 0x3F000000`
31474/`mtc1` 31478; `sub.s` 31523) — port passes `y`. Retail y<0:
`H + S - (h*0.5 - (h>>1))` (31536-31552) — port `H + S - h*0.5`
(floor(h/2) px shift). Retail passes flipX slot = constant 0
(`sw $zero,0x18($sp)` 31669; incoming flipX only negates the sub-pixel x
fraction, `beqz` 31502/`neg.s` 31504) — port passes `flipX?1:0` through
(left dual-wield icon mirrored). Minor: dropped ±fracW x term
(31480-31512) and forced `G_TF_POINT` prologue. Retail prologue has zero
early-return guards (FID-0064-class census clean).

### FID-0068 — `sub_GAME_7F087E74` (P2, renderer)

US ASM `src/game/bondview.c:17756-18273` vs live C `17339-17751`.
Renderdata base = sp+0x1b8 (field stores 18192-18195 pin +0x04/+0x08/
+0x0c/+0x30). Retail writes the watch tint to sp+0x1ec = **envcolour**
(+0x34): 205 for watch states 5/12 (18203-18205), else
`r<<24|g<<16|b<<8|a` from `lbu` player+0xfdc..0xfdf (18207-18217). Port
writes `renderdata.cullmode` (+0x3c, inert — 205 matches no CULLMODE_*)
via LE-byte-reversed `*(u32*)&tileColor` (C 17677/17679) → pause arm loses
room tint and the raise/lower darkening (envcolour feeds gDPSetFogColor in
the PropType-4 material path, model.c:9344-9366). Second divergence:
guPerspective aspect retail `0x3FBA2E8C = 1.4545455` (17820-17821) vs C
literal `1.456f` (17425).

### FID-0069 — `sub_GAME_7F04EA68` (P2, sim)

ASM `src/game/chrobjhandler.c:39219-39554` vs live C `38959-39247`.
Retail skips the whole impact-FX block when `shotdata->weapon == 23`
(`li $at,23` 39354; `beq` 39356) = ITEM_WATCHLASER (enum position; anchor
ITEM_TRIGGER=30 matches the validated detonator repro). C 39069 gates on
`ITEM_TASER` (=31). Effect: watch-laser prop hits spawn impact FX + one
extra `randomGetNext` draw vs retail. Corroboration: chrprop.c:3040 uses
ITEM_WATCHLASER for the identical construct.

### FID-0070 — `maybe_mp_interface` (P2, renderer; both region variants)

LEFTOVERDEBUG ASM `src/game/bondview.c:21409-21735` (@7F089208) and retail
US `21739-22065` (@7F089370) vs live C `21194-21406`. Both variants call
only `bondviewGetIfCurrentPlayerHealthShowTime` for the health-bar gate
(debug jal word `0FC227AF` → 0x7F089EBC; DamageShowTime is the adjacent
getter 0x7F089EA4 per chrobjhandler.c:17119 `0FC227A9`; naming fidelity
cross-checked via `sub_GAME_7F088618`'s jal `0FC22186` → exactly
0x7F088618). C 21253 ORs in `bondviewGetIfCurrentPlayerDamageShowTime()`
(`damageshowtime >= 0`, active during damage flashes; idles at -1 per
bondview.c:2466/13655) → port draws the health/armour bars during damage
flashes retail gates behind `sub_GAME_7F0C6048`
(`!gameover && (mpmenuon | healthdisplaytime>0)`, mp_watch.c:8709) and
skips the `healthdisplaytime -= g_ClockTimer` decrement in that window.

### FID-0071 — `bgTestHitOnObj` (P3, sim)

ASM `src/game/chrobjhandler.c:36044-36617` vs live C `35471-35789` +
helper `35443-35483`. Retail post-hit texture backward-scan (loop
36310-36317: `addiu $v0,-8`; `sltu $at,$s3,$v0`; `beq $s5(253),$t2` exit;
`bnezl $at` loop with `lbu $t2,-8($v0)` delay) yields `texturenum = -1`
whenever it terminates at `gdlstart` — even when `*gdlstart` is the
SETTIMG (`bnel $v0,$s3` 36320 falls through to `li $a0,-1` 36323).
Identical structure in the TRI4 path (36557-36580). The port helper scans
inclusive of `gdlstart` (`while (search >= gdlstart)`, break only after
examining it, C 35452-35480).

### FID-0072 — `sub_GAME_7F06359C` (P3, sim)

ASM `src/game/gun.c:16597-17568` vs live C `16183-16607`. (1) All seven
held-button depress cases add `f20 = -10.0` (0xC1200000, 16861-16862) to
the **pos** block y (arg0 of `sub_GAME_7F06351C`, from `D_80035D44`;
i==4 anchor `swc1 sp+0x194` 17047-17049; also 17449/17092/17137/17182/
17227/17272) — pre-rotation in the composition (C 16152-16163: T(arg0)
before Yrot·Xrot, T(arg4) after). Port adds to the vertices-derived
**rot** block (arg4) instead (C 16424-16426 et al.) → depression vector
untilted by the -π/3 X rotation. (2) Retail hand slot is 0/GUNRIGHT
(`move $a0,$zero; li $a1,85` 16687-16689, all queries a0=0; GUNRIGHT=0
per GUNHAND enum) — port uses GUNLEFT throughout (C 16256-16266). (3)
Phantom per-frame save/restore of `prev_watchmenu_item` (C 16256,
16601-16606) with no ASM counterpart. Caught in cross-review: the entry's
first-pass reviewer missed (2); the sibling FID-0073 review flagged it and
it was confirmed against the enum + ASM.

### FID-0073 — `set_enviro_fog_for_items_in_solo_watch_menu` (P3, sim)

ASM `src/game/gun.c:15779-16118` vs live C `15578-15790`. Retail performs
a single `sub_GAME_7F05DA8C(0, weaponnum)` set (15829; {30,23}→60 remap
15821-15827) and never restores — every exit path is the bare epilogue,
and `jal sub_GAME_7F05DAE4` appears in zero GLOBAL_ASM blocks repo-wide.
Port saves `prev_watchmenu_item` (15619) and restores/clears on every exit
(15782-15787), reverting `hands[GUNRIGHT].weaponnum_watchmenu` within the
same draw call (retail pins it while the menu is open; teardown lives at
bondview.c:10258/10347).

---

## Verified-equivalent

- **`sub_GAME_7F08AAE8`** (US, bondview.c:23224-23622 vs C 23057-23215) —
  upper-text HUD window + debug manpos line. Key anchors: 151-byte
  (`0x97`) stringbuffer stride chain 23245-23252; four early gates
  23235-23257; menus-call `y-2` main vs undecremented `y2` debug
  (23331-23338 / 23481-23490, spot-verified both sides); compass
  trunc-toward-zero ×180/π with `.late_rodata` 0x40490fdb; five rodata
  format strings byte-verified.
- **`sub_GAME_7F088CD8`** (bondview.c:20824-21186 vs C 20597-20822) —
  end-credits scroll renderer. Key anchors: three early-outs incl. `bnel`
  likely-delay `lw $ra` (20840-20852, spot-verified); signed div-16 with
  +15 negative bias (20868-20883); alignment grouping ==1/==2/default
  (20997-21016, spot-verified); mid-loop `i++` in always-executed delay
  slot (20915-20916); TextId1==0/TextId2!=0 `langGet(0)` quirk preserved.
- **`sub_GAME_7F04AC20`** (chrobjhandler.c:32123-32488 vs C 31743-32146)
  — recursive prop DL builder. Key anchors: entry `flags&2` `beql`
  early-out (32157-32161, spot-verified); screen-1 0x10000000 vs screen-2
  0x30000000 monitor mask difference (32190-32270, spot-verified);
  needsAnimation rwdata-vs-file Vertices compare; alpha ×50+0x64 clamp;
  door pre/post subdraw emits; child 0x20/0x24 recursion. NATIVE_PORT
  glass portal-projection skip (C 31804-31819) documented as a commented
  native camera-space matrix substrate adaptation (faithful transcription
  would double-apply the camera basis on native); pixel-verifiable via
  the renderer lanes if ever suspected.
